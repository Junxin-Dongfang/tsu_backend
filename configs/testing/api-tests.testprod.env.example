# Shared template for test-server and prod-server. Copy to the environment's secret store.
BASE_URL=https://{ENV_HOST}
ADMIN_USERNAME=${ADMIN_USERNAME}
ADMIN_PASSWORD=${ADMIN_PASSWORD:-password}
PLAYER_EMAIL_SUFFIX=ci.tsu
PLAYER_USERNAME_PREFIX=ci-player
GAME_USERNAME=
GAME_PASSWORD=
SMOKE_JUNIT_FILE=/var/log/tsu/api-smoke.xml
