// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package auth

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// PermissionGroup is an object representing the database table.
type PermissionGroup struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	Code        string      `boil:"code" json:"code" toml:"code" yaml:"code"`
	Name        string      `boil:"name" json:"name" toml:"name" yaml:"name"`
	Description null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	Icon        null.String `boil:"icon" json:"icon,omitempty" toml:"icon" yaml:"icon,omitempty"`
	Color       null.String `boil:"color" json:"color,omitempty" toml:"color" yaml:"color,omitempty"`
	// 排序权重,数值越小越靠前
	SortOrder int         `boil:"sort_order" json:"sort_order" toml:"sort_order" yaml:"sort_order"`
	ParentID  null.String `boil:"parent_id" json:"parent_id,omitempty" toml:"parent_id" yaml:"parent_id,omitempty"`
	// 分组层级,用于树形结构展示
	Level     int       `boil:"level" json:"level" toml:"level" yaml:"level"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *permissionGroupR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L permissionGroupL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var PermissionGroupColumns = struct {
	ID          string
	Code        string
	Name        string
	Description string
	Icon        string
	Color       string
	SortOrder   string
	ParentID    string
	Level       string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "id",
	Code:        "code",
	Name:        "name",
	Description: "description",
	Icon:        "icon",
	Color:       "color",
	SortOrder:   "sort_order",
	ParentID:    "parent_id",
	Level:       "level",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
}

var PermissionGroupTableColumns = struct {
	ID          string
	Code        string
	Name        string
	Description string
	Icon        string
	Color       string
	SortOrder   string
	ParentID    string
	Level       string
	CreatedAt   string
	UpdatedAt   string
}{
	ID:          "permission_groups.id",
	Code:        "permission_groups.code",
	Name:        "permission_groups.name",
	Description: "permission_groups.description",
	Icon:        "permission_groups.icon",
	Color:       "permission_groups.color",
	SortOrder:   "permission_groups.sort_order",
	ParentID:    "permission_groups.parent_id",
	Level:       "permission_groups.level",
	CreatedAt:   "permission_groups.created_at",
	UpdatedAt:   "permission_groups.updated_at",
}

// Generated where

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_String) LIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" LIKE ?", x)
}
func (w whereHelpernull_String) NLIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT LIKE ?", x)
}
func (w whereHelpernull_String) ILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" ILIKE ?", x)
}
func (w whereHelpernull_String) NILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT ILIKE ?", x)
}
func (w whereHelpernull_String) SIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" SIMILAR TO ?", x)
}
func (w whereHelpernull_String) NSIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT SIMILAR TO ?", x)
}
func (w whereHelpernull_String) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_String) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var PermissionGroupWhere = struct {
	ID          whereHelperstring
	Code        whereHelperstring
	Name        whereHelperstring
	Description whereHelpernull_String
	Icon        whereHelpernull_String
	Color       whereHelpernull_String
	SortOrder   whereHelperint
	ParentID    whereHelpernull_String
	Level       whereHelperint
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"auth\".\"permission_groups\".\"id\""},
	Code:        whereHelperstring{field: "\"auth\".\"permission_groups\".\"code\""},
	Name:        whereHelperstring{field: "\"auth\".\"permission_groups\".\"name\""},
	Description: whereHelpernull_String{field: "\"auth\".\"permission_groups\".\"description\""},
	Icon:        whereHelpernull_String{field: "\"auth\".\"permission_groups\".\"icon\""},
	Color:       whereHelpernull_String{field: "\"auth\".\"permission_groups\".\"color\""},
	SortOrder:   whereHelperint{field: "\"auth\".\"permission_groups\".\"sort_order\""},
	ParentID:    whereHelpernull_String{field: "\"auth\".\"permission_groups\".\"parent_id\""},
	Level:       whereHelperint{field: "\"auth\".\"permission_groups\".\"level\""},
	CreatedAt:   whereHelpertime_Time{field: "\"auth\".\"permission_groups\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"auth\".\"permission_groups\".\"updated_at\""},
}

// PermissionGroupRels is where relationship names are stored.
var PermissionGroupRels = struct {
	Parent                      string
	GroupPermissionGroupMembers string
	ParentPermissionGroups      string
}{
	Parent:                      "Parent",
	GroupPermissionGroupMembers: "GroupPermissionGroupMembers",
	ParentPermissionGroups:      "ParentPermissionGroups",
}

// permissionGroupR is where relationships are stored.
type permissionGroupR struct {
	Parent                      *PermissionGroup           `boil:"Parent" json:"Parent" toml:"Parent" yaml:"Parent"`
	GroupPermissionGroupMembers PermissionGroupMemberSlice `boil:"GroupPermissionGroupMembers" json:"GroupPermissionGroupMembers" toml:"GroupPermissionGroupMembers" yaml:"GroupPermissionGroupMembers"`
	ParentPermissionGroups      PermissionGroupSlice       `boil:"ParentPermissionGroups" json:"ParentPermissionGroups" toml:"ParentPermissionGroups" yaml:"ParentPermissionGroups"`
}

// NewStruct creates a new relationship struct
func (*permissionGroupR) NewStruct() *permissionGroupR {
	return &permissionGroupR{}
}

func (o *PermissionGroup) GetParent() *PermissionGroup {
	if o == nil {
		return nil
	}

	return o.R.GetParent()
}

func (r *permissionGroupR) GetParent() *PermissionGroup {
	if r == nil {
		return nil
	}

	return r.Parent
}

func (o *PermissionGroup) GetGroupPermissionGroupMembers() PermissionGroupMemberSlice {
	if o == nil {
		return nil
	}

	return o.R.GetGroupPermissionGroupMembers()
}

func (r *permissionGroupR) GetGroupPermissionGroupMembers() PermissionGroupMemberSlice {
	if r == nil {
		return nil
	}

	return r.GroupPermissionGroupMembers
}

func (o *PermissionGroup) GetParentPermissionGroups() PermissionGroupSlice {
	if o == nil {
		return nil
	}

	return o.R.GetParentPermissionGroups()
}

func (r *permissionGroupR) GetParentPermissionGroups() PermissionGroupSlice {
	if r == nil {
		return nil
	}

	return r.ParentPermissionGroups
}

// permissionGroupL is where Load methods for each relationship are stored.
type permissionGroupL struct{}

var (
	permissionGroupAllColumns            = []string{"id", "code", "name", "description", "icon", "color", "sort_order", "parent_id", "level", "created_at", "updated_at"}
	permissionGroupColumnsWithoutDefault = []string{"code", "name"}
	permissionGroupColumnsWithDefault    = []string{"id", "description", "icon", "color", "sort_order", "parent_id", "level", "created_at", "updated_at"}
	permissionGroupPrimaryKeyColumns     = []string{"id"}
	permissionGroupGeneratedColumns      = []string{}
)

type (
	// PermissionGroupSlice is an alias for a slice of pointers to PermissionGroup.
	// This should almost always be used instead of []PermissionGroup.
	PermissionGroupSlice []*PermissionGroup
	// PermissionGroupHook is the signature for custom PermissionGroup hook methods
	PermissionGroupHook func(context.Context, boil.ContextExecutor, *PermissionGroup) error

	permissionGroupQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	permissionGroupType                 = reflect.TypeOf(&PermissionGroup{})
	permissionGroupMapping              = queries.MakeStructMapping(permissionGroupType)
	permissionGroupPrimaryKeyMapping, _ = queries.BindMapping(permissionGroupType, permissionGroupMapping, permissionGroupPrimaryKeyColumns)
	permissionGroupInsertCacheMut       sync.RWMutex
	permissionGroupInsertCache          = make(map[string]insertCache)
	permissionGroupUpdateCacheMut       sync.RWMutex
	permissionGroupUpdateCache          = make(map[string]updateCache)
	permissionGroupUpsertCacheMut       sync.RWMutex
	permissionGroupUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var permissionGroupAfterSelectMu sync.Mutex
var permissionGroupAfterSelectHooks []PermissionGroupHook

var permissionGroupBeforeInsertMu sync.Mutex
var permissionGroupBeforeInsertHooks []PermissionGroupHook
var permissionGroupAfterInsertMu sync.Mutex
var permissionGroupAfterInsertHooks []PermissionGroupHook

var permissionGroupBeforeUpdateMu sync.Mutex
var permissionGroupBeforeUpdateHooks []PermissionGroupHook
var permissionGroupAfterUpdateMu sync.Mutex
var permissionGroupAfterUpdateHooks []PermissionGroupHook

var permissionGroupBeforeDeleteMu sync.Mutex
var permissionGroupBeforeDeleteHooks []PermissionGroupHook
var permissionGroupAfterDeleteMu sync.Mutex
var permissionGroupAfterDeleteHooks []PermissionGroupHook

var permissionGroupBeforeUpsertMu sync.Mutex
var permissionGroupBeforeUpsertHooks []PermissionGroupHook
var permissionGroupAfterUpsertMu sync.Mutex
var permissionGroupAfterUpsertHooks []PermissionGroupHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *PermissionGroup) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *PermissionGroup) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *PermissionGroup) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *PermissionGroup) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *PermissionGroup) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *PermissionGroup) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *PermissionGroup) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *PermissionGroup) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *PermissionGroup) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range permissionGroupAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddPermissionGroupHook registers your hook function for all future operations.
func AddPermissionGroupHook(hookPoint boil.HookPoint, permissionGroupHook PermissionGroupHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		permissionGroupAfterSelectMu.Lock()
		permissionGroupAfterSelectHooks = append(permissionGroupAfterSelectHooks, permissionGroupHook)
		permissionGroupAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		permissionGroupBeforeInsertMu.Lock()
		permissionGroupBeforeInsertHooks = append(permissionGroupBeforeInsertHooks, permissionGroupHook)
		permissionGroupBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		permissionGroupAfterInsertMu.Lock()
		permissionGroupAfterInsertHooks = append(permissionGroupAfterInsertHooks, permissionGroupHook)
		permissionGroupAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		permissionGroupBeforeUpdateMu.Lock()
		permissionGroupBeforeUpdateHooks = append(permissionGroupBeforeUpdateHooks, permissionGroupHook)
		permissionGroupBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		permissionGroupAfterUpdateMu.Lock()
		permissionGroupAfterUpdateHooks = append(permissionGroupAfterUpdateHooks, permissionGroupHook)
		permissionGroupAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		permissionGroupBeforeDeleteMu.Lock()
		permissionGroupBeforeDeleteHooks = append(permissionGroupBeforeDeleteHooks, permissionGroupHook)
		permissionGroupBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		permissionGroupAfterDeleteMu.Lock()
		permissionGroupAfterDeleteHooks = append(permissionGroupAfterDeleteHooks, permissionGroupHook)
		permissionGroupAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		permissionGroupBeforeUpsertMu.Lock()
		permissionGroupBeforeUpsertHooks = append(permissionGroupBeforeUpsertHooks, permissionGroupHook)
		permissionGroupBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		permissionGroupAfterUpsertMu.Lock()
		permissionGroupAfterUpsertHooks = append(permissionGroupAfterUpsertHooks, permissionGroupHook)
		permissionGroupAfterUpsertMu.Unlock()
	}
}

// OneG returns a single permissionGroup record from the query using the global executor.
func (q permissionGroupQuery) OneG(ctx context.Context) (*PermissionGroup, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single permissionGroup record from the query using the global executor, and panics on error.
func (q permissionGroupQuery) OneGP(ctx context.Context) *PermissionGroup {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single permissionGroup record from the query, and panics on error.
func (q permissionGroupQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *PermissionGroup {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single permissionGroup record from the query.
func (q permissionGroupQuery) One(ctx context.Context, exec boil.ContextExecutor) (*PermissionGroup, error) {
	o := &PermissionGroup{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "auth: failed to execute a one query for permission_groups")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all PermissionGroup records from the query using the global executor.
func (q permissionGroupQuery) AllG(ctx context.Context) (PermissionGroupSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all PermissionGroup records from the query using the global executor, and panics on error.
func (q permissionGroupQuery) AllGP(ctx context.Context) PermissionGroupSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all PermissionGroup records from the query, and panics on error.
func (q permissionGroupQuery) AllP(ctx context.Context, exec boil.ContextExecutor) PermissionGroupSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all PermissionGroup records from the query.
func (q permissionGroupQuery) All(ctx context.Context, exec boil.ContextExecutor) (PermissionGroupSlice, error) {
	var o []*PermissionGroup

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "auth: failed to assign all query results to PermissionGroup slice")
	}

	if len(permissionGroupAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all PermissionGroup records in the query using the global executor
func (q permissionGroupQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all PermissionGroup records in the query using the global executor, and panics on error.
func (q permissionGroupQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all PermissionGroup records in the query, and panics on error.
func (q permissionGroupQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all PermissionGroup records in the query.
func (q permissionGroupQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "auth: failed to count permission_groups rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q permissionGroupQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q permissionGroupQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q permissionGroupQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q permissionGroupQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "auth: failed to check if permission_groups exists")
	}

	return count > 0, nil
}

// Parent pointed to by the foreign key.
func (o *PermissionGroup) Parent(mods ...qm.QueryMod) permissionGroupQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ParentID),
	}

	queryMods = append(queryMods, mods...)

	return PermissionGroups(queryMods...)
}

// GroupPermissionGroupMembers retrieves all the permission_group_member's PermissionGroupMembers with an executor via group_id column.
func (o *PermissionGroup) GroupPermissionGroupMembers(mods ...qm.QueryMod) permissionGroupMemberQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"auth\".\"permission_group_members\".\"group_id\"=?", o.ID),
	)

	return PermissionGroupMembers(queryMods...)
}

// ParentPermissionGroups retrieves all the permission_group's PermissionGroups with an executor via parent_id column.
func (o *PermissionGroup) ParentPermissionGroups(mods ...qm.QueryMod) permissionGroupQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"auth\".\"permission_groups\".\"parent_id\"=?", o.ID),
	)

	return PermissionGroups(queryMods...)
}

// LoadParent allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (permissionGroupL) LoadParent(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissionGroup interface{}, mods queries.Applicator) error {
	var slice []*PermissionGroup
	var object *PermissionGroup

	if singular {
		var ok bool
		object, ok = maybePermissionGroup.(*PermissionGroup)
		if !ok {
			object = new(PermissionGroup)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissionGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissionGroup))
			}
		}
	} else {
		s, ok := maybePermissionGroup.(*[]*PermissionGroup)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissionGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissionGroup))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissionGroupR{}
		}
		if !queries.IsNil(object.ParentID) {
			args[object.ParentID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissionGroupR{}
			}

			if !queries.IsNil(obj.ParentID) {
				args[obj.ParentID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`auth.permission_groups`),
		qm.WhereIn(`auth.permission_groups.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PermissionGroup")
	}

	var resultSlice []*PermissionGroup
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PermissionGroup")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for permission_groups")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permission_groups")
	}

	if len(permissionGroupAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Parent = foreign
		if foreign.R == nil {
			foreign.R = &permissionGroupR{}
		}
		foreign.R.ParentPermissionGroups = append(foreign.R.ParentPermissionGroups, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ParentID, foreign.ID) {
				local.R.Parent = foreign
				if foreign.R == nil {
					foreign.R = &permissionGroupR{}
				}
				foreign.R.ParentPermissionGroups = append(foreign.R.ParentPermissionGroups, local)
				break
			}
		}
	}

	return nil
}

// LoadGroupPermissionGroupMembers allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (permissionGroupL) LoadGroupPermissionGroupMembers(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissionGroup interface{}, mods queries.Applicator) error {
	var slice []*PermissionGroup
	var object *PermissionGroup

	if singular {
		var ok bool
		object, ok = maybePermissionGroup.(*PermissionGroup)
		if !ok {
			object = new(PermissionGroup)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissionGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissionGroup))
			}
		}
	} else {
		s, ok := maybePermissionGroup.(*[]*PermissionGroup)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissionGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissionGroup))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissionGroupR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissionGroupR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`auth.permission_group_members`),
		qm.WhereIn(`auth.permission_group_members.group_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permission_group_members")
	}

	var resultSlice []*PermissionGroupMember
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice permission_group_members")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permission_group_members")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permission_group_members")
	}

	if len(permissionGroupMemberAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.GroupPermissionGroupMembers = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissionGroupMemberR{}
			}
			foreign.R.Group = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.GroupID {
				local.R.GroupPermissionGroupMembers = append(local.R.GroupPermissionGroupMembers, foreign)
				if foreign.R == nil {
					foreign.R = &permissionGroupMemberR{}
				}
				foreign.R.Group = local
				break
			}
		}
	}

	return nil
}

// LoadParentPermissionGroups allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (permissionGroupL) LoadParentPermissionGroups(ctx context.Context, e boil.ContextExecutor, singular bool, maybePermissionGroup interface{}, mods queries.Applicator) error {
	var slice []*PermissionGroup
	var object *PermissionGroup

	if singular {
		var ok bool
		object, ok = maybePermissionGroup.(*PermissionGroup)
		if !ok {
			object = new(PermissionGroup)
			ok = queries.SetFromEmbeddedStruct(&object, &maybePermissionGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybePermissionGroup))
			}
		}
	} else {
		s, ok := maybePermissionGroup.(*[]*PermissionGroup)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybePermissionGroup)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybePermissionGroup))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &permissionGroupR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &permissionGroupR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`auth.permission_groups`),
		qm.WhereIn(`auth.permission_groups.parent_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load permission_groups")
	}

	var resultSlice []*PermissionGroup
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice permission_groups")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on permission_groups")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for permission_groups")
	}

	if len(permissionGroupAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.ParentPermissionGroups = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &permissionGroupR{}
			}
			foreign.R.Parent = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.ParentID) {
				local.R.ParentPermissionGroups = append(local.R.ParentPermissionGroups, foreign)
				if foreign.R == nil {
					foreign.R = &permissionGroupR{}
				}
				foreign.R.Parent = local
				break
			}
		}
	}

	return nil
}

// SetParentG of the permissionGroup to the related item.
// Sets o.R.Parent to related.
// Adds o to related.R.ParentPermissionGroups.
// Uses the global database handle.
func (o *PermissionGroup) SetParentG(ctx context.Context, insert bool, related *PermissionGroup) error {
	return o.SetParent(ctx, boil.GetContextDB(), insert, related)
}

// SetParentP of the permissionGroup to the related item.
// Sets o.R.Parent to related.
// Adds o to related.R.ParentPermissionGroups.
// Panics on error.
func (o *PermissionGroup) SetParentP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PermissionGroup) {
	if err := o.SetParent(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetParentGP of the permissionGroup to the related item.
// Sets o.R.Parent to related.
// Adds o to related.R.ParentPermissionGroups.
// Uses the global database handle and panics on error.
func (o *PermissionGroup) SetParentGP(ctx context.Context, insert bool, related *PermissionGroup) {
	if err := o.SetParent(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetParent of the permissionGroup to the related item.
// Sets o.R.Parent to related.
// Adds o to related.R.ParentPermissionGroups.
func (o *PermissionGroup) SetParent(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PermissionGroup) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"auth\".\"permission_groups\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"parent_id"}),
		strmangle.WhereClause("\"", "\"", 2, permissionGroupPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ParentID, related.ID)
	if o.R == nil {
		o.R = &permissionGroupR{
			Parent: related,
		}
	} else {
		o.R.Parent = related
	}

	if related.R == nil {
		related.R = &permissionGroupR{
			ParentPermissionGroups: PermissionGroupSlice{o},
		}
	} else {
		related.R.ParentPermissionGroups = append(related.R.ParentPermissionGroups, o)
	}

	return nil
}

// RemoveParentG relationship.
// Sets o.R.Parent to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *PermissionGroup) RemoveParentG(ctx context.Context, related *PermissionGroup) error {
	return o.RemoveParent(ctx, boil.GetContextDB(), related)
}

// RemoveParentP relationship.
// Sets o.R.Parent to nil.
// Removes o from all passed in related items' relationships struct.
// Panics on error.
func (o *PermissionGroup) RemoveParentP(ctx context.Context, exec boil.ContextExecutor, related *PermissionGroup) {
	if err := o.RemoveParent(ctx, exec, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveParentGP relationship.
// Sets o.R.Parent to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle and panics on error.
func (o *PermissionGroup) RemoveParentGP(ctx context.Context, related *PermissionGroup) {
	if err := o.RemoveParent(ctx, boil.GetContextDB(), related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveParent relationship.
// Sets o.R.Parent to nil.
// Removes o from all passed in related items' relationships struct.
func (o *PermissionGroup) RemoveParent(ctx context.Context, exec boil.ContextExecutor, related *PermissionGroup) error {
	var err error

	queries.SetScanner(&o.ParentID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("parent_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Parent = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ParentPermissionGroups {
		if queries.Equal(o.ParentID, ri.ParentID) {
			continue
		}

		ln := len(related.R.ParentPermissionGroups)
		if ln > 1 && i < ln-1 {
			related.R.ParentPermissionGroups[i] = related.R.ParentPermissionGroups[ln-1]
		}
		related.R.ParentPermissionGroups = related.R.ParentPermissionGroups[:ln-1]
		break
	}
	return nil
}

// AddGroupPermissionGroupMembersG adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.GroupPermissionGroupMembers.
// Sets related.R.Group appropriately.
// Uses the global database handle.
func (o *PermissionGroup) AddGroupPermissionGroupMembersG(ctx context.Context, insert bool, related ...*PermissionGroupMember) error {
	return o.AddGroupPermissionGroupMembers(ctx, boil.GetContextDB(), insert, related...)
}

// AddGroupPermissionGroupMembersP adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.GroupPermissionGroupMembers.
// Sets related.R.Group appropriately.
// Panics on error.
func (o *PermissionGroup) AddGroupPermissionGroupMembersP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissionGroupMember) {
	if err := o.AddGroupPermissionGroupMembers(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddGroupPermissionGroupMembersGP adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.GroupPermissionGroupMembers.
// Sets related.R.Group appropriately.
// Uses the global database handle and panics on error.
func (o *PermissionGroup) AddGroupPermissionGroupMembersGP(ctx context.Context, insert bool, related ...*PermissionGroupMember) {
	if err := o.AddGroupPermissionGroupMembers(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddGroupPermissionGroupMembers adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.GroupPermissionGroupMembers.
// Sets related.R.Group appropriately.
func (o *PermissionGroup) AddGroupPermissionGroupMembers(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissionGroupMember) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.GroupID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"auth\".\"permission_group_members\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"group_id"}),
				strmangle.WhereClause("\"", "\"", 2, permissionGroupMemberPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.GroupID = o.ID
		}
	}

	if o.R == nil {
		o.R = &permissionGroupR{
			GroupPermissionGroupMembers: related,
		}
	} else {
		o.R.GroupPermissionGroupMembers = append(o.R.GroupPermissionGroupMembers, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissionGroupMemberR{
				Group: o,
			}
		} else {
			rel.R.Group = o
		}
	}
	return nil
}

// AddParentPermissionGroupsG adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.ParentPermissionGroups.
// Sets related.R.Parent appropriately.
// Uses the global database handle.
func (o *PermissionGroup) AddParentPermissionGroupsG(ctx context.Context, insert bool, related ...*PermissionGroup) error {
	return o.AddParentPermissionGroups(ctx, boil.GetContextDB(), insert, related...)
}

// AddParentPermissionGroupsP adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.ParentPermissionGroups.
// Sets related.R.Parent appropriately.
// Panics on error.
func (o *PermissionGroup) AddParentPermissionGroupsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissionGroup) {
	if err := o.AddParentPermissionGroups(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddParentPermissionGroupsGP adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.ParentPermissionGroups.
// Sets related.R.Parent appropriately.
// Uses the global database handle and panics on error.
func (o *PermissionGroup) AddParentPermissionGroupsGP(ctx context.Context, insert bool, related ...*PermissionGroup) {
	if err := o.AddParentPermissionGroups(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddParentPermissionGroups adds the given related objects to the existing relationships
// of the permission_group, optionally inserting them as new records.
// Appends related to o.R.ParentPermissionGroups.
// Sets related.R.Parent appropriately.
func (o *PermissionGroup) AddParentPermissionGroups(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissionGroup) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ParentID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"auth\".\"permission_groups\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"parent_id"}),
				strmangle.WhereClause("\"", "\"", 2, permissionGroupPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ParentID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &permissionGroupR{
			ParentPermissionGroups: related,
		}
	} else {
		o.R.ParentPermissionGroups = append(o.R.ParentPermissionGroups, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &permissionGroupR{
				Parent: o,
			}
		} else {
			rel.R.Parent = o
		}
	}
	return nil
}

// SetParentPermissionGroupsG removes all previously related items of the
// permission_group replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Parent's ParentPermissionGroups accordingly.
// Replaces o.R.ParentPermissionGroups with related.
// Sets related.R.Parent's ParentPermissionGroups accordingly.
// Uses the global database handle.
func (o *PermissionGroup) SetParentPermissionGroupsG(ctx context.Context, insert bool, related ...*PermissionGroup) error {
	return o.SetParentPermissionGroups(ctx, boil.GetContextDB(), insert, related...)
}

// SetParentPermissionGroupsP removes all previously related items of the
// permission_group replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Parent's ParentPermissionGroups accordingly.
// Replaces o.R.ParentPermissionGroups with related.
// Sets related.R.Parent's ParentPermissionGroups accordingly.
// Panics on error.
func (o *PermissionGroup) SetParentPermissionGroupsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissionGroup) {
	if err := o.SetParentPermissionGroups(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetParentPermissionGroupsGP removes all previously related items of the
// permission_group replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Parent's ParentPermissionGroups accordingly.
// Replaces o.R.ParentPermissionGroups with related.
// Sets related.R.Parent's ParentPermissionGroups accordingly.
// Uses the global database handle and panics on error.
func (o *PermissionGroup) SetParentPermissionGroupsGP(ctx context.Context, insert bool, related ...*PermissionGroup) {
	if err := o.SetParentPermissionGroups(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetParentPermissionGroups removes all previously related items of the
// permission_group replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Parent's ParentPermissionGroups accordingly.
// Replaces o.R.ParentPermissionGroups with related.
// Sets related.R.Parent's ParentPermissionGroups accordingly.
func (o *PermissionGroup) SetParentPermissionGroups(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*PermissionGroup) error {
	query := "update \"auth\".\"permission_groups\" set \"parent_id\" = null where \"parent_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.ParentPermissionGroups {
			queries.SetScanner(&rel.ParentID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Parent = nil
		}
		o.R.ParentPermissionGroups = nil
	}

	return o.AddParentPermissionGroups(ctx, exec, insert, related...)
}

// RemoveParentPermissionGroupsG relationships from objects passed in.
// Removes related items from R.ParentPermissionGroups (uses pointer comparison, removal does not keep order)
// Sets related.R.Parent.
// Uses the global database handle.
func (o *PermissionGroup) RemoveParentPermissionGroupsG(ctx context.Context, related ...*PermissionGroup) error {
	return o.RemoveParentPermissionGroups(ctx, boil.GetContextDB(), related...)
}

// RemoveParentPermissionGroupsP relationships from objects passed in.
// Removes related items from R.ParentPermissionGroups (uses pointer comparison, removal does not keep order)
// Sets related.R.Parent.
// Panics on error.
func (o *PermissionGroup) RemoveParentPermissionGroupsP(ctx context.Context, exec boil.ContextExecutor, related ...*PermissionGroup) {
	if err := o.RemoveParentPermissionGroups(ctx, exec, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveParentPermissionGroupsGP relationships from objects passed in.
// Removes related items from R.ParentPermissionGroups (uses pointer comparison, removal does not keep order)
// Sets related.R.Parent.
// Uses the global database handle and panics on error.
func (o *PermissionGroup) RemoveParentPermissionGroupsGP(ctx context.Context, related ...*PermissionGroup) {
	if err := o.RemoveParentPermissionGroups(ctx, boil.GetContextDB(), related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveParentPermissionGroups relationships from objects passed in.
// Removes related items from R.ParentPermissionGroups (uses pointer comparison, removal does not keep order)
// Sets related.R.Parent.
func (o *PermissionGroup) RemoveParentPermissionGroups(ctx context.Context, exec boil.ContextExecutor, related ...*PermissionGroup) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ParentID, nil)
		if rel.R != nil {
			rel.R.Parent = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("parent_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.ParentPermissionGroups {
			if rel != ri {
				continue
			}

			ln := len(o.R.ParentPermissionGroups)
			if ln > 1 && i < ln-1 {
				o.R.ParentPermissionGroups[i] = o.R.ParentPermissionGroups[ln-1]
			}
			o.R.ParentPermissionGroups = o.R.ParentPermissionGroups[:ln-1]
			break
		}
	}

	return nil
}

// PermissionGroups retrieves all the records using an executor.
func PermissionGroups(mods ...qm.QueryMod) permissionGroupQuery {
	mods = append(mods, qm.From("\"auth\".\"permission_groups\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"auth\".\"permission_groups\".*"})
	}

	return permissionGroupQuery{q}
}

// FindPermissionGroupG retrieves a single record by ID.
func FindPermissionGroupG(ctx context.Context, iD string, selectCols ...string) (*PermissionGroup, error) {
	return FindPermissionGroup(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindPermissionGroupP retrieves a single record by ID with an executor, and panics on error.
func FindPermissionGroupP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *PermissionGroup {
	retobj, err := FindPermissionGroup(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindPermissionGroupGP retrieves a single record by ID, and panics on error.
func FindPermissionGroupGP(ctx context.Context, iD string, selectCols ...string) *PermissionGroup {
	retobj, err := FindPermissionGroup(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindPermissionGroup retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindPermissionGroup(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*PermissionGroup, error) {
	permissionGroupObj := &PermissionGroup{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"auth\".\"permission_groups\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, permissionGroupObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "auth: unable to select from permission_groups")
	}

	if err = permissionGroupObj.doAfterSelectHooks(ctx, exec); err != nil {
		return permissionGroupObj, err
	}

	return permissionGroupObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *PermissionGroup) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *PermissionGroup) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *PermissionGroup) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *PermissionGroup) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("auth: no permission_groups provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(permissionGroupColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	permissionGroupInsertCacheMut.RLock()
	cache, cached := permissionGroupInsertCache[key]
	permissionGroupInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			permissionGroupAllColumns,
			permissionGroupColumnsWithDefault,
			permissionGroupColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(permissionGroupType, permissionGroupMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(permissionGroupType, permissionGroupMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"auth\".\"permission_groups\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"auth\".\"permission_groups\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "auth: unable to insert into permission_groups")
	}

	if !cached {
		permissionGroupInsertCacheMut.Lock()
		permissionGroupInsertCache[key] = cache
		permissionGroupInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single PermissionGroup record using the global executor.
// See Update for more documentation.
func (o *PermissionGroup) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the PermissionGroup, and panics on error.
// See Update for more documentation.
func (o *PermissionGroup) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single PermissionGroup record using the global executor. Panics on error.
// See Update for more documentation.
func (o *PermissionGroup) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the PermissionGroup.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *PermissionGroup) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	permissionGroupUpdateCacheMut.RLock()
	cache, cached := permissionGroupUpdateCache[key]
	permissionGroupUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			permissionGroupAllColumns,
			permissionGroupPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("auth: unable to update permission_groups, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"auth\".\"permission_groups\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, permissionGroupPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(permissionGroupType, permissionGroupMapping, append(wl, permissionGroupPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to update permission_groups row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "auth: failed to get rows affected by update for permission_groups")
	}

	if !cached {
		permissionGroupUpdateCacheMut.Lock()
		permissionGroupUpdateCache[key] = cache
		permissionGroupUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q permissionGroupQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q permissionGroupQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q permissionGroupQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q permissionGroupQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to update all for permission_groups")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to retrieve rows affected for permission_groups")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o PermissionGroupSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o PermissionGroupSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o PermissionGroupSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o PermissionGroupSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("auth: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissionGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"auth\".\"permission_groups\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, permissionGroupPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to update all in permissionGroup slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to retrieve rows affected all in update all permissionGroup")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *PermissionGroup) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *PermissionGroup) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *PermissionGroup) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *PermissionGroup) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("auth: no permission_groups provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(permissionGroupColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	permissionGroupUpsertCacheMut.RLock()
	cache, cached := permissionGroupUpsertCache[key]
	permissionGroupUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			permissionGroupAllColumns,
			permissionGroupColumnsWithDefault,
			permissionGroupColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			permissionGroupAllColumns,
			permissionGroupPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("auth: unable to upsert permission_groups, could not build update column list")
		}

		ret := strmangle.SetComplement(permissionGroupAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(permissionGroupPrimaryKeyColumns) == 0 {
				return errors.New("auth: unable to upsert permission_groups, could not build conflict column list")
			}

			conflict = make([]string, len(permissionGroupPrimaryKeyColumns))
			copy(conflict, permissionGroupPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"auth\".\"permission_groups\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(permissionGroupType, permissionGroupMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(permissionGroupType, permissionGroupMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "auth: unable to upsert permission_groups")
	}

	if !cached {
		permissionGroupUpsertCacheMut.Lock()
		permissionGroupUpsertCache[key] = cache
		permissionGroupUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single PermissionGroup record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *PermissionGroup) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single PermissionGroup record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PermissionGroup) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single PermissionGroup record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *PermissionGroup) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single PermissionGroup record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *PermissionGroup) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("auth: no PermissionGroup provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), permissionGroupPrimaryKeyMapping)
	sql := "DELETE FROM \"auth\".\"permission_groups\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to delete from permission_groups")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "auth: failed to get rows affected by delete for permission_groups")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q permissionGroupQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q permissionGroupQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q permissionGroupQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q permissionGroupQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("auth: no permissionGroupQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to delete all from permission_groups")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "auth: failed to get rows affected by deleteall for permission_groups")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o PermissionGroupSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o PermissionGroupSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o PermissionGroupSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o PermissionGroupSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(permissionGroupBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissionGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"auth\".\"permission_groups\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, permissionGroupPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "auth: unable to delete all from permissionGroup slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "auth: failed to get rows affected by deleteall for permission_groups")
	}

	if len(permissionGroupAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *PermissionGroup) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("auth: no PermissionGroup provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *PermissionGroup) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *PermissionGroup) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *PermissionGroup) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindPermissionGroup(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PermissionGroupSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("auth: empty PermissionGroupSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PermissionGroupSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *PermissionGroupSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *PermissionGroupSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := PermissionGroupSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), permissionGroupPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"auth\".\"permission_groups\".* FROM \"auth\".\"permission_groups\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, permissionGroupPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "auth: unable to reload all in PermissionGroupSlice")
	}

	*o = slice

	return nil
}

// PermissionGroupExistsG checks if the PermissionGroup row exists.
func PermissionGroupExistsG(ctx context.Context, iD string) (bool, error) {
	return PermissionGroupExists(ctx, boil.GetContextDB(), iD)
}

// PermissionGroupExistsP checks if the PermissionGroup row exists. Panics on error.
func PermissionGroupExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := PermissionGroupExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// PermissionGroupExistsGP checks if the PermissionGroup row exists. Panics on error.
func PermissionGroupExistsGP(ctx context.Context, iD string) bool {
	e, err := PermissionGroupExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// PermissionGroupExists checks if the PermissionGroup row exists.
func PermissionGroupExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"auth\".\"permission_groups\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "auth: unable to check if permission_groups exists")
	}

	return exists, nil
}

// Exists checks if the PermissionGroup row exists.
func (o *PermissionGroup) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return PermissionGroupExists(ctx, exec, o.ID)
}
