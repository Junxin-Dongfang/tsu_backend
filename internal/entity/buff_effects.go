// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// BuffEffect is an object representing the database table.
type BuffEffect struct {
	ID                 string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	BuffID             string    `boil:"buff_id" json:"buff_id" toml:"buff_id" yaml:"buff_id"`
	EffectID           string    `boil:"effect_id" json:"effect_id" toml:"effect_id" yaml:"effect_id"`
	TriggerTiming      string    `boil:"trigger_timing" json:"trigger_timing" toml:"trigger_timing" yaml:"trigger_timing"`
	ExecutionOrder     null.Int  `boil:"execution_order" json:"execution_order,omitempty" toml:"execution_order" yaml:"execution_order,omitempty"`
	ParameterOverrides null.JSON `boil:"parameter_overrides" json:"parameter_overrides,omitempty" toml:"parameter_overrides" yaml:"parameter_overrides,omitempty"`
	CreatedAt          null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	DeletedAt          null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *buffEffectR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L buffEffectL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BuffEffectColumns = struct {
	ID                 string
	BuffID             string
	EffectID           string
	TriggerTiming      string
	ExecutionOrder     string
	ParameterOverrides string
	CreatedAt          string
	DeletedAt          string
}{
	ID:                 "id",
	BuffID:             "buff_id",
	EffectID:           "effect_id",
	TriggerTiming:      "trigger_timing",
	ExecutionOrder:     "execution_order",
	ParameterOverrides: "parameter_overrides",
	CreatedAt:          "created_at",
	DeletedAt:          "deleted_at",
}

var BuffEffectTableColumns = struct {
	ID                 string
	BuffID             string
	EffectID           string
	TriggerTiming      string
	ExecutionOrder     string
	ParameterOverrides string
	CreatedAt          string
	DeletedAt          string
}{
	ID:                 "buff_effects.id",
	BuffID:             "buff_effects.buff_id",
	EffectID:           "buff_effects.effect_id",
	TriggerTiming:      "buff_effects.trigger_timing",
	ExecutionOrder:     "buff_effects.execution_order",
	ParameterOverrides: "buff_effects.parameter_overrides",
	CreatedAt:          "buff_effects.created_at",
	DeletedAt:          "buff_effects.deleted_at",
}

// Generated where

var BuffEffectWhere = struct {
	ID                 whereHelperstring
	BuffID             whereHelperstring
	EffectID           whereHelperstring
	TriggerTiming      whereHelperstring
	ExecutionOrder     whereHelpernull_Int
	ParameterOverrides whereHelpernull_JSON
	CreatedAt          whereHelpernull_Time
	DeletedAt          whereHelpernull_Time
}{
	ID:                 whereHelperstring{field: "\"buff_effects\".\"id\""},
	BuffID:             whereHelperstring{field: "\"buff_effects\".\"buff_id\""},
	EffectID:           whereHelperstring{field: "\"buff_effects\".\"effect_id\""},
	TriggerTiming:      whereHelperstring{field: "\"buff_effects\".\"trigger_timing\""},
	ExecutionOrder:     whereHelpernull_Int{field: "\"buff_effects\".\"execution_order\""},
	ParameterOverrides: whereHelpernull_JSON{field: "\"buff_effects\".\"parameter_overrides\""},
	CreatedAt:          whereHelpernull_Time{field: "\"buff_effects\".\"created_at\""},
	DeletedAt:          whereHelpernull_Time{field: "\"buff_effects\".\"deleted_at\""},
}

// BuffEffectRels is where relationship names are stored.
var BuffEffectRels = struct {
	Buff   string
	Effect string
}{
	Buff:   "Buff",
	Effect: "Effect",
}

// buffEffectR is where relationships are stored.
type buffEffectR struct {
	Buff   *Buff   `boil:"Buff" json:"Buff" toml:"Buff" yaml:"Buff"`
	Effect *Effect `boil:"Effect" json:"Effect" toml:"Effect" yaml:"Effect"`
}

// NewStruct creates a new relationship struct
func (*buffEffectR) NewStruct() *buffEffectR {
	return &buffEffectR{}
}

func (o *BuffEffect) GetBuff() *Buff {
	if o == nil {
		return nil
	}

	return o.R.GetBuff()
}

func (r *buffEffectR) GetBuff() *Buff {
	if r == nil {
		return nil
	}

	return r.Buff
}

func (o *BuffEffect) GetEffect() *Effect {
	if o == nil {
		return nil
	}

	return o.R.GetEffect()
}

func (r *buffEffectR) GetEffect() *Effect {
	if r == nil {
		return nil
	}

	return r.Effect
}

// buffEffectL is where Load methods for each relationship are stored.
type buffEffectL struct{}

var (
	buffEffectAllColumns            = []string{"id", "buff_id", "effect_id", "trigger_timing", "execution_order", "parameter_overrides", "created_at", "deleted_at"}
	buffEffectColumnsWithoutDefault = []string{"buff_id", "effect_id", "trigger_timing"}
	buffEffectColumnsWithDefault    = []string{"id", "execution_order", "parameter_overrides", "created_at", "deleted_at"}
	buffEffectPrimaryKeyColumns     = []string{"id"}
	buffEffectGeneratedColumns      = []string{}
)

type (
	// BuffEffectSlice is an alias for a slice of pointers to BuffEffect.
	// This should almost always be used instead of []BuffEffect.
	BuffEffectSlice []*BuffEffect
	// BuffEffectHook is the signature for custom BuffEffect hook methods
	BuffEffectHook func(context.Context, boil.ContextExecutor, *BuffEffect) error

	buffEffectQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	buffEffectType                 = reflect.TypeOf(&BuffEffect{})
	buffEffectMapping              = queries.MakeStructMapping(buffEffectType)
	buffEffectPrimaryKeyMapping, _ = queries.BindMapping(buffEffectType, buffEffectMapping, buffEffectPrimaryKeyColumns)
	buffEffectInsertCacheMut       sync.RWMutex
	buffEffectInsertCache          = make(map[string]insertCache)
	buffEffectUpdateCacheMut       sync.RWMutex
	buffEffectUpdateCache          = make(map[string]updateCache)
	buffEffectUpsertCacheMut       sync.RWMutex
	buffEffectUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var buffEffectAfterSelectMu sync.Mutex
var buffEffectAfterSelectHooks []BuffEffectHook

var buffEffectBeforeInsertMu sync.Mutex
var buffEffectBeforeInsertHooks []BuffEffectHook
var buffEffectAfterInsertMu sync.Mutex
var buffEffectAfterInsertHooks []BuffEffectHook

var buffEffectBeforeUpdateMu sync.Mutex
var buffEffectBeforeUpdateHooks []BuffEffectHook
var buffEffectAfterUpdateMu sync.Mutex
var buffEffectAfterUpdateHooks []BuffEffectHook

var buffEffectBeforeDeleteMu sync.Mutex
var buffEffectBeforeDeleteHooks []BuffEffectHook
var buffEffectAfterDeleteMu sync.Mutex
var buffEffectAfterDeleteHooks []BuffEffectHook

var buffEffectBeforeUpsertMu sync.Mutex
var buffEffectBeforeUpsertHooks []BuffEffectHook
var buffEffectAfterUpsertMu sync.Mutex
var buffEffectAfterUpsertHooks []BuffEffectHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BuffEffect) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BuffEffect) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BuffEffect) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BuffEffect) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BuffEffect) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BuffEffect) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BuffEffect) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BuffEffect) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BuffEffect) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range buffEffectAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBuffEffectHook registers your hook function for all future operations.
func AddBuffEffectHook(hookPoint boil.HookPoint, buffEffectHook BuffEffectHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		buffEffectAfterSelectMu.Lock()
		buffEffectAfterSelectHooks = append(buffEffectAfterSelectHooks, buffEffectHook)
		buffEffectAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		buffEffectBeforeInsertMu.Lock()
		buffEffectBeforeInsertHooks = append(buffEffectBeforeInsertHooks, buffEffectHook)
		buffEffectBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		buffEffectAfterInsertMu.Lock()
		buffEffectAfterInsertHooks = append(buffEffectAfterInsertHooks, buffEffectHook)
		buffEffectAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		buffEffectBeforeUpdateMu.Lock()
		buffEffectBeforeUpdateHooks = append(buffEffectBeforeUpdateHooks, buffEffectHook)
		buffEffectBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		buffEffectAfterUpdateMu.Lock()
		buffEffectAfterUpdateHooks = append(buffEffectAfterUpdateHooks, buffEffectHook)
		buffEffectAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		buffEffectBeforeDeleteMu.Lock()
		buffEffectBeforeDeleteHooks = append(buffEffectBeforeDeleteHooks, buffEffectHook)
		buffEffectBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		buffEffectAfterDeleteMu.Lock()
		buffEffectAfterDeleteHooks = append(buffEffectAfterDeleteHooks, buffEffectHook)
		buffEffectAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		buffEffectBeforeUpsertMu.Lock()
		buffEffectBeforeUpsertHooks = append(buffEffectBeforeUpsertHooks, buffEffectHook)
		buffEffectBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		buffEffectAfterUpsertMu.Lock()
		buffEffectAfterUpsertHooks = append(buffEffectAfterUpsertHooks, buffEffectHook)
		buffEffectAfterUpsertMu.Unlock()
	}
}

// OneG returns a single buffEffect record from the query using the global executor.
func (q buffEffectQuery) OneG(ctx context.Context) (*BuffEffect, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single buffEffect record from the query using the global executor, and panics on error.
func (q buffEffectQuery) OneGP(ctx context.Context) *BuffEffect {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single buffEffect record from the query, and panics on error.
func (q buffEffectQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *BuffEffect {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single buffEffect record from the query.
func (q buffEffectQuery) One(ctx context.Context, exec boil.ContextExecutor) (*BuffEffect, error) {
	o := &BuffEffect{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for buff_effects")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all BuffEffect records from the query using the global executor.
func (q buffEffectQuery) AllG(ctx context.Context) (BuffEffectSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all BuffEffect records from the query using the global executor, and panics on error.
func (q buffEffectQuery) AllGP(ctx context.Context) BuffEffectSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all BuffEffect records from the query, and panics on error.
func (q buffEffectQuery) AllP(ctx context.Context, exec boil.ContextExecutor) BuffEffectSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all BuffEffect records from the query.
func (q buffEffectQuery) All(ctx context.Context, exec boil.ContextExecutor) (BuffEffectSlice, error) {
	var o []*BuffEffect

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to BuffEffect slice")
	}

	if len(buffEffectAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all BuffEffect records in the query using the global executor
func (q buffEffectQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all BuffEffect records in the query using the global executor, and panics on error.
func (q buffEffectQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all BuffEffect records in the query, and panics on error.
func (q buffEffectQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all BuffEffect records in the query.
func (q buffEffectQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count buff_effects rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q buffEffectQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q buffEffectQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q buffEffectQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q buffEffectQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if buff_effects exists")
	}

	return count > 0, nil
}

// Buff pointed to by the foreign key.
func (o *BuffEffect) Buff(mods ...qm.QueryMod) buffQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BuffID),
	}

	queryMods = append(queryMods, mods...)

	return Buffs(queryMods...)
}

// Effect pointed to by the foreign key.
func (o *BuffEffect) Effect(mods ...qm.QueryMod) effectQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.EffectID),
	}

	queryMods = append(queryMods, mods...)

	return Effects(queryMods...)
}

// LoadBuff allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (buffEffectL) LoadBuff(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBuffEffect interface{}, mods queries.Applicator) error {
	var slice []*BuffEffect
	var object *BuffEffect

	if singular {
		var ok bool
		object, ok = maybeBuffEffect.(*BuffEffect)
		if !ok {
			object = new(BuffEffect)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeBuffEffect)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeBuffEffect))
			}
		}
	} else {
		s, ok := maybeBuffEffect.(*[]*BuffEffect)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeBuffEffect)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeBuffEffect))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &buffEffectR{}
		}
		args[object.BuffID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &buffEffectR{}
			}

			args[obj.BuffID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`buffs`),
		qm.WhereIn(`buffs.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`buffs.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Buff")
	}

	var resultSlice []*Buff
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Buff")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for buffs")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for buffs")
	}

	if len(buffAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Buff = foreign
		if foreign.R == nil {
			foreign.R = &buffR{}
		}
		foreign.R.BuffEffects = append(foreign.R.BuffEffects, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BuffID == foreign.ID {
				local.R.Buff = foreign
				if foreign.R == nil {
					foreign.R = &buffR{}
				}
				foreign.R.BuffEffects = append(foreign.R.BuffEffects, local)
				break
			}
		}
	}

	return nil
}

// LoadEffect allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (buffEffectL) LoadEffect(ctx context.Context, e boil.ContextExecutor, singular bool, maybeBuffEffect interface{}, mods queries.Applicator) error {
	var slice []*BuffEffect
	var object *BuffEffect

	if singular {
		var ok bool
		object, ok = maybeBuffEffect.(*BuffEffect)
		if !ok {
			object = new(BuffEffect)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeBuffEffect)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeBuffEffect))
			}
		}
	} else {
		s, ok := maybeBuffEffect.(*[]*BuffEffect)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeBuffEffect)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeBuffEffect))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &buffEffectR{}
		}
		args[object.EffectID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &buffEffectR{}
			}

			args[obj.EffectID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`effects`),
		qm.WhereIn(`effects.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`effects.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Effect")
	}

	var resultSlice []*Effect
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Effect")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for effects")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for effects")
	}

	if len(effectAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Effect = foreign
		if foreign.R == nil {
			foreign.R = &effectR{}
		}
		foreign.R.BuffEffects = append(foreign.R.BuffEffects, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.EffectID == foreign.ID {
				local.R.Effect = foreign
				if foreign.R == nil {
					foreign.R = &effectR{}
				}
				foreign.R.BuffEffects = append(foreign.R.BuffEffects, local)
				break
			}
		}
	}

	return nil
}

// SetBuffG of the buffEffect to the related item.
// Sets o.R.Buff to related.
// Adds o to related.R.BuffEffects.
// Uses the global database handle.
func (o *BuffEffect) SetBuffG(ctx context.Context, insert bool, related *Buff) error {
	return o.SetBuff(ctx, boil.GetContextDB(), insert, related)
}

// SetBuffP of the buffEffect to the related item.
// Sets o.R.Buff to related.
// Adds o to related.R.BuffEffects.
// Panics on error.
func (o *BuffEffect) SetBuffP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Buff) {
	if err := o.SetBuff(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetBuffGP of the buffEffect to the related item.
// Sets o.R.Buff to related.
// Adds o to related.R.BuffEffects.
// Uses the global database handle and panics on error.
func (o *BuffEffect) SetBuffGP(ctx context.Context, insert bool, related *Buff) {
	if err := o.SetBuff(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetBuff of the buffEffect to the related item.
// Sets o.R.Buff to related.
// Adds o to related.R.BuffEffects.
func (o *BuffEffect) SetBuff(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Buff) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"buff_effects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"buff_id"}),
		strmangle.WhereClause("\"", "\"", 2, buffEffectPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BuffID = related.ID
	if o.R == nil {
		o.R = &buffEffectR{
			Buff: related,
		}
	} else {
		o.R.Buff = related
	}

	if related.R == nil {
		related.R = &buffR{
			BuffEffects: BuffEffectSlice{o},
		}
	} else {
		related.R.BuffEffects = append(related.R.BuffEffects, o)
	}

	return nil
}

// SetEffectG of the buffEffect to the related item.
// Sets o.R.Effect to related.
// Adds o to related.R.BuffEffects.
// Uses the global database handle.
func (o *BuffEffect) SetEffectG(ctx context.Context, insert bool, related *Effect) error {
	return o.SetEffect(ctx, boil.GetContextDB(), insert, related)
}

// SetEffectP of the buffEffect to the related item.
// Sets o.R.Effect to related.
// Adds o to related.R.BuffEffects.
// Panics on error.
func (o *BuffEffect) SetEffectP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Effect) {
	if err := o.SetEffect(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetEffectGP of the buffEffect to the related item.
// Sets o.R.Effect to related.
// Adds o to related.R.BuffEffects.
// Uses the global database handle and panics on error.
func (o *BuffEffect) SetEffectGP(ctx context.Context, insert bool, related *Effect) {
	if err := o.SetEffect(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetEffect of the buffEffect to the related item.
// Sets o.R.Effect to related.
// Adds o to related.R.BuffEffects.
func (o *BuffEffect) SetEffect(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Effect) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"buff_effects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"effect_id"}),
		strmangle.WhereClause("\"", "\"", 2, buffEffectPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.EffectID = related.ID
	if o.R == nil {
		o.R = &buffEffectR{
			Effect: related,
		}
	} else {
		o.R.Effect = related
	}

	if related.R == nil {
		related.R = &effectR{
			BuffEffects: BuffEffectSlice{o},
		}
	} else {
		related.R.BuffEffects = append(related.R.BuffEffects, o)
	}

	return nil
}

// BuffEffects retrieves all the records using an executor.
func BuffEffects(mods ...qm.QueryMod) buffEffectQuery {
	mods = append(mods, qm.From("\"buff_effects\""), qmhelper.WhereIsNull("\"buff_effects\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"buff_effects\".*"})
	}

	return buffEffectQuery{q}
}

// FindBuffEffectG retrieves a single record by ID.
func FindBuffEffectG(ctx context.Context, iD string, selectCols ...string) (*BuffEffect, error) {
	return FindBuffEffect(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindBuffEffectP retrieves a single record by ID with an executor, and panics on error.
func FindBuffEffectP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *BuffEffect {
	retobj, err := FindBuffEffect(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindBuffEffectGP retrieves a single record by ID, and panics on error.
func FindBuffEffectGP(ctx context.Context, iD string, selectCols ...string) *BuffEffect {
	retobj, err := FindBuffEffect(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindBuffEffect retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBuffEffect(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*BuffEffect, error) {
	buffEffectObj := &BuffEffect{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"buff_effects\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, buffEffectObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from buff_effects")
	}

	if err = buffEffectObj.doAfterSelectHooks(ctx, exec); err != nil {
		return buffEffectObj, err
	}

	return buffEffectObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *BuffEffect) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *BuffEffect) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *BuffEffect) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BuffEffect) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no buff_effects provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(buffEffectColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	buffEffectInsertCacheMut.RLock()
	cache, cached := buffEffectInsertCache[key]
	buffEffectInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			buffEffectAllColumns,
			buffEffectColumnsWithDefault,
			buffEffectColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(buffEffectType, buffEffectMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(buffEffectType, buffEffectMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"buff_effects\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"buff_effects\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into buff_effects")
	}

	if !cached {
		buffEffectInsertCacheMut.Lock()
		buffEffectInsertCache[key] = cache
		buffEffectInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single BuffEffect record using the global executor.
// See Update for more documentation.
func (o *BuffEffect) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the BuffEffect, and panics on error.
// See Update for more documentation.
func (o *BuffEffect) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single BuffEffect record using the global executor. Panics on error.
// See Update for more documentation.
func (o *BuffEffect) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the BuffEffect.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BuffEffect) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	buffEffectUpdateCacheMut.RLock()
	cache, cached := buffEffectUpdateCache[key]
	buffEffectUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			buffEffectAllColumns,
			buffEffectPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update buff_effects, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"buff_effects\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, buffEffectPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(buffEffectType, buffEffectMapping, append(wl, buffEffectPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update buff_effects row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for buff_effects")
	}

	if !cached {
		buffEffectUpdateCacheMut.Lock()
		buffEffectUpdateCache[key] = cache
		buffEffectUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q buffEffectQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q buffEffectQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q buffEffectQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q buffEffectQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for buff_effects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for buff_effects")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o BuffEffectSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o BuffEffectSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o BuffEffectSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BuffEffectSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffEffectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"buff_effects\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, buffEffectPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in buffEffect slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all buffEffect")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *BuffEffect) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *BuffEffect) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *BuffEffect) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BuffEffect) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no buff_effects provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(buffEffectColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	buffEffectUpsertCacheMut.RLock()
	cache, cached := buffEffectUpsertCache[key]
	buffEffectUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			buffEffectAllColumns,
			buffEffectColumnsWithDefault,
			buffEffectColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			buffEffectAllColumns,
			buffEffectPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert buff_effects, could not build update column list")
		}

		ret := strmangle.SetComplement(buffEffectAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(buffEffectPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert buff_effects, could not build conflict column list")
			}

			conflict = make([]string, len(buffEffectPrimaryKeyColumns))
			copy(conflict, buffEffectPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"buff_effects\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(buffEffectType, buffEffectMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(buffEffectType, buffEffectMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert buff_effects")
	}

	if !cached {
		buffEffectUpsertCacheMut.Lock()
		buffEffectUpsertCache[key] = cache
		buffEffectUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single BuffEffect record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *BuffEffect) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single BuffEffect record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *BuffEffect) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single BuffEffect record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *BuffEffect) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single BuffEffect record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BuffEffect) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no BuffEffect provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), buffEffectPrimaryKeyMapping)
		sql = "DELETE FROM \"buff_effects\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"buff_effects\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(buffEffectType, buffEffectMapping, append(wl, buffEffectPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from buff_effects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for buff_effects")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q buffEffectQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q buffEffectQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q buffEffectQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q buffEffectQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no buffEffectQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from buff_effects")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for buff_effects")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o BuffEffectSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o BuffEffectSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o BuffEffectSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BuffEffectSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(buffEffectBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffEffectPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"buff_effects\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, buffEffectPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffEffectPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"buff_effects\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, buffEffectPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from buffEffect slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for buff_effects")
	}

	if len(buffEffectAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *BuffEffect) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("entity: no BuffEffect provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *BuffEffect) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *BuffEffect) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BuffEffect) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindBuffEffect(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BuffEffectSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("entity: empty BuffEffectSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *BuffEffectSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *BuffEffectSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BuffEffectSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BuffEffectSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), buffEffectPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"buff_effects\".* FROM \"buff_effects\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, buffEffectPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in BuffEffectSlice")
	}

	*o = slice

	return nil
}

// BuffEffectExistsG checks if the BuffEffect row exists.
func BuffEffectExistsG(ctx context.Context, iD string) (bool, error) {
	return BuffEffectExists(ctx, boil.GetContextDB(), iD)
}

// BuffEffectExistsP checks if the BuffEffect row exists. Panics on error.
func BuffEffectExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := BuffEffectExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// BuffEffectExistsGP checks if the BuffEffect row exists. Panics on error.
func BuffEffectExistsGP(ctx context.Context, iD string) bool {
	e, err := BuffEffectExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// BuffEffectExists checks if the BuffEffect row exists.
func BuffEffectExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"buff_effects\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if buff_effects exists")
	}

	return exists, nil
}

// Exists checks if the BuffEffect row exists.
func (o *BuffEffect) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return BuffEffectExists(ctx, exec, o.ID)
}
