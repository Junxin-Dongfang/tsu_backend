// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ClassAttributeBonuse is an object representing the database table.
type ClassAttributeBonuse struct {
	ID                 string        `boil:"id" json:"id" toml:"id" yaml:"id"`
	ClassID            string        `boil:"class_id" json:"class_id" toml:"class_id" yaml:"class_id"`
	AttributeID        string        `boil:"attribute_id" json:"attribute_id" toml:"attribute_id" yaml:"attribute_id"`
	BaseBonusValue     types.Decimal `boil:"base_bonus_value" json:"base_bonus_value" toml:"base_bonus_value" yaml:"base_bonus_value"`
	PerLevelBonusValue types.Decimal `boil:"per_level_bonus_value" json:"per_level_bonus_value" toml:"per_level_bonus_value" yaml:"per_level_bonus_value"`
	CreatedAt          null.Time     `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt          null.Time     `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *classAttributeBonuseR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L classAttributeBonuseL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ClassAttributeBonuseColumns = struct {
	ID                 string
	ClassID            string
	AttributeID        string
	BaseBonusValue     string
	PerLevelBonusValue string
	CreatedAt          string
	UpdatedAt          string
}{
	ID:                 "id",
	ClassID:            "class_id",
	AttributeID:        "attribute_id",
	BaseBonusValue:     "base_bonus_value",
	PerLevelBonusValue: "per_level_bonus_value",
	CreatedAt:          "created_at",
	UpdatedAt:          "updated_at",
}

var ClassAttributeBonuseTableColumns = struct {
	ID                 string
	ClassID            string
	AttributeID        string
	BaseBonusValue     string
	PerLevelBonusValue string
	CreatedAt          string
	UpdatedAt          string
}{
	ID:                 "class_attribute_bonuses.id",
	ClassID:            "class_attribute_bonuses.class_id",
	AttributeID:        "class_attribute_bonuses.attribute_id",
	BaseBonusValue:     "class_attribute_bonuses.base_bonus_value",
	PerLevelBonusValue: "class_attribute_bonuses.per_level_bonus_value",
	CreatedAt:          "class_attribute_bonuses.created_at",
	UpdatedAt:          "class_attribute_bonuses.updated_at",
}

// Generated where

type whereHelpertypes_Decimal struct{ field string }

func (w whereHelpertypes_Decimal) EQ(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertypes_Decimal) NEQ(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertypes_Decimal) LT(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_Decimal) LTE(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_Decimal) GT(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_Decimal) GTE(x types.Decimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var ClassAttributeBonuseWhere = struct {
	ID                 whereHelperstring
	ClassID            whereHelperstring
	AttributeID        whereHelperstring
	BaseBonusValue     whereHelpertypes_Decimal
	PerLevelBonusValue whereHelpertypes_Decimal
	CreatedAt          whereHelpernull_Time
	UpdatedAt          whereHelpernull_Time
}{
	ID:                 whereHelperstring{field: "\"class_attribute_bonuses\".\"id\""},
	ClassID:            whereHelperstring{field: "\"class_attribute_bonuses\".\"class_id\""},
	AttributeID:        whereHelperstring{field: "\"class_attribute_bonuses\".\"attribute_id\""},
	BaseBonusValue:     whereHelpertypes_Decimal{field: "\"class_attribute_bonuses\".\"base_bonus_value\""},
	PerLevelBonusValue: whereHelpertypes_Decimal{field: "\"class_attribute_bonuses\".\"per_level_bonus_value\""},
	CreatedAt:          whereHelpernull_Time{field: "\"class_attribute_bonuses\".\"created_at\""},
	UpdatedAt:          whereHelpernull_Time{field: "\"class_attribute_bonuses\".\"updated_at\""},
}

// ClassAttributeBonuseRels is where relationship names are stored.
var ClassAttributeBonuseRels = struct {
	Attribute string
	Class     string
}{
	Attribute: "Attribute",
	Class:     "Class",
}

// classAttributeBonuseR is where relationships are stored.
type classAttributeBonuseR struct {
	Attribute *HeroAttributeType `boil:"Attribute" json:"Attribute" toml:"Attribute" yaml:"Attribute"`
	Class     *Class             `boil:"Class" json:"Class" toml:"Class" yaml:"Class"`
}

// NewStruct creates a new relationship struct
func (*classAttributeBonuseR) NewStruct() *classAttributeBonuseR {
	return &classAttributeBonuseR{}
}

func (o *ClassAttributeBonuse) GetAttribute() *HeroAttributeType {
	if o == nil {
		return nil
	}

	return o.R.GetAttribute()
}

func (r *classAttributeBonuseR) GetAttribute() *HeroAttributeType {
	if r == nil {
		return nil
	}

	return r.Attribute
}

func (o *ClassAttributeBonuse) GetClass() *Class {
	if o == nil {
		return nil
	}

	return o.R.GetClass()
}

func (r *classAttributeBonuseR) GetClass() *Class {
	if r == nil {
		return nil
	}

	return r.Class
}

// classAttributeBonuseL is where Load methods for each relationship are stored.
type classAttributeBonuseL struct{}

var (
	classAttributeBonuseAllColumns            = []string{"id", "class_id", "attribute_id", "base_bonus_value", "per_level_bonus_value", "created_at", "updated_at"}
	classAttributeBonuseColumnsWithoutDefault = []string{"class_id", "attribute_id", "base_bonus_value", "per_level_bonus_value"}
	classAttributeBonuseColumnsWithDefault    = []string{"id", "created_at", "updated_at"}
	classAttributeBonusePrimaryKeyColumns     = []string{"id"}
	classAttributeBonuseGeneratedColumns      = []string{}
)

type (
	// ClassAttributeBonuseSlice is an alias for a slice of pointers to ClassAttributeBonuse.
	// This should almost always be used instead of []ClassAttributeBonuse.
	ClassAttributeBonuseSlice []*ClassAttributeBonuse
	// ClassAttributeBonuseHook is the signature for custom ClassAttributeBonuse hook methods
	ClassAttributeBonuseHook func(context.Context, boil.ContextExecutor, *ClassAttributeBonuse) error

	classAttributeBonuseQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	classAttributeBonuseType                 = reflect.TypeOf(&ClassAttributeBonuse{})
	classAttributeBonuseMapping              = queries.MakeStructMapping(classAttributeBonuseType)
	classAttributeBonusePrimaryKeyMapping, _ = queries.BindMapping(classAttributeBonuseType, classAttributeBonuseMapping, classAttributeBonusePrimaryKeyColumns)
	classAttributeBonuseInsertCacheMut       sync.RWMutex
	classAttributeBonuseInsertCache          = make(map[string]insertCache)
	classAttributeBonuseUpdateCacheMut       sync.RWMutex
	classAttributeBonuseUpdateCache          = make(map[string]updateCache)
	classAttributeBonuseUpsertCacheMut       sync.RWMutex
	classAttributeBonuseUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var classAttributeBonuseAfterSelectMu sync.Mutex
var classAttributeBonuseAfterSelectHooks []ClassAttributeBonuseHook

var classAttributeBonuseBeforeInsertMu sync.Mutex
var classAttributeBonuseBeforeInsertHooks []ClassAttributeBonuseHook
var classAttributeBonuseAfterInsertMu sync.Mutex
var classAttributeBonuseAfterInsertHooks []ClassAttributeBonuseHook

var classAttributeBonuseBeforeUpdateMu sync.Mutex
var classAttributeBonuseBeforeUpdateHooks []ClassAttributeBonuseHook
var classAttributeBonuseAfterUpdateMu sync.Mutex
var classAttributeBonuseAfterUpdateHooks []ClassAttributeBonuseHook

var classAttributeBonuseBeforeDeleteMu sync.Mutex
var classAttributeBonuseBeforeDeleteHooks []ClassAttributeBonuseHook
var classAttributeBonuseAfterDeleteMu sync.Mutex
var classAttributeBonuseAfterDeleteHooks []ClassAttributeBonuseHook

var classAttributeBonuseBeforeUpsertMu sync.Mutex
var classAttributeBonuseBeforeUpsertHooks []ClassAttributeBonuseHook
var classAttributeBonuseAfterUpsertMu sync.Mutex
var classAttributeBonuseAfterUpsertHooks []ClassAttributeBonuseHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ClassAttributeBonuse) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ClassAttributeBonuse) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ClassAttributeBonuse) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ClassAttributeBonuse) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ClassAttributeBonuse) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ClassAttributeBonuse) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ClassAttributeBonuse) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ClassAttributeBonuse) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ClassAttributeBonuse) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range classAttributeBonuseAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddClassAttributeBonuseHook registers your hook function for all future operations.
func AddClassAttributeBonuseHook(hookPoint boil.HookPoint, classAttributeBonuseHook ClassAttributeBonuseHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		classAttributeBonuseAfterSelectMu.Lock()
		classAttributeBonuseAfterSelectHooks = append(classAttributeBonuseAfterSelectHooks, classAttributeBonuseHook)
		classAttributeBonuseAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		classAttributeBonuseBeforeInsertMu.Lock()
		classAttributeBonuseBeforeInsertHooks = append(classAttributeBonuseBeforeInsertHooks, classAttributeBonuseHook)
		classAttributeBonuseBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		classAttributeBonuseAfterInsertMu.Lock()
		classAttributeBonuseAfterInsertHooks = append(classAttributeBonuseAfterInsertHooks, classAttributeBonuseHook)
		classAttributeBonuseAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		classAttributeBonuseBeforeUpdateMu.Lock()
		classAttributeBonuseBeforeUpdateHooks = append(classAttributeBonuseBeforeUpdateHooks, classAttributeBonuseHook)
		classAttributeBonuseBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		classAttributeBonuseAfterUpdateMu.Lock()
		classAttributeBonuseAfterUpdateHooks = append(classAttributeBonuseAfterUpdateHooks, classAttributeBonuseHook)
		classAttributeBonuseAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		classAttributeBonuseBeforeDeleteMu.Lock()
		classAttributeBonuseBeforeDeleteHooks = append(classAttributeBonuseBeforeDeleteHooks, classAttributeBonuseHook)
		classAttributeBonuseBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		classAttributeBonuseAfterDeleteMu.Lock()
		classAttributeBonuseAfterDeleteHooks = append(classAttributeBonuseAfterDeleteHooks, classAttributeBonuseHook)
		classAttributeBonuseAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		classAttributeBonuseBeforeUpsertMu.Lock()
		classAttributeBonuseBeforeUpsertHooks = append(classAttributeBonuseBeforeUpsertHooks, classAttributeBonuseHook)
		classAttributeBonuseBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		classAttributeBonuseAfterUpsertMu.Lock()
		classAttributeBonuseAfterUpsertHooks = append(classAttributeBonuseAfterUpsertHooks, classAttributeBonuseHook)
		classAttributeBonuseAfterUpsertMu.Unlock()
	}
}

// One returns a single classAttributeBonuse record from the query.
func (q classAttributeBonuseQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ClassAttributeBonuse, error) {
	o := &ClassAttributeBonuse{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for class_attribute_bonuses")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all ClassAttributeBonuse records from the query.
func (q classAttributeBonuseQuery) All(ctx context.Context, exec boil.ContextExecutor) (ClassAttributeBonuseSlice, error) {
	var o []*ClassAttributeBonuse

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to ClassAttributeBonuse slice")
	}

	if len(classAttributeBonuseAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all ClassAttributeBonuse records in the query.
func (q classAttributeBonuseQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count class_attribute_bonuses rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q classAttributeBonuseQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if class_attribute_bonuses exists")
	}

	return count > 0, nil
}

// Attribute pointed to by the foreign key.
func (o *ClassAttributeBonuse) Attribute(mods ...qm.QueryMod) heroAttributeTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.AttributeID),
	}

	queryMods = append(queryMods, mods...)

	return HeroAttributeTypes(queryMods...)
}

// Class pointed to by the foreign key.
func (o *ClassAttributeBonuse) Class(mods ...qm.QueryMod) classQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ClassID),
	}

	queryMods = append(queryMods, mods...)

	return Classes(queryMods...)
}

// LoadAttribute allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (classAttributeBonuseL) LoadAttribute(ctx context.Context, e boil.ContextExecutor, singular bool, maybeClassAttributeBonuse interface{}, mods queries.Applicator) error {
	var slice []*ClassAttributeBonuse
	var object *ClassAttributeBonuse

	if singular {
		var ok bool
		object, ok = maybeClassAttributeBonuse.(*ClassAttributeBonuse)
		if !ok {
			object = new(ClassAttributeBonuse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeClassAttributeBonuse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeClassAttributeBonuse))
			}
		}
	} else {
		s, ok := maybeClassAttributeBonuse.(*[]*ClassAttributeBonuse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeClassAttributeBonuse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeClassAttributeBonuse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &classAttributeBonuseR{}
		}
		args[object.AttributeID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &classAttributeBonuseR{}
			}

			args[obj.AttributeID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`hero_attribute_type`),
		qm.WhereIn(`hero_attribute_type.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load HeroAttributeType")
	}

	var resultSlice []*HeroAttributeType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice HeroAttributeType")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for hero_attribute_type")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for hero_attribute_type")
	}

	if len(heroAttributeTypeAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Attribute = foreign
		if foreign.R == nil {
			foreign.R = &heroAttributeTypeR{}
		}
		foreign.R.AttributeClassAttributeBonuses = append(foreign.R.AttributeClassAttributeBonuses, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.AttributeID == foreign.ID {
				local.R.Attribute = foreign
				if foreign.R == nil {
					foreign.R = &heroAttributeTypeR{}
				}
				foreign.R.AttributeClassAttributeBonuses = append(foreign.R.AttributeClassAttributeBonuses, local)
				break
			}
		}
	}

	return nil
}

// LoadClass allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (classAttributeBonuseL) LoadClass(ctx context.Context, e boil.ContextExecutor, singular bool, maybeClassAttributeBonuse interface{}, mods queries.Applicator) error {
	var slice []*ClassAttributeBonuse
	var object *ClassAttributeBonuse

	if singular {
		var ok bool
		object, ok = maybeClassAttributeBonuse.(*ClassAttributeBonuse)
		if !ok {
			object = new(ClassAttributeBonuse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeClassAttributeBonuse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeClassAttributeBonuse))
			}
		}
	} else {
		s, ok := maybeClassAttributeBonuse.(*[]*ClassAttributeBonuse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeClassAttributeBonuse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeClassAttributeBonuse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &classAttributeBonuseR{}
		}
		args[object.ClassID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &classAttributeBonuseR{}
			}

			args[obj.ClassID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`classes`),
		qm.WhereIn(`classes.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Class")
	}

	var resultSlice []*Class
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Class")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for classes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for classes")
	}

	if len(classAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Class = foreign
		if foreign.R == nil {
			foreign.R = &classR{}
		}
		foreign.R.ClassAttributeBonuses = append(foreign.R.ClassAttributeBonuses, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ClassID == foreign.ID {
				local.R.Class = foreign
				if foreign.R == nil {
					foreign.R = &classR{}
				}
				foreign.R.ClassAttributeBonuses = append(foreign.R.ClassAttributeBonuses, local)
				break
			}
		}
	}

	return nil
}

// SetAttribute of the classAttributeBonuse to the related item.
// Sets o.R.Attribute to related.
// Adds o to related.R.AttributeClassAttributeBonuses.
func (o *ClassAttributeBonuse) SetAttribute(ctx context.Context, exec boil.ContextExecutor, insert bool, related *HeroAttributeType) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"class_attribute_bonuses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"attribute_id"}),
		strmangle.WhereClause("\"", "\"", 2, classAttributeBonusePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.AttributeID = related.ID
	if o.R == nil {
		o.R = &classAttributeBonuseR{
			Attribute: related,
		}
	} else {
		o.R.Attribute = related
	}

	if related.R == nil {
		related.R = &heroAttributeTypeR{
			AttributeClassAttributeBonuses: ClassAttributeBonuseSlice{o},
		}
	} else {
		related.R.AttributeClassAttributeBonuses = append(related.R.AttributeClassAttributeBonuses, o)
	}

	return nil
}

// SetClass of the classAttributeBonuse to the related item.
// Sets o.R.Class to related.
// Adds o to related.R.ClassAttributeBonuses.
func (o *ClassAttributeBonuse) SetClass(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Class) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"class_attribute_bonuses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"class_id"}),
		strmangle.WhereClause("\"", "\"", 2, classAttributeBonusePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ClassID = related.ID
	if o.R == nil {
		o.R = &classAttributeBonuseR{
			Class: related,
		}
	} else {
		o.R.Class = related
	}

	if related.R == nil {
		related.R = &classR{
			ClassAttributeBonuses: ClassAttributeBonuseSlice{o},
		}
	} else {
		related.R.ClassAttributeBonuses = append(related.R.ClassAttributeBonuses, o)
	}

	return nil
}

// ClassAttributeBonuses retrieves all the records using an executor.
func ClassAttributeBonuses(mods ...qm.QueryMod) classAttributeBonuseQuery {
	mods = append(mods, qm.From("\"class_attribute_bonuses\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"class_attribute_bonuses\".*"})
	}

	return classAttributeBonuseQuery{q}
}

// FindClassAttributeBonuse retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindClassAttributeBonuse(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*ClassAttributeBonuse, error) {
	classAttributeBonuseObj := &ClassAttributeBonuse{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"class_attribute_bonuses\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, classAttributeBonuseObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from class_attribute_bonuses")
	}

	if err = classAttributeBonuseObj.doAfterSelectHooks(ctx, exec); err != nil {
		return classAttributeBonuseObj, err
	}

	return classAttributeBonuseObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ClassAttributeBonuse) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no class_attribute_bonuses provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(classAttributeBonuseColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	classAttributeBonuseInsertCacheMut.RLock()
	cache, cached := classAttributeBonuseInsertCache[key]
	classAttributeBonuseInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			classAttributeBonuseAllColumns,
			classAttributeBonuseColumnsWithDefault,
			classAttributeBonuseColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(classAttributeBonuseType, classAttributeBonuseMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(classAttributeBonuseType, classAttributeBonuseMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"class_attribute_bonuses\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"class_attribute_bonuses\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into class_attribute_bonuses")
	}

	if !cached {
		classAttributeBonuseInsertCacheMut.Lock()
		classAttributeBonuseInsertCache[key] = cache
		classAttributeBonuseInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the ClassAttributeBonuse.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ClassAttributeBonuse) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	classAttributeBonuseUpdateCacheMut.RLock()
	cache, cached := classAttributeBonuseUpdateCache[key]
	classAttributeBonuseUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			classAttributeBonuseAllColumns,
			classAttributeBonusePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update class_attribute_bonuses, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"class_attribute_bonuses\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, classAttributeBonusePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(classAttributeBonuseType, classAttributeBonuseMapping, append(wl, classAttributeBonusePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update class_attribute_bonuses row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for class_attribute_bonuses")
	}

	if !cached {
		classAttributeBonuseUpdateCacheMut.Lock()
		classAttributeBonuseUpdateCache[key] = cache
		classAttributeBonuseUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q classAttributeBonuseQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for class_attribute_bonuses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for class_attribute_bonuses")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ClassAttributeBonuseSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), classAttributeBonusePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"class_attribute_bonuses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, classAttributeBonusePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in classAttributeBonuse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all classAttributeBonuse")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ClassAttributeBonuse) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no class_attribute_bonuses provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(classAttributeBonuseColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	classAttributeBonuseUpsertCacheMut.RLock()
	cache, cached := classAttributeBonuseUpsertCache[key]
	classAttributeBonuseUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			classAttributeBonuseAllColumns,
			classAttributeBonuseColumnsWithDefault,
			classAttributeBonuseColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			classAttributeBonuseAllColumns,
			classAttributeBonusePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert class_attribute_bonuses, could not build update column list")
		}

		ret := strmangle.SetComplement(classAttributeBonuseAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(classAttributeBonusePrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert class_attribute_bonuses, could not build conflict column list")
			}

			conflict = make([]string, len(classAttributeBonusePrimaryKeyColumns))
			copy(conflict, classAttributeBonusePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"class_attribute_bonuses\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(classAttributeBonuseType, classAttributeBonuseMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(classAttributeBonuseType, classAttributeBonuseMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert class_attribute_bonuses")
	}

	if !cached {
		classAttributeBonuseUpsertCacheMut.Lock()
		classAttributeBonuseUpsertCache[key] = cache
		classAttributeBonuseUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single ClassAttributeBonuse record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ClassAttributeBonuse) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no ClassAttributeBonuse provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), classAttributeBonusePrimaryKeyMapping)
	sql := "DELETE FROM \"class_attribute_bonuses\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from class_attribute_bonuses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for class_attribute_bonuses")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q classAttributeBonuseQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no classAttributeBonuseQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from class_attribute_bonuses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for class_attribute_bonuses")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ClassAttributeBonuseSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(classAttributeBonuseBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), classAttributeBonusePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"class_attribute_bonuses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, classAttributeBonusePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from classAttributeBonuse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for class_attribute_bonuses")
	}

	if len(classAttributeBonuseAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ClassAttributeBonuse) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindClassAttributeBonuse(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ClassAttributeBonuseSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ClassAttributeBonuseSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), classAttributeBonusePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"class_attribute_bonuses\".* FROM \"class_attribute_bonuses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, classAttributeBonusePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in ClassAttributeBonuseSlice")
	}

	*o = slice

	return nil
}

// ClassAttributeBonuseExists checks if the ClassAttributeBonuse row exists.
func ClassAttributeBonuseExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"class_attribute_bonuses\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if class_attribute_bonuses exists")
	}

	return exists, nil
}

// Exists checks if the ClassAttributeBonuse row exists.
func (o *ClassAttributeBonuse) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ClassAttributeBonuseExists(ctx, exec, o.ID)
}
