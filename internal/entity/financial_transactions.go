// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// FinancialTransaction is an object representing the database table.
type FinancialTransaction struct {
	ID              string        `boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID          string        `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	TransactionType string        `boil:"transaction_type" json:"transaction_type" toml:"transaction_type" yaml:"transaction_type"`
	Amount          types.Decimal `boil:"amount" json:"amount" toml:"amount" yaml:"amount"`
	Currency        string        `boil:"currency" json:"currency" toml:"currency" yaml:"currency"`
	Diamonds        null.Int64    `boil:"diamonds" json:"diamonds,omitempty" toml:"diamonds" yaml:"diamonds,omitempty"`
	Description     null.String   `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	PaymentProvider null.String   `boil:"payment_provider" json:"payment_provider,omitempty" toml:"payment_provider" yaml:"payment_provider,omitempty"`
	PaymentParams   null.JSON     `boil:"payment_params" json:"payment_params,omitempty" toml:"payment_params" yaml:"payment_params,omitempty"`
	TransactionTime time.Time     `boil:"transaction_time" json:"transaction_time" toml:"transaction_time" yaml:"transaction_time"`
	CreatedAt       time.Time     `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt       time.Time     `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt       null.Time     `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *financialTransactionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L financialTransactionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FinancialTransactionColumns = struct {
	ID              string
	UserID          string
	TransactionType string
	Amount          string
	Currency        string
	Diamonds        string
	Description     string
	PaymentProvider string
	PaymentParams   string
	TransactionTime string
	CreatedAt       string
	UpdatedAt       string
	DeletedAt       string
}{
	ID:              "id",
	UserID:          "user_id",
	TransactionType: "transaction_type",
	Amount:          "amount",
	Currency:        "currency",
	Diamonds:        "diamonds",
	Description:     "description",
	PaymentProvider: "payment_provider",
	PaymentParams:   "payment_params",
	TransactionTime: "transaction_time",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
	DeletedAt:       "deleted_at",
}

var FinancialTransactionTableColumns = struct {
	ID              string
	UserID          string
	TransactionType string
	Amount          string
	Currency        string
	Diamonds        string
	Description     string
	PaymentProvider string
	PaymentParams   string
	TransactionTime string
	CreatedAt       string
	UpdatedAt       string
	DeletedAt       string
}{
	ID:              "financial_transactions.id",
	UserID:          "financial_transactions.user_id",
	TransactionType: "financial_transactions.transaction_type",
	Amount:          "financial_transactions.amount",
	Currency:        "financial_transactions.currency",
	Diamonds:        "financial_transactions.diamonds",
	Description:     "financial_transactions.description",
	PaymentProvider: "financial_transactions.payment_provider",
	PaymentParams:   "financial_transactions.payment_params",
	TransactionTime: "financial_transactions.transaction_time",
	CreatedAt:       "financial_transactions.created_at",
	UpdatedAt:       "financial_transactions.updated_at",
	DeletedAt:       "financial_transactions.deleted_at",
}

// Generated where

type whereHelpernull_Int64 struct{ field string }

func (w whereHelpernull_Int64) EQ(x null.Int64) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Int64) NEQ(x null.Int64) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Int64) LT(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Int64) LTE(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Int64) GT(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Int64) GTE(x null.Int64) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_Int64) IN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_Int64) NIN(slice []int64) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_Int64) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Int64) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelpertime_Time struct{ field string }

func (w whereHelpertime_Time) EQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.EQ, x)
}
func (w whereHelpertime_Time) NEQ(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.NEQ, x)
}
func (w whereHelpertime_Time) LT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertime_Time) LTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertime_Time) GT(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertime_Time) GTE(x time.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

var FinancialTransactionWhere = struct {
	ID              whereHelperstring
	UserID          whereHelperstring
	TransactionType whereHelperstring
	Amount          whereHelpertypes_Decimal
	Currency        whereHelperstring
	Diamonds        whereHelpernull_Int64
	Description     whereHelpernull_String
	PaymentProvider whereHelpernull_String
	PaymentParams   whereHelpernull_JSON
	TransactionTime whereHelpertime_Time
	CreatedAt       whereHelpertime_Time
	UpdatedAt       whereHelpertime_Time
	DeletedAt       whereHelpernull_Time
}{
	ID:              whereHelperstring{field: "\"financial_transactions\".\"id\""},
	UserID:          whereHelperstring{field: "\"financial_transactions\".\"user_id\""},
	TransactionType: whereHelperstring{field: "\"financial_transactions\".\"transaction_type\""},
	Amount:          whereHelpertypes_Decimal{field: "\"financial_transactions\".\"amount\""},
	Currency:        whereHelperstring{field: "\"financial_transactions\".\"currency\""},
	Diamonds:        whereHelpernull_Int64{field: "\"financial_transactions\".\"diamonds\""},
	Description:     whereHelpernull_String{field: "\"financial_transactions\".\"description\""},
	PaymentProvider: whereHelpernull_String{field: "\"financial_transactions\".\"payment_provider\""},
	PaymentParams:   whereHelpernull_JSON{field: "\"financial_transactions\".\"payment_params\""},
	TransactionTime: whereHelpertime_Time{field: "\"financial_transactions\".\"transaction_time\""},
	CreatedAt:       whereHelpertime_Time{field: "\"financial_transactions\".\"created_at\""},
	UpdatedAt:       whereHelpertime_Time{field: "\"financial_transactions\".\"updated_at\""},
	DeletedAt:       whereHelpernull_Time{field: "\"financial_transactions\".\"deleted_at\""},
}

// FinancialTransactionRels is where relationship names are stored.
var FinancialTransactionRels = struct {
	User string
}{
	User: "User",
}

// financialTransactionR is where relationships are stored.
type financialTransactionR struct {
	User *User `boil:"User" json:"User" toml:"User" yaml:"User"`
}

// NewStruct creates a new relationship struct
func (*financialTransactionR) NewStruct() *financialTransactionR {
	return &financialTransactionR{}
}

func (o *FinancialTransaction) GetUser() *User {
	if o == nil {
		return nil
	}

	return o.R.GetUser()
}

func (r *financialTransactionR) GetUser() *User {
	if r == nil {
		return nil
	}

	return r.User
}

// financialTransactionL is where Load methods for each relationship are stored.
type financialTransactionL struct{}

var (
	financialTransactionAllColumns            = []string{"id", "user_id", "transaction_type", "amount", "currency", "diamonds", "description", "payment_provider", "payment_params", "transaction_time", "created_at", "updated_at", "deleted_at"}
	financialTransactionColumnsWithoutDefault = []string{"user_id", "transaction_type", "amount"}
	financialTransactionColumnsWithDefault    = []string{"id", "currency", "diamonds", "description", "payment_provider", "payment_params", "transaction_time", "created_at", "updated_at", "deleted_at"}
	financialTransactionPrimaryKeyColumns     = []string{"id"}
	financialTransactionGeneratedColumns      = []string{}
)

type (
	// FinancialTransactionSlice is an alias for a slice of pointers to FinancialTransaction.
	// This should almost always be used instead of []FinancialTransaction.
	FinancialTransactionSlice []*FinancialTransaction
	// FinancialTransactionHook is the signature for custom FinancialTransaction hook methods
	FinancialTransactionHook func(context.Context, boil.ContextExecutor, *FinancialTransaction) error

	financialTransactionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	financialTransactionType                 = reflect.TypeOf(&FinancialTransaction{})
	financialTransactionMapping              = queries.MakeStructMapping(financialTransactionType)
	financialTransactionPrimaryKeyMapping, _ = queries.BindMapping(financialTransactionType, financialTransactionMapping, financialTransactionPrimaryKeyColumns)
	financialTransactionInsertCacheMut       sync.RWMutex
	financialTransactionInsertCache          = make(map[string]insertCache)
	financialTransactionUpdateCacheMut       sync.RWMutex
	financialTransactionUpdateCache          = make(map[string]updateCache)
	financialTransactionUpsertCacheMut       sync.RWMutex
	financialTransactionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var financialTransactionAfterSelectMu sync.Mutex
var financialTransactionAfterSelectHooks []FinancialTransactionHook

var financialTransactionBeforeInsertMu sync.Mutex
var financialTransactionBeforeInsertHooks []FinancialTransactionHook
var financialTransactionAfterInsertMu sync.Mutex
var financialTransactionAfterInsertHooks []FinancialTransactionHook

var financialTransactionBeforeUpdateMu sync.Mutex
var financialTransactionBeforeUpdateHooks []FinancialTransactionHook
var financialTransactionAfterUpdateMu sync.Mutex
var financialTransactionAfterUpdateHooks []FinancialTransactionHook

var financialTransactionBeforeDeleteMu sync.Mutex
var financialTransactionBeforeDeleteHooks []FinancialTransactionHook
var financialTransactionAfterDeleteMu sync.Mutex
var financialTransactionAfterDeleteHooks []FinancialTransactionHook

var financialTransactionBeforeUpsertMu sync.Mutex
var financialTransactionBeforeUpsertHooks []FinancialTransactionHook
var financialTransactionAfterUpsertMu sync.Mutex
var financialTransactionAfterUpsertHooks []FinancialTransactionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *FinancialTransaction) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *FinancialTransaction) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *FinancialTransaction) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *FinancialTransaction) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *FinancialTransaction) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *FinancialTransaction) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *FinancialTransaction) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *FinancialTransaction) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *FinancialTransaction) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range financialTransactionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFinancialTransactionHook registers your hook function for all future operations.
func AddFinancialTransactionHook(hookPoint boil.HookPoint, financialTransactionHook FinancialTransactionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		financialTransactionAfterSelectMu.Lock()
		financialTransactionAfterSelectHooks = append(financialTransactionAfterSelectHooks, financialTransactionHook)
		financialTransactionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		financialTransactionBeforeInsertMu.Lock()
		financialTransactionBeforeInsertHooks = append(financialTransactionBeforeInsertHooks, financialTransactionHook)
		financialTransactionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		financialTransactionAfterInsertMu.Lock()
		financialTransactionAfterInsertHooks = append(financialTransactionAfterInsertHooks, financialTransactionHook)
		financialTransactionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		financialTransactionBeforeUpdateMu.Lock()
		financialTransactionBeforeUpdateHooks = append(financialTransactionBeforeUpdateHooks, financialTransactionHook)
		financialTransactionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		financialTransactionAfterUpdateMu.Lock()
		financialTransactionAfterUpdateHooks = append(financialTransactionAfterUpdateHooks, financialTransactionHook)
		financialTransactionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		financialTransactionBeforeDeleteMu.Lock()
		financialTransactionBeforeDeleteHooks = append(financialTransactionBeforeDeleteHooks, financialTransactionHook)
		financialTransactionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		financialTransactionAfterDeleteMu.Lock()
		financialTransactionAfterDeleteHooks = append(financialTransactionAfterDeleteHooks, financialTransactionHook)
		financialTransactionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		financialTransactionBeforeUpsertMu.Lock()
		financialTransactionBeforeUpsertHooks = append(financialTransactionBeforeUpsertHooks, financialTransactionHook)
		financialTransactionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		financialTransactionAfterUpsertMu.Lock()
		financialTransactionAfterUpsertHooks = append(financialTransactionAfterUpsertHooks, financialTransactionHook)
		financialTransactionAfterUpsertMu.Unlock()
	}
}

// One returns a single financialTransaction record from the query.
func (q financialTransactionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*FinancialTransaction, error) {
	o := &FinancialTransaction{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: failed to execute a one query for financial_transactions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all FinancialTransaction records from the query.
func (q financialTransactionQuery) All(ctx context.Context, exec boil.ContextExecutor) (FinancialTransactionSlice, error) {
	var o []*FinancialTransaction

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "models: failed to assign all query results to FinancialTransaction slice")
	}

	if len(financialTransactionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all FinancialTransaction records in the query.
func (q financialTransactionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to count financial_transactions rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q financialTransactionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "models: failed to check if financial_transactions exists")
	}

	return count > 0, nil
}

// User pointed to by the foreign key.
func (o *FinancialTransaction) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	return Users(queryMods...)
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (financialTransactionL) LoadUser(ctx context.Context, e boil.ContextExecutor, singular bool, maybeFinancialTransaction interface{}, mods queries.Applicator) error {
	var slice []*FinancialTransaction
	var object *FinancialTransaction

	if singular {
		var ok bool
		object, ok = maybeFinancialTransaction.(*FinancialTransaction)
		if !ok {
			object = new(FinancialTransaction)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeFinancialTransaction)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeFinancialTransaction))
			}
		}
	} else {
		s, ok := maybeFinancialTransaction.(*[]*FinancialTransaction)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeFinancialTransaction)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeFinancialTransaction))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &financialTransactionR{}
		}
		args[object.UserID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &financialTransactionR{}
			}

			args[obj.UserID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`users`),
		qm.WhereIn(`users.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(userAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.FinancialTransactions = append(foreign.R.FinancialTransactions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.FinancialTransactions = append(foreign.R.FinancialTransactions, local)
				break
			}
		}
	}

	return nil
}

// SetUser of the financialTransaction to the related item.
// Sets o.R.User to related.
// Adds o to related.R.FinancialTransactions.
func (o *FinancialTransaction) SetUser(ctx context.Context, exec boil.ContextExecutor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"financial_transactions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, financialTransactionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &financialTransactionR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			FinancialTransactions: FinancialTransactionSlice{o},
		}
	} else {
		related.R.FinancialTransactions = append(related.R.FinancialTransactions, o)
	}

	return nil
}

// FinancialTransactions retrieves all the records using an executor.
func FinancialTransactions(mods ...qm.QueryMod) financialTransactionQuery {
	mods = append(mods, qm.From("\"financial_transactions\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"financial_transactions\".*"})
	}

	return financialTransactionQuery{q}
}

// FindFinancialTransaction retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFinancialTransaction(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*FinancialTransaction, error) {
	financialTransactionObj := &FinancialTransaction{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"financial_transactions\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, financialTransactionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "models: unable to select from financial_transactions")
	}

	if err = financialTransactionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return financialTransactionObj, err
	}

	return financialTransactionObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *FinancialTransaction) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("models: no financial_transactions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(financialTransactionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	financialTransactionInsertCacheMut.RLock()
	cache, cached := financialTransactionInsertCache[key]
	financialTransactionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			financialTransactionAllColumns,
			financialTransactionColumnsWithDefault,
			financialTransactionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(financialTransactionType, financialTransactionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(financialTransactionType, financialTransactionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"financial_transactions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"financial_transactions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "models: unable to insert into financial_transactions")
	}

	if !cached {
		financialTransactionInsertCacheMut.Lock()
		financialTransactionInsertCache[key] = cache
		financialTransactionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// Update uses an executor to update the FinancialTransaction.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *FinancialTransaction) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	financialTransactionUpdateCacheMut.RLock()
	cache, cached := financialTransactionUpdateCache[key]
	financialTransactionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			financialTransactionAllColumns,
			financialTransactionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("models: unable to update financial_transactions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"financial_transactions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, financialTransactionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(financialTransactionType, financialTransactionMapping, append(wl, financialTransactionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update financial_transactions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by update for financial_transactions")
	}

	if !cached {
		financialTransactionUpdateCacheMut.Lock()
		financialTransactionUpdateCache[key] = cache
		financialTransactionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAll updates all rows with the specified column values.
func (q financialTransactionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all for financial_transactions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected for financial_transactions")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FinancialTransactionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("models: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), financialTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"financial_transactions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, financialTransactionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to update all in financialTransaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to retrieve rows affected all in update all financialTransaction")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *FinancialTransaction) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("models: no financial_transactions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(financialTransactionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	financialTransactionUpsertCacheMut.RLock()
	cache, cached := financialTransactionUpsertCache[key]
	financialTransactionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			financialTransactionAllColumns,
			financialTransactionColumnsWithDefault,
			financialTransactionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			financialTransactionAllColumns,
			financialTransactionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("models: unable to upsert financial_transactions, could not build update column list")
		}

		ret := strmangle.SetComplement(financialTransactionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(financialTransactionPrimaryKeyColumns) == 0 {
				return errors.New("models: unable to upsert financial_transactions, could not build conflict column list")
			}

			conflict = make([]string, len(financialTransactionPrimaryKeyColumns))
			copy(conflict, financialTransactionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"financial_transactions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(financialTransactionType, financialTransactionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(financialTransactionType, financialTransactionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "models: unable to upsert financial_transactions")
	}

	if !cached {
		financialTransactionUpsertCacheMut.Lock()
		financialTransactionUpsertCache[key] = cache
		financialTransactionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// Delete deletes a single FinancialTransaction record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *FinancialTransaction) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("models: no FinancialTransaction provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), financialTransactionPrimaryKeyMapping)
	sql := "DELETE FROM \"financial_transactions\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete from financial_transactions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by delete for financial_transactions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q financialTransactionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("models: no financialTransactionQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from financial_transactions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for financial_transactions")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FinancialTransactionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(financialTransactionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), financialTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"financial_transactions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, financialTransactionPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "models: unable to delete all from financialTransaction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "models: failed to get rows affected by deleteall for financial_transactions")
	}

	if len(financialTransactionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *FinancialTransaction) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindFinancialTransaction(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FinancialTransactionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FinancialTransactionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), financialTransactionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"financial_transactions\".* FROM \"financial_transactions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, financialTransactionPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "models: unable to reload all in FinancialTransactionSlice")
	}

	*o = slice

	return nil
}

// FinancialTransactionExists checks if the FinancialTransaction row exists.
func FinancialTransactionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"financial_transactions\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "models: unable to check if financial_transactions exists")
	}

	return exists, nil
}

// Exists checks if the FinancialTransaction row exists.
func (o *FinancialTransaction) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return FinancialTransactionExists(ctx, exec, o.ID)
}
