// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ActionCategory is an object representing the database table.
type ActionCategory struct {
	ID           string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	CategoryCode string      `boil:"category_code" json:"category_code" toml:"category_code" yaml:"category_code"`
	CategoryName string      `boil:"category_name" json:"category_name" toml:"category_name" yaml:"category_name"`
	Description  null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	IsActive     null.Bool   `boil:"is_active" json:"is_active,omitempty" toml:"is_active" yaml:"is_active,omitempty"`
	CreatedAt    null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt    null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	DeletedAt    null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *actionCategoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L actionCategoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ActionCategoryColumns = struct {
	ID           string
	CategoryCode string
	CategoryName string
	Description  string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "id",
	CategoryCode: "category_code",
	CategoryName: "category_name",
	Description:  "description",
	IsActive:     "is_active",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
}

var ActionCategoryTableColumns = struct {
	ID           string
	CategoryCode string
	CategoryName string
	Description  string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "action_categories.id",
	CategoryCode: "action_categories.category_code",
	CategoryName: "action_categories.category_name",
	Description:  "action_categories.description",
	IsActive:     "action_categories.is_active",
	CreatedAt:    "action_categories.created_at",
	UpdatedAt:    "action_categories.updated_at",
	DeletedAt:    "action_categories.deleted_at",
}

// Generated where

type whereHelperstring struct{ field string }

func (w whereHelperstring) EQ(x string) qm.QueryMod      { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperstring) NEQ(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperstring) LT(x string) qm.QueryMod      { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperstring) LTE(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperstring) GT(x string) qm.QueryMod      { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperstring) GTE(x string) qm.QueryMod     { return qmhelper.Where(w.field, qmhelper.GTE, x) }
func (w whereHelperstring) LIKE(x string) qm.QueryMod    { return qm.Where(w.field+" LIKE ?", x) }
func (w whereHelperstring) NLIKE(x string) qm.QueryMod   { return qm.Where(w.field+" NOT LIKE ?", x) }
func (w whereHelperstring) ILIKE(x string) qm.QueryMod   { return qm.Where(w.field+" ILIKE ?", x) }
func (w whereHelperstring) NILIKE(x string) qm.QueryMod  { return qm.Where(w.field+" NOT ILIKE ?", x) }
func (w whereHelperstring) SIMILAR(x string) qm.QueryMod { return qm.Where(w.field+" SIMILAR TO ?", x) }
func (w whereHelperstring) NSIMILAR(x string) qm.QueryMod {
	return qm.Where(w.field+" NOT SIMILAR TO ?", x)
}
func (w whereHelperstring) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelperstring) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}
func (w whereHelpernull_String) LIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" LIKE ?", x)
}
func (w whereHelpernull_String) NLIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT LIKE ?", x)
}
func (w whereHelpernull_String) ILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" ILIKE ?", x)
}
func (w whereHelpernull_String) NILIKE(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT ILIKE ?", x)
}
func (w whereHelpernull_String) SIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" SIMILAR TO ?", x)
}
func (w whereHelpernull_String) NSIMILAR(x null.String) qm.QueryMod {
	return qm.Where(w.field+" NOT SIMILAR TO ?", x)
}
func (w whereHelpernull_String) IN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereIn(fmt.Sprintf("%s IN ?", w.field), values...)
}
func (w whereHelpernull_String) NIN(slice []string) qm.QueryMod {
	values := make([]interface{}, 0, len(slice))
	for _, value := range slice {
		values = append(values, value)
	}
	return qm.WhereNotIn(fmt.Sprintf("%s NOT IN ?", w.field), values...)
}

func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelpernull_Bool struct{ field string }

func (w whereHelpernull_Bool) EQ(x null.Bool) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Bool) NEQ(x null.Bool) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Bool) LT(x null.Bool) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Bool) LTE(x null.Bool) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Bool) GT(x null.Bool) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Bool) GTE(x null.Bool) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpernull_Bool) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Bool) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

type whereHelpernull_Time struct{ field string }

func (w whereHelpernull_Time) EQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_Time) NEQ(x null.Time) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_Time) LT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_Time) LTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_Time) GT(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_Time) GTE(x null.Time) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpernull_Time) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_Time) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var ActionCategoryWhere = struct {
	ID           whereHelperstring
	CategoryCode whereHelperstring
	CategoryName whereHelperstring
	Description  whereHelpernull_String
	IsActive     whereHelpernull_Bool
	CreatedAt    whereHelpernull_Time
	UpdatedAt    whereHelpernull_Time
	DeletedAt    whereHelpernull_Time
}{
	ID:           whereHelperstring{field: "\"game_config\".\"action_categories\".\"id\""},
	CategoryCode: whereHelperstring{field: "\"game_config\".\"action_categories\".\"category_code\""},
	CategoryName: whereHelperstring{field: "\"game_config\".\"action_categories\".\"category_name\""},
	Description:  whereHelpernull_String{field: "\"game_config\".\"action_categories\".\"description\""},
	IsActive:     whereHelpernull_Bool{field: "\"game_config\".\"action_categories\".\"is_active\""},
	CreatedAt:    whereHelpernull_Time{field: "\"game_config\".\"action_categories\".\"created_at\""},
	UpdatedAt:    whereHelpernull_Time{field: "\"game_config\".\"action_categories\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"game_config\".\"action_categories\".\"deleted_at\""},
}

// ActionCategoryRels is where relationship names are stored.
var ActionCategoryRels = struct {
	Actions string
}{
	Actions: "Actions",
}

// actionCategoryR is where relationships are stored.
type actionCategoryR struct {
	Actions ActionSlice `boil:"Actions" json:"Actions" toml:"Actions" yaml:"Actions"`
}

// NewStruct creates a new relationship struct
func (*actionCategoryR) NewStruct() *actionCategoryR {
	return &actionCategoryR{}
}

func (o *ActionCategory) GetActions() ActionSlice {
	if o == nil {
		return nil
	}

	return o.R.GetActions()
}

func (r *actionCategoryR) GetActions() ActionSlice {
	if r == nil {
		return nil
	}

	return r.Actions
}

// actionCategoryL is where Load methods for each relationship are stored.
type actionCategoryL struct{}

var (
	actionCategoryAllColumns            = []string{"id", "category_code", "category_name", "description", "is_active", "created_at", "updated_at", "deleted_at"}
	actionCategoryColumnsWithoutDefault = []string{"category_code", "category_name"}
	actionCategoryColumnsWithDefault    = []string{"id", "description", "is_active", "created_at", "updated_at", "deleted_at"}
	actionCategoryPrimaryKeyColumns     = []string{"id"}
	actionCategoryGeneratedColumns      = []string{}
)

type (
	// ActionCategorySlice is an alias for a slice of pointers to ActionCategory.
	// This should almost always be used instead of []ActionCategory.
	ActionCategorySlice []*ActionCategory
	// ActionCategoryHook is the signature for custom ActionCategory hook methods
	ActionCategoryHook func(context.Context, boil.ContextExecutor, *ActionCategory) error

	actionCategoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	actionCategoryType                 = reflect.TypeOf(&ActionCategory{})
	actionCategoryMapping              = queries.MakeStructMapping(actionCategoryType)
	actionCategoryPrimaryKeyMapping, _ = queries.BindMapping(actionCategoryType, actionCategoryMapping, actionCategoryPrimaryKeyColumns)
	actionCategoryInsertCacheMut       sync.RWMutex
	actionCategoryInsertCache          = make(map[string]insertCache)
	actionCategoryUpdateCacheMut       sync.RWMutex
	actionCategoryUpdateCache          = make(map[string]updateCache)
	actionCategoryUpsertCacheMut       sync.RWMutex
	actionCategoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var actionCategoryAfterSelectMu sync.Mutex
var actionCategoryAfterSelectHooks []ActionCategoryHook

var actionCategoryBeforeInsertMu sync.Mutex
var actionCategoryBeforeInsertHooks []ActionCategoryHook
var actionCategoryAfterInsertMu sync.Mutex
var actionCategoryAfterInsertHooks []ActionCategoryHook

var actionCategoryBeforeUpdateMu sync.Mutex
var actionCategoryBeforeUpdateHooks []ActionCategoryHook
var actionCategoryAfterUpdateMu sync.Mutex
var actionCategoryAfterUpdateHooks []ActionCategoryHook

var actionCategoryBeforeDeleteMu sync.Mutex
var actionCategoryBeforeDeleteHooks []ActionCategoryHook
var actionCategoryAfterDeleteMu sync.Mutex
var actionCategoryAfterDeleteHooks []ActionCategoryHook

var actionCategoryBeforeUpsertMu sync.Mutex
var actionCategoryBeforeUpsertHooks []ActionCategoryHook
var actionCategoryAfterUpsertMu sync.Mutex
var actionCategoryAfterUpsertHooks []ActionCategoryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ActionCategory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ActionCategory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ActionCategory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ActionCategory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ActionCategory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ActionCategory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ActionCategory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ActionCategory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ActionCategory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range actionCategoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddActionCategoryHook registers your hook function for all future operations.
func AddActionCategoryHook(hookPoint boil.HookPoint, actionCategoryHook ActionCategoryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		actionCategoryAfterSelectMu.Lock()
		actionCategoryAfterSelectHooks = append(actionCategoryAfterSelectHooks, actionCategoryHook)
		actionCategoryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		actionCategoryBeforeInsertMu.Lock()
		actionCategoryBeforeInsertHooks = append(actionCategoryBeforeInsertHooks, actionCategoryHook)
		actionCategoryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		actionCategoryAfterInsertMu.Lock()
		actionCategoryAfterInsertHooks = append(actionCategoryAfterInsertHooks, actionCategoryHook)
		actionCategoryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		actionCategoryBeforeUpdateMu.Lock()
		actionCategoryBeforeUpdateHooks = append(actionCategoryBeforeUpdateHooks, actionCategoryHook)
		actionCategoryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		actionCategoryAfterUpdateMu.Lock()
		actionCategoryAfterUpdateHooks = append(actionCategoryAfterUpdateHooks, actionCategoryHook)
		actionCategoryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		actionCategoryBeforeDeleteMu.Lock()
		actionCategoryBeforeDeleteHooks = append(actionCategoryBeforeDeleteHooks, actionCategoryHook)
		actionCategoryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		actionCategoryAfterDeleteMu.Lock()
		actionCategoryAfterDeleteHooks = append(actionCategoryAfterDeleteHooks, actionCategoryHook)
		actionCategoryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		actionCategoryBeforeUpsertMu.Lock()
		actionCategoryBeforeUpsertHooks = append(actionCategoryBeforeUpsertHooks, actionCategoryHook)
		actionCategoryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		actionCategoryAfterUpsertMu.Lock()
		actionCategoryAfterUpsertHooks = append(actionCategoryAfterUpsertHooks, actionCategoryHook)
		actionCategoryAfterUpsertMu.Unlock()
	}
}

// OneG returns a single actionCategory record from the query using the global executor.
func (q actionCategoryQuery) OneG(ctx context.Context) (*ActionCategory, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single actionCategory record from the query using the global executor, and panics on error.
func (q actionCategoryQuery) OneGP(ctx context.Context) *ActionCategory {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single actionCategory record from the query, and panics on error.
func (q actionCategoryQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *ActionCategory {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single actionCategory record from the query.
func (q actionCategoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ActionCategory, error) {
	o := &ActionCategory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for action_categories")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all ActionCategory records from the query using the global executor.
func (q actionCategoryQuery) AllG(ctx context.Context) (ActionCategorySlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all ActionCategory records from the query using the global executor, and panics on error.
func (q actionCategoryQuery) AllGP(ctx context.Context) ActionCategorySlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all ActionCategory records from the query, and panics on error.
func (q actionCategoryQuery) AllP(ctx context.Context, exec boil.ContextExecutor) ActionCategorySlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all ActionCategory records from the query.
func (q actionCategoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (ActionCategorySlice, error) {
	var o []*ActionCategory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to ActionCategory slice")
	}

	if len(actionCategoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all ActionCategory records in the query using the global executor
func (q actionCategoryQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all ActionCategory records in the query using the global executor, and panics on error.
func (q actionCategoryQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all ActionCategory records in the query, and panics on error.
func (q actionCategoryQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all ActionCategory records in the query.
func (q actionCategoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count action_categories rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q actionCategoryQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q actionCategoryQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q actionCategoryQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q actionCategoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if action_categories exists")
	}

	return count > 0, nil
}

// Actions retrieves all the action's Actions with an executor.
func (o *ActionCategory) Actions(mods ...qm.QueryMod) actionQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_config\".\"actions\".\"action_category_id\"=?", o.ID),
	)

	return Actions(queryMods...)
}

// LoadActions allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (actionCategoryL) LoadActions(ctx context.Context, e boil.ContextExecutor, singular bool, maybeActionCategory interface{}, mods queries.Applicator) error {
	var slice []*ActionCategory
	var object *ActionCategory

	if singular {
		var ok bool
		object, ok = maybeActionCategory.(*ActionCategory)
		if !ok {
			object = new(ActionCategory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeActionCategory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeActionCategory))
			}
		}
	} else {
		s, ok := maybeActionCategory.(*[]*ActionCategory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeActionCategory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeActionCategory))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &actionCategoryR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &actionCategoryR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.actions`),
		qm.WhereIn(`game_config.actions.action_category_id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.actions.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load actions")
	}

	var resultSlice []*Action
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice actions")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on actions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for actions")
	}

	if len(actionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.Actions = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &actionR{}
			}
			foreign.R.ActionCategory = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.ActionCategoryID) {
				local.R.Actions = append(local.R.Actions, foreign)
				if foreign.R == nil {
					foreign.R = &actionR{}
				}
				foreign.R.ActionCategory = local
				break
			}
		}
	}

	return nil
}

// AddActionsG adds the given related objects to the existing relationships
// of the action_category, optionally inserting them as new records.
// Appends related to o.R.Actions.
// Sets related.R.ActionCategory appropriately.
// Uses the global database handle.
func (o *ActionCategory) AddActionsG(ctx context.Context, insert bool, related ...*Action) error {
	return o.AddActions(ctx, boil.GetContextDB(), insert, related...)
}

// AddActionsP adds the given related objects to the existing relationships
// of the action_category, optionally inserting them as new records.
// Appends related to o.R.Actions.
// Sets related.R.ActionCategory appropriately.
// Panics on error.
func (o *ActionCategory) AddActionsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Action) {
	if err := o.AddActions(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddActionsGP adds the given related objects to the existing relationships
// of the action_category, optionally inserting them as new records.
// Appends related to o.R.Actions.
// Sets related.R.ActionCategory appropriately.
// Uses the global database handle and panics on error.
func (o *ActionCategory) AddActionsGP(ctx context.Context, insert bool, related ...*Action) {
	if err := o.AddActions(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddActions adds the given related objects to the existing relationships
// of the action_category, optionally inserting them as new records.
// Appends related to o.R.Actions.
// Sets related.R.ActionCategory appropriately.
func (o *ActionCategory) AddActions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Action) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.ActionCategoryID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_config\".\"actions\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"action_category_id"}),
				strmangle.WhereClause("\"", "\"", 2, actionPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.ActionCategoryID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &actionCategoryR{
			Actions: related,
		}
	} else {
		o.R.Actions = append(o.R.Actions, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &actionR{
				ActionCategory: o,
			}
		} else {
			rel.R.ActionCategory = o
		}
	}
	return nil
}

// SetActionsG removes all previously related items of the
// action_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ActionCategory's Actions accordingly.
// Replaces o.R.Actions with related.
// Sets related.R.ActionCategory's Actions accordingly.
// Uses the global database handle.
func (o *ActionCategory) SetActionsG(ctx context.Context, insert bool, related ...*Action) error {
	return o.SetActions(ctx, boil.GetContextDB(), insert, related...)
}

// SetActionsP removes all previously related items of the
// action_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ActionCategory's Actions accordingly.
// Replaces o.R.Actions with related.
// Sets related.R.ActionCategory's Actions accordingly.
// Panics on error.
func (o *ActionCategory) SetActionsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Action) {
	if err := o.SetActions(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetActionsGP removes all previously related items of the
// action_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ActionCategory's Actions accordingly.
// Replaces o.R.Actions with related.
// Sets related.R.ActionCategory's Actions accordingly.
// Uses the global database handle and panics on error.
func (o *ActionCategory) SetActionsGP(ctx context.Context, insert bool, related ...*Action) {
	if err := o.SetActions(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetActions removes all previously related items of the
// action_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.ActionCategory's Actions accordingly.
// Replaces o.R.Actions with related.
// Sets related.R.ActionCategory's Actions accordingly.
func (o *ActionCategory) SetActions(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Action) error {
	query := "update \"game_config\".\"actions\" set \"action_category_id\" = null where \"action_category_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.Actions {
			queries.SetScanner(&rel.ActionCategoryID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.ActionCategory = nil
		}
		o.R.Actions = nil
	}

	return o.AddActions(ctx, exec, insert, related...)
}

// RemoveActionsG relationships from objects passed in.
// Removes related items from R.Actions (uses pointer comparison, removal does not keep order)
// Sets related.R.ActionCategory.
// Uses the global database handle.
func (o *ActionCategory) RemoveActionsG(ctx context.Context, related ...*Action) error {
	return o.RemoveActions(ctx, boil.GetContextDB(), related...)
}

// RemoveActionsP relationships from objects passed in.
// Removes related items from R.Actions (uses pointer comparison, removal does not keep order)
// Sets related.R.ActionCategory.
// Panics on error.
func (o *ActionCategory) RemoveActionsP(ctx context.Context, exec boil.ContextExecutor, related ...*Action) {
	if err := o.RemoveActions(ctx, exec, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveActionsGP relationships from objects passed in.
// Removes related items from R.Actions (uses pointer comparison, removal does not keep order)
// Sets related.R.ActionCategory.
// Uses the global database handle and panics on error.
func (o *ActionCategory) RemoveActionsGP(ctx context.Context, related ...*Action) {
	if err := o.RemoveActions(ctx, boil.GetContextDB(), related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveActions relationships from objects passed in.
// Removes related items from R.Actions (uses pointer comparison, removal does not keep order)
// Sets related.R.ActionCategory.
func (o *ActionCategory) RemoveActions(ctx context.Context, exec boil.ContextExecutor, related ...*Action) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.ActionCategoryID, nil)
		if rel.R != nil {
			rel.R.ActionCategory = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("action_category_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.Actions {
			if rel != ri {
				continue
			}

			ln := len(o.R.Actions)
			if ln > 1 && i < ln-1 {
				o.R.Actions[i] = o.R.Actions[ln-1]
			}
			o.R.Actions = o.R.Actions[:ln-1]
			break
		}
	}

	return nil
}

// ActionCategories retrieves all the records using an executor.
func ActionCategories(mods ...qm.QueryMod) actionCategoryQuery {
	mods = append(mods, qm.From("\"game_config\".\"action_categories\""), qmhelper.WhereIsNull("\"game_config\".\"action_categories\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"action_categories\".*"})
	}

	return actionCategoryQuery{q}
}

// FindActionCategoryG retrieves a single record by ID.
func FindActionCategoryG(ctx context.Context, iD string, selectCols ...string) (*ActionCategory, error) {
	return FindActionCategory(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindActionCategoryP retrieves a single record by ID with an executor, and panics on error.
func FindActionCategoryP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *ActionCategory {
	retobj, err := FindActionCategory(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindActionCategoryGP retrieves a single record by ID, and panics on error.
func FindActionCategoryGP(ctx context.Context, iD string, selectCols ...string) *ActionCategory {
	retobj, err := FindActionCategory(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindActionCategory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindActionCategory(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*ActionCategory, error) {
	actionCategoryObj := &ActionCategory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"action_categories\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, actionCategoryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from action_categories")
	}

	if err = actionCategoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return actionCategoryObj, err
	}

	return actionCategoryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ActionCategory) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *ActionCategory) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *ActionCategory) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ActionCategory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no action_categories provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(actionCategoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	actionCategoryInsertCacheMut.RLock()
	cache, cached := actionCategoryInsertCache[key]
	actionCategoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			actionCategoryAllColumns,
			actionCategoryColumnsWithDefault,
			actionCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(actionCategoryType, actionCategoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(actionCategoryType, actionCategoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"action_categories\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"action_categories\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into action_categories")
	}

	if !cached {
		actionCategoryInsertCacheMut.Lock()
		actionCategoryInsertCache[key] = cache
		actionCategoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single ActionCategory record using the global executor.
// See Update for more documentation.
func (o *ActionCategory) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the ActionCategory, and panics on error.
// See Update for more documentation.
func (o *ActionCategory) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single ActionCategory record using the global executor. Panics on error.
// See Update for more documentation.
func (o *ActionCategory) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the ActionCategory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ActionCategory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	actionCategoryUpdateCacheMut.RLock()
	cache, cached := actionCategoryUpdateCache[key]
	actionCategoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			actionCategoryAllColumns,
			actionCategoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update action_categories, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"action_categories\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, actionCategoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(actionCategoryType, actionCategoryMapping, append(wl, actionCategoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update action_categories row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for action_categories")
	}

	if !cached {
		actionCategoryUpdateCacheMut.Lock()
		actionCategoryUpdateCache[key] = cache
		actionCategoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q actionCategoryQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q actionCategoryQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q actionCategoryQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q actionCategoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for action_categories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for action_categories")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ActionCategorySlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o ActionCategorySlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o ActionCategorySlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ActionCategorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), actionCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"action_categories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, actionCategoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in actionCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all actionCategory")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ActionCategory) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *ActionCategory) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *ActionCategory) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ActionCategory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no action_categories provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(actionCategoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	actionCategoryUpsertCacheMut.RLock()
	cache, cached := actionCategoryUpsertCache[key]
	actionCategoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			actionCategoryAllColumns,
			actionCategoryColumnsWithDefault,
			actionCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			actionCategoryAllColumns,
			actionCategoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert action_categories, could not build update column list")
		}

		ret := strmangle.SetComplement(actionCategoryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(actionCategoryPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert action_categories, could not build conflict column list")
			}

			conflict = make([]string, len(actionCategoryPrimaryKeyColumns))
			copy(conflict, actionCategoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"action_categories\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(actionCategoryType, actionCategoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(actionCategoryType, actionCategoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert action_categories")
	}

	if !cached {
		actionCategoryUpsertCacheMut.Lock()
		actionCategoryUpsertCache[key] = cache
		actionCategoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single ActionCategory record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ActionCategory) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single ActionCategory record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ActionCategory) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single ActionCategory record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ActionCategory) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single ActionCategory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ActionCategory) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no ActionCategory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), actionCategoryPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"action_categories\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"action_categories\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(actionCategoryType, actionCategoryMapping, append(wl, actionCategoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from action_categories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for action_categories")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q actionCategoryQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q actionCategoryQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q actionCategoryQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q actionCategoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no actionCategoryQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from action_categories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for action_categories")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o ActionCategorySlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o ActionCategorySlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o ActionCategorySlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ActionCategorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(actionCategoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), actionCategoryPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"action_categories\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, actionCategoryPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), actionCategoryPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"action_categories\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, actionCategoryPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from actionCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for action_categories")
	}

	if len(actionCategoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ActionCategory) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no ActionCategory provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *ActionCategory) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *ActionCategory) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ActionCategory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindActionCategory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ActionCategorySlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty ActionCategorySlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ActionCategorySlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ActionCategorySlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ActionCategorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ActionCategorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), actionCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"action_categories\".* FROM \"game_config\".\"action_categories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, actionCategoryPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in ActionCategorySlice")
	}

	*o = slice

	return nil
}

// ActionCategoryExistsG checks if the ActionCategory row exists.
func ActionCategoryExistsG(ctx context.Context, iD string) (bool, error) {
	return ActionCategoryExists(ctx, boil.GetContextDB(), iD)
}

// ActionCategoryExistsP checks if the ActionCategory row exists. Panics on error.
func ActionCategoryExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := ActionCategoryExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ActionCategoryExistsGP checks if the ActionCategory row exists. Panics on error.
func ActionCategoryExistsGP(ctx context.Context, iD string) bool {
	e, err := ActionCategoryExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ActionCategoryExists checks if the ActionCategory row exists.
func ActionCategoryExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"action_categories\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if action_categories exists")
	}

	return exists, nil
}

// Exists checks if the ActionCategory row exists.
func (o *ActionCategory) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ActionCategoryExists(ctx, exec, o.ID)
}
