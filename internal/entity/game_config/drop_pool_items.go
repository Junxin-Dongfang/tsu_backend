// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// DropPoolItem is an object representing the database table.
type DropPoolItem struct {
	ID         string `boil:"id" json:"id" toml:"id" yaml:"id"`
	DropPoolID string `boil:"drop_pool_id" json:"drop_pool_id" toml:"drop_pool_id" yaml:"drop_pool_id"`
	ItemID     string `boil:"item_id" json:"item_id" toml:"item_id" yaml:"item_id"`
	// 掉落权重 - 用于计算相对概率,权重越高掉落概率越大
	DropWeight int `boil:"drop_weight" json:"drop_weight" toml:"drop_weight" yaml:"drop_weight"`
	// 固定掉落概率 - 如果设置则忽略权重,直接使用此概率
	DropRate       types.NullDecimal `boil:"drop_rate" json:"drop_rate,omitempty" toml:"drop_rate" yaml:"drop_rate,omitempty"`
	QualityWeights null.JSON         `boil:"quality_weights" json:"quality_weights,omitempty" toml:"quality_weights" yaml:"quality_weights,omitempty"`
	MinQuantity    null.Int          `boil:"min_quantity" json:"min_quantity,omitempty" toml:"min_quantity" yaml:"min_quantity,omitempty"`
	MaxQuantity    null.Int          `boil:"max_quantity" json:"max_quantity,omitempty" toml:"max_quantity" yaml:"max_quantity,omitempty"`
	MinLevel       null.Int16        `boil:"min_level" json:"min_level,omitempty" toml:"min_level" yaml:"min_level,omitempty"`
	MaxLevel       null.Int16        `boil:"max_level" json:"max_level,omitempty" toml:"max_level" yaml:"max_level,omitempty"`
	IsActive       bool              `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	CreatedAt      time.Time         `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt      time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt      null.Time         `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *dropPoolItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dropPoolItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DropPoolItemColumns = struct {
	ID             string
	DropPoolID     string
	ItemID         string
	DropWeight     string
	DropRate       string
	QualityWeights string
	MinQuantity    string
	MaxQuantity    string
	MinLevel       string
	MaxLevel       string
	IsActive       string
	CreatedAt      string
	UpdatedAt      string
	DeletedAt      string
}{
	ID:             "id",
	DropPoolID:     "drop_pool_id",
	ItemID:         "item_id",
	DropWeight:     "drop_weight",
	DropRate:       "drop_rate",
	QualityWeights: "quality_weights",
	MinQuantity:    "min_quantity",
	MaxQuantity:    "max_quantity",
	MinLevel:       "min_level",
	MaxLevel:       "max_level",
	IsActive:       "is_active",
	CreatedAt:      "created_at",
	UpdatedAt:      "updated_at",
	DeletedAt:      "deleted_at",
}

var DropPoolItemTableColumns = struct {
	ID             string
	DropPoolID     string
	ItemID         string
	DropWeight     string
	DropRate       string
	QualityWeights string
	MinQuantity    string
	MaxQuantity    string
	MinLevel       string
	MaxLevel       string
	IsActive       string
	CreatedAt      string
	UpdatedAt      string
	DeletedAt      string
}{
	ID:             "drop_pool_items.id",
	DropPoolID:     "drop_pool_items.drop_pool_id",
	ItemID:         "drop_pool_items.item_id",
	DropWeight:     "drop_pool_items.drop_weight",
	DropRate:       "drop_pool_items.drop_rate",
	QualityWeights: "drop_pool_items.quality_weights",
	MinQuantity:    "drop_pool_items.min_quantity",
	MaxQuantity:    "drop_pool_items.max_quantity",
	MinLevel:       "drop_pool_items.min_level",
	MaxLevel:       "drop_pool_items.max_level",
	IsActive:       "drop_pool_items.is_active",
	CreatedAt:      "drop_pool_items.created_at",
	UpdatedAt:      "drop_pool_items.updated_at",
	DeletedAt:      "drop_pool_items.deleted_at",
}

// Generated where

type whereHelpertypes_NullDecimal struct{ field string }

func (w whereHelpertypes_NullDecimal) EQ(x types.NullDecimal) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpertypes_NullDecimal) NEQ(x types.NullDecimal) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpertypes_NullDecimal) LT(x types.NullDecimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpertypes_NullDecimal) LTE(x types.NullDecimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpertypes_NullDecimal) GT(x types.NullDecimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpertypes_NullDecimal) GTE(x types.NullDecimal) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpertypes_NullDecimal) IsNull() qm.QueryMod { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpertypes_NullDecimal) IsNotNull() qm.QueryMod {
	return qmhelper.WhereIsNotNull(w.field)
}

var DropPoolItemWhere = struct {
	ID             whereHelperstring
	DropPoolID     whereHelperstring
	ItemID         whereHelperstring
	DropWeight     whereHelperint
	DropRate       whereHelpertypes_NullDecimal
	QualityWeights whereHelpernull_JSON
	MinQuantity    whereHelpernull_Int
	MaxQuantity    whereHelpernull_Int
	MinLevel       whereHelpernull_Int16
	MaxLevel       whereHelpernull_Int16
	IsActive       whereHelperbool
	CreatedAt      whereHelpertime_Time
	UpdatedAt      whereHelpertime_Time
	DeletedAt      whereHelpernull_Time
}{
	ID:             whereHelperstring{field: "\"game_config\".\"drop_pool_items\".\"id\""},
	DropPoolID:     whereHelperstring{field: "\"game_config\".\"drop_pool_items\".\"drop_pool_id\""},
	ItemID:         whereHelperstring{field: "\"game_config\".\"drop_pool_items\".\"item_id\""},
	DropWeight:     whereHelperint{field: "\"game_config\".\"drop_pool_items\".\"drop_weight\""},
	DropRate:       whereHelpertypes_NullDecimal{field: "\"game_config\".\"drop_pool_items\".\"drop_rate\""},
	QualityWeights: whereHelpernull_JSON{field: "\"game_config\".\"drop_pool_items\".\"quality_weights\""},
	MinQuantity:    whereHelpernull_Int{field: "\"game_config\".\"drop_pool_items\".\"min_quantity\""},
	MaxQuantity:    whereHelpernull_Int{field: "\"game_config\".\"drop_pool_items\".\"max_quantity\""},
	MinLevel:       whereHelpernull_Int16{field: "\"game_config\".\"drop_pool_items\".\"min_level\""},
	MaxLevel:       whereHelpernull_Int16{field: "\"game_config\".\"drop_pool_items\".\"max_level\""},
	IsActive:       whereHelperbool{field: "\"game_config\".\"drop_pool_items\".\"is_active\""},
	CreatedAt:      whereHelpertime_Time{field: "\"game_config\".\"drop_pool_items\".\"created_at\""},
	UpdatedAt:      whereHelpertime_Time{field: "\"game_config\".\"drop_pool_items\".\"updated_at\""},
	DeletedAt:      whereHelpernull_Time{field: "\"game_config\".\"drop_pool_items\".\"deleted_at\""},
}

// DropPoolItemRels is where relationship names are stored.
var DropPoolItemRels = struct {
	DropPool string
	Item     string
}{
	DropPool: "DropPool",
	Item:     "Item",
}

// dropPoolItemR is where relationships are stored.
type dropPoolItemR struct {
	DropPool *DropPool `boil:"DropPool" json:"DropPool" toml:"DropPool" yaml:"DropPool"`
	Item     *Item     `boil:"Item" json:"Item" toml:"Item" yaml:"Item"`
}

// NewStruct creates a new relationship struct
func (*dropPoolItemR) NewStruct() *dropPoolItemR {
	return &dropPoolItemR{}
}

func (o *DropPoolItem) GetDropPool() *DropPool {
	if o == nil {
		return nil
	}

	return o.R.GetDropPool()
}

func (r *dropPoolItemR) GetDropPool() *DropPool {
	if r == nil {
		return nil
	}

	return r.DropPool
}

func (o *DropPoolItem) GetItem() *Item {
	if o == nil {
		return nil
	}

	return o.R.GetItem()
}

func (r *dropPoolItemR) GetItem() *Item {
	if r == nil {
		return nil
	}

	return r.Item
}

// dropPoolItemL is where Load methods for each relationship are stored.
type dropPoolItemL struct{}

var (
	dropPoolItemAllColumns            = []string{"id", "drop_pool_id", "item_id", "drop_weight", "drop_rate", "quality_weights", "min_quantity", "max_quantity", "min_level", "max_level", "is_active", "created_at", "updated_at", "deleted_at"}
	dropPoolItemColumnsWithoutDefault = []string{"drop_pool_id", "item_id"}
	dropPoolItemColumnsWithDefault    = []string{"id", "drop_weight", "drop_rate", "quality_weights", "min_quantity", "max_quantity", "min_level", "max_level", "is_active", "created_at", "updated_at", "deleted_at"}
	dropPoolItemPrimaryKeyColumns     = []string{"id"}
	dropPoolItemGeneratedColumns      = []string{}
)

type (
	// DropPoolItemSlice is an alias for a slice of pointers to DropPoolItem.
	// This should almost always be used instead of []DropPoolItem.
	DropPoolItemSlice []*DropPoolItem
	// DropPoolItemHook is the signature for custom DropPoolItem hook methods
	DropPoolItemHook func(context.Context, boil.ContextExecutor, *DropPoolItem) error

	dropPoolItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dropPoolItemType                 = reflect.TypeOf(&DropPoolItem{})
	dropPoolItemMapping              = queries.MakeStructMapping(dropPoolItemType)
	dropPoolItemPrimaryKeyMapping, _ = queries.BindMapping(dropPoolItemType, dropPoolItemMapping, dropPoolItemPrimaryKeyColumns)
	dropPoolItemInsertCacheMut       sync.RWMutex
	dropPoolItemInsertCache          = make(map[string]insertCache)
	dropPoolItemUpdateCacheMut       sync.RWMutex
	dropPoolItemUpdateCache          = make(map[string]updateCache)
	dropPoolItemUpsertCacheMut       sync.RWMutex
	dropPoolItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var dropPoolItemAfterSelectMu sync.Mutex
var dropPoolItemAfterSelectHooks []DropPoolItemHook

var dropPoolItemBeforeInsertMu sync.Mutex
var dropPoolItemBeforeInsertHooks []DropPoolItemHook
var dropPoolItemAfterInsertMu sync.Mutex
var dropPoolItemAfterInsertHooks []DropPoolItemHook

var dropPoolItemBeforeUpdateMu sync.Mutex
var dropPoolItemBeforeUpdateHooks []DropPoolItemHook
var dropPoolItemAfterUpdateMu sync.Mutex
var dropPoolItemAfterUpdateHooks []DropPoolItemHook

var dropPoolItemBeforeDeleteMu sync.Mutex
var dropPoolItemBeforeDeleteHooks []DropPoolItemHook
var dropPoolItemAfterDeleteMu sync.Mutex
var dropPoolItemAfterDeleteHooks []DropPoolItemHook

var dropPoolItemBeforeUpsertMu sync.Mutex
var dropPoolItemBeforeUpsertHooks []DropPoolItemHook
var dropPoolItemAfterUpsertMu sync.Mutex
var dropPoolItemAfterUpsertHooks []DropPoolItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DropPoolItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DropPoolItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DropPoolItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DropPoolItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DropPoolItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DropPoolItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DropPoolItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DropPoolItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DropPoolItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dropPoolItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDropPoolItemHook registers your hook function for all future operations.
func AddDropPoolItemHook(hookPoint boil.HookPoint, dropPoolItemHook DropPoolItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		dropPoolItemAfterSelectMu.Lock()
		dropPoolItemAfterSelectHooks = append(dropPoolItemAfterSelectHooks, dropPoolItemHook)
		dropPoolItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		dropPoolItemBeforeInsertMu.Lock()
		dropPoolItemBeforeInsertHooks = append(dropPoolItemBeforeInsertHooks, dropPoolItemHook)
		dropPoolItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		dropPoolItemAfterInsertMu.Lock()
		dropPoolItemAfterInsertHooks = append(dropPoolItemAfterInsertHooks, dropPoolItemHook)
		dropPoolItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		dropPoolItemBeforeUpdateMu.Lock()
		dropPoolItemBeforeUpdateHooks = append(dropPoolItemBeforeUpdateHooks, dropPoolItemHook)
		dropPoolItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		dropPoolItemAfterUpdateMu.Lock()
		dropPoolItemAfterUpdateHooks = append(dropPoolItemAfterUpdateHooks, dropPoolItemHook)
		dropPoolItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		dropPoolItemBeforeDeleteMu.Lock()
		dropPoolItemBeforeDeleteHooks = append(dropPoolItemBeforeDeleteHooks, dropPoolItemHook)
		dropPoolItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		dropPoolItemAfterDeleteMu.Lock()
		dropPoolItemAfterDeleteHooks = append(dropPoolItemAfterDeleteHooks, dropPoolItemHook)
		dropPoolItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		dropPoolItemBeforeUpsertMu.Lock()
		dropPoolItemBeforeUpsertHooks = append(dropPoolItemBeforeUpsertHooks, dropPoolItemHook)
		dropPoolItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		dropPoolItemAfterUpsertMu.Lock()
		dropPoolItemAfterUpsertHooks = append(dropPoolItemAfterUpsertHooks, dropPoolItemHook)
		dropPoolItemAfterUpsertMu.Unlock()
	}
}

// OneG returns a single dropPoolItem record from the query using the global executor.
func (q dropPoolItemQuery) OneG(ctx context.Context) (*DropPoolItem, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single dropPoolItem record from the query using the global executor, and panics on error.
func (q dropPoolItemQuery) OneGP(ctx context.Context) *DropPoolItem {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single dropPoolItem record from the query, and panics on error.
func (q dropPoolItemQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *DropPoolItem {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single dropPoolItem record from the query.
func (q dropPoolItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DropPoolItem, error) {
	o := &DropPoolItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for drop_pool_items")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all DropPoolItem records from the query using the global executor.
func (q dropPoolItemQuery) AllG(ctx context.Context) (DropPoolItemSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all DropPoolItem records from the query using the global executor, and panics on error.
func (q dropPoolItemQuery) AllGP(ctx context.Context) DropPoolItemSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all DropPoolItem records from the query, and panics on error.
func (q dropPoolItemQuery) AllP(ctx context.Context, exec boil.ContextExecutor) DropPoolItemSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all DropPoolItem records from the query.
func (q dropPoolItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (DropPoolItemSlice, error) {
	var o []*DropPoolItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to DropPoolItem slice")
	}

	if len(dropPoolItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all DropPoolItem records in the query using the global executor
func (q dropPoolItemQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all DropPoolItem records in the query using the global executor, and panics on error.
func (q dropPoolItemQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all DropPoolItem records in the query, and panics on error.
func (q dropPoolItemQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all DropPoolItem records in the query.
func (q dropPoolItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count drop_pool_items rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q dropPoolItemQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q dropPoolItemQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q dropPoolItemQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q dropPoolItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if drop_pool_items exists")
	}

	return count > 0, nil
}

// DropPool pointed to by the foreign key.
func (o *DropPoolItem) DropPool(mods ...qm.QueryMod) dropPoolQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.DropPoolID),
	}

	queryMods = append(queryMods, mods...)

	return DropPools(queryMods...)
}

// Item pointed to by the foreign key.
func (o *DropPoolItem) Item(mods ...qm.QueryMod) itemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ItemID),
	}

	queryMods = append(queryMods, mods...)

	return Items(queryMods...)
}

// LoadDropPool allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dropPoolItemL) LoadDropPool(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDropPoolItem interface{}, mods queries.Applicator) error {
	var slice []*DropPoolItem
	var object *DropPoolItem

	if singular {
		var ok bool
		object, ok = maybeDropPoolItem.(*DropPoolItem)
		if !ok {
			object = new(DropPoolItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDropPoolItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDropPoolItem))
			}
		}
	} else {
		s, ok := maybeDropPoolItem.(*[]*DropPoolItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDropPoolItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDropPoolItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &dropPoolItemR{}
		}
		args[object.DropPoolID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dropPoolItemR{}
			}

			args[obj.DropPoolID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.drop_pools`),
		qm.WhereIn(`game_config.drop_pools.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.drop_pools.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load DropPool")
	}

	var resultSlice []*DropPool
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice DropPool")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for drop_pools")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for drop_pools")
	}

	if len(dropPoolAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.DropPool = foreign
		if foreign.R == nil {
			foreign.R = &dropPoolR{}
		}
		foreign.R.DropPoolItems = append(foreign.R.DropPoolItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.DropPoolID == foreign.ID {
				local.R.DropPool = foreign
				if foreign.R == nil {
					foreign.R = &dropPoolR{}
				}
				foreign.R.DropPoolItems = append(foreign.R.DropPoolItems, local)
				break
			}
		}
	}

	return nil
}

// LoadItem allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (dropPoolItemL) LoadItem(ctx context.Context, e boil.ContextExecutor, singular bool, maybeDropPoolItem interface{}, mods queries.Applicator) error {
	var slice []*DropPoolItem
	var object *DropPoolItem

	if singular {
		var ok bool
		object, ok = maybeDropPoolItem.(*DropPoolItem)
		if !ok {
			object = new(DropPoolItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeDropPoolItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeDropPoolItem))
			}
		}
	} else {
		s, ok := maybeDropPoolItem.(*[]*DropPoolItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeDropPoolItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeDropPoolItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &dropPoolItemR{}
		}
		args[object.ItemID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &dropPoolItemR{}
			}

			args[obj.ItemID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.items`),
		qm.WhereIn(`game_config.items.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.items.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Item")
	}

	var resultSlice []*Item
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Item")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for items")
	}

	if len(itemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Item = foreign
		if foreign.R == nil {
			foreign.R = &itemR{}
		}
		foreign.R.DropPoolItems = append(foreign.R.DropPoolItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ItemID == foreign.ID {
				local.R.Item = foreign
				if foreign.R == nil {
					foreign.R = &itemR{}
				}
				foreign.R.DropPoolItems = append(foreign.R.DropPoolItems, local)
				break
			}
		}
	}

	return nil
}

// SetDropPoolG of the dropPoolItem to the related item.
// Sets o.R.DropPool to related.
// Adds o to related.R.DropPoolItems.
// Uses the global database handle.
func (o *DropPoolItem) SetDropPoolG(ctx context.Context, insert bool, related *DropPool) error {
	return o.SetDropPool(ctx, boil.GetContextDB(), insert, related)
}

// SetDropPoolP of the dropPoolItem to the related item.
// Sets o.R.DropPool to related.
// Adds o to related.R.DropPoolItems.
// Panics on error.
func (o *DropPoolItem) SetDropPoolP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DropPool) {
	if err := o.SetDropPool(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetDropPoolGP of the dropPoolItem to the related item.
// Sets o.R.DropPool to related.
// Adds o to related.R.DropPoolItems.
// Uses the global database handle and panics on error.
func (o *DropPoolItem) SetDropPoolGP(ctx context.Context, insert bool, related *DropPool) {
	if err := o.SetDropPool(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetDropPool of the dropPoolItem to the related item.
// Sets o.R.DropPool to related.
// Adds o to related.R.DropPoolItems.
func (o *DropPoolItem) SetDropPool(ctx context.Context, exec boil.ContextExecutor, insert bool, related *DropPool) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"drop_pool_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"drop_pool_id"}),
		strmangle.WhereClause("\"", "\"", 2, dropPoolItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.DropPoolID = related.ID
	if o.R == nil {
		o.R = &dropPoolItemR{
			DropPool: related,
		}
	} else {
		o.R.DropPool = related
	}

	if related.R == nil {
		related.R = &dropPoolR{
			DropPoolItems: DropPoolItemSlice{o},
		}
	} else {
		related.R.DropPoolItems = append(related.R.DropPoolItems, o)
	}

	return nil
}

// SetItemG of the dropPoolItem to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.DropPoolItems.
// Uses the global database handle.
func (o *DropPoolItem) SetItemG(ctx context.Context, insert bool, related *Item) error {
	return o.SetItem(ctx, boil.GetContextDB(), insert, related)
}

// SetItemP of the dropPoolItem to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.DropPoolItems.
// Panics on error.
func (o *DropPoolItem) SetItemP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Item) {
	if err := o.SetItem(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetItemGP of the dropPoolItem to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.DropPoolItems.
// Uses the global database handle and panics on error.
func (o *DropPoolItem) SetItemGP(ctx context.Context, insert bool, related *Item) {
	if err := o.SetItem(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetItem of the dropPoolItem to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.DropPoolItems.
func (o *DropPoolItem) SetItem(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Item) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"drop_pool_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"item_id"}),
		strmangle.WhereClause("\"", "\"", 2, dropPoolItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ItemID = related.ID
	if o.R == nil {
		o.R = &dropPoolItemR{
			Item: related,
		}
	} else {
		o.R.Item = related
	}

	if related.R == nil {
		related.R = &itemR{
			DropPoolItems: DropPoolItemSlice{o},
		}
	} else {
		related.R.DropPoolItems = append(related.R.DropPoolItems, o)
	}

	return nil
}

// DropPoolItems retrieves all the records using an executor.
func DropPoolItems(mods ...qm.QueryMod) dropPoolItemQuery {
	mods = append(mods, qm.From("\"game_config\".\"drop_pool_items\""), qmhelper.WhereIsNull("\"game_config\".\"drop_pool_items\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"drop_pool_items\".*"})
	}

	return dropPoolItemQuery{q}
}

// FindDropPoolItemG retrieves a single record by ID.
func FindDropPoolItemG(ctx context.Context, iD string, selectCols ...string) (*DropPoolItem, error) {
	return FindDropPoolItem(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindDropPoolItemP retrieves a single record by ID with an executor, and panics on error.
func FindDropPoolItemP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *DropPoolItem {
	retobj, err := FindDropPoolItem(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDropPoolItemGP retrieves a single record by ID, and panics on error.
func FindDropPoolItemGP(ctx context.Context, iD string, selectCols ...string) *DropPoolItem {
	retobj, err := FindDropPoolItem(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDropPoolItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDropPoolItem(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*DropPoolItem, error) {
	dropPoolItemObj := &DropPoolItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"drop_pool_items\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dropPoolItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from drop_pool_items")
	}

	if err = dropPoolItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return dropPoolItemObj, err
	}

	return dropPoolItemObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *DropPoolItem) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *DropPoolItem) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *DropPoolItem) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DropPoolItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no drop_pool_items provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dropPoolItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dropPoolItemInsertCacheMut.RLock()
	cache, cached := dropPoolItemInsertCache[key]
	dropPoolItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dropPoolItemAllColumns,
			dropPoolItemColumnsWithDefault,
			dropPoolItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dropPoolItemType, dropPoolItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dropPoolItemType, dropPoolItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"drop_pool_items\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"drop_pool_items\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into drop_pool_items")
	}

	if !cached {
		dropPoolItemInsertCacheMut.Lock()
		dropPoolItemInsertCache[key] = cache
		dropPoolItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single DropPoolItem record using the global executor.
// See Update for more documentation.
func (o *DropPoolItem) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the DropPoolItem, and panics on error.
// See Update for more documentation.
func (o *DropPoolItem) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single DropPoolItem record using the global executor. Panics on error.
// See Update for more documentation.
func (o *DropPoolItem) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the DropPoolItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DropPoolItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	dropPoolItemUpdateCacheMut.RLock()
	cache, cached := dropPoolItemUpdateCache[key]
	dropPoolItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dropPoolItemAllColumns,
			dropPoolItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update drop_pool_items, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"drop_pool_items\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, dropPoolItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dropPoolItemType, dropPoolItemMapping, append(wl, dropPoolItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update drop_pool_items row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for drop_pool_items")
	}

	if !cached {
		dropPoolItemUpdateCacheMut.Lock()
		dropPoolItemUpdateCache[key] = cache
		dropPoolItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q dropPoolItemQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q dropPoolItemQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q dropPoolItemQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q dropPoolItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for drop_pool_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for drop_pool_items")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o DropPoolItemSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o DropPoolItemSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o DropPoolItemSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DropPoolItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dropPoolItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"drop_pool_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, dropPoolItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in dropPoolItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all dropPoolItem")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *DropPoolItem) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *DropPoolItem) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *DropPoolItem) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DropPoolItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no drop_pool_items provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dropPoolItemColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dropPoolItemUpsertCacheMut.RLock()
	cache, cached := dropPoolItemUpsertCache[key]
	dropPoolItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			dropPoolItemAllColumns,
			dropPoolItemColumnsWithDefault,
			dropPoolItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dropPoolItemAllColumns,
			dropPoolItemPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert drop_pool_items, could not build update column list")
		}

		ret := strmangle.SetComplement(dropPoolItemAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(dropPoolItemPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert drop_pool_items, could not build conflict column list")
			}

			conflict = make([]string, len(dropPoolItemPrimaryKeyColumns))
			copy(conflict, dropPoolItemPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"drop_pool_items\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(dropPoolItemType, dropPoolItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dropPoolItemType, dropPoolItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert drop_pool_items")
	}

	if !cached {
		dropPoolItemUpsertCacheMut.Lock()
		dropPoolItemUpsertCache[key] = cache
		dropPoolItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single DropPoolItem record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *DropPoolItem) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single DropPoolItem record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DropPoolItem) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single DropPoolItem record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DropPoolItem) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single DropPoolItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DropPoolItem) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no DropPoolItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dropPoolItemPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"drop_pool_items\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"drop_pool_items\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(dropPoolItemType, dropPoolItemMapping, append(wl, dropPoolItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from drop_pool_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for drop_pool_items")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q dropPoolItemQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q dropPoolItemQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q dropPoolItemQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q dropPoolItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no dropPoolItemQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from drop_pool_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for drop_pool_items")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o DropPoolItemSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o DropPoolItemSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o DropPoolItemSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DropPoolItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(dropPoolItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dropPoolItemPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"drop_pool_items\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dropPoolItemPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dropPoolItemPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"drop_pool_items\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, dropPoolItemPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from dropPoolItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for drop_pool_items")
	}

	if len(dropPoolItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *DropPoolItem) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no DropPoolItem provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *DropPoolItem) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *DropPoolItem) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DropPoolItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDropPoolItem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DropPoolItemSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty DropPoolItemSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DropPoolItemSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DropPoolItemSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DropPoolItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DropPoolItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dropPoolItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"drop_pool_items\".* FROM \"game_config\".\"drop_pool_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dropPoolItemPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in DropPoolItemSlice")
	}

	*o = slice

	return nil
}

// DropPoolItemExistsG checks if the DropPoolItem row exists.
func DropPoolItemExistsG(ctx context.Context, iD string) (bool, error) {
	return DropPoolItemExists(ctx, boil.GetContextDB(), iD)
}

// DropPoolItemExistsP checks if the DropPoolItem row exists. Panics on error.
func DropPoolItemExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := DropPoolItemExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DropPoolItemExistsGP checks if the DropPoolItem row exists. Panics on error.
func DropPoolItemExistsGP(ctx context.Context, iD string) bool {
	e, err := DropPoolItemExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DropPoolItemExists checks if the DropPoolItem row exists.
func DropPoolItemExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"drop_pool_items\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if drop_pool_items exists")
	}

	return exists, nil
}

// Exists checks if the DropPoolItem row exists.
func (o *DropPoolItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DropPoolItemExists(ctx, exec, o.ID)
}
