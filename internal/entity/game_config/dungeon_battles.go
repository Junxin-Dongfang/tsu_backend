// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// DungeonBattle is an object representing the database table.
type DungeonBattle struct {
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 战斗代码 - 全局唯一标识符
	BattleCode string `boil:"battle_code" json:"battle_code" toml:"battle_code" yaml:"battle_code"`
	// 场地配置 - JSONB格式,定义战斗场地和场地事件
	LocationConfig types.JSON `boil:"location_config" json:"location_config" toml:"location_config" yaml:"location_config"`
	// 全程状态 - JSONB格式,定义战斗全程的Buff/Debuff
	GlobalBuffs types.JSON `boil:"global_buffs" json:"global_buffs" toml:"global_buffs" yaml:"global_buffs"`
	// 怪物配置 - JSONB格式,定义怪物种类、位置、等级
	MonsterSetup types.JSON `boil:"monster_setup" json:"monster_setup" toml:"monster_setup" yaml:"monster_setup"`
	// 战斗开始描述 - 战斗开始时显示的文本
	BattleStartDesc null.String `boil:"battle_start_desc" json:"battle_start_desc,omitempty" toml:"battle_start_desc" yaml:"battle_start_desc,omitempty"`
	// 战斗成功描述 - 战斗胜利时显示的文本
	BattleSuccessDesc null.String `boil:"battle_success_desc" json:"battle_success_desc,omitempty" toml:"battle_success_desc" yaml:"battle_success_desc,omitempty"`
	// 战斗失败描述 - 战斗失败时显示的文本
	BattleFailureDesc null.String `boil:"battle_failure_desc" json:"battle_failure_desc,omitempty" toml:"battle_failure_desc" yaml:"battle_failure_desc,omitempty"`
	IsActive          bool        `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	CreatedAt         time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt         null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *dungeonBattleR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dungeonBattleL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DungeonBattleColumns = struct {
	ID                string
	BattleCode        string
	LocationConfig    string
	GlobalBuffs       string
	MonsterSetup      string
	BattleStartDesc   string
	BattleSuccessDesc string
	BattleFailureDesc string
	IsActive          string
	CreatedAt         string
	UpdatedAt         string
	DeletedAt         string
}{
	ID:                "id",
	BattleCode:        "battle_code",
	LocationConfig:    "location_config",
	GlobalBuffs:       "global_buffs",
	MonsterSetup:      "monster_setup",
	BattleStartDesc:   "battle_start_desc",
	BattleSuccessDesc: "battle_success_desc",
	BattleFailureDesc: "battle_failure_desc",
	IsActive:          "is_active",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
	DeletedAt:         "deleted_at",
}

var DungeonBattleTableColumns = struct {
	ID                string
	BattleCode        string
	LocationConfig    string
	GlobalBuffs       string
	MonsterSetup      string
	BattleStartDesc   string
	BattleSuccessDesc string
	BattleFailureDesc string
	IsActive          string
	CreatedAt         string
	UpdatedAt         string
	DeletedAt         string
}{
	ID:                "dungeon_battles.id",
	BattleCode:        "dungeon_battles.battle_code",
	LocationConfig:    "dungeon_battles.location_config",
	GlobalBuffs:       "dungeon_battles.global_buffs",
	MonsterSetup:      "dungeon_battles.monster_setup",
	BattleStartDesc:   "dungeon_battles.battle_start_desc",
	BattleSuccessDesc: "dungeon_battles.battle_success_desc",
	BattleFailureDesc: "dungeon_battles.battle_failure_desc",
	IsActive:          "dungeon_battles.is_active",
	CreatedAt:         "dungeon_battles.created_at",
	UpdatedAt:         "dungeon_battles.updated_at",
	DeletedAt:         "dungeon_battles.deleted_at",
}

// Generated where

var DungeonBattleWhere = struct {
	ID                whereHelperstring
	BattleCode        whereHelperstring
	LocationConfig    whereHelpertypes_JSON
	GlobalBuffs       whereHelpertypes_JSON
	MonsterSetup      whereHelpertypes_JSON
	BattleStartDesc   whereHelpernull_String
	BattleSuccessDesc whereHelpernull_String
	BattleFailureDesc whereHelpernull_String
	IsActive          whereHelperbool
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
	DeletedAt         whereHelpernull_Time
}{
	ID:                whereHelperstring{field: "\"game_config\".\"dungeon_battles\".\"id\""},
	BattleCode:        whereHelperstring{field: "\"game_config\".\"dungeon_battles\".\"battle_code\""},
	LocationConfig:    whereHelpertypes_JSON{field: "\"game_config\".\"dungeon_battles\".\"location_config\""},
	GlobalBuffs:       whereHelpertypes_JSON{field: "\"game_config\".\"dungeon_battles\".\"global_buffs\""},
	MonsterSetup:      whereHelpertypes_JSON{field: "\"game_config\".\"dungeon_battles\".\"monster_setup\""},
	BattleStartDesc:   whereHelpernull_String{field: "\"game_config\".\"dungeon_battles\".\"battle_start_desc\""},
	BattleSuccessDesc: whereHelpernull_String{field: "\"game_config\".\"dungeon_battles\".\"battle_success_desc\""},
	BattleFailureDesc: whereHelpernull_String{field: "\"game_config\".\"dungeon_battles\".\"battle_failure_desc\""},
	IsActive:          whereHelperbool{field: "\"game_config\".\"dungeon_battles\".\"is_active\""},
	CreatedAt:         whereHelpertime_Time{field: "\"game_config\".\"dungeon_battles\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"game_config\".\"dungeon_battles\".\"updated_at\""},
	DeletedAt:         whereHelpernull_Time{field: "\"game_config\".\"dungeon_battles\".\"deleted_at\""},
}

// DungeonBattleRels is where relationship names are stored.
var DungeonBattleRels = struct {
}{}

// dungeonBattleR is where relationships are stored.
type dungeonBattleR struct {
}

// NewStruct creates a new relationship struct
func (*dungeonBattleR) NewStruct() *dungeonBattleR {
	return &dungeonBattleR{}
}

// dungeonBattleL is where Load methods for each relationship are stored.
type dungeonBattleL struct{}

var (
	dungeonBattleAllColumns            = []string{"id", "battle_code", "location_config", "global_buffs", "monster_setup", "battle_start_desc", "battle_success_desc", "battle_failure_desc", "is_active", "created_at", "updated_at", "deleted_at"}
	dungeonBattleColumnsWithoutDefault = []string{"battle_code"}
	dungeonBattleColumnsWithDefault    = []string{"id", "location_config", "global_buffs", "monster_setup", "battle_start_desc", "battle_success_desc", "battle_failure_desc", "is_active", "created_at", "updated_at", "deleted_at"}
	dungeonBattlePrimaryKeyColumns     = []string{"id"}
	dungeonBattleGeneratedColumns      = []string{}
)

type (
	// DungeonBattleSlice is an alias for a slice of pointers to DungeonBattle.
	// This should almost always be used instead of []DungeonBattle.
	DungeonBattleSlice []*DungeonBattle
	// DungeonBattleHook is the signature for custom DungeonBattle hook methods
	DungeonBattleHook func(context.Context, boil.ContextExecutor, *DungeonBattle) error

	dungeonBattleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dungeonBattleType                 = reflect.TypeOf(&DungeonBattle{})
	dungeonBattleMapping              = queries.MakeStructMapping(dungeonBattleType)
	dungeonBattlePrimaryKeyMapping, _ = queries.BindMapping(dungeonBattleType, dungeonBattleMapping, dungeonBattlePrimaryKeyColumns)
	dungeonBattleInsertCacheMut       sync.RWMutex
	dungeonBattleInsertCache          = make(map[string]insertCache)
	dungeonBattleUpdateCacheMut       sync.RWMutex
	dungeonBattleUpdateCache          = make(map[string]updateCache)
	dungeonBattleUpsertCacheMut       sync.RWMutex
	dungeonBattleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var dungeonBattleAfterSelectMu sync.Mutex
var dungeonBattleAfterSelectHooks []DungeonBattleHook

var dungeonBattleBeforeInsertMu sync.Mutex
var dungeonBattleBeforeInsertHooks []DungeonBattleHook
var dungeonBattleAfterInsertMu sync.Mutex
var dungeonBattleAfterInsertHooks []DungeonBattleHook

var dungeonBattleBeforeUpdateMu sync.Mutex
var dungeonBattleBeforeUpdateHooks []DungeonBattleHook
var dungeonBattleAfterUpdateMu sync.Mutex
var dungeonBattleAfterUpdateHooks []DungeonBattleHook

var dungeonBattleBeforeDeleteMu sync.Mutex
var dungeonBattleBeforeDeleteHooks []DungeonBattleHook
var dungeonBattleAfterDeleteMu sync.Mutex
var dungeonBattleAfterDeleteHooks []DungeonBattleHook

var dungeonBattleBeforeUpsertMu sync.Mutex
var dungeonBattleBeforeUpsertHooks []DungeonBattleHook
var dungeonBattleAfterUpsertMu sync.Mutex
var dungeonBattleAfterUpsertHooks []DungeonBattleHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DungeonBattle) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DungeonBattle) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DungeonBattle) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DungeonBattle) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DungeonBattle) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DungeonBattle) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DungeonBattle) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DungeonBattle) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DungeonBattle) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonBattleAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDungeonBattleHook registers your hook function for all future operations.
func AddDungeonBattleHook(hookPoint boil.HookPoint, dungeonBattleHook DungeonBattleHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		dungeonBattleAfterSelectMu.Lock()
		dungeonBattleAfterSelectHooks = append(dungeonBattleAfterSelectHooks, dungeonBattleHook)
		dungeonBattleAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		dungeonBattleBeforeInsertMu.Lock()
		dungeonBattleBeforeInsertHooks = append(dungeonBattleBeforeInsertHooks, dungeonBattleHook)
		dungeonBattleBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		dungeonBattleAfterInsertMu.Lock()
		dungeonBattleAfterInsertHooks = append(dungeonBattleAfterInsertHooks, dungeonBattleHook)
		dungeonBattleAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		dungeonBattleBeforeUpdateMu.Lock()
		dungeonBattleBeforeUpdateHooks = append(dungeonBattleBeforeUpdateHooks, dungeonBattleHook)
		dungeonBattleBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		dungeonBattleAfterUpdateMu.Lock()
		dungeonBattleAfterUpdateHooks = append(dungeonBattleAfterUpdateHooks, dungeonBattleHook)
		dungeonBattleAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		dungeonBattleBeforeDeleteMu.Lock()
		dungeonBattleBeforeDeleteHooks = append(dungeonBattleBeforeDeleteHooks, dungeonBattleHook)
		dungeonBattleBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		dungeonBattleAfterDeleteMu.Lock()
		dungeonBattleAfterDeleteHooks = append(dungeonBattleAfterDeleteHooks, dungeonBattleHook)
		dungeonBattleAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		dungeonBattleBeforeUpsertMu.Lock()
		dungeonBattleBeforeUpsertHooks = append(dungeonBattleBeforeUpsertHooks, dungeonBattleHook)
		dungeonBattleBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		dungeonBattleAfterUpsertMu.Lock()
		dungeonBattleAfterUpsertHooks = append(dungeonBattleAfterUpsertHooks, dungeonBattleHook)
		dungeonBattleAfterUpsertMu.Unlock()
	}
}

// OneG returns a single dungeonBattle record from the query using the global executor.
func (q dungeonBattleQuery) OneG(ctx context.Context) (*DungeonBattle, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single dungeonBattle record from the query using the global executor, and panics on error.
func (q dungeonBattleQuery) OneGP(ctx context.Context) *DungeonBattle {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single dungeonBattle record from the query, and panics on error.
func (q dungeonBattleQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *DungeonBattle {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single dungeonBattle record from the query.
func (q dungeonBattleQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DungeonBattle, error) {
	o := &DungeonBattle{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for dungeon_battles")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all DungeonBattle records from the query using the global executor.
func (q dungeonBattleQuery) AllG(ctx context.Context) (DungeonBattleSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all DungeonBattle records from the query using the global executor, and panics on error.
func (q dungeonBattleQuery) AllGP(ctx context.Context) DungeonBattleSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all DungeonBattle records from the query, and panics on error.
func (q dungeonBattleQuery) AllP(ctx context.Context, exec boil.ContextExecutor) DungeonBattleSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all DungeonBattle records from the query.
func (q dungeonBattleQuery) All(ctx context.Context, exec boil.ContextExecutor) (DungeonBattleSlice, error) {
	var o []*DungeonBattle

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to DungeonBattle slice")
	}

	if len(dungeonBattleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all DungeonBattle records in the query using the global executor
func (q dungeonBattleQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all DungeonBattle records in the query using the global executor, and panics on error.
func (q dungeonBattleQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all DungeonBattle records in the query, and panics on error.
func (q dungeonBattleQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all DungeonBattle records in the query.
func (q dungeonBattleQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count dungeon_battles rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q dungeonBattleQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q dungeonBattleQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q dungeonBattleQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q dungeonBattleQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if dungeon_battles exists")
	}

	return count > 0, nil
}

// DungeonBattles retrieves all the records using an executor.
func DungeonBattles(mods ...qm.QueryMod) dungeonBattleQuery {
	mods = append(mods, qm.From("\"game_config\".\"dungeon_battles\""), qmhelper.WhereIsNull("\"game_config\".\"dungeon_battles\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"dungeon_battles\".*"})
	}

	return dungeonBattleQuery{q}
}

// FindDungeonBattleG retrieves a single record by ID.
func FindDungeonBattleG(ctx context.Context, iD string, selectCols ...string) (*DungeonBattle, error) {
	return FindDungeonBattle(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindDungeonBattleP retrieves a single record by ID with an executor, and panics on error.
func FindDungeonBattleP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *DungeonBattle {
	retobj, err := FindDungeonBattle(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDungeonBattleGP retrieves a single record by ID, and panics on error.
func FindDungeonBattleGP(ctx context.Context, iD string, selectCols ...string) *DungeonBattle {
	retobj, err := FindDungeonBattle(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDungeonBattle retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDungeonBattle(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*DungeonBattle, error) {
	dungeonBattleObj := &DungeonBattle{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"dungeon_battles\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dungeonBattleObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from dungeon_battles")
	}

	if err = dungeonBattleObj.doAfterSelectHooks(ctx, exec); err != nil {
		return dungeonBattleObj, err
	}

	return dungeonBattleObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *DungeonBattle) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *DungeonBattle) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *DungeonBattle) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DungeonBattle) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no dungeon_battles provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dungeonBattleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dungeonBattleInsertCacheMut.RLock()
	cache, cached := dungeonBattleInsertCache[key]
	dungeonBattleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dungeonBattleAllColumns,
			dungeonBattleColumnsWithDefault,
			dungeonBattleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dungeonBattleType, dungeonBattleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dungeonBattleType, dungeonBattleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"dungeon_battles\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"dungeon_battles\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into dungeon_battles")
	}

	if !cached {
		dungeonBattleInsertCacheMut.Lock()
		dungeonBattleInsertCache[key] = cache
		dungeonBattleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single DungeonBattle record using the global executor.
// See Update for more documentation.
func (o *DungeonBattle) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the DungeonBattle, and panics on error.
// See Update for more documentation.
func (o *DungeonBattle) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single DungeonBattle record using the global executor. Panics on error.
// See Update for more documentation.
func (o *DungeonBattle) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the DungeonBattle.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DungeonBattle) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	dungeonBattleUpdateCacheMut.RLock()
	cache, cached := dungeonBattleUpdateCache[key]
	dungeonBattleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dungeonBattleAllColumns,
			dungeonBattlePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update dungeon_battles, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"dungeon_battles\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, dungeonBattlePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dungeonBattleType, dungeonBattleMapping, append(wl, dungeonBattlePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update dungeon_battles row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for dungeon_battles")
	}

	if !cached {
		dungeonBattleUpdateCacheMut.Lock()
		dungeonBattleUpdateCache[key] = cache
		dungeonBattleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q dungeonBattleQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q dungeonBattleQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q dungeonBattleQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q dungeonBattleQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for dungeon_battles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for dungeon_battles")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o DungeonBattleSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o DungeonBattleSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o DungeonBattleSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DungeonBattleSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonBattlePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"dungeon_battles\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, dungeonBattlePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in dungeonBattle slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all dungeonBattle")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *DungeonBattle) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *DungeonBattle) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *DungeonBattle) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DungeonBattle) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no dungeon_battles provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dungeonBattleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dungeonBattleUpsertCacheMut.RLock()
	cache, cached := dungeonBattleUpsertCache[key]
	dungeonBattleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			dungeonBattleAllColumns,
			dungeonBattleColumnsWithDefault,
			dungeonBattleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dungeonBattleAllColumns,
			dungeonBattlePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert dungeon_battles, could not build update column list")
		}

		ret := strmangle.SetComplement(dungeonBattleAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(dungeonBattlePrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert dungeon_battles, could not build conflict column list")
			}

			conflict = make([]string, len(dungeonBattlePrimaryKeyColumns))
			copy(conflict, dungeonBattlePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"dungeon_battles\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(dungeonBattleType, dungeonBattleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dungeonBattleType, dungeonBattleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert dungeon_battles")
	}

	if !cached {
		dungeonBattleUpsertCacheMut.Lock()
		dungeonBattleUpsertCache[key] = cache
		dungeonBattleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single DungeonBattle record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *DungeonBattle) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single DungeonBattle record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DungeonBattle) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single DungeonBattle record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DungeonBattle) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single DungeonBattle record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DungeonBattle) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no DungeonBattle provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dungeonBattlePrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"dungeon_battles\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"dungeon_battles\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(dungeonBattleType, dungeonBattleMapping, append(wl, dungeonBattlePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from dungeon_battles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for dungeon_battles")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q dungeonBattleQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q dungeonBattleQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q dungeonBattleQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q dungeonBattleQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no dungeonBattleQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from dungeon_battles")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for dungeon_battles")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o DungeonBattleSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o DungeonBattleSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o DungeonBattleSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DungeonBattleSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(dungeonBattleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonBattlePrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"dungeon_battles\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dungeonBattlePrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonBattlePrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"dungeon_battles\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, dungeonBattlePrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from dungeonBattle slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for dungeon_battles")
	}

	if len(dungeonBattleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *DungeonBattle) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no DungeonBattle provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *DungeonBattle) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *DungeonBattle) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DungeonBattle) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDungeonBattle(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DungeonBattleSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty DungeonBattleSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DungeonBattleSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DungeonBattleSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DungeonBattleSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DungeonBattleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonBattlePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"dungeon_battles\".* FROM \"game_config\".\"dungeon_battles\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dungeonBattlePrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in DungeonBattleSlice")
	}

	*o = slice

	return nil
}

// DungeonBattleExistsG checks if the DungeonBattle row exists.
func DungeonBattleExistsG(ctx context.Context, iD string) (bool, error) {
	return DungeonBattleExists(ctx, boil.GetContextDB(), iD)
}

// DungeonBattleExistsP checks if the DungeonBattle row exists. Panics on error.
func DungeonBattleExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := DungeonBattleExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DungeonBattleExistsGP checks if the DungeonBattle row exists. Panics on error.
func DungeonBattleExistsGP(ctx context.Context, iD string) bool {
	e, err := DungeonBattleExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DungeonBattleExists checks if the DungeonBattle row exists.
func DungeonBattleExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"dungeon_battles\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if dungeon_battles exists")
	}

	return exists, nil
}

// Exists checks if the DungeonBattle row exists.
func (o *DungeonBattle) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DungeonBattleExists(ctx, exec, o.ID)
}
