// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// DungeonEvent is an object representing the database table.
type DungeonEvent struct {
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 事件代码 - 全局唯一标识符
	EventCode string `boil:"event_code" json:"event_code" toml:"event_code" yaml:"event_code"`
	// 事件描述 - 事件发生时显示的文本
	EventDescription null.String `boil:"event_description" json:"event_description,omitempty" toml:"event_description" yaml:"event_description,omitempty"`
	// 施加效果 - JSONB格式,定义事件施加的Buff/Debuff
	ApplyEffects types.JSON `boil:"apply_effects" json:"apply_effects" toml:"apply_effects" yaml:"apply_effects"`
	// 掉落配置 - JSONB格式,定义掉落池和保底物品
	DropConfig types.JSON `boil:"drop_config" json:"drop_config" toml:"drop_config" yaml:"drop_config"`
	// 经验奖励 - 事件完成后获得的经验值
	RewardExp null.Int `boil:"reward_exp" json:"reward_exp,omitempty" toml:"reward_exp" yaml:"reward_exp,omitempty"`
	// 事件结束描述 - 事件结束时显示的文本
	EventEndDesc null.String `boil:"event_end_desc" json:"event_end_desc,omitempty" toml:"event_end_desc" yaml:"event_end_desc,omitempty"`
	IsActive     bool        `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	CreatedAt    time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt    null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *dungeonEventR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L dungeonEventL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var DungeonEventColumns = struct {
	ID               string
	EventCode        string
	EventDescription string
	ApplyEffects     string
	DropConfig       string
	RewardExp        string
	EventEndDesc     string
	IsActive         string
	CreatedAt        string
	UpdatedAt        string
	DeletedAt        string
}{
	ID:               "id",
	EventCode:        "event_code",
	EventDescription: "event_description",
	ApplyEffects:     "apply_effects",
	DropConfig:       "drop_config",
	RewardExp:        "reward_exp",
	EventEndDesc:     "event_end_desc",
	IsActive:         "is_active",
	CreatedAt:        "created_at",
	UpdatedAt:        "updated_at",
	DeletedAt:        "deleted_at",
}

var DungeonEventTableColumns = struct {
	ID               string
	EventCode        string
	EventDescription string
	ApplyEffects     string
	DropConfig       string
	RewardExp        string
	EventEndDesc     string
	IsActive         string
	CreatedAt        string
	UpdatedAt        string
	DeletedAt        string
}{
	ID:               "dungeon_events.id",
	EventCode:        "dungeon_events.event_code",
	EventDescription: "dungeon_events.event_description",
	ApplyEffects:     "dungeon_events.apply_effects",
	DropConfig:       "dungeon_events.drop_config",
	RewardExp:        "dungeon_events.reward_exp",
	EventEndDesc:     "dungeon_events.event_end_desc",
	IsActive:         "dungeon_events.is_active",
	CreatedAt:        "dungeon_events.created_at",
	UpdatedAt:        "dungeon_events.updated_at",
	DeletedAt:        "dungeon_events.deleted_at",
}

// Generated where

var DungeonEventWhere = struct {
	ID               whereHelperstring
	EventCode        whereHelperstring
	EventDescription whereHelpernull_String
	ApplyEffects     whereHelpertypes_JSON
	DropConfig       whereHelpertypes_JSON
	RewardExp        whereHelpernull_Int
	EventEndDesc     whereHelpernull_String
	IsActive         whereHelperbool
	CreatedAt        whereHelpertime_Time
	UpdatedAt        whereHelpertime_Time
	DeletedAt        whereHelpernull_Time
}{
	ID:               whereHelperstring{field: "\"game_config\".\"dungeon_events\".\"id\""},
	EventCode:        whereHelperstring{field: "\"game_config\".\"dungeon_events\".\"event_code\""},
	EventDescription: whereHelpernull_String{field: "\"game_config\".\"dungeon_events\".\"event_description\""},
	ApplyEffects:     whereHelpertypes_JSON{field: "\"game_config\".\"dungeon_events\".\"apply_effects\""},
	DropConfig:       whereHelpertypes_JSON{field: "\"game_config\".\"dungeon_events\".\"drop_config\""},
	RewardExp:        whereHelpernull_Int{field: "\"game_config\".\"dungeon_events\".\"reward_exp\""},
	EventEndDesc:     whereHelpernull_String{field: "\"game_config\".\"dungeon_events\".\"event_end_desc\""},
	IsActive:         whereHelperbool{field: "\"game_config\".\"dungeon_events\".\"is_active\""},
	CreatedAt:        whereHelpertime_Time{field: "\"game_config\".\"dungeon_events\".\"created_at\""},
	UpdatedAt:        whereHelpertime_Time{field: "\"game_config\".\"dungeon_events\".\"updated_at\""},
	DeletedAt:        whereHelpernull_Time{field: "\"game_config\".\"dungeon_events\".\"deleted_at\""},
}

// DungeonEventRels is where relationship names are stored.
var DungeonEventRels = struct {
}{}

// dungeonEventR is where relationships are stored.
type dungeonEventR struct {
}

// NewStruct creates a new relationship struct
func (*dungeonEventR) NewStruct() *dungeonEventR {
	return &dungeonEventR{}
}

// dungeonEventL is where Load methods for each relationship are stored.
type dungeonEventL struct{}

var (
	dungeonEventAllColumns            = []string{"id", "event_code", "event_description", "apply_effects", "drop_config", "reward_exp", "event_end_desc", "is_active", "created_at", "updated_at", "deleted_at"}
	dungeonEventColumnsWithoutDefault = []string{"event_code"}
	dungeonEventColumnsWithDefault    = []string{"id", "event_description", "apply_effects", "drop_config", "reward_exp", "event_end_desc", "is_active", "created_at", "updated_at", "deleted_at"}
	dungeonEventPrimaryKeyColumns     = []string{"id"}
	dungeonEventGeneratedColumns      = []string{}
)

type (
	// DungeonEventSlice is an alias for a slice of pointers to DungeonEvent.
	// This should almost always be used instead of []DungeonEvent.
	DungeonEventSlice []*DungeonEvent
	// DungeonEventHook is the signature for custom DungeonEvent hook methods
	DungeonEventHook func(context.Context, boil.ContextExecutor, *DungeonEvent) error

	dungeonEventQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	dungeonEventType                 = reflect.TypeOf(&DungeonEvent{})
	dungeonEventMapping              = queries.MakeStructMapping(dungeonEventType)
	dungeonEventPrimaryKeyMapping, _ = queries.BindMapping(dungeonEventType, dungeonEventMapping, dungeonEventPrimaryKeyColumns)
	dungeonEventInsertCacheMut       sync.RWMutex
	dungeonEventInsertCache          = make(map[string]insertCache)
	dungeonEventUpdateCacheMut       sync.RWMutex
	dungeonEventUpdateCache          = make(map[string]updateCache)
	dungeonEventUpsertCacheMut       sync.RWMutex
	dungeonEventUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var dungeonEventAfterSelectMu sync.Mutex
var dungeonEventAfterSelectHooks []DungeonEventHook

var dungeonEventBeforeInsertMu sync.Mutex
var dungeonEventBeforeInsertHooks []DungeonEventHook
var dungeonEventAfterInsertMu sync.Mutex
var dungeonEventAfterInsertHooks []DungeonEventHook

var dungeonEventBeforeUpdateMu sync.Mutex
var dungeonEventBeforeUpdateHooks []DungeonEventHook
var dungeonEventAfterUpdateMu sync.Mutex
var dungeonEventAfterUpdateHooks []DungeonEventHook

var dungeonEventBeforeDeleteMu sync.Mutex
var dungeonEventBeforeDeleteHooks []DungeonEventHook
var dungeonEventAfterDeleteMu sync.Mutex
var dungeonEventAfterDeleteHooks []DungeonEventHook

var dungeonEventBeforeUpsertMu sync.Mutex
var dungeonEventBeforeUpsertHooks []DungeonEventHook
var dungeonEventAfterUpsertMu sync.Mutex
var dungeonEventAfterUpsertHooks []DungeonEventHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *DungeonEvent) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *DungeonEvent) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *DungeonEvent) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *DungeonEvent) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *DungeonEvent) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *DungeonEvent) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *DungeonEvent) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *DungeonEvent) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *DungeonEvent) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range dungeonEventAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddDungeonEventHook registers your hook function for all future operations.
func AddDungeonEventHook(hookPoint boil.HookPoint, dungeonEventHook DungeonEventHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		dungeonEventAfterSelectMu.Lock()
		dungeonEventAfterSelectHooks = append(dungeonEventAfterSelectHooks, dungeonEventHook)
		dungeonEventAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		dungeonEventBeforeInsertMu.Lock()
		dungeonEventBeforeInsertHooks = append(dungeonEventBeforeInsertHooks, dungeonEventHook)
		dungeonEventBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		dungeonEventAfterInsertMu.Lock()
		dungeonEventAfterInsertHooks = append(dungeonEventAfterInsertHooks, dungeonEventHook)
		dungeonEventAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		dungeonEventBeforeUpdateMu.Lock()
		dungeonEventBeforeUpdateHooks = append(dungeonEventBeforeUpdateHooks, dungeonEventHook)
		dungeonEventBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		dungeonEventAfterUpdateMu.Lock()
		dungeonEventAfterUpdateHooks = append(dungeonEventAfterUpdateHooks, dungeonEventHook)
		dungeonEventAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		dungeonEventBeforeDeleteMu.Lock()
		dungeonEventBeforeDeleteHooks = append(dungeonEventBeforeDeleteHooks, dungeonEventHook)
		dungeonEventBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		dungeonEventAfterDeleteMu.Lock()
		dungeonEventAfterDeleteHooks = append(dungeonEventAfterDeleteHooks, dungeonEventHook)
		dungeonEventAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		dungeonEventBeforeUpsertMu.Lock()
		dungeonEventBeforeUpsertHooks = append(dungeonEventBeforeUpsertHooks, dungeonEventHook)
		dungeonEventBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		dungeonEventAfterUpsertMu.Lock()
		dungeonEventAfterUpsertHooks = append(dungeonEventAfterUpsertHooks, dungeonEventHook)
		dungeonEventAfterUpsertMu.Unlock()
	}
}

// OneG returns a single dungeonEvent record from the query using the global executor.
func (q dungeonEventQuery) OneG(ctx context.Context) (*DungeonEvent, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single dungeonEvent record from the query using the global executor, and panics on error.
func (q dungeonEventQuery) OneGP(ctx context.Context) *DungeonEvent {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single dungeonEvent record from the query, and panics on error.
func (q dungeonEventQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *DungeonEvent {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single dungeonEvent record from the query.
func (q dungeonEventQuery) One(ctx context.Context, exec boil.ContextExecutor) (*DungeonEvent, error) {
	o := &DungeonEvent{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for dungeon_events")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all DungeonEvent records from the query using the global executor.
func (q dungeonEventQuery) AllG(ctx context.Context) (DungeonEventSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all DungeonEvent records from the query using the global executor, and panics on error.
func (q dungeonEventQuery) AllGP(ctx context.Context) DungeonEventSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all DungeonEvent records from the query, and panics on error.
func (q dungeonEventQuery) AllP(ctx context.Context, exec boil.ContextExecutor) DungeonEventSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all DungeonEvent records from the query.
func (q dungeonEventQuery) All(ctx context.Context, exec boil.ContextExecutor) (DungeonEventSlice, error) {
	var o []*DungeonEvent

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to DungeonEvent slice")
	}

	if len(dungeonEventAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all DungeonEvent records in the query using the global executor
func (q dungeonEventQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all DungeonEvent records in the query using the global executor, and panics on error.
func (q dungeonEventQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all DungeonEvent records in the query, and panics on error.
func (q dungeonEventQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all DungeonEvent records in the query.
func (q dungeonEventQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count dungeon_events rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q dungeonEventQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q dungeonEventQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q dungeonEventQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q dungeonEventQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if dungeon_events exists")
	}

	return count > 0, nil
}

// DungeonEvents retrieves all the records using an executor.
func DungeonEvents(mods ...qm.QueryMod) dungeonEventQuery {
	mods = append(mods, qm.From("\"game_config\".\"dungeon_events\""), qmhelper.WhereIsNull("\"game_config\".\"dungeon_events\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"dungeon_events\".*"})
	}

	return dungeonEventQuery{q}
}

// FindDungeonEventG retrieves a single record by ID.
func FindDungeonEventG(ctx context.Context, iD string, selectCols ...string) (*DungeonEvent, error) {
	return FindDungeonEvent(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindDungeonEventP retrieves a single record by ID with an executor, and panics on error.
func FindDungeonEventP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *DungeonEvent {
	retobj, err := FindDungeonEvent(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDungeonEventGP retrieves a single record by ID, and panics on error.
func FindDungeonEventGP(ctx context.Context, iD string, selectCols ...string) *DungeonEvent {
	retobj, err := FindDungeonEvent(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindDungeonEvent retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindDungeonEvent(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*DungeonEvent, error) {
	dungeonEventObj := &DungeonEvent{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"dungeon_events\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, dungeonEventObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from dungeon_events")
	}

	if err = dungeonEventObj.doAfterSelectHooks(ctx, exec); err != nil {
		return dungeonEventObj, err
	}

	return dungeonEventObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *DungeonEvent) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *DungeonEvent) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *DungeonEvent) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *DungeonEvent) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no dungeon_events provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dungeonEventColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	dungeonEventInsertCacheMut.RLock()
	cache, cached := dungeonEventInsertCache[key]
	dungeonEventInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			dungeonEventAllColumns,
			dungeonEventColumnsWithDefault,
			dungeonEventColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(dungeonEventType, dungeonEventMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(dungeonEventType, dungeonEventMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"dungeon_events\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"dungeon_events\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into dungeon_events")
	}

	if !cached {
		dungeonEventInsertCacheMut.Lock()
		dungeonEventInsertCache[key] = cache
		dungeonEventInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single DungeonEvent record using the global executor.
// See Update for more documentation.
func (o *DungeonEvent) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the DungeonEvent, and panics on error.
// See Update for more documentation.
func (o *DungeonEvent) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single DungeonEvent record using the global executor. Panics on error.
// See Update for more documentation.
func (o *DungeonEvent) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the DungeonEvent.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *DungeonEvent) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	dungeonEventUpdateCacheMut.RLock()
	cache, cached := dungeonEventUpdateCache[key]
	dungeonEventUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			dungeonEventAllColumns,
			dungeonEventPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update dungeon_events, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"dungeon_events\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, dungeonEventPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(dungeonEventType, dungeonEventMapping, append(wl, dungeonEventPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update dungeon_events row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for dungeon_events")
	}

	if !cached {
		dungeonEventUpdateCacheMut.Lock()
		dungeonEventUpdateCache[key] = cache
		dungeonEventUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q dungeonEventQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q dungeonEventQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q dungeonEventQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q dungeonEventQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for dungeon_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for dungeon_events")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o DungeonEventSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o DungeonEventSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o DungeonEventSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o DungeonEventSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"dungeon_events\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, dungeonEventPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in dungeonEvent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all dungeonEvent")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *DungeonEvent) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *DungeonEvent) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *DungeonEvent) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *DungeonEvent) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no dungeon_events provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(dungeonEventColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	dungeonEventUpsertCacheMut.RLock()
	cache, cached := dungeonEventUpsertCache[key]
	dungeonEventUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			dungeonEventAllColumns,
			dungeonEventColumnsWithDefault,
			dungeonEventColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			dungeonEventAllColumns,
			dungeonEventPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert dungeon_events, could not build update column list")
		}

		ret := strmangle.SetComplement(dungeonEventAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(dungeonEventPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert dungeon_events, could not build conflict column list")
			}

			conflict = make([]string, len(dungeonEventPrimaryKeyColumns))
			copy(conflict, dungeonEventPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"dungeon_events\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(dungeonEventType, dungeonEventMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(dungeonEventType, dungeonEventMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert dungeon_events")
	}

	if !cached {
		dungeonEventUpsertCacheMut.Lock()
		dungeonEventUpsertCache[key] = cache
		dungeonEventUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single DungeonEvent record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *DungeonEvent) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single DungeonEvent record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DungeonEvent) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single DungeonEvent record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *DungeonEvent) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single DungeonEvent record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *DungeonEvent) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no DungeonEvent provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), dungeonEventPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"dungeon_events\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"dungeon_events\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(dungeonEventType, dungeonEventMapping, append(wl, dungeonEventPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from dungeon_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for dungeon_events")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q dungeonEventQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q dungeonEventQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q dungeonEventQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q dungeonEventQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no dungeonEventQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from dungeon_events")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for dungeon_events")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o DungeonEventSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o DungeonEventSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o DungeonEventSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o DungeonEventSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(dungeonEventBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonEventPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"dungeon_events\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dungeonEventPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonEventPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"dungeon_events\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, dungeonEventPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from dungeonEvent slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for dungeon_events")
	}

	if len(dungeonEventAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *DungeonEvent) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no DungeonEvent provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *DungeonEvent) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *DungeonEvent) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *DungeonEvent) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindDungeonEvent(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DungeonEventSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty DungeonEventSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DungeonEventSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *DungeonEventSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *DungeonEventSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := DungeonEventSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), dungeonEventPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"dungeon_events\".* FROM \"game_config\".\"dungeon_events\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, dungeonEventPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in DungeonEventSlice")
	}

	*o = slice

	return nil
}

// DungeonEventExistsG checks if the DungeonEvent row exists.
func DungeonEventExistsG(ctx context.Context, iD string) (bool, error) {
	return DungeonEventExists(ctx, boil.GetContextDB(), iD)
}

// DungeonEventExistsP checks if the DungeonEvent row exists. Panics on error.
func DungeonEventExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := DungeonEventExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DungeonEventExistsGP checks if the DungeonEvent row exists. Panics on error.
func DungeonEventExistsGP(ctx context.Context, iD string) bool {
	e, err := DungeonEventExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// DungeonEventExists checks if the DungeonEvent row exists.
func DungeonEventExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"dungeon_events\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if dungeon_events exists")
	}

	return exists, nil
}

// Exists checks if the DungeonEvent row exists.
func (o *DungeonEvent) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return DungeonEventExists(ctx, exec, o.ID)
}
