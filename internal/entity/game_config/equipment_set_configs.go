// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// EquipmentSetConfig is an object representing the database table.
type EquipmentSetConfig struct {
	ID          string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	SetCode     string      `boil:"set_code" json:"set_code" toml:"set_code" yaml:"set_code"`
	SetName     string      `boil:"set_name" json:"set_name" toml:"set_name" yaml:"set_name"`
	Description null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	SetTagID    null.String `boil:"set_tag_id" json:"set_tag_id,omitempty" toml:"set_tag_id" yaml:"set_tag_id,omitempty"`
	// 套装效果 - 格式: [{"piece_count":2,"effects":[...]},{"piece_count":4,"effects":[...]}]
	SetEffects types.JSON `boil:"set_effects" json:"set_effects" toml:"set_effects" yaml:"set_effects"`
	IsActive   bool       `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	CreatedAt  time.Time  `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time  `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt  null.Time  `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *equipmentSetConfigR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L equipmentSetConfigL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EquipmentSetConfigColumns = struct {
	ID          string
	SetCode     string
	SetName     string
	Description string
	SetTagID    string
	SetEffects  string
	IsActive    string
	CreatedAt   string
	UpdatedAt   string
	DeletedAt   string
}{
	ID:          "id",
	SetCode:     "set_code",
	SetName:     "set_name",
	Description: "description",
	SetTagID:    "set_tag_id",
	SetEffects:  "set_effects",
	IsActive:    "is_active",
	CreatedAt:   "created_at",
	UpdatedAt:   "updated_at",
	DeletedAt:   "deleted_at",
}

var EquipmentSetConfigTableColumns = struct {
	ID          string
	SetCode     string
	SetName     string
	Description string
	SetTagID    string
	SetEffects  string
	IsActive    string
	CreatedAt   string
	UpdatedAt   string
	DeletedAt   string
}{
	ID:          "equipment_set_configs.id",
	SetCode:     "equipment_set_configs.set_code",
	SetName:     "equipment_set_configs.set_name",
	Description: "equipment_set_configs.description",
	SetTagID:    "equipment_set_configs.set_tag_id",
	SetEffects:  "equipment_set_configs.set_effects",
	IsActive:    "equipment_set_configs.is_active",
	CreatedAt:   "equipment_set_configs.created_at",
	UpdatedAt:   "equipment_set_configs.updated_at",
	DeletedAt:   "equipment_set_configs.deleted_at",
}

// Generated where

var EquipmentSetConfigWhere = struct {
	ID          whereHelperstring
	SetCode     whereHelperstring
	SetName     whereHelperstring
	Description whereHelpernull_String
	SetTagID    whereHelpernull_String
	SetEffects  whereHelpertypes_JSON
	IsActive    whereHelperbool
	CreatedAt   whereHelpertime_Time
	UpdatedAt   whereHelpertime_Time
	DeletedAt   whereHelpernull_Time
}{
	ID:          whereHelperstring{field: "\"game_config\".\"equipment_set_configs\".\"id\""},
	SetCode:     whereHelperstring{field: "\"game_config\".\"equipment_set_configs\".\"set_code\""},
	SetName:     whereHelperstring{field: "\"game_config\".\"equipment_set_configs\".\"set_name\""},
	Description: whereHelpernull_String{field: "\"game_config\".\"equipment_set_configs\".\"description\""},
	SetTagID:    whereHelpernull_String{field: "\"game_config\".\"equipment_set_configs\".\"set_tag_id\""},
	SetEffects:  whereHelpertypes_JSON{field: "\"game_config\".\"equipment_set_configs\".\"set_effects\""},
	IsActive:    whereHelperbool{field: "\"game_config\".\"equipment_set_configs\".\"is_active\""},
	CreatedAt:   whereHelpertime_Time{field: "\"game_config\".\"equipment_set_configs\".\"created_at\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"game_config\".\"equipment_set_configs\".\"updated_at\""},
	DeletedAt:   whereHelpernull_Time{field: "\"game_config\".\"equipment_set_configs\".\"deleted_at\""},
}

// EquipmentSetConfigRels is where relationship names are stored.
var EquipmentSetConfigRels = struct {
	SetTag   string
	SetItems string
}{
	SetTag:   "SetTag",
	SetItems: "SetItems",
}

// equipmentSetConfigR is where relationships are stored.
type equipmentSetConfigR struct {
	SetTag   *Tag      `boil:"SetTag" json:"SetTag" toml:"SetTag" yaml:"SetTag"`
	SetItems ItemSlice `boil:"SetItems" json:"SetItems" toml:"SetItems" yaml:"SetItems"`
}

// NewStruct creates a new relationship struct
func (*equipmentSetConfigR) NewStruct() *equipmentSetConfigR {
	return &equipmentSetConfigR{}
}

func (o *EquipmentSetConfig) GetSetTag() *Tag {
	if o == nil {
		return nil
	}

	return o.R.GetSetTag()
}

func (r *equipmentSetConfigR) GetSetTag() *Tag {
	if r == nil {
		return nil
	}

	return r.SetTag
}

func (o *EquipmentSetConfig) GetSetItems() ItemSlice {
	if o == nil {
		return nil
	}

	return o.R.GetSetItems()
}

func (r *equipmentSetConfigR) GetSetItems() ItemSlice {
	if r == nil {
		return nil
	}

	return r.SetItems
}

// equipmentSetConfigL is where Load methods for each relationship are stored.
type equipmentSetConfigL struct{}

var (
	equipmentSetConfigAllColumns            = []string{"id", "set_code", "set_name", "description", "set_tag_id", "set_effects", "is_active", "created_at", "updated_at", "deleted_at"}
	equipmentSetConfigColumnsWithoutDefault = []string{"set_code", "set_name", "set_effects"}
	equipmentSetConfigColumnsWithDefault    = []string{"id", "description", "set_tag_id", "is_active", "created_at", "updated_at", "deleted_at"}
	equipmentSetConfigPrimaryKeyColumns     = []string{"id"}
	equipmentSetConfigGeneratedColumns      = []string{}
)

type (
	// EquipmentSetConfigSlice is an alias for a slice of pointers to EquipmentSetConfig.
	// This should almost always be used instead of []EquipmentSetConfig.
	EquipmentSetConfigSlice []*EquipmentSetConfig
	// EquipmentSetConfigHook is the signature for custom EquipmentSetConfig hook methods
	EquipmentSetConfigHook func(context.Context, boil.ContextExecutor, *EquipmentSetConfig) error

	equipmentSetConfigQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	equipmentSetConfigType                 = reflect.TypeOf(&EquipmentSetConfig{})
	equipmentSetConfigMapping              = queries.MakeStructMapping(equipmentSetConfigType)
	equipmentSetConfigPrimaryKeyMapping, _ = queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, equipmentSetConfigPrimaryKeyColumns)
	equipmentSetConfigInsertCacheMut       sync.RWMutex
	equipmentSetConfigInsertCache          = make(map[string]insertCache)
	equipmentSetConfigUpdateCacheMut       sync.RWMutex
	equipmentSetConfigUpdateCache          = make(map[string]updateCache)
	equipmentSetConfigUpsertCacheMut       sync.RWMutex
	equipmentSetConfigUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var equipmentSetConfigAfterSelectMu sync.Mutex
var equipmentSetConfigAfterSelectHooks []EquipmentSetConfigHook

var equipmentSetConfigBeforeInsertMu sync.Mutex
var equipmentSetConfigBeforeInsertHooks []EquipmentSetConfigHook
var equipmentSetConfigAfterInsertMu sync.Mutex
var equipmentSetConfigAfterInsertHooks []EquipmentSetConfigHook

var equipmentSetConfigBeforeUpdateMu sync.Mutex
var equipmentSetConfigBeforeUpdateHooks []EquipmentSetConfigHook
var equipmentSetConfigAfterUpdateMu sync.Mutex
var equipmentSetConfigAfterUpdateHooks []EquipmentSetConfigHook

var equipmentSetConfigBeforeDeleteMu sync.Mutex
var equipmentSetConfigBeforeDeleteHooks []EquipmentSetConfigHook
var equipmentSetConfigAfterDeleteMu sync.Mutex
var equipmentSetConfigAfterDeleteHooks []EquipmentSetConfigHook

var equipmentSetConfigBeforeUpsertMu sync.Mutex
var equipmentSetConfigBeforeUpsertHooks []EquipmentSetConfigHook
var equipmentSetConfigAfterUpsertMu sync.Mutex
var equipmentSetConfigAfterUpsertHooks []EquipmentSetConfigHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EquipmentSetConfig) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EquipmentSetConfig) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EquipmentSetConfig) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EquipmentSetConfig) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EquipmentSetConfig) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EquipmentSetConfig) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EquipmentSetConfig) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EquipmentSetConfig) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EquipmentSetConfig) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSetConfigAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEquipmentSetConfigHook registers your hook function for all future operations.
func AddEquipmentSetConfigHook(hookPoint boil.HookPoint, equipmentSetConfigHook EquipmentSetConfigHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		equipmentSetConfigAfterSelectMu.Lock()
		equipmentSetConfigAfterSelectHooks = append(equipmentSetConfigAfterSelectHooks, equipmentSetConfigHook)
		equipmentSetConfigAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		equipmentSetConfigBeforeInsertMu.Lock()
		equipmentSetConfigBeforeInsertHooks = append(equipmentSetConfigBeforeInsertHooks, equipmentSetConfigHook)
		equipmentSetConfigBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		equipmentSetConfigAfterInsertMu.Lock()
		equipmentSetConfigAfterInsertHooks = append(equipmentSetConfigAfterInsertHooks, equipmentSetConfigHook)
		equipmentSetConfigAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		equipmentSetConfigBeforeUpdateMu.Lock()
		equipmentSetConfigBeforeUpdateHooks = append(equipmentSetConfigBeforeUpdateHooks, equipmentSetConfigHook)
		equipmentSetConfigBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		equipmentSetConfigAfterUpdateMu.Lock()
		equipmentSetConfigAfterUpdateHooks = append(equipmentSetConfigAfterUpdateHooks, equipmentSetConfigHook)
		equipmentSetConfigAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		equipmentSetConfigBeforeDeleteMu.Lock()
		equipmentSetConfigBeforeDeleteHooks = append(equipmentSetConfigBeforeDeleteHooks, equipmentSetConfigHook)
		equipmentSetConfigBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		equipmentSetConfigAfterDeleteMu.Lock()
		equipmentSetConfigAfterDeleteHooks = append(equipmentSetConfigAfterDeleteHooks, equipmentSetConfigHook)
		equipmentSetConfigAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		equipmentSetConfigBeforeUpsertMu.Lock()
		equipmentSetConfigBeforeUpsertHooks = append(equipmentSetConfigBeforeUpsertHooks, equipmentSetConfigHook)
		equipmentSetConfigBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		equipmentSetConfigAfterUpsertMu.Lock()
		equipmentSetConfigAfterUpsertHooks = append(equipmentSetConfigAfterUpsertHooks, equipmentSetConfigHook)
		equipmentSetConfigAfterUpsertMu.Unlock()
	}
}

// OneG returns a single equipmentSetConfig record from the query using the global executor.
func (q equipmentSetConfigQuery) OneG(ctx context.Context) (*EquipmentSetConfig, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single equipmentSetConfig record from the query using the global executor, and panics on error.
func (q equipmentSetConfigQuery) OneGP(ctx context.Context) *EquipmentSetConfig {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single equipmentSetConfig record from the query, and panics on error.
func (q equipmentSetConfigQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *EquipmentSetConfig {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single equipmentSetConfig record from the query.
func (q equipmentSetConfigQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EquipmentSetConfig, error) {
	o := &EquipmentSetConfig{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for equipment_set_configs")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all EquipmentSetConfig records from the query using the global executor.
func (q equipmentSetConfigQuery) AllG(ctx context.Context) (EquipmentSetConfigSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all EquipmentSetConfig records from the query using the global executor, and panics on error.
func (q equipmentSetConfigQuery) AllGP(ctx context.Context) EquipmentSetConfigSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all EquipmentSetConfig records from the query, and panics on error.
func (q equipmentSetConfigQuery) AllP(ctx context.Context, exec boil.ContextExecutor) EquipmentSetConfigSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all EquipmentSetConfig records from the query.
func (q equipmentSetConfigQuery) All(ctx context.Context, exec boil.ContextExecutor) (EquipmentSetConfigSlice, error) {
	var o []*EquipmentSetConfig

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to EquipmentSetConfig slice")
	}

	if len(equipmentSetConfigAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all EquipmentSetConfig records in the query using the global executor
func (q equipmentSetConfigQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all EquipmentSetConfig records in the query using the global executor, and panics on error.
func (q equipmentSetConfigQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all EquipmentSetConfig records in the query, and panics on error.
func (q equipmentSetConfigQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all EquipmentSetConfig records in the query.
func (q equipmentSetConfigQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count equipment_set_configs rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q equipmentSetConfigQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q equipmentSetConfigQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q equipmentSetConfigQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q equipmentSetConfigQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if equipment_set_configs exists")
	}

	return count > 0, nil
}

// SetTag pointed to by the foreign key.
func (o *EquipmentSetConfig) SetTag(mods ...qm.QueryMod) tagQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SetTagID),
	}

	queryMods = append(queryMods, mods...)

	return Tags(queryMods...)
}

// SetItems retrieves all the item's Items with an executor via set_id column.
func (o *EquipmentSetConfig) SetItems(mods ...qm.QueryMod) itemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_config\".\"items\".\"set_id\"=?", o.ID),
	)

	return Items(queryMods...)
}

// LoadSetTag allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (equipmentSetConfigL) LoadSetTag(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEquipmentSetConfig interface{}, mods queries.Applicator) error {
	var slice []*EquipmentSetConfig
	var object *EquipmentSetConfig

	if singular {
		var ok bool
		object, ok = maybeEquipmentSetConfig.(*EquipmentSetConfig)
		if !ok {
			object = new(EquipmentSetConfig)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEquipmentSetConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEquipmentSetConfig))
			}
		}
	} else {
		s, ok := maybeEquipmentSetConfig.(*[]*EquipmentSetConfig)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEquipmentSetConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEquipmentSetConfig))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &equipmentSetConfigR{}
		}
		if !queries.IsNil(object.SetTagID) {
			args[object.SetTagID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &equipmentSetConfigR{}
			}

			if !queries.IsNil(obj.SetTagID) {
				args[obj.SetTagID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.tags`),
		qm.WhereIn(`game_config.tags.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.tags.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Tag")
	}

	var resultSlice []*Tag
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Tag")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tags")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tags")
	}

	if len(tagAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.SetTag = foreign
		if foreign.R == nil {
			foreign.R = &tagR{}
		}
		foreign.R.SetTagEquipmentSetConfigs = append(foreign.R.SetTagEquipmentSetConfigs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.SetTagID, foreign.ID) {
				local.R.SetTag = foreign
				if foreign.R == nil {
					foreign.R = &tagR{}
				}
				foreign.R.SetTagEquipmentSetConfigs = append(foreign.R.SetTagEquipmentSetConfigs, local)
				break
			}
		}
	}

	return nil
}

// LoadSetItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (equipmentSetConfigL) LoadSetItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeEquipmentSetConfig interface{}, mods queries.Applicator) error {
	var slice []*EquipmentSetConfig
	var object *EquipmentSetConfig

	if singular {
		var ok bool
		object, ok = maybeEquipmentSetConfig.(*EquipmentSetConfig)
		if !ok {
			object = new(EquipmentSetConfig)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeEquipmentSetConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeEquipmentSetConfig))
			}
		}
	} else {
		s, ok := maybeEquipmentSetConfig.(*[]*EquipmentSetConfig)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeEquipmentSetConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeEquipmentSetConfig))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &equipmentSetConfigR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &equipmentSetConfigR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.items`),
		qm.WhereIn(`game_config.items.set_id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.items.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load items")
	}

	var resultSlice []*Item
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice items")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for items")
	}

	if len(itemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.SetItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &itemR{}
			}
			foreign.R.Set = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.SetID) {
				local.R.SetItems = append(local.R.SetItems, foreign)
				if foreign.R == nil {
					foreign.R = &itemR{}
				}
				foreign.R.Set = local
				break
			}
		}
	}

	return nil
}

// SetSetTagG of the equipmentSetConfig to the related item.
// Sets o.R.SetTag to related.
// Adds o to related.R.SetTagEquipmentSetConfigs.
// Uses the global database handle.
func (o *EquipmentSetConfig) SetSetTagG(ctx context.Context, insert bool, related *Tag) error {
	return o.SetSetTag(ctx, boil.GetContextDB(), insert, related)
}

// SetSetTagP of the equipmentSetConfig to the related item.
// Sets o.R.SetTag to related.
// Adds o to related.R.SetTagEquipmentSetConfigs.
// Panics on error.
func (o *EquipmentSetConfig) SetSetTagP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tag) {
	if err := o.SetSetTag(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSetTagGP of the equipmentSetConfig to the related item.
// Sets o.R.SetTag to related.
// Adds o to related.R.SetTagEquipmentSetConfigs.
// Uses the global database handle and panics on error.
func (o *EquipmentSetConfig) SetSetTagGP(ctx context.Context, insert bool, related *Tag) {
	if err := o.SetSetTag(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSetTag of the equipmentSetConfig to the related item.
// Sets o.R.SetTag to related.
// Adds o to related.R.SetTagEquipmentSetConfigs.
func (o *EquipmentSetConfig) SetSetTag(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tag) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"equipment_set_configs\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"set_tag_id"}),
		strmangle.WhereClause("\"", "\"", 2, equipmentSetConfigPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.SetTagID, related.ID)
	if o.R == nil {
		o.R = &equipmentSetConfigR{
			SetTag: related,
		}
	} else {
		o.R.SetTag = related
	}

	if related.R == nil {
		related.R = &tagR{
			SetTagEquipmentSetConfigs: EquipmentSetConfigSlice{o},
		}
	} else {
		related.R.SetTagEquipmentSetConfigs = append(related.R.SetTagEquipmentSetConfigs, o)
	}

	return nil
}

// RemoveSetTagG relationship.
// Sets o.R.SetTag to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *EquipmentSetConfig) RemoveSetTagG(ctx context.Context, related *Tag) error {
	return o.RemoveSetTag(ctx, boil.GetContextDB(), related)
}

// RemoveSetTagP relationship.
// Sets o.R.SetTag to nil.
// Removes o from all passed in related items' relationships struct.
// Panics on error.
func (o *EquipmentSetConfig) RemoveSetTagP(ctx context.Context, exec boil.ContextExecutor, related *Tag) {
	if err := o.RemoveSetTag(ctx, exec, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveSetTagGP relationship.
// Sets o.R.SetTag to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle and panics on error.
func (o *EquipmentSetConfig) RemoveSetTagGP(ctx context.Context, related *Tag) {
	if err := o.RemoveSetTag(ctx, boil.GetContextDB(), related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveSetTag relationship.
// Sets o.R.SetTag to nil.
// Removes o from all passed in related items' relationships struct.
func (o *EquipmentSetConfig) RemoveSetTag(ctx context.Context, exec boil.ContextExecutor, related *Tag) error {
	var err error

	queries.SetScanner(&o.SetTagID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("set_tag_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.SetTag = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.SetTagEquipmentSetConfigs {
		if queries.Equal(o.SetTagID, ri.SetTagID) {
			continue
		}

		ln := len(related.R.SetTagEquipmentSetConfigs)
		if ln > 1 && i < ln-1 {
			related.R.SetTagEquipmentSetConfigs[i] = related.R.SetTagEquipmentSetConfigs[ln-1]
		}
		related.R.SetTagEquipmentSetConfigs = related.R.SetTagEquipmentSetConfigs[:ln-1]
		break
	}
	return nil
}

// AddSetItemsG adds the given related objects to the existing relationships
// of the equipment_set_config, optionally inserting them as new records.
// Appends related to o.R.SetItems.
// Sets related.R.Set appropriately.
// Uses the global database handle.
func (o *EquipmentSetConfig) AddSetItemsG(ctx context.Context, insert bool, related ...*Item) error {
	return o.AddSetItems(ctx, boil.GetContextDB(), insert, related...)
}

// AddSetItemsP adds the given related objects to the existing relationships
// of the equipment_set_config, optionally inserting them as new records.
// Appends related to o.R.SetItems.
// Sets related.R.Set appropriately.
// Panics on error.
func (o *EquipmentSetConfig) AddSetItemsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Item) {
	if err := o.AddSetItems(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddSetItemsGP adds the given related objects to the existing relationships
// of the equipment_set_config, optionally inserting them as new records.
// Appends related to o.R.SetItems.
// Sets related.R.Set appropriately.
// Uses the global database handle and panics on error.
func (o *EquipmentSetConfig) AddSetItemsGP(ctx context.Context, insert bool, related ...*Item) {
	if err := o.AddSetItems(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddSetItems adds the given related objects to the existing relationships
// of the equipment_set_config, optionally inserting them as new records.
// Appends related to o.R.SetItems.
// Sets related.R.Set appropriately.
func (o *EquipmentSetConfig) AddSetItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Item) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.SetID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_config\".\"items\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"set_id"}),
				strmangle.WhereClause("\"", "\"", 2, itemPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.SetID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &equipmentSetConfigR{
			SetItems: related,
		}
	} else {
		o.R.SetItems = append(o.R.SetItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &itemR{
				Set: o,
			}
		} else {
			rel.R.Set = o
		}
	}
	return nil
}

// SetSetItemsG removes all previously related items of the
// equipment_set_config replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Set's SetItems accordingly.
// Replaces o.R.SetItems with related.
// Sets related.R.Set's SetItems accordingly.
// Uses the global database handle.
func (o *EquipmentSetConfig) SetSetItemsG(ctx context.Context, insert bool, related ...*Item) error {
	return o.SetSetItems(ctx, boil.GetContextDB(), insert, related...)
}

// SetSetItemsP removes all previously related items of the
// equipment_set_config replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Set's SetItems accordingly.
// Replaces o.R.SetItems with related.
// Sets related.R.Set's SetItems accordingly.
// Panics on error.
func (o *EquipmentSetConfig) SetSetItemsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Item) {
	if err := o.SetSetItems(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSetItemsGP removes all previously related items of the
// equipment_set_config replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Set's SetItems accordingly.
// Replaces o.R.SetItems with related.
// Sets related.R.Set's SetItems accordingly.
// Uses the global database handle and panics on error.
func (o *EquipmentSetConfig) SetSetItemsGP(ctx context.Context, insert bool, related ...*Item) {
	if err := o.SetSetItems(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSetItems removes all previously related items of the
// equipment_set_config replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Set's SetItems accordingly.
// Replaces o.R.SetItems with related.
// Sets related.R.Set's SetItems accordingly.
func (o *EquipmentSetConfig) SetSetItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Item) error {
	query := "update \"game_config\".\"items\" set \"set_id\" = null where \"set_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.SetItems {
			queries.SetScanner(&rel.SetID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Set = nil
		}
		o.R.SetItems = nil
	}

	return o.AddSetItems(ctx, exec, insert, related...)
}

// RemoveSetItemsG relationships from objects passed in.
// Removes related items from R.SetItems (uses pointer comparison, removal does not keep order)
// Sets related.R.Set.
// Uses the global database handle.
func (o *EquipmentSetConfig) RemoveSetItemsG(ctx context.Context, related ...*Item) error {
	return o.RemoveSetItems(ctx, boil.GetContextDB(), related...)
}

// RemoveSetItemsP relationships from objects passed in.
// Removes related items from R.SetItems (uses pointer comparison, removal does not keep order)
// Sets related.R.Set.
// Panics on error.
func (o *EquipmentSetConfig) RemoveSetItemsP(ctx context.Context, exec boil.ContextExecutor, related ...*Item) {
	if err := o.RemoveSetItems(ctx, exec, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveSetItemsGP relationships from objects passed in.
// Removes related items from R.SetItems (uses pointer comparison, removal does not keep order)
// Sets related.R.Set.
// Uses the global database handle and panics on error.
func (o *EquipmentSetConfig) RemoveSetItemsGP(ctx context.Context, related ...*Item) {
	if err := o.RemoveSetItems(ctx, boil.GetContextDB(), related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveSetItems relationships from objects passed in.
// Removes related items from R.SetItems (uses pointer comparison, removal does not keep order)
// Sets related.R.Set.
func (o *EquipmentSetConfig) RemoveSetItems(ctx context.Context, exec boil.ContextExecutor, related ...*Item) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.SetID, nil)
		if rel.R != nil {
			rel.R.Set = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("set_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.SetItems {
			if rel != ri {
				continue
			}

			ln := len(o.R.SetItems)
			if ln > 1 && i < ln-1 {
				o.R.SetItems[i] = o.R.SetItems[ln-1]
			}
			o.R.SetItems = o.R.SetItems[:ln-1]
			break
		}
	}

	return nil
}

// EquipmentSetConfigs retrieves all the records using an executor.
func EquipmentSetConfigs(mods ...qm.QueryMod) equipmentSetConfigQuery {
	mods = append(mods, qm.From("\"game_config\".\"equipment_set_configs\""), qmhelper.WhereIsNull("\"game_config\".\"equipment_set_configs\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"equipment_set_configs\".*"})
	}

	return equipmentSetConfigQuery{q}
}

// FindEquipmentSetConfigG retrieves a single record by ID.
func FindEquipmentSetConfigG(ctx context.Context, iD string, selectCols ...string) (*EquipmentSetConfig, error) {
	return FindEquipmentSetConfig(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindEquipmentSetConfigP retrieves a single record by ID with an executor, and panics on error.
func FindEquipmentSetConfigP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *EquipmentSetConfig {
	retobj, err := FindEquipmentSetConfig(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindEquipmentSetConfigGP retrieves a single record by ID, and panics on error.
func FindEquipmentSetConfigGP(ctx context.Context, iD string, selectCols ...string) *EquipmentSetConfig {
	retobj, err := FindEquipmentSetConfig(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindEquipmentSetConfig retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEquipmentSetConfig(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*EquipmentSetConfig, error) {
	equipmentSetConfigObj := &EquipmentSetConfig{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"equipment_set_configs\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, equipmentSetConfigObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from equipment_set_configs")
	}

	if err = equipmentSetConfigObj.doAfterSelectHooks(ctx, exec); err != nil {
		return equipmentSetConfigObj, err
	}

	return equipmentSetConfigObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *EquipmentSetConfig) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *EquipmentSetConfig) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *EquipmentSetConfig) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EquipmentSetConfig) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no equipment_set_configs provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(equipmentSetConfigColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	equipmentSetConfigInsertCacheMut.RLock()
	cache, cached := equipmentSetConfigInsertCache[key]
	equipmentSetConfigInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			equipmentSetConfigAllColumns,
			equipmentSetConfigColumnsWithDefault,
			equipmentSetConfigColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"equipment_set_configs\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"equipment_set_configs\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into equipment_set_configs")
	}

	if !cached {
		equipmentSetConfigInsertCacheMut.Lock()
		equipmentSetConfigInsertCache[key] = cache
		equipmentSetConfigInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single EquipmentSetConfig record using the global executor.
// See Update for more documentation.
func (o *EquipmentSetConfig) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the EquipmentSetConfig, and panics on error.
// See Update for more documentation.
func (o *EquipmentSetConfig) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single EquipmentSetConfig record using the global executor. Panics on error.
// See Update for more documentation.
func (o *EquipmentSetConfig) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the EquipmentSetConfig.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EquipmentSetConfig) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	equipmentSetConfigUpdateCacheMut.RLock()
	cache, cached := equipmentSetConfigUpdateCache[key]
	equipmentSetConfigUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			equipmentSetConfigAllColumns,
			equipmentSetConfigPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update equipment_set_configs, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"equipment_set_configs\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, equipmentSetConfigPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, append(wl, equipmentSetConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update equipment_set_configs row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for equipment_set_configs")
	}

	if !cached {
		equipmentSetConfigUpdateCacheMut.Lock()
		equipmentSetConfigUpdateCache[key] = cache
		equipmentSetConfigUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q equipmentSetConfigQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q equipmentSetConfigQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q equipmentSetConfigQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q equipmentSetConfigQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for equipment_set_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for equipment_set_configs")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o EquipmentSetConfigSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o EquipmentSetConfigSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o EquipmentSetConfigSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EquipmentSetConfigSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSetConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"equipment_set_configs\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, equipmentSetConfigPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in equipmentSetConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all equipmentSetConfig")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *EquipmentSetConfig) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *EquipmentSetConfig) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *EquipmentSetConfig) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EquipmentSetConfig) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no equipment_set_configs provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(equipmentSetConfigColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	equipmentSetConfigUpsertCacheMut.RLock()
	cache, cached := equipmentSetConfigUpsertCache[key]
	equipmentSetConfigUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			equipmentSetConfigAllColumns,
			equipmentSetConfigColumnsWithDefault,
			equipmentSetConfigColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			equipmentSetConfigAllColumns,
			equipmentSetConfigPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert equipment_set_configs, could not build update column list")
		}

		ret := strmangle.SetComplement(equipmentSetConfigAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(equipmentSetConfigPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert equipment_set_configs, could not build conflict column list")
			}

			conflict = make([]string, len(equipmentSetConfigPrimaryKeyColumns))
			copy(conflict, equipmentSetConfigPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"equipment_set_configs\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert equipment_set_configs")
	}

	if !cached {
		equipmentSetConfigUpsertCacheMut.Lock()
		equipmentSetConfigUpsertCache[key] = cache
		equipmentSetConfigUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single EquipmentSetConfig record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *EquipmentSetConfig) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single EquipmentSetConfig record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *EquipmentSetConfig) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single EquipmentSetConfig record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *EquipmentSetConfig) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single EquipmentSetConfig record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EquipmentSetConfig) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no EquipmentSetConfig provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), equipmentSetConfigPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"equipment_set_configs\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"equipment_set_configs\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(equipmentSetConfigType, equipmentSetConfigMapping, append(wl, equipmentSetConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from equipment_set_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for equipment_set_configs")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q equipmentSetConfigQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q equipmentSetConfigQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q equipmentSetConfigQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q equipmentSetConfigQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no equipmentSetConfigQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from equipment_set_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for equipment_set_configs")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o EquipmentSetConfigSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o EquipmentSetConfigSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o EquipmentSetConfigSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EquipmentSetConfigSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(equipmentSetConfigBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSetConfigPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"equipment_set_configs\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, equipmentSetConfigPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSetConfigPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"equipment_set_configs\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, equipmentSetConfigPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from equipmentSetConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for equipment_set_configs")
	}

	if len(equipmentSetConfigAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *EquipmentSetConfig) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no EquipmentSetConfig provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *EquipmentSetConfig) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *EquipmentSetConfig) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EquipmentSetConfig) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEquipmentSetConfig(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EquipmentSetConfigSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty EquipmentSetConfigSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *EquipmentSetConfigSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *EquipmentSetConfigSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EquipmentSetConfigSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EquipmentSetConfigSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSetConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"equipment_set_configs\".* FROM \"game_config\".\"equipment_set_configs\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, equipmentSetConfigPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in EquipmentSetConfigSlice")
	}

	*o = slice

	return nil
}

// EquipmentSetConfigExistsG checks if the EquipmentSetConfig row exists.
func EquipmentSetConfigExistsG(ctx context.Context, iD string) (bool, error) {
	return EquipmentSetConfigExists(ctx, boil.GetContextDB(), iD)
}

// EquipmentSetConfigExistsP checks if the EquipmentSetConfig row exists. Panics on error.
func EquipmentSetConfigExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := EquipmentSetConfigExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// EquipmentSetConfigExistsGP checks if the EquipmentSetConfig row exists. Panics on error.
func EquipmentSetConfigExistsGP(ctx context.Context, iD string) bool {
	e, err := EquipmentSetConfigExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// EquipmentSetConfigExists checks if the EquipmentSetConfig row exists.
func EquipmentSetConfigExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"equipment_set_configs\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if equipment_set_configs exists")
	}

	return exists, nil
}

// Exists checks if the EquipmentSetConfig row exists.
func (o *EquipmentSetConfig) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EquipmentSetConfigExists(ctx, exec, o.ID)
}
