// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// EquipmentSlot is an object representing the database table.
type EquipmentSlot struct {
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 槽位代码（唯一标识）
	SlotCode string `boil:"slot_code" json:"slot_code" toml:"slot_code" yaml:"slot_code"`
	// 槽位显示名称
	SlotName string `boil:"slot_name" json:"slot_name" toml:"slot_name" yaml:"slot_name"`
	// 槽位类型分类
	SlotType string `boil:"slot_type" json:"slot_type" toml:"slot_type" yaml:"slot_type"`
	// UI显示顺序（数值越小越靠前）
	DisplayOrder int `boil:"display_order" json:"display_order" toml:"display_order" yaml:"display_order"`
	// 槽位图标路径
	Icon null.String `boil:"icon" json:"icon,omitempty" toml:"icon" yaml:"icon,omitempty"`
	// 槽位描述
	Description null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	// 是否启用
	IsActive  bool      `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	// 软删除时间戳
	DeletedAt null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *equipmentSlotR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L equipmentSlotL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var EquipmentSlotColumns = struct {
	ID           string
	SlotCode     string
	SlotName     string
	SlotType     string
	DisplayOrder string
	Icon         string
	Description  string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "id",
	SlotCode:     "slot_code",
	SlotName:     "slot_name",
	SlotType:     "slot_type",
	DisplayOrder: "display_order",
	Icon:         "icon",
	Description:  "description",
	IsActive:     "is_active",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
}

var EquipmentSlotTableColumns = struct {
	ID           string
	SlotCode     string
	SlotName     string
	SlotType     string
	DisplayOrder string
	Icon         string
	Description  string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "equipment_slots.id",
	SlotCode:     "equipment_slots.slot_code",
	SlotName:     "equipment_slots.slot_name",
	SlotType:     "equipment_slots.slot_type",
	DisplayOrder: "equipment_slots.display_order",
	Icon:         "equipment_slots.icon",
	Description:  "equipment_slots.description",
	IsActive:     "equipment_slots.is_active",
	CreatedAt:    "equipment_slots.created_at",
	UpdatedAt:    "equipment_slots.updated_at",
	DeletedAt:    "equipment_slots.deleted_at",
}

// Generated where

var EquipmentSlotWhere = struct {
	ID           whereHelperstring
	SlotCode     whereHelperstring
	SlotName     whereHelperstring
	SlotType     whereHelperstring
	DisplayOrder whereHelperint
	Icon         whereHelpernull_String
	Description  whereHelpernull_String
	IsActive     whereHelperbool
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
	DeletedAt    whereHelpernull_Time
}{
	ID:           whereHelperstring{field: "\"game_config\".\"equipment_slots\".\"id\""},
	SlotCode:     whereHelperstring{field: "\"game_config\".\"equipment_slots\".\"slot_code\""},
	SlotName:     whereHelperstring{field: "\"game_config\".\"equipment_slots\".\"slot_name\""},
	SlotType:     whereHelperstring{field: "\"game_config\".\"equipment_slots\".\"slot_type\""},
	DisplayOrder: whereHelperint{field: "\"game_config\".\"equipment_slots\".\"display_order\""},
	Icon:         whereHelpernull_String{field: "\"game_config\".\"equipment_slots\".\"icon\""},
	Description:  whereHelpernull_String{field: "\"game_config\".\"equipment_slots\".\"description\""},
	IsActive:     whereHelperbool{field: "\"game_config\".\"equipment_slots\".\"is_active\""},
	CreatedAt:    whereHelpertime_Time{field: "\"game_config\".\"equipment_slots\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"game_config\".\"equipment_slots\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"game_config\".\"equipment_slots\".\"deleted_at\""},
}

// EquipmentSlotRels is where relationship names are stored.
var EquipmentSlotRels = struct {
}{}

// equipmentSlotR is where relationships are stored.
type equipmentSlotR struct {
}

// NewStruct creates a new relationship struct
func (*equipmentSlotR) NewStruct() *equipmentSlotR {
	return &equipmentSlotR{}
}

// equipmentSlotL is where Load methods for each relationship are stored.
type equipmentSlotL struct{}

var (
	equipmentSlotAllColumns            = []string{"id", "slot_code", "slot_name", "slot_type", "display_order", "icon", "description", "is_active", "created_at", "updated_at", "deleted_at"}
	equipmentSlotColumnsWithoutDefault = []string{"slot_code", "slot_name", "slot_type"}
	equipmentSlotColumnsWithDefault    = []string{"id", "display_order", "icon", "description", "is_active", "created_at", "updated_at", "deleted_at"}
	equipmentSlotPrimaryKeyColumns     = []string{"id"}
	equipmentSlotGeneratedColumns      = []string{}
)

type (
	// EquipmentSlotSlice is an alias for a slice of pointers to EquipmentSlot.
	// This should almost always be used instead of []EquipmentSlot.
	EquipmentSlotSlice []*EquipmentSlot
	// EquipmentSlotHook is the signature for custom EquipmentSlot hook methods
	EquipmentSlotHook func(context.Context, boil.ContextExecutor, *EquipmentSlot) error

	equipmentSlotQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	equipmentSlotType                 = reflect.TypeOf(&EquipmentSlot{})
	equipmentSlotMapping              = queries.MakeStructMapping(equipmentSlotType)
	equipmentSlotPrimaryKeyMapping, _ = queries.BindMapping(equipmentSlotType, equipmentSlotMapping, equipmentSlotPrimaryKeyColumns)
	equipmentSlotInsertCacheMut       sync.RWMutex
	equipmentSlotInsertCache          = make(map[string]insertCache)
	equipmentSlotUpdateCacheMut       sync.RWMutex
	equipmentSlotUpdateCache          = make(map[string]updateCache)
	equipmentSlotUpsertCacheMut       sync.RWMutex
	equipmentSlotUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var equipmentSlotAfterSelectMu sync.Mutex
var equipmentSlotAfterSelectHooks []EquipmentSlotHook

var equipmentSlotBeforeInsertMu sync.Mutex
var equipmentSlotBeforeInsertHooks []EquipmentSlotHook
var equipmentSlotAfterInsertMu sync.Mutex
var equipmentSlotAfterInsertHooks []EquipmentSlotHook

var equipmentSlotBeforeUpdateMu sync.Mutex
var equipmentSlotBeforeUpdateHooks []EquipmentSlotHook
var equipmentSlotAfterUpdateMu sync.Mutex
var equipmentSlotAfterUpdateHooks []EquipmentSlotHook

var equipmentSlotBeforeDeleteMu sync.Mutex
var equipmentSlotBeforeDeleteHooks []EquipmentSlotHook
var equipmentSlotAfterDeleteMu sync.Mutex
var equipmentSlotAfterDeleteHooks []EquipmentSlotHook

var equipmentSlotBeforeUpsertMu sync.Mutex
var equipmentSlotBeforeUpsertHooks []EquipmentSlotHook
var equipmentSlotAfterUpsertMu sync.Mutex
var equipmentSlotAfterUpsertHooks []EquipmentSlotHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *EquipmentSlot) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *EquipmentSlot) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *EquipmentSlot) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *EquipmentSlot) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *EquipmentSlot) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *EquipmentSlot) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *EquipmentSlot) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *EquipmentSlot) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *EquipmentSlot) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range equipmentSlotAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddEquipmentSlotHook registers your hook function for all future operations.
func AddEquipmentSlotHook(hookPoint boil.HookPoint, equipmentSlotHook EquipmentSlotHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		equipmentSlotAfterSelectMu.Lock()
		equipmentSlotAfterSelectHooks = append(equipmentSlotAfterSelectHooks, equipmentSlotHook)
		equipmentSlotAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		equipmentSlotBeforeInsertMu.Lock()
		equipmentSlotBeforeInsertHooks = append(equipmentSlotBeforeInsertHooks, equipmentSlotHook)
		equipmentSlotBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		equipmentSlotAfterInsertMu.Lock()
		equipmentSlotAfterInsertHooks = append(equipmentSlotAfterInsertHooks, equipmentSlotHook)
		equipmentSlotAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		equipmentSlotBeforeUpdateMu.Lock()
		equipmentSlotBeforeUpdateHooks = append(equipmentSlotBeforeUpdateHooks, equipmentSlotHook)
		equipmentSlotBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		equipmentSlotAfterUpdateMu.Lock()
		equipmentSlotAfterUpdateHooks = append(equipmentSlotAfterUpdateHooks, equipmentSlotHook)
		equipmentSlotAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		equipmentSlotBeforeDeleteMu.Lock()
		equipmentSlotBeforeDeleteHooks = append(equipmentSlotBeforeDeleteHooks, equipmentSlotHook)
		equipmentSlotBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		equipmentSlotAfterDeleteMu.Lock()
		equipmentSlotAfterDeleteHooks = append(equipmentSlotAfterDeleteHooks, equipmentSlotHook)
		equipmentSlotAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		equipmentSlotBeforeUpsertMu.Lock()
		equipmentSlotBeforeUpsertHooks = append(equipmentSlotBeforeUpsertHooks, equipmentSlotHook)
		equipmentSlotBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		equipmentSlotAfterUpsertMu.Lock()
		equipmentSlotAfterUpsertHooks = append(equipmentSlotAfterUpsertHooks, equipmentSlotHook)
		equipmentSlotAfterUpsertMu.Unlock()
	}
}

// OneG returns a single equipmentSlot record from the query using the global executor.
func (q equipmentSlotQuery) OneG(ctx context.Context) (*EquipmentSlot, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single equipmentSlot record from the query using the global executor, and panics on error.
func (q equipmentSlotQuery) OneGP(ctx context.Context) *EquipmentSlot {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single equipmentSlot record from the query, and panics on error.
func (q equipmentSlotQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *EquipmentSlot {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single equipmentSlot record from the query.
func (q equipmentSlotQuery) One(ctx context.Context, exec boil.ContextExecutor) (*EquipmentSlot, error) {
	o := &EquipmentSlot{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for equipment_slots")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all EquipmentSlot records from the query using the global executor.
func (q equipmentSlotQuery) AllG(ctx context.Context) (EquipmentSlotSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all EquipmentSlot records from the query using the global executor, and panics on error.
func (q equipmentSlotQuery) AllGP(ctx context.Context) EquipmentSlotSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all EquipmentSlot records from the query, and panics on error.
func (q equipmentSlotQuery) AllP(ctx context.Context, exec boil.ContextExecutor) EquipmentSlotSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all EquipmentSlot records from the query.
func (q equipmentSlotQuery) All(ctx context.Context, exec boil.ContextExecutor) (EquipmentSlotSlice, error) {
	var o []*EquipmentSlot

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to EquipmentSlot slice")
	}

	if len(equipmentSlotAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all EquipmentSlot records in the query using the global executor
func (q equipmentSlotQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all EquipmentSlot records in the query using the global executor, and panics on error.
func (q equipmentSlotQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all EquipmentSlot records in the query, and panics on error.
func (q equipmentSlotQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all EquipmentSlot records in the query.
func (q equipmentSlotQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count equipment_slots rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q equipmentSlotQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q equipmentSlotQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q equipmentSlotQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q equipmentSlotQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if equipment_slots exists")
	}

	return count > 0, nil
}

// EquipmentSlots retrieves all the records using an executor.
func EquipmentSlots(mods ...qm.QueryMod) equipmentSlotQuery {
	mods = append(mods, qm.From("\"game_config\".\"equipment_slots\""), qmhelper.WhereIsNull("\"game_config\".\"equipment_slots\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"equipment_slots\".*"})
	}

	return equipmentSlotQuery{q}
}

// FindEquipmentSlotG retrieves a single record by ID.
func FindEquipmentSlotG(ctx context.Context, iD string, selectCols ...string) (*EquipmentSlot, error) {
	return FindEquipmentSlot(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindEquipmentSlotP retrieves a single record by ID with an executor, and panics on error.
func FindEquipmentSlotP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *EquipmentSlot {
	retobj, err := FindEquipmentSlot(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindEquipmentSlotGP retrieves a single record by ID, and panics on error.
func FindEquipmentSlotGP(ctx context.Context, iD string, selectCols ...string) *EquipmentSlot {
	retobj, err := FindEquipmentSlot(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindEquipmentSlot retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindEquipmentSlot(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*EquipmentSlot, error) {
	equipmentSlotObj := &EquipmentSlot{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"equipment_slots\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, equipmentSlotObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from equipment_slots")
	}

	if err = equipmentSlotObj.doAfterSelectHooks(ctx, exec); err != nil {
		return equipmentSlotObj, err
	}

	return equipmentSlotObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *EquipmentSlot) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *EquipmentSlot) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *EquipmentSlot) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *EquipmentSlot) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no equipment_slots provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(equipmentSlotColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	equipmentSlotInsertCacheMut.RLock()
	cache, cached := equipmentSlotInsertCache[key]
	equipmentSlotInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			equipmentSlotAllColumns,
			equipmentSlotColumnsWithDefault,
			equipmentSlotColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(equipmentSlotType, equipmentSlotMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(equipmentSlotType, equipmentSlotMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"equipment_slots\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"equipment_slots\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into equipment_slots")
	}

	if !cached {
		equipmentSlotInsertCacheMut.Lock()
		equipmentSlotInsertCache[key] = cache
		equipmentSlotInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single EquipmentSlot record using the global executor.
// See Update for more documentation.
func (o *EquipmentSlot) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the EquipmentSlot, and panics on error.
// See Update for more documentation.
func (o *EquipmentSlot) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single EquipmentSlot record using the global executor. Panics on error.
// See Update for more documentation.
func (o *EquipmentSlot) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the EquipmentSlot.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *EquipmentSlot) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	equipmentSlotUpdateCacheMut.RLock()
	cache, cached := equipmentSlotUpdateCache[key]
	equipmentSlotUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			equipmentSlotAllColumns,
			equipmentSlotPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update equipment_slots, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"equipment_slots\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, equipmentSlotPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(equipmentSlotType, equipmentSlotMapping, append(wl, equipmentSlotPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update equipment_slots row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for equipment_slots")
	}

	if !cached {
		equipmentSlotUpdateCacheMut.Lock()
		equipmentSlotUpdateCache[key] = cache
		equipmentSlotUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q equipmentSlotQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q equipmentSlotQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q equipmentSlotQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q equipmentSlotQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for equipment_slots")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for equipment_slots")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o EquipmentSlotSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o EquipmentSlotSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o EquipmentSlotSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o EquipmentSlotSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSlotPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"equipment_slots\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, equipmentSlotPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in equipmentSlot slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all equipmentSlot")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *EquipmentSlot) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *EquipmentSlot) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *EquipmentSlot) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *EquipmentSlot) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no equipment_slots provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(equipmentSlotColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	equipmentSlotUpsertCacheMut.RLock()
	cache, cached := equipmentSlotUpsertCache[key]
	equipmentSlotUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			equipmentSlotAllColumns,
			equipmentSlotColumnsWithDefault,
			equipmentSlotColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			equipmentSlotAllColumns,
			equipmentSlotPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert equipment_slots, could not build update column list")
		}

		ret := strmangle.SetComplement(equipmentSlotAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(equipmentSlotPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert equipment_slots, could not build conflict column list")
			}

			conflict = make([]string, len(equipmentSlotPrimaryKeyColumns))
			copy(conflict, equipmentSlotPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"equipment_slots\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(equipmentSlotType, equipmentSlotMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(equipmentSlotType, equipmentSlotMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert equipment_slots")
	}

	if !cached {
		equipmentSlotUpsertCacheMut.Lock()
		equipmentSlotUpsertCache[key] = cache
		equipmentSlotUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single EquipmentSlot record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *EquipmentSlot) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single EquipmentSlot record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *EquipmentSlot) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single EquipmentSlot record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *EquipmentSlot) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single EquipmentSlot record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *EquipmentSlot) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no EquipmentSlot provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), equipmentSlotPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"equipment_slots\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"equipment_slots\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(equipmentSlotType, equipmentSlotMapping, append(wl, equipmentSlotPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from equipment_slots")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for equipment_slots")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q equipmentSlotQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q equipmentSlotQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q equipmentSlotQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q equipmentSlotQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no equipmentSlotQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from equipment_slots")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for equipment_slots")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o EquipmentSlotSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o EquipmentSlotSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o EquipmentSlotSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o EquipmentSlotSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(equipmentSlotBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSlotPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"equipment_slots\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, equipmentSlotPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSlotPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"equipment_slots\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, equipmentSlotPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from equipmentSlot slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for equipment_slots")
	}

	if len(equipmentSlotAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *EquipmentSlot) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no EquipmentSlot provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *EquipmentSlot) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *EquipmentSlot) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *EquipmentSlot) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindEquipmentSlot(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EquipmentSlotSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty EquipmentSlotSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *EquipmentSlotSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *EquipmentSlotSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *EquipmentSlotSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := EquipmentSlotSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), equipmentSlotPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"equipment_slots\".* FROM \"game_config\".\"equipment_slots\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, equipmentSlotPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in EquipmentSlotSlice")
	}

	*o = slice

	return nil
}

// EquipmentSlotExistsG checks if the EquipmentSlot row exists.
func EquipmentSlotExistsG(ctx context.Context, iD string) (bool, error) {
	return EquipmentSlotExists(ctx, boil.GetContextDB(), iD)
}

// EquipmentSlotExistsP checks if the EquipmentSlot row exists. Panics on error.
func EquipmentSlotExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := EquipmentSlotExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// EquipmentSlotExistsGP checks if the EquipmentSlot row exists. Panics on error.
func EquipmentSlotExistsGP(ctx context.Context, iD string) bool {
	e, err := EquipmentSlotExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// EquipmentSlotExists checks if the EquipmentSlot row exists.
func EquipmentSlotExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"equipment_slots\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if equipment_slots exists")
	}

	return exists, nil
}

// Exists checks if the EquipmentSlot row exists.
func (o *EquipmentSlot) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return EquipmentSlotExists(ctx, exec, o.ID)
}
