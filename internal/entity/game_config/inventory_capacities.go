// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// InventoryCapacity is an object representing the database table.
type InventoryCapacity struct {
	Location string `boil:"location" json:"location" toml:"location" yaml:"location"`
	MaxSlots int    `boil:"max_slots" json:"max_slots" toml:"max_slots" yaml:"max_slots"`
	MaxStack int    `boil:"max_stack" json:"max_stack" toml:"max_stack" yaml:"max_stack"`

	R *inventoryCapacityR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L inventoryCapacityL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var InventoryCapacityColumns = struct {
	Location string
	MaxSlots string
	MaxStack string
}{
	Location: "location",
	MaxSlots: "max_slots",
	MaxStack: "max_stack",
}

var InventoryCapacityTableColumns = struct {
	Location string
	MaxSlots string
	MaxStack string
}{
	Location: "inventory_capacities.location",
	MaxSlots: "inventory_capacities.max_slots",
	MaxStack: "inventory_capacities.max_stack",
}

// Generated where

var InventoryCapacityWhere = struct {
	Location whereHelperstring
	MaxSlots whereHelperint
	MaxStack whereHelperint
}{
	Location: whereHelperstring{field: "\"game_config\".\"inventory_capacities\".\"location\""},
	MaxSlots: whereHelperint{field: "\"game_config\".\"inventory_capacities\".\"max_slots\""},
	MaxStack: whereHelperint{field: "\"game_config\".\"inventory_capacities\".\"max_stack\""},
}

// InventoryCapacityRels is where relationship names are stored.
var InventoryCapacityRels = struct {
}{}

// inventoryCapacityR is where relationships are stored.
type inventoryCapacityR struct {
}

// NewStruct creates a new relationship struct
func (*inventoryCapacityR) NewStruct() *inventoryCapacityR {
	return &inventoryCapacityR{}
}

// inventoryCapacityL is where Load methods for each relationship are stored.
type inventoryCapacityL struct{}

var (
	inventoryCapacityAllColumns            = []string{"location", "max_slots", "max_stack"}
	inventoryCapacityColumnsWithoutDefault = []string{"location", "max_slots", "max_stack"}
	inventoryCapacityColumnsWithDefault    = []string{}
	inventoryCapacityPrimaryKeyColumns     = []string{"location"}
	inventoryCapacityGeneratedColumns      = []string{}
)

type (
	// InventoryCapacitySlice is an alias for a slice of pointers to InventoryCapacity.
	// This should almost always be used instead of []InventoryCapacity.
	InventoryCapacitySlice []*InventoryCapacity
	// InventoryCapacityHook is the signature for custom InventoryCapacity hook methods
	InventoryCapacityHook func(context.Context, boil.ContextExecutor, *InventoryCapacity) error

	inventoryCapacityQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	inventoryCapacityType                 = reflect.TypeOf(&InventoryCapacity{})
	inventoryCapacityMapping              = queries.MakeStructMapping(inventoryCapacityType)
	inventoryCapacityPrimaryKeyMapping, _ = queries.BindMapping(inventoryCapacityType, inventoryCapacityMapping, inventoryCapacityPrimaryKeyColumns)
	inventoryCapacityInsertCacheMut       sync.RWMutex
	inventoryCapacityInsertCache          = make(map[string]insertCache)
	inventoryCapacityUpdateCacheMut       sync.RWMutex
	inventoryCapacityUpdateCache          = make(map[string]updateCache)
	inventoryCapacityUpsertCacheMut       sync.RWMutex
	inventoryCapacityUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var inventoryCapacityAfterSelectMu sync.Mutex
var inventoryCapacityAfterSelectHooks []InventoryCapacityHook

var inventoryCapacityBeforeInsertMu sync.Mutex
var inventoryCapacityBeforeInsertHooks []InventoryCapacityHook
var inventoryCapacityAfterInsertMu sync.Mutex
var inventoryCapacityAfterInsertHooks []InventoryCapacityHook

var inventoryCapacityBeforeUpdateMu sync.Mutex
var inventoryCapacityBeforeUpdateHooks []InventoryCapacityHook
var inventoryCapacityAfterUpdateMu sync.Mutex
var inventoryCapacityAfterUpdateHooks []InventoryCapacityHook

var inventoryCapacityBeforeDeleteMu sync.Mutex
var inventoryCapacityBeforeDeleteHooks []InventoryCapacityHook
var inventoryCapacityAfterDeleteMu sync.Mutex
var inventoryCapacityAfterDeleteHooks []InventoryCapacityHook

var inventoryCapacityBeforeUpsertMu sync.Mutex
var inventoryCapacityBeforeUpsertHooks []InventoryCapacityHook
var inventoryCapacityAfterUpsertMu sync.Mutex
var inventoryCapacityAfterUpsertHooks []InventoryCapacityHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *InventoryCapacity) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *InventoryCapacity) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *InventoryCapacity) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *InventoryCapacity) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *InventoryCapacity) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *InventoryCapacity) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *InventoryCapacity) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *InventoryCapacity) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *InventoryCapacity) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range inventoryCapacityAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddInventoryCapacityHook registers your hook function for all future operations.
func AddInventoryCapacityHook(hookPoint boil.HookPoint, inventoryCapacityHook InventoryCapacityHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		inventoryCapacityAfterSelectMu.Lock()
		inventoryCapacityAfterSelectHooks = append(inventoryCapacityAfterSelectHooks, inventoryCapacityHook)
		inventoryCapacityAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		inventoryCapacityBeforeInsertMu.Lock()
		inventoryCapacityBeforeInsertHooks = append(inventoryCapacityBeforeInsertHooks, inventoryCapacityHook)
		inventoryCapacityBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		inventoryCapacityAfterInsertMu.Lock()
		inventoryCapacityAfterInsertHooks = append(inventoryCapacityAfterInsertHooks, inventoryCapacityHook)
		inventoryCapacityAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		inventoryCapacityBeforeUpdateMu.Lock()
		inventoryCapacityBeforeUpdateHooks = append(inventoryCapacityBeforeUpdateHooks, inventoryCapacityHook)
		inventoryCapacityBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		inventoryCapacityAfterUpdateMu.Lock()
		inventoryCapacityAfterUpdateHooks = append(inventoryCapacityAfterUpdateHooks, inventoryCapacityHook)
		inventoryCapacityAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		inventoryCapacityBeforeDeleteMu.Lock()
		inventoryCapacityBeforeDeleteHooks = append(inventoryCapacityBeforeDeleteHooks, inventoryCapacityHook)
		inventoryCapacityBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		inventoryCapacityAfterDeleteMu.Lock()
		inventoryCapacityAfterDeleteHooks = append(inventoryCapacityAfterDeleteHooks, inventoryCapacityHook)
		inventoryCapacityAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		inventoryCapacityBeforeUpsertMu.Lock()
		inventoryCapacityBeforeUpsertHooks = append(inventoryCapacityBeforeUpsertHooks, inventoryCapacityHook)
		inventoryCapacityBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		inventoryCapacityAfterUpsertMu.Lock()
		inventoryCapacityAfterUpsertHooks = append(inventoryCapacityAfterUpsertHooks, inventoryCapacityHook)
		inventoryCapacityAfterUpsertMu.Unlock()
	}
}

// OneG returns a single inventoryCapacity record from the query using the global executor.
func (q inventoryCapacityQuery) OneG(ctx context.Context) (*InventoryCapacity, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single inventoryCapacity record from the query using the global executor, and panics on error.
func (q inventoryCapacityQuery) OneGP(ctx context.Context) *InventoryCapacity {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single inventoryCapacity record from the query, and panics on error.
func (q inventoryCapacityQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *InventoryCapacity {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single inventoryCapacity record from the query.
func (q inventoryCapacityQuery) One(ctx context.Context, exec boil.ContextExecutor) (*InventoryCapacity, error) {
	o := &InventoryCapacity{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for inventory_capacities")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all InventoryCapacity records from the query using the global executor.
func (q inventoryCapacityQuery) AllG(ctx context.Context) (InventoryCapacitySlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all InventoryCapacity records from the query using the global executor, and panics on error.
func (q inventoryCapacityQuery) AllGP(ctx context.Context) InventoryCapacitySlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all InventoryCapacity records from the query, and panics on error.
func (q inventoryCapacityQuery) AllP(ctx context.Context, exec boil.ContextExecutor) InventoryCapacitySlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all InventoryCapacity records from the query.
func (q inventoryCapacityQuery) All(ctx context.Context, exec boil.ContextExecutor) (InventoryCapacitySlice, error) {
	var o []*InventoryCapacity

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to InventoryCapacity slice")
	}

	if len(inventoryCapacityAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all InventoryCapacity records in the query using the global executor
func (q inventoryCapacityQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all InventoryCapacity records in the query using the global executor, and panics on error.
func (q inventoryCapacityQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all InventoryCapacity records in the query, and panics on error.
func (q inventoryCapacityQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all InventoryCapacity records in the query.
func (q inventoryCapacityQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count inventory_capacities rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q inventoryCapacityQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q inventoryCapacityQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q inventoryCapacityQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q inventoryCapacityQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if inventory_capacities exists")
	}

	return count > 0, nil
}

// InventoryCapacities retrieves all the records using an executor.
func InventoryCapacities(mods ...qm.QueryMod) inventoryCapacityQuery {
	mods = append(mods, qm.From("\"game_config\".\"inventory_capacities\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"inventory_capacities\".*"})
	}

	return inventoryCapacityQuery{q}
}

// FindInventoryCapacityG retrieves a single record by ID.
func FindInventoryCapacityG(ctx context.Context, location string, selectCols ...string) (*InventoryCapacity, error) {
	return FindInventoryCapacity(ctx, boil.GetContextDB(), location, selectCols...)
}

// FindInventoryCapacityP retrieves a single record by ID with an executor, and panics on error.
func FindInventoryCapacityP(ctx context.Context, exec boil.ContextExecutor, location string, selectCols ...string) *InventoryCapacity {
	retobj, err := FindInventoryCapacity(ctx, exec, location, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindInventoryCapacityGP retrieves a single record by ID, and panics on error.
func FindInventoryCapacityGP(ctx context.Context, location string, selectCols ...string) *InventoryCapacity {
	retobj, err := FindInventoryCapacity(ctx, boil.GetContextDB(), location, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindInventoryCapacity retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindInventoryCapacity(ctx context.Context, exec boil.ContextExecutor, location string, selectCols ...string) (*InventoryCapacity, error) {
	inventoryCapacityObj := &InventoryCapacity{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"inventory_capacities\" where \"location\"=$1", sel,
	)

	q := queries.Raw(query, location)

	err := q.Bind(ctx, exec, inventoryCapacityObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from inventory_capacities")
	}

	if err = inventoryCapacityObj.doAfterSelectHooks(ctx, exec); err != nil {
		return inventoryCapacityObj, err
	}

	return inventoryCapacityObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *InventoryCapacity) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *InventoryCapacity) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *InventoryCapacity) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *InventoryCapacity) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no inventory_capacities provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(inventoryCapacityColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	inventoryCapacityInsertCacheMut.RLock()
	cache, cached := inventoryCapacityInsertCache[key]
	inventoryCapacityInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			inventoryCapacityAllColumns,
			inventoryCapacityColumnsWithDefault,
			inventoryCapacityColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(inventoryCapacityType, inventoryCapacityMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(inventoryCapacityType, inventoryCapacityMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"inventory_capacities\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"inventory_capacities\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into inventory_capacities")
	}

	if !cached {
		inventoryCapacityInsertCacheMut.Lock()
		inventoryCapacityInsertCache[key] = cache
		inventoryCapacityInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single InventoryCapacity record using the global executor.
// See Update for more documentation.
func (o *InventoryCapacity) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the InventoryCapacity, and panics on error.
// See Update for more documentation.
func (o *InventoryCapacity) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single InventoryCapacity record using the global executor. Panics on error.
// See Update for more documentation.
func (o *InventoryCapacity) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the InventoryCapacity.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *InventoryCapacity) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	inventoryCapacityUpdateCacheMut.RLock()
	cache, cached := inventoryCapacityUpdateCache[key]
	inventoryCapacityUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			inventoryCapacityAllColumns,
			inventoryCapacityPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update inventory_capacities, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"inventory_capacities\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, inventoryCapacityPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(inventoryCapacityType, inventoryCapacityMapping, append(wl, inventoryCapacityPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update inventory_capacities row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for inventory_capacities")
	}

	if !cached {
		inventoryCapacityUpdateCacheMut.Lock()
		inventoryCapacityUpdateCache[key] = cache
		inventoryCapacityUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q inventoryCapacityQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q inventoryCapacityQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q inventoryCapacityQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q inventoryCapacityQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for inventory_capacities")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for inventory_capacities")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o InventoryCapacitySlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o InventoryCapacitySlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o InventoryCapacitySlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o InventoryCapacitySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), inventoryCapacityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"inventory_capacities\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, inventoryCapacityPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in inventoryCapacity slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all inventoryCapacity")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *InventoryCapacity) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *InventoryCapacity) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *InventoryCapacity) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *InventoryCapacity) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no inventory_capacities provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(inventoryCapacityColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	inventoryCapacityUpsertCacheMut.RLock()
	cache, cached := inventoryCapacityUpsertCache[key]
	inventoryCapacityUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			inventoryCapacityAllColumns,
			inventoryCapacityColumnsWithDefault,
			inventoryCapacityColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			inventoryCapacityAllColumns,
			inventoryCapacityPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert inventory_capacities, could not build update column list")
		}

		ret := strmangle.SetComplement(inventoryCapacityAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(inventoryCapacityPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert inventory_capacities, could not build conflict column list")
			}

			conflict = make([]string, len(inventoryCapacityPrimaryKeyColumns))
			copy(conflict, inventoryCapacityPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"inventory_capacities\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(inventoryCapacityType, inventoryCapacityMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(inventoryCapacityType, inventoryCapacityMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert inventory_capacities")
	}

	if !cached {
		inventoryCapacityUpsertCacheMut.Lock()
		inventoryCapacityUpsertCache[key] = cache
		inventoryCapacityUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single InventoryCapacity record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *InventoryCapacity) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single InventoryCapacity record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *InventoryCapacity) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single InventoryCapacity record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *InventoryCapacity) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single InventoryCapacity record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *InventoryCapacity) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no InventoryCapacity provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), inventoryCapacityPrimaryKeyMapping)
	sql := "DELETE FROM \"game_config\".\"inventory_capacities\" WHERE \"location\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from inventory_capacities")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for inventory_capacities")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q inventoryCapacityQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q inventoryCapacityQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q inventoryCapacityQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q inventoryCapacityQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no inventoryCapacityQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from inventory_capacities")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for inventory_capacities")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o InventoryCapacitySlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o InventoryCapacitySlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o InventoryCapacitySlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o InventoryCapacitySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(inventoryCapacityBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), inventoryCapacityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_config\".\"inventory_capacities\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, inventoryCapacityPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from inventoryCapacity slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for inventory_capacities")
	}

	if len(inventoryCapacityAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *InventoryCapacity) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no InventoryCapacity provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *InventoryCapacity) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *InventoryCapacity) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *InventoryCapacity) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindInventoryCapacity(ctx, exec, o.Location)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *InventoryCapacitySlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty InventoryCapacitySlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *InventoryCapacitySlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *InventoryCapacitySlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *InventoryCapacitySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := InventoryCapacitySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), inventoryCapacityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"inventory_capacities\".* FROM \"game_config\".\"inventory_capacities\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, inventoryCapacityPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in InventoryCapacitySlice")
	}

	*o = slice

	return nil
}

// InventoryCapacityExistsG checks if the InventoryCapacity row exists.
func InventoryCapacityExistsG(ctx context.Context, location string) (bool, error) {
	return InventoryCapacityExists(ctx, boil.GetContextDB(), location)
}

// InventoryCapacityExistsP checks if the InventoryCapacity row exists. Panics on error.
func InventoryCapacityExistsP(ctx context.Context, exec boil.ContextExecutor, location string) bool {
	e, err := InventoryCapacityExists(ctx, exec, location)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// InventoryCapacityExistsGP checks if the InventoryCapacity row exists. Panics on error.
func InventoryCapacityExistsGP(ctx context.Context, location string) bool {
	e, err := InventoryCapacityExists(ctx, boil.GetContextDB(), location)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// InventoryCapacityExists checks if the InventoryCapacity row exists.
func InventoryCapacityExists(ctx context.Context, exec boil.ContextExecutor, location string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"inventory_capacities\" where \"location\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, location)
	}
	row := exec.QueryRowContext(ctx, sql, location)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if inventory_capacities exists")
	}

	return exists, nil
}

// Exists checks if the InventoryCapacity row exists.
func (o *InventoryCapacity) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return InventoryCapacityExists(ctx, exec, o.Location)
}
