// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// MetadataDictionary is an object representing the database table.
type MetadataDictionary struct {
	ID           string `boil:"id" json:"id" toml:"id" yaml:"id"`
	VariableCode string `boil:"variable_code" json:"variable_code" toml:"variable_code" yaml:"variable_code"`
	VariableName string `boil:"variable_name" json:"variable_name" toml:"variable_name" yaml:"variable_name"`
	// 变量类型：基础属性、目标选择、技能数据、装备数据
	VariableType string `boil:"variable_type" json:"variable_type" toml:"variable_type" yaml:"variable_type"`
	// 变量作用域：角色、动作、技能、装备、全局
	Scope       string      `boil:"scope" json:"scope" toml:"scope" yaml:"scope"`
	DataType    string      `boil:"data_type" json:"data_type" toml:"data_type" yaml:"data_type"`
	Description null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	Example     null.String `boil:"example" json:"example,omitempty" toml:"example" yaml:"example,omitempty"`
	IsActive    null.Bool   `boil:"is_active" json:"is_active,omitempty" toml:"is_active" yaml:"is_active,omitempty"`
	CreatedAt   null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt   null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	DeletedAt   null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	// 字典分类: formula(公式变量), action_attribute(动作属性), effect_param(效果参数)等
	DictCategory string `boil:"dict_category" json:"dict_category" toml:"dict_category" yaml:"dict_category"`
	// 扩展元数据,JSON格式,可存储如scalable(是否可配置)、scaling_types(支持的成长类型)等信息
	Metadata null.JSON `boil:"metadata" json:"metadata,omitempty" toml:"metadata" yaml:"metadata,omitempty"`

	R *metadataDictionaryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L metadataDictionaryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MetadataDictionaryColumns = struct {
	ID           string
	VariableCode string
	VariableName string
	VariableType string
	Scope        string
	DataType     string
	Description  string
	Example      string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
	DictCategory string
	Metadata     string
}{
	ID:           "id",
	VariableCode: "variable_code",
	VariableName: "variable_name",
	VariableType: "variable_type",
	Scope:        "scope",
	DataType:     "data_type",
	Description:  "description",
	Example:      "example",
	IsActive:     "is_active",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
	DictCategory: "dict_category",
	Metadata:     "metadata",
}

var MetadataDictionaryTableColumns = struct {
	ID           string
	VariableCode string
	VariableName string
	VariableType string
	Scope        string
	DataType     string
	Description  string
	Example      string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
	DictCategory string
	Metadata     string
}{
	ID:           "metadata_dictionary.id",
	VariableCode: "metadata_dictionary.variable_code",
	VariableName: "metadata_dictionary.variable_name",
	VariableType: "metadata_dictionary.variable_type",
	Scope:        "metadata_dictionary.scope",
	DataType:     "metadata_dictionary.data_type",
	Description:  "metadata_dictionary.description",
	Example:      "metadata_dictionary.example",
	IsActive:     "metadata_dictionary.is_active",
	CreatedAt:    "metadata_dictionary.created_at",
	UpdatedAt:    "metadata_dictionary.updated_at",
	DeletedAt:    "metadata_dictionary.deleted_at",
	DictCategory: "metadata_dictionary.dict_category",
	Metadata:     "metadata_dictionary.metadata",
}

// Generated where

var MetadataDictionaryWhere = struct {
	ID           whereHelperstring
	VariableCode whereHelperstring
	VariableName whereHelperstring
	VariableType whereHelperstring
	Scope        whereHelperstring
	DataType     whereHelperstring
	Description  whereHelpernull_String
	Example      whereHelpernull_String
	IsActive     whereHelpernull_Bool
	CreatedAt    whereHelpernull_Time
	UpdatedAt    whereHelpernull_Time
	DeletedAt    whereHelpernull_Time
	DictCategory whereHelperstring
	Metadata     whereHelpernull_JSON
}{
	ID:           whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"id\""},
	VariableCode: whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"variable_code\""},
	VariableName: whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"variable_name\""},
	VariableType: whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"variable_type\""},
	Scope:        whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"scope\""},
	DataType:     whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"data_type\""},
	Description:  whereHelpernull_String{field: "\"game_config\".\"metadata_dictionary\".\"description\""},
	Example:      whereHelpernull_String{field: "\"game_config\".\"metadata_dictionary\".\"example\""},
	IsActive:     whereHelpernull_Bool{field: "\"game_config\".\"metadata_dictionary\".\"is_active\""},
	CreatedAt:    whereHelpernull_Time{field: "\"game_config\".\"metadata_dictionary\".\"created_at\""},
	UpdatedAt:    whereHelpernull_Time{field: "\"game_config\".\"metadata_dictionary\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"game_config\".\"metadata_dictionary\".\"deleted_at\""},
	DictCategory: whereHelperstring{field: "\"game_config\".\"metadata_dictionary\".\"dict_category\""},
	Metadata:     whereHelpernull_JSON{field: "\"game_config\".\"metadata_dictionary\".\"metadata\""},
}

// MetadataDictionaryRels is where relationship names are stored.
var MetadataDictionaryRels = struct {
}{}

// metadataDictionaryR is where relationships are stored.
type metadataDictionaryR struct {
}

// NewStruct creates a new relationship struct
func (*metadataDictionaryR) NewStruct() *metadataDictionaryR {
	return &metadataDictionaryR{}
}

// metadataDictionaryL is where Load methods for each relationship are stored.
type metadataDictionaryL struct{}

var (
	metadataDictionaryAllColumns            = []string{"id", "variable_code", "variable_name", "variable_type", "scope", "data_type", "description", "example", "is_active", "created_at", "updated_at", "deleted_at", "dict_category", "metadata"}
	metadataDictionaryColumnsWithoutDefault = []string{"variable_code", "variable_name", "variable_type", "scope", "data_type"}
	metadataDictionaryColumnsWithDefault    = []string{"id", "description", "example", "is_active", "created_at", "updated_at", "deleted_at", "dict_category", "metadata"}
	metadataDictionaryPrimaryKeyColumns     = []string{"id"}
	metadataDictionaryGeneratedColumns      = []string{}
)

type (
	// MetadataDictionarySlice is an alias for a slice of pointers to MetadataDictionary.
	// This should almost always be used instead of []MetadataDictionary.
	MetadataDictionarySlice []*MetadataDictionary
	// MetadataDictionaryHook is the signature for custom MetadataDictionary hook methods
	MetadataDictionaryHook func(context.Context, boil.ContextExecutor, *MetadataDictionary) error

	metadataDictionaryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	metadataDictionaryType                 = reflect.TypeOf(&MetadataDictionary{})
	metadataDictionaryMapping              = queries.MakeStructMapping(metadataDictionaryType)
	metadataDictionaryPrimaryKeyMapping, _ = queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, metadataDictionaryPrimaryKeyColumns)
	metadataDictionaryInsertCacheMut       sync.RWMutex
	metadataDictionaryInsertCache          = make(map[string]insertCache)
	metadataDictionaryUpdateCacheMut       sync.RWMutex
	metadataDictionaryUpdateCache          = make(map[string]updateCache)
	metadataDictionaryUpsertCacheMut       sync.RWMutex
	metadataDictionaryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var metadataDictionaryAfterSelectMu sync.Mutex
var metadataDictionaryAfterSelectHooks []MetadataDictionaryHook

var metadataDictionaryBeforeInsertMu sync.Mutex
var metadataDictionaryBeforeInsertHooks []MetadataDictionaryHook
var metadataDictionaryAfterInsertMu sync.Mutex
var metadataDictionaryAfterInsertHooks []MetadataDictionaryHook

var metadataDictionaryBeforeUpdateMu sync.Mutex
var metadataDictionaryBeforeUpdateHooks []MetadataDictionaryHook
var metadataDictionaryAfterUpdateMu sync.Mutex
var metadataDictionaryAfterUpdateHooks []MetadataDictionaryHook

var metadataDictionaryBeforeDeleteMu sync.Mutex
var metadataDictionaryBeforeDeleteHooks []MetadataDictionaryHook
var metadataDictionaryAfterDeleteMu sync.Mutex
var metadataDictionaryAfterDeleteHooks []MetadataDictionaryHook

var metadataDictionaryBeforeUpsertMu sync.Mutex
var metadataDictionaryBeforeUpsertHooks []MetadataDictionaryHook
var metadataDictionaryAfterUpsertMu sync.Mutex
var metadataDictionaryAfterUpsertHooks []MetadataDictionaryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MetadataDictionary) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MetadataDictionary) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MetadataDictionary) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MetadataDictionary) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MetadataDictionary) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MetadataDictionary) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MetadataDictionary) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MetadataDictionary) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MetadataDictionary) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range metadataDictionaryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMetadataDictionaryHook registers your hook function for all future operations.
func AddMetadataDictionaryHook(hookPoint boil.HookPoint, metadataDictionaryHook MetadataDictionaryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		metadataDictionaryAfterSelectMu.Lock()
		metadataDictionaryAfterSelectHooks = append(metadataDictionaryAfterSelectHooks, metadataDictionaryHook)
		metadataDictionaryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		metadataDictionaryBeforeInsertMu.Lock()
		metadataDictionaryBeforeInsertHooks = append(metadataDictionaryBeforeInsertHooks, metadataDictionaryHook)
		metadataDictionaryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		metadataDictionaryAfterInsertMu.Lock()
		metadataDictionaryAfterInsertHooks = append(metadataDictionaryAfterInsertHooks, metadataDictionaryHook)
		metadataDictionaryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		metadataDictionaryBeforeUpdateMu.Lock()
		metadataDictionaryBeforeUpdateHooks = append(metadataDictionaryBeforeUpdateHooks, metadataDictionaryHook)
		metadataDictionaryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		metadataDictionaryAfterUpdateMu.Lock()
		metadataDictionaryAfterUpdateHooks = append(metadataDictionaryAfterUpdateHooks, metadataDictionaryHook)
		metadataDictionaryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		metadataDictionaryBeforeDeleteMu.Lock()
		metadataDictionaryBeforeDeleteHooks = append(metadataDictionaryBeforeDeleteHooks, metadataDictionaryHook)
		metadataDictionaryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		metadataDictionaryAfterDeleteMu.Lock()
		metadataDictionaryAfterDeleteHooks = append(metadataDictionaryAfterDeleteHooks, metadataDictionaryHook)
		metadataDictionaryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		metadataDictionaryBeforeUpsertMu.Lock()
		metadataDictionaryBeforeUpsertHooks = append(metadataDictionaryBeforeUpsertHooks, metadataDictionaryHook)
		metadataDictionaryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		metadataDictionaryAfterUpsertMu.Lock()
		metadataDictionaryAfterUpsertHooks = append(metadataDictionaryAfterUpsertHooks, metadataDictionaryHook)
		metadataDictionaryAfterUpsertMu.Unlock()
	}
}

// OneG returns a single metadataDictionary record from the query using the global executor.
func (q metadataDictionaryQuery) OneG(ctx context.Context) (*MetadataDictionary, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single metadataDictionary record from the query using the global executor, and panics on error.
func (q metadataDictionaryQuery) OneGP(ctx context.Context) *MetadataDictionary {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single metadataDictionary record from the query, and panics on error.
func (q metadataDictionaryQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *MetadataDictionary {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single metadataDictionary record from the query.
func (q metadataDictionaryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MetadataDictionary, error) {
	o := &MetadataDictionary{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for metadata_dictionary")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all MetadataDictionary records from the query using the global executor.
func (q metadataDictionaryQuery) AllG(ctx context.Context) (MetadataDictionarySlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all MetadataDictionary records from the query using the global executor, and panics on error.
func (q metadataDictionaryQuery) AllGP(ctx context.Context) MetadataDictionarySlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all MetadataDictionary records from the query, and panics on error.
func (q metadataDictionaryQuery) AllP(ctx context.Context, exec boil.ContextExecutor) MetadataDictionarySlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all MetadataDictionary records from the query.
func (q metadataDictionaryQuery) All(ctx context.Context, exec boil.ContextExecutor) (MetadataDictionarySlice, error) {
	var o []*MetadataDictionary

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to MetadataDictionary slice")
	}

	if len(metadataDictionaryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all MetadataDictionary records in the query using the global executor
func (q metadataDictionaryQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all MetadataDictionary records in the query using the global executor, and panics on error.
func (q metadataDictionaryQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all MetadataDictionary records in the query, and panics on error.
func (q metadataDictionaryQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all MetadataDictionary records in the query.
func (q metadataDictionaryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count metadata_dictionary rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q metadataDictionaryQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q metadataDictionaryQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q metadataDictionaryQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q metadataDictionaryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if metadata_dictionary exists")
	}

	return count > 0, nil
}

// MetadataDictionaries retrieves all the records using an executor.
func MetadataDictionaries(mods ...qm.QueryMod) metadataDictionaryQuery {
	mods = append(mods, qm.From("\"game_config\".\"metadata_dictionary\""), qmhelper.WhereIsNull("\"game_config\".\"metadata_dictionary\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"metadata_dictionary\".*"})
	}

	return metadataDictionaryQuery{q}
}

// FindMetadataDictionaryG retrieves a single record by ID.
func FindMetadataDictionaryG(ctx context.Context, iD string, selectCols ...string) (*MetadataDictionary, error) {
	return FindMetadataDictionary(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindMetadataDictionaryP retrieves a single record by ID with an executor, and panics on error.
func FindMetadataDictionaryP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *MetadataDictionary {
	retobj, err := FindMetadataDictionary(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindMetadataDictionaryGP retrieves a single record by ID, and panics on error.
func FindMetadataDictionaryGP(ctx context.Context, iD string, selectCols ...string) *MetadataDictionary {
	retobj, err := FindMetadataDictionary(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindMetadataDictionary retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMetadataDictionary(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*MetadataDictionary, error) {
	metadataDictionaryObj := &MetadataDictionary{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"metadata_dictionary\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, metadataDictionaryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from metadata_dictionary")
	}

	if err = metadataDictionaryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return metadataDictionaryObj, err
	}

	return metadataDictionaryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *MetadataDictionary) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *MetadataDictionary) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *MetadataDictionary) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MetadataDictionary) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no metadata_dictionary provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(metadataDictionaryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	metadataDictionaryInsertCacheMut.RLock()
	cache, cached := metadataDictionaryInsertCache[key]
	metadataDictionaryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			metadataDictionaryAllColumns,
			metadataDictionaryColumnsWithDefault,
			metadataDictionaryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"metadata_dictionary\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"metadata_dictionary\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into metadata_dictionary")
	}

	if !cached {
		metadataDictionaryInsertCacheMut.Lock()
		metadataDictionaryInsertCache[key] = cache
		metadataDictionaryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single MetadataDictionary record using the global executor.
// See Update for more documentation.
func (o *MetadataDictionary) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the MetadataDictionary, and panics on error.
// See Update for more documentation.
func (o *MetadataDictionary) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single MetadataDictionary record using the global executor. Panics on error.
// See Update for more documentation.
func (o *MetadataDictionary) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the MetadataDictionary.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MetadataDictionary) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	metadataDictionaryUpdateCacheMut.RLock()
	cache, cached := metadataDictionaryUpdateCache[key]
	metadataDictionaryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			metadataDictionaryAllColumns,
			metadataDictionaryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update metadata_dictionary, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"metadata_dictionary\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, metadataDictionaryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, append(wl, metadataDictionaryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update metadata_dictionary row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for metadata_dictionary")
	}

	if !cached {
		metadataDictionaryUpdateCacheMut.Lock()
		metadataDictionaryUpdateCache[key] = cache
		metadataDictionaryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q metadataDictionaryQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q metadataDictionaryQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q metadataDictionaryQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q metadataDictionaryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for metadata_dictionary")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for metadata_dictionary")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o MetadataDictionarySlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o MetadataDictionarySlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o MetadataDictionarySlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MetadataDictionarySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), metadataDictionaryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"metadata_dictionary\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, metadataDictionaryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in metadataDictionary slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all metadataDictionary")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *MetadataDictionary) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *MetadataDictionary) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *MetadataDictionary) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MetadataDictionary) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no metadata_dictionary provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(metadataDictionaryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	metadataDictionaryUpsertCacheMut.RLock()
	cache, cached := metadataDictionaryUpsertCache[key]
	metadataDictionaryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			metadataDictionaryAllColumns,
			metadataDictionaryColumnsWithDefault,
			metadataDictionaryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			metadataDictionaryAllColumns,
			metadataDictionaryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert metadata_dictionary, could not build update column list")
		}

		ret := strmangle.SetComplement(metadataDictionaryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(metadataDictionaryPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert metadata_dictionary, could not build conflict column list")
			}

			conflict = make([]string, len(metadataDictionaryPrimaryKeyColumns))
			copy(conflict, metadataDictionaryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"metadata_dictionary\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert metadata_dictionary")
	}

	if !cached {
		metadataDictionaryUpsertCacheMut.Lock()
		metadataDictionaryUpsertCache[key] = cache
		metadataDictionaryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single MetadataDictionary record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *MetadataDictionary) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single MetadataDictionary record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *MetadataDictionary) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single MetadataDictionary record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *MetadataDictionary) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single MetadataDictionary record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MetadataDictionary) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no MetadataDictionary provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), metadataDictionaryPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"metadata_dictionary\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"metadata_dictionary\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(metadataDictionaryType, metadataDictionaryMapping, append(wl, metadataDictionaryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from metadata_dictionary")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for metadata_dictionary")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q metadataDictionaryQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q metadataDictionaryQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q metadataDictionaryQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q metadataDictionaryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no metadataDictionaryQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from metadata_dictionary")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for metadata_dictionary")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o MetadataDictionarySlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o MetadataDictionarySlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o MetadataDictionarySlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MetadataDictionarySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(metadataDictionaryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), metadataDictionaryPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"metadata_dictionary\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, metadataDictionaryPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), metadataDictionaryPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"metadata_dictionary\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, metadataDictionaryPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from metadataDictionary slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for metadata_dictionary")
	}

	if len(metadataDictionaryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *MetadataDictionary) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no MetadataDictionary provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *MetadataDictionary) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *MetadataDictionary) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MetadataDictionary) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMetadataDictionary(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MetadataDictionarySlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty MetadataDictionarySlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *MetadataDictionarySlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *MetadataDictionarySlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MetadataDictionarySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MetadataDictionarySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), metadataDictionaryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"metadata_dictionary\".* FROM \"game_config\".\"metadata_dictionary\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, metadataDictionaryPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in MetadataDictionarySlice")
	}

	*o = slice

	return nil
}

// MetadataDictionaryExistsG checks if the MetadataDictionary row exists.
func MetadataDictionaryExistsG(ctx context.Context, iD string) (bool, error) {
	return MetadataDictionaryExists(ctx, boil.GetContextDB(), iD)
}

// MetadataDictionaryExistsP checks if the MetadataDictionary row exists. Panics on error.
func MetadataDictionaryExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := MetadataDictionaryExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// MetadataDictionaryExistsGP checks if the MetadataDictionary row exists. Panics on error.
func MetadataDictionaryExistsGP(ctx context.Context, iD string) bool {
	e, err := MetadataDictionaryExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// MetadataDictionaryExists checks if the MetadataDictionary row exists.
func MetadataDictionaryExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"metadata_dictionary\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if metadata_dictionary exists")
	}

	return exists, nil
}

// Exists checks if the MetadataDictionary row exists.
func (o *MetadataDictionary) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MetadataDictionaryExists(ctx, exec, o.ID)
}
