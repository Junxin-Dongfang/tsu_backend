// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// MonsterSkill is an object representing the database table.
type MonsterSkill struct {
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 怪物ID（外键）
	MonsterID string `boil:"monster_id" json:"monster_id" toml:"monster_id" yaml:"monster_id"`
	// 技能ID（外键）
	SkillID string `boil:"skill_id" json:"skill_id" toml:"skill_id" yaml:"skill_id"`
	// 技能等级（1-20）
	SkillLevel int16 `boil:"skill_level" json:"skill_level" toml:"skill_level" yaml:"skill_level"`
	// 获得的动作列表
	GainActions  types.StringArray `boil:"gain_actions" json:"gain_actions,omitempty" toml:"gain_actions" yaml:"gain_actions,omitempty"`
	IsActive     null.Bool         `boil:"is_active" json:"is_active,omitempty" toml:"is_active" yaml:"is_active,omitempty"`
	DisplayOrder null.Int          `boil:"display_order" json:"display_order,omitempty" toml:"display_order" yaml:"display_order,omitempty"`
	CreatedAt    time.Time         `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt    time.Time         `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt    null.Time         `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *monsterSkillR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L monsterSkillL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MonsterSkillColumns = struct {
	ID           string
	MonsterID    string
	SkillID      string
	SkillLevel   string
	GainActions  string
	IsActive     string
	DisplayOrder string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "id",
	MonsterID:    "monster_id",
	SkillID:      "skill_id",
	SkillLevel:   "skill_level",
	GainActions:  "gain_actions",
	IsActive:     "is_active",
	DisplayOrder: "display_order",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
}

var MonsterSkillTableColumns = struct {
	ID           string
	MonsterID    string
	SkillID      string
	SkillLevel   string
	GainActions  string
	IsActive     string
	DisplayOrder string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "monster_skills.id",
	MonsterID:    "monster_skills.monster_id",
	SkillID:      "monster_skills.skill_id",
	SkillLevel:   "monster_skills.skill_level",
	GainActions:  "monster_skills.gain_actions",
	IsActive:     "monster_skills.is_active",
	DisplayOrder: "monster_skills.display_order",
	CreatedAt:    "monster_skills.created_at",
	UpdatedAt:    "monster_skills.updated_at",
	DeletedAt:    "monster_skills.deleted_at",
}

// Generated where

var MonsterSkillWhere = struct {
	ID           whereHelperstring
	MonsterID    whereHelperstring
	SkillID      whereHelperstring
	SkillLevel   whereHelperint16
	GainActions  whereHelpertypes_StringArray
	IsActive     whereHelpernull_Bool
	DisplayOrder whereHelpernull_Int
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
	DeletedAt    whereHelpernull_Time
}{
	ID:           whereHelperstring{field: "\"game_config\".\"monster_skills\".\"id\""},
	MonsterID:    whereHelperstring{field: "\"game_config\".\"monster_skills\".\"monster_id\""},
	SkillID:      whereHelperstring{field: "\"game_config\".\"monster_skills\".\"skill_id\""},
	SkillLevel:   whereHelperint16{field: "\"game_config\".\"monster_skills\".\"skill_level\""},
	GainActions:  whereHelpertypes_StringArray{field: "\"game_config\".\"monster_skills\".\"gain_actions\""},
	IsActive:     whereHelpernull_Bool{field: "\"game_config\".\"monster_skills\".\"is_active\""},
	DisplayOrder: whereHelpernull_Int{field: "\"game_config\".\"monster_skills\".\"display_order\""},
	CreatedAt:    whereHelpertime_Time{field: "\"game_config\".\"monster_skills\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"game_config\".\"monster_skills\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"game_config\".\"monster_skills\".\"deleted_at\""},
}

// MonsterSkillRels is where relationship names are stored.
var MonsterSkillRels = struct {
	Monster string
	Skill   string
}{
	Monster: "Monster",
	Skill:   "Skill",
}

// monsterSkillR is where relationships are stored.
type monsterSkillR struct {
	Monster *Monster `boil:"Monster" json:"Monster" toml:"Monster" yaml:"Monster"`
	Skill   *Skill   `boil:"Skill" json:"Skill" toml:"Skill" yaml:"Skill"`
}

// NewStruct creates a new relationship struct
func (*monsterSkillR) NewStruct() *monsterSkillR {
	return &monsterSkillR{}
}

func (o *MonsterSkill) GetMonster() *Monster {
	if o == nil {
		return nil
	}

	return o.R.GetMonster()
}

func (r *monsterSkillR) GetMonster() *Monster {
	if r == nil {
		return nil
	}

	return r.Monster
}

func (o *MonsterSkill) GetSkill() *Skill {
	if o == nil {
		return nil
	}

	return o.R.GetSkill()
}

func (r *monsterSkillR) GetSkill() *Skill {
	if r == nil {
		return nil
	}

	return r.Skill
}

// monsterSkillL is where Load methods for each relationship are stored.
type monsterSkillL struct{}

var (
	monsterSkillAllColumns            = []string{"id", "monster_id", "skill_id", "skill_level", "gain_actions", "is_active", "display_order", "created_at", "updated_at", "deleted_at"}
	monsterSkillColumnsWithoutDefault = []string{"monster_id", "skill_id"}
	monsterSkillColumnsWithDefault    = []string{"id", "skill_level", "gain_actions", "is_active", "display_order", "created_at", "updated_at", "deleted_at"}
	monsterSkillPrimaryKeyColumns     = []string{"id"}
	monsterSkillGeneratedColumns      = []string{}
)

type (
	// MonsterSkillSlice is an alias for a slice of pointers to MonsterSkill.
	// This should almost always be used instead of []MonsterSkill.
	MonsterSkillSlice []*MonsterSkill
	// MonsterSkillHook is the signature for custom MonsterSkill hook methods
	MonsterSkillHook func(context.Context, boil.ContextExecutor, *MonsterSkill) error

	monsterSkillQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	monsterSkillType                 = reflect.TypeOf(&MonsterSkill{})
	monsterSkillMapping              = queries.MakeStructMapping(monsterSkillType)
	monsterSkillPrimaryKeyMapping, _ = queries.BindMapping(monsterSkillType, monsterSkillMapping, monsterSkillPrimaryKeyColumns)
	monsterSkillInsertCacheMut       sync.RWMutex
	monsterSkillInsertCache          = make(map[string]insertCache)
	monsterSkillUpdateCacheMut       sync.RWMutex
	monsterSkillUpdateCache          = make(map[string]updateCache)
	monsterSkillUpsertCacheMut       sync.RWMutex
	monsterSkillUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var monsterSkillAfterSelectMu sync.Mutex
var monsterSkillAfterSelectHooks []MonsterSkillHook

var monsterSkillBeforeInsertMu sync.Mutex
var monsterSkillBeforeInsertHooks []MonsterSkillHook
var monsterSkillAfterInsertMu sync.Mutex
var monsterSkillAfterInsertHooks []MonsterSkillHook

var monsterSkillBeforeUpdateMu sync.Mutex
var monsterSkillBeforeUpdateHooks []MonsterSkillHook
var monsterSkillAfterUpdateMu sync.Mutex
var monsterSkillAfterUpdateHooks []MonsterSkillHook

var monsterSkillBeforeDeleteMu sync.Mutex
var monsterSkillBeforeDeleteHooks []MonsterSkillHook
var monsterSkillAfterDeleteMu sync.Mutex
var monsterSkillAfterDeleteHooks []MonsterSkillHook

var monsterSkillBeforeUpsertMu sync.Mutex
var monsterSkillBeforeUpsertHooks []MonsterSkillHook
var monsterSkillAfterUpsertMu sync.Mutex
var monsterSkillAfterUpsertHooks []MonsterSkillHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MonsterSkill) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MonsterSkill) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MonsterSkill) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MonsterSkill) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MonsterSkill) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MonsterSkill) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MonsterSkill) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MonsterSkill) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MonsterSkill) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range monsterSkillAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMonsterSkillHook registers your hook function for all future operations.
func AddMonsterSkillHook(hookPoint boil.HookPoint, monsterSkillHook MonsterSkillHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		monsterSkillAfterSelectMu.Lock()
		monsterSkillAfterSelectHooks = append(monsterSkillAfterSelectHooks, monsterSkillHook)
		monsterSkillAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		monsterSkillBeforeInsertMu.Lock()
		monsterSkillBeforeInsertHooks = append(monsterSkillBeforeInsertHooks, monsterSkillHook)
		monsterSkillBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		monsterSkillAfterInsertMu.Lock()
		monsterSkillAfterInsertHooks = append(monsterSkillAfterInsertHooks, monsterSkillHook)
		monsterSkillAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		monsterSkillBeforeUpdateMu.Lock()
		monsterSkillBeforeUpdateHooks = append(monsterSkillBeforeUpdateHooks, monsterSkillHook)
		monsterSkillBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		monsterSkillAfterUpdateMu.Lock()
		monsterSkillAfterUpdateHooks = append(monsterSkillAfterUpdateHooks, monsterSkillHook)
		monsterSkillAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		monsterSkillBeforeDeleteMu.Lock()
		monsterSkillBeforeDeleteHooks = append(monsterSkillBeforeDeleteHooks, monsterSkillHook)
		monsterSkillBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		monsterSkillAfterDeleteMu.Lock()
		monsterSkillAfterDeleteHooks = append(monsterSkillAfterDeleteHooks, monsterSkillHook)
		monsterSkillAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		monsterSkillBeforeUpsertMu.Lock()
		monsterSkillBeforeUpsertHooks = append(monsterSkillBeforeUpsertHooks, monsterSkillHook)
		monsterSkillBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		monsterSkillAfterUpsertMu.Lock()
		monsterSkillAfterUpsertHooks = append(monsterSkillAfterUpsertHooks, monsterSkillHook)
		monsterSkillAfterUpsertMu.Unlock()
	}
}

// OneG returns a single monsterSkill record from the query using the global executor.
func (q monsterSkillQuery) OneG(ctx context.Context) (*MonsterSkill, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single monsterSkill record from the query using the global executor, and panics on error.
func (q monsterSkillQuery) OneGP(ctx context.Context) *MonsterSkill {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single monsterSkill record from the query, and panics on error.
func (q monsterSkillQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *MonsterSkill {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single monsterSkill record from the query.
func (q monsterSkillQuery) One(ctx context.Context, exec boil.ContextExecutor) (*MonsterSkill, error) {
	o := &MonsterSkill{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for monster_skills")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all MonsterSkill records from the query using the global executor.
func (q monsterSkillQuery) AllG(ctx context.Context) (MonsterSkillSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all MonsterSkill records from the query using the global executor, and panics on error.
func (q monsterSkillQuery) AllGP(ctx context.Context) MonsterSkillSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all MonsterSkill records from the query, and panics on error.
func (q monsterSkillQuery) AllP(ctx context.Context, exec boil.ContextExecutor) MonsterSkillSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all MonsterSkill records from the query.
func (q monsterSkillQuery) All(ctx context.Context, exec boil.ContextExecutor) (MonsterSkillSlice, error) {
	var o []*MonsterSkill

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to MonsterSkill slice")
	}

	if len(monsterSkillAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all MonsterSkill records in the query using the global executor
func (q monsterSkillQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all MonsterSkill records in the query using the global executor, and panics on error.
func (q monsterSkillQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all MonsterSkill records in the query, and panics on error.
func (q monsterSkillQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all MonsterSkill records in the query.
func (q monsterSkillQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count monster_skills rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q monsterSkillQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q monsterSkillQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q monsterSkillQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q monsterSkillQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if monster_skills exists")
	}

	return count > 0, nil
}

// Monster pointed to by the foreign key.
func (o *MonsterSkill) Monster(mods ...qm.QueryMod) monsterQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.MonsterID),
	}

	queryMods = append(queryMods, mods...)

	return Monsters(queryMods...)
}

// Skill pointed to by the foreign key.
func (o *MonsterSkill) Skill(mods ...qm.QueryMod) skillQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SkillID),
	}

	queryMods = append(queryMods, mods...)

	return Skills(queryMods...)
}

// LoadMonster allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (monsterSkillL) LoadMonster(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMonsterSkill interface{}, mods queries.Applicator) error {
	var slice []*MonsterSkill
	var object *MonsterSkill

	if singular {
		var ok bool
		object, ok = maybeMonsterSkill.(*MonsterSkill)
		if !ok {
			object = new(MonsterSkill)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMonsterSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMonsterSkill))
			}
		}
	} else {
		s, ok := maybeMonsterSkill.(*[]*MonsterSkill)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMonsterSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMonsterSkill))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &monsterSkillR{}
		}
		args[object.MonsterID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &monsterSkillR{}
			}

			args[obj.MonsterID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.monsters`),
		qm.WhereIn(`game_config.monsters.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.monsters.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Monster")
	}

	var resultSlice []*Monster
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Monster")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for monsters")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for monsters")
	}

	if len(monsterAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Monster = foreign
		if foreign.R == nil {
			foreign.R = &monsterR{}
		}
		foreign.R.MonsterSkills = append(foreign.R.MonsterSkills, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.MonsterID == foreign.ID {
				local.R.Monster = foreign
				if foreign.R == nil {
					foreign.R = &monsterR{}
				}
				foreign.R.MonsterSkills = append(foreign.R.MonsterSkills, local)
				break
			}
		}
	}

	return nil
}

// LoadSkill allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (monsterSkillL) LoadSkill(ctx context.Context, e boil.ContextExecutor, singular bool, maybeMonsterSkill interface{}, mods queries.Applicator) error {
	var slice []*MonsterSkill
	var object *MonsterSkill

	if singular {
		var ok bool
		object, ok = maybeMonsterSkill.(*MonsterSkill)
		if !ok {
			object = new(MonsterSkill)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeMonsterSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeMonsterSkill))
			}
		}
	} else {
		s, ok := maybeMonsterSkill.(*[]*MonsterSkill)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeMonsterSkill)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeMonsterSkill))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &monsterSkillR{}
		}
		args[object.SkillID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &monsterSkillR{}
			}

			args[obj.SkillID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.skills`),
		qm.WhereIn(`game_config.skills.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.skills.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Skill")
	}

	var resultSlice []*Skill
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Skill")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for skills")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for skills")
	}

	if len(skillAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Skill = foreign
		if foreign.R == nil {
			foreign.R = &skillR{}
		}
		foreign.R.MonsterSkills = append(foreign.R.MonsterSkills, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SkillID == foreign.ID {
				local.R.Skill = foreign
				if foreign.R == nil {
					foreign.R = &skillR{}
				}
				foreign.R.MonsterSkills = append(foreign.R.MonsterSkills, local)
				break
			}
		}
	}

	return nil
}

// SetMonsterG of the monsterSkill to the related item.
// Sets o.R.Monster to related.
// Adds o to related.R.MonsterSkills.
// Uses the global database handle.
func (o *MonsterSkill) SetMonsterG(ctx context.Context, insert bool, related *Monster) error {
	return o.SetMonster(ctx, boil.GetContextDB(), insert, related)
}

// SetMonsterP of the monsterSkill to the related item.
// Sets o.R.Monster to related.
// Adds o to related.R.MonsterSkills.
// Panics on error.
func (o *MonsterSkill) SetMonsterP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Monster) {
	if err := o.SetMonster(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetMonsterGP of the monsterSkill to the related item.
// Sets o.R.Monster to related.
// Adds o to related.R.MonsterSkills.
// Uses the global database handle and panics on error.
func (o *MonsterSkill) SetMonsterGP(ctx context.Context, insert bool, related *Monster) {
	if err := o.SetMonster(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetMonster of the monsterSkill to the related item.
// Sets o.R.Monster to related.
// Adds o to related.R.MonsterSkills.
func (o *MonsterSkill) SetMonster(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Monster) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"monster_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"monster_id"}),
		strmangle.WhereClause("\"", "\"", 2, monsterSkillPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.MonsterID = related.ID
	if o.R == nil {
		o.R = &monsterSkillR{
			Monster: related,
		}
	} else {
		o.R.Monster = related
	}

	if related.R == nil {
		related.R = &monsterR{
			MonsterSkills: MonsterSkillSlice{o},
		}
	} else {
		related.R.MonsterSkills = append(related.R.MonsterSkills, o)
	}

	return nil
}

// SetSkillG of the monsterSkill to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.MonsterSkills.
// Uses the global database handle.
func (o *MonsterSkill) SetSkillG(ctx context.Context, insert bool, related *Skill) error {
	return o.SetSkill(ctx, boil.GetContextDB(), insert, related)
}

// SetSkillP of the monsterSkill to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.MonsterSkills.
// Panics on error.
func (o *MonsterSkill) SetSkillP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Skill) {
	if err := o.SetSkill(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSkillGP of the monsterSkill to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.MonsterSkills.
// Uses the global database handle and panics on error.
func (o *MonsterSkill) SetSkillGP(ctx context.Context, insert bool, related *Skill) {
	if err := o.SetSkill(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSkill of the monsterSkill to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.MonsterSkills.
func (o *MonsterSkill) SetSkill(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Skill) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"monster_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"skill_id"}),
		strmangle.WhereClause("\"", "\"", 2, monsterSkillPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SkillID = related.ID
	if o.R == nil {
		o.R = &monsterSkillR{
			Skill: related,
		}
	} else {
		o.R.Skill = related
	}

	if related.R == nil {
		related.R = &skillR{
			MonsterSkills: MonsterSkillSlice{o},
		}
	} else {
		related.R.MonsterSkills = append(related.R.MonsterSkills, o)
	}

	return nil
}

// MonsterSkills retrieves all the records using an executor.
func MonsterSkills(mods ...qm.QueryMod) monsterSkillQuery {
	mods = append(mods, qm.From("\"game_config\".\"monster_skills\""), qmhelper.WhereIsNull("\"game_config\".\"monster_skills\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"monster_skills\".*"})
	}

	return monsterSkillQuery{q}
}

// FindMonsterSkillG retrieves a single record by ID.
func FindMonsterSkillG(ctx context.Context, iD string, selectCols ...string) (*MonsterSkill, error) {
	return FindMonsterSkill(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindMonsterSkillP retrieves a single record by ID with an executor, and panics on error.
func FindMonsterSkillP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *MonsterSkill {
	retobj, err := FindMonsterSkill(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindMonsterSkillGP retrieves a single record by ID, and panics on error.
func FindMonsterSkillGP(ctx context.Context, iD string, selectCols ...string) *MonsterSkill {
	retobj, err := FindMonsterSkill(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindMonsterSkill retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMonsterSkill(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*MonsterSkill, error) {
	monsterSkillObj := &MonsterSkill{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"monster_skills\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, monsterSkillObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from monster_skills")
	}

	if err = monsterSkillObj.doAfterSelectHooks(ctx, exec); err != nil {
		return monsterSkillObj, err
	}

	return monsterSkillObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *MonsterSkill) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *MonsterSkill) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *MonsterSkill) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MonsterSkill) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no monster_skills provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(monsterSkillColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	monsterSkillInsertCacheMut.RLock()
	cache, cached := monsterSkillInsertCache[key]
	monsterSkillInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			monsterSkillAllColumns,
			monsterSkillColumnsWithDefault,
			monsterSkillColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(monsterSkillType, monsterSkillMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(monsterSkillType, monsterSkillMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"monster_skills\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"monster_skills\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into monster_skills")
	}

	if !cached {
		monsterSkillInsertCacheMut.Lock()
		monsterSkillInsertCache[key] = cache
		monsterSkillInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single MonsterSkill record using the global executor.
// See Update for more documentation.
func (o *MonsterSkill) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the MonsterSkill, and panics on error.
// See Update for more documentation.
func (o *MonsterSkill) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single MonsterSkill record using the global executor. Panics on error.
// See Update for more documentation.
func (o *MonsterSkill) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the MonsterSkill.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MonsterSkill) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	monsterSkillUpdateCacheMut.RLock()
	cache, cached := monsterSkillUpdateCache[key]
	monsterSkillUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			monsterSkillAllColumns,
			monsterSkillPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update monster_skills, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"monster_skills\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, monsterSkillPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(monsterSkillType, monsterSkillMapping, append(wl, monsterSkillPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update monster_skills row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for monster_skills")
	}

	if !cached {
		monsterSkillUpdateCacheMut.Lock()
		monsterSkillUpdateCache[key] = cache
		monsterSkillUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q monsterSkillQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q monsterSkillQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q monsterSkillQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q monsterSkillQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for monster_skills")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for monster_skills")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o MonsterSkillSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o MonsterSkillSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o MonsterSkillSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MonsterSkillSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterSkillPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"monster_skills\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, monsterSkillPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in monsterSkill slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all monsterSkill")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *MonsterSkill) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *MonsterSkill) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *MonsterSkill) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MonsterSkill) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no monster_skills provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(monsterSkillColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	monsterSkillUpsertCacheMut.RLock()
	cache, cached := monsterSkillUpsertCache[key]
	monsterSkillUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			monsterSkillAllColumns,
			monsterSkillColumnsWithDefault,
			monsterSkillColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			monsterSkillAllColumns,
			monsterSkillPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert monster_skills, could not build update column list")
		}

		ret := strmangle.SetComplement(monsterSkillAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(monsterSkillPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert monster_skills, could not build conflict column list")
			}

			conflict = make([]string, len(monsterSkillPrimaryKeyColumns))
			copy(conflict, monsterSkillPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"monster_skills\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(monsterSkillType, monsterSkillMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(monsterSkillType, monsterSkillMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert monster_skills")
	}

	if !cached {
		monsterSkillUpsertCacheMut.Lock()
		monsterSkillUpsertCache[key] = cache
		monsterSkillUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single MonsterSkill record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *MonsterSkill) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single MonsterSkill record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *MonsterSkill) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single MonsterSkill record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *MonsterSkill) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single MonsterSkill record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MonsterSkill) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no MonsterSkill provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), monsterSkillPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"monster_skills\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"monster_skills\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(monsterSkillType, monsterSkillMapping, append(wl, monsterSkillPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from monster_skills")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for monster_skills")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q monsterSkillQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q monsterSkillQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q monsterSkillQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q monsterSkillQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no monsterSkillQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from monster_skills")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for monster_skills")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o MonsterSkillSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o MonsterSkillSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o MonsterSkillSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MonsterSkillSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(monsterSkillBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterSkillPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"monster_skills\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, monsterSkillPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterSkillPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"monster_skills\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, monsterSkillPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from monsterSkill slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for monster_skills")
	}

	if len(monsterSkillAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *MonsterSkill) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no MonsterSkill provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *MonsterSkill) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *MonsterSkill) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MonsterSkill) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindMonsterSkill(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MonsterSkillSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty MonsterSkillSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *MonsterSkillSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *MonsterSkillSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MonsterSkillSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MonsterSkillSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), monsterSkillPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"monster_skills\".* FROM \"game_config\".\"monster_skills\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, monsterSkillPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in MonsterSkillSlice")
	}

	*o = slice

	return nil
}

// MonsterSkillExistsG checks if the MonsterSkill row exists.
func MonsterSkillExistsG(ctx context.Context, iD string) (bool, error) {
	return MonsterSkillExists(ctx, boil.GetContextDB(), iD)
}

// MonsterSkillExistsP checks if the MonsterSkill row exists. Panics on error.
func MonsterSkillExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := MonsterSkillExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// MonsterSkillExistsGP checks if the MonsterSkill row exists. Panics on error.
func MonsterSkillExistsGP(ctx context.Context, iD string) bool {
	e, err := MonsterSkillExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// MonsterSkillExists checks if the MonsterSkill row exists.
func MonsterSkillExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"monster_skills\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if monster_skills exists")
	}

	return exists, nil
}

// Exists checks if the MonsterSkill row exists.
func (o *MonsterSkill) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return MonsterSkillExists(ctx, exec, o.ID)
}
