// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// SocketTypeConfig is an object representing the database table.
type SocketTypeConfig struct {
	ID             string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	SocketTypeCode string      `boil:"socket_type_code" json:"socket_type_code" toml:"socket_type_code" yaml:"socket_type_code"`
	SocketTypeName string      `boil:"socket_type_name" json:"socket_type_name" toml:"socket_type_name" yaml:"socket_type_name"`
	Description    null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	SocketLayout   types.JSON  `boil:"socket_layout" json:"socket_layout" toml:"socket_layout" yaml:"socket_layout"`
	CreatedAt      time.Time   `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt      time.Time   `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt      null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *socketTypeConfigR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L socketTypeConfigL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SocketTypeConfigColumns = struct {
	ID             string
	SocketTypeCode string
	SocketTypeName string
	Description    string
	SocketLayout   string
	CreatedAt      string
	UpdatedAt      string
	DeletedAt      string
}{
	ID:             "id",
	SocketTypeCode: "socket_type_code",
	SocketTypeName: "socket_type_name",
	Description:    "description",
	SocketLayout:   "socket_layout",
	CreatedAt:      "created_at",
	UpdatedAt:      "updated_at",
	DeletedAt:      "deleted_at",
}

var SocketTypeConfigTableColumns = struct {
	ID             string
	SocketTypeCode string
	SocketTypeName string
	Description    string
	SocketLayout   string
	CreatedAt      string
	UpdatedAt      string
	DeletedAt      string
}{
	ID:             "socket_type_configs.id",
	SocketTypeCode: "socket_type_configs.socket_type_code",
	SocketTypeName: "socket_type_configs.socket_type_name",
	Description:    "socket_type_configs.description",
	SocketLayout:   "socket_type_configs.socket_layout",
	CreatedAt:      "socket_type_configs.created_at",
	UpdatedAt:      "socket_type_configs.updated_at",
	DeletedAt:      "socket_type_configs.deleted_at",
}

// Generated where

var SocketTypeConfigWhere = struct {
	ID             whereHelperstring
	SocketTypeCode whereHelperstring
	SocketTypeName whereHelperstring
	Description    whereHelpernull_String
	SocketLayout   whereHelpertypes_JSON
	CreatedAt      whereHelpertime_Time
	UpdatedAt      whereHelpertime_Time
	DeletedAt      whereHelpernull_Time
}{
	ID:             whereHelperstring{field: "\"game_config\".\"socket_type_configs\".\"id\""},
	SocketTypeCode: whereHelperstring{field: "\"game_config\".\"socket_type_configs\".\"socket_type_code\""},
	SocketTypeName: whereHelperstring{field: "\"game_config\".\"socket_type_configs\".\"socket_type_name\""},
	Description:    whereHelpernull_String{field: "\"game_config\".\"socket_type_configs\".\"description\""},
	SocketLayout:   whereHelpertypes_JSON{field: "\"game_config\".\"socket_type_configs\".\"socket_layout\""},
	CreatedAt:      whereHelpertime_Time{field: "\"game_config\".\"socket_type_configs\".\"created_at\""},
	UpdatedAt:      whereHelpertime_Time{field: "\"game_config\".\"socket_type_configs\".\"updated_at\""},
	DeletedAt:      whereHelpernull_Time{field: "\"game_config\".\"socket_type_configs\".\"deleted_at\""},
}

// SocketTypeConfigRels is where relationship names are stored.
var SocketTypeConfigRels = struct {
}{}

// socketTypeConfigR is where relationships are stored.
type socketTypeConfigR struct {
}

// NewStruct creates a new relationship struct
func (*socketTypeConfigR) NewStruct() *socketTypeConfigR {
	return &socketTypeConfigR{}
}

// socketTypeConfigL is where Load methods for each relationship are stored.
type socketTypeConfigL struct{}

var (
	socketTypeConfigAllColumns            = []string{"id", "socket_type_code", "socket_type_name", "description", "socket_layout", "created_at", "updated_at", "deleted_at"}
	socketTypeConfigColumnsWithoutDefault = []string{"socket_type_code", "socket_type_name", "socket_layout"}
	socketTypeConfigColumnsWithDefault    = []string{"id", "description", "created_at", "updated_at", "deleted_at"}
	socketTypeConfigPrimaryKeyColumns     = []string{"id"}
	socketTypeConfigGeneratedColumns      = []string{}
)

type (
	// SocketTypeConfigSlice is an alias for a slice of pointers to SocketTypeConfig.
	// This should almost always be used instead of []SocketTypeConfig.
	SocketTypeConfigSlice []*SocketTypeConfig
	// SocketTypeConfigHook is the signature for custom SocketTypeConfig hook methods
	SocketTypeConfigHook func(context.Context, boil.ContextExecutor, *SocketTypeConfig) error

	socketTypeConfigQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	socketTypeConfigType                 = reflect.TypeOf(&SocketTypeConfig{})
	socketTypeConfigMapping              = queries.MakeStructMapping(socketTypeConfigType)
	socketTypeConfigPrimaryKeyMapping, _ = queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, socketTypeConfigPrimaryKeyColumns)
	socketTypeConfigInsertCacheMut       sync.RWMutex
	socketTypeConfigInsertCache          = make(map[string]insertCache)
	socketTypeConfigUpdateCacheMut       sync.RWMutex
	socketTypeConfigUpdateCache          = make(map[string]updateCache)
	socketTypeConfigUpsertCacheMut       sync.RWMutex
	socketTypeConfigUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var socketTypeConfigAfterSelectMu sync.Mutex
var socketTypeConfigAfterSelectHooks []SocketTypeConfigHook

var socketTypeConfigBeforeInsertMu sync.Mutex
var socketTypeConfigBeforeInsertHooks []SocketTypeConfigHook
var socketTypeConfigAfterInsertMu sync.Mutex
var socketTypeConfigAfterInsertHooks []SocketTypeConfigHook

var socketTypeConfigBeforeUpdateMu sync.Mutex
var socketTypeConfigBeforeUpdateHooks []SocketTypeConfigHook
var socketTypeConfigAfterUpdateMu sync.Mutex
var socketTypeConfigAfterUpdateHooks []SocketTypeConfigHook

var socketTypeConfigBeforeDeleteMu sync.Mutex
var socketTypeConfigBeforeDeleteHooks []SocketTypeConfigHook
var socketTypeConfigAfterDeleteMu sync.Mutex
var socketTypeConfigAfterDeleteHooks []SocketTypeConfigHook

var socketTypeConfigBeforeUpsertMu sync.Mutex
var socketTypeConfigBeforeUpsertHooks []SocketTypeConfigHook
var socketTypeConfigAfterUpsertMu sync.Mutex
var socketTypeConfigAfterUpsertHooks []SocketTypeConfigHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SocketTypeConfig) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SocketTypeConfig) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SocketTypeConfig) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SocketTypeConfig) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SocketTypeConfig) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SocketTypeConfig) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SocketTypeConfig) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SocketTypeConfig) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SocketTypeConfig) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range socketTypeConfigAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSocketTypeConfigHook registers your hook function for all future operations.
func AddSocketTypeConfigHook(hookPoint boil.HookPoint, socketTypeConfigHook SocketTypeConfigHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		socketTypeConfigAfterSelectMu.Lock()
		socketTypeConfigAfterSelectHooks = append(socketTypeConfigAfterSelectHooks, socketTypeConfigHook)
		socketTypeConfigAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		socketTypeConfigBeforeInsertMu.Lock()
		socketTypeConfigBeforeInsertHooks = append(socketTypeConfigBeforeInsertHooks, socketTypeConfigHook)
		socketTypeConfigBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		socketTypeConfigAfterInsertMu.Lock()
		socketTypeConfigAfterInsertHooks = append(socketTypeConfigAfterInsertHooks, socketTypeConfigHook)
		socketTypeConfigAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		socketTypeConfigBeforeUpdateMu.Lock()
		socketTypeConfigBeforeUpdateHooks = append(socketTypeConfigBeforeUpdateHooks, socketTypeConfigHook)
		socketTypeConfigBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		socketTypeConfigAfterUpdateMu.Lock()
		socketTypeConfigAfterUpdateHooks = append(socketTypeConfigAfterUpdateHooks, socketTypeConfigHook)
		socketTypeConfigAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		socketTypeConfigBeforeDeleteMu.Lock()
		socketTypeConfigBeforeDeleteHooks = append(socketTypeConfigBeforeDeleteHooks, socketTypeConfigHook)
		socketTypeConfigBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		socketTypeConfigAfterDeleteMu.Lock()
		socketTypeConfigAfterDeleteHooks = append(socketTypeConfigAfterDeleteHooks, socketTypeConfigHook)
		socketTypeConfigAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		socketTypeConfigBeforeUpsertMu.Lock()
		socketTypeConfigBeforeUpsertHooks = append(socketTypeConfigBeforeUpsertHooks, socketTypeConfigHook)
		socketTypeConfigBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		socketTypeConfigAfterUpsertMu.Lock()
		socketTypeConfigAfterUpsertHooks = append(socketTypeConfigAfterUpsertHooks, socketTypeConfigHook)
		socketTypeConfigAfterUpsertMu.Unlock()
	}
}

// OneG returns a single socketTypeConfig record from the query using the global executor.
func (q socketTypeConfigQuery) OneG(ctx context.Context) (*SocketTypeConfig, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single socketTypeConfig record from the query using the global executor, and panics on error.
func (q socketTypeConfigQuery) OneGP(ctx context.Context) *SocketTypeConfig {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single socketTypeConfig record from the query, and panics on error.
func (q socketTypeConfigQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *SocketTypeConfig {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single socketTypeConfig record from the query.
func (q socketTypeConfigQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SocketTypeConfig, error) {
	o := &SocketTypeConfig{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for socket_type_configs")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all SocketTypeConfig records from the query using the global executor.
func (q socketTypeConfigQuery) AllG(ctx context.Context) (SocketTypeConfigSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all SocketTypeConfig records from the query using the global executor, and panics on error.
func (q socketTypeConfigQuery) AllGP(ctx context.Context) SocketTypeConfigSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all SocketTypeConfig records from the query, and panics on error.
func (q socketTypeConfigQuery) AllP(ctx context.Context, exec boil.ContextExecutor) SocketTypeConfigSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SocketTypeConfig records from the query.
func (q socketTypeConfigQuery) All(ctx context.Context, exec boil.ContextExecutor) (SocketTypeConfigSlice, error) {
	var o []*SocketTypeConfig

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to SocketTypeConfig slice")
	}

	if len(socketTypeConfigAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all SocketTypeConfig records in the query using the global executor
func (q socketTypeConfigQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all SocketTypeConfig records in the query using the global executor, and panics on error.
func (q socketTypeConfigQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all SocketTypeConfig records in the query, and panics on error.
func (q socketTypeConfigQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SocketTypeConfig records in the query.
func (q socketTypeConfigQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count socket_type_configs rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q socketTypeConfigQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q socketTypeConfigQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q socketTypeConfigQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q socketTypeConfigQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if socket_type_configs exists")
	}

	return count > 0, nil
}

// SocketTypeConfigs retrieves all the records using an executor.
func SocketTypeConfigs(mods ...qm.QueryMod) socketTypeConfigQuery {
	mods = append(mods, qm.From("\"game_config\".\"socket_type_configs\""), qmhelper.WhereIsNull("\"game_config\".\"socket_type_configs\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"socket_type_configs\".*"})
	}

	return socketTypeConfigQuery{q}
}

// FindSocketTypeConfigG retrieves a single record by ID.
func FindSocketTypeConfigG(ctx context.Context, iD string, selectCols ...string) (*SocketTypeConfig, error) {
	return FindSocketTypeConfig(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindSocketTypeConfigP retrieves a single record by ID with an executor, and panics on error.
func FindSocketTypeConfigP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *SocketTypeConfig {
	retobj, err := FindSocketTypeConfig(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSocketTypeConfigGP retrieves a single record by ID, and panics on error.
func FindSocketTypeConfigGP(ctx context.Context, iD string, selectCols ...string) *SocketTypeConfig {
	retobj, err := FindSocketTypeConfig(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSocketTypeConfig retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSocketTypeConfig(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SocketTypeConfig, error) {
	socketTypeConfigObj := &SocketTypeConfig{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"socket_type_configs\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, socketTypeConfigObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from socket_type_configs")
	}

	if err = socketTypeConfigObj.doAfterSelectHooks(ctx, exec); err != nil {
		return socketTypeConfigObj, err
	}

	return socketTypeConfigObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SocketTypeConfig) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SocketTypeConfig) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SocketTypeConfig) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SocketTypeConfig) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no socket_type_configs provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(socketTypeConfigColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	socketTypeConfigInsertCacheMut.RLock()
	cache, cached := socketTypeConfigInsertCache[key]
	socketTypeConfigInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			socketTypeConfigAllColumns,
			socketTypeConfigColumnsWithDefault,
			socketTypeConfigColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"socket_type_configs\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"socket_type_configs\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into socket_type_configs")
	}

	if !cached {
		socketTypeConfigInsertCacheMut.Lock()
		socketTypeConfigInsertCache[key] = cache
		socketTypeConfigInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single SocketTypeConfig record using the global executor.
// See Update for more documentation.
func (o *SocketTypeConfig) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the SocketTypeConfig, and panics on error.
// See Update for more documentation.
func (o *SocketTypeConfig) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single SocketTypeConfig record using the global executor. Panics on error.
// See Update for more documentation.
func (o *SocketTypeConfig) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the SocketTypeConfig.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SocketTypeConfig) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	socketTypeConfigUpdateCacheMut.RLock()
	cache, cached := socketTypeConfigUpdateCache[key]
	socketTypeConfigUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			socketTypeConfigAllColumns,
			socketTypeConfigPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update socket_type_configs, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"socket_type_configs\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, socketTypeConfigPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, append(wl, socketTypeConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update socket_type_configs row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for socket_type_configs")
	}

	if !cached {
		socketTypeConfigUpdateCacheMut.Lock()
		socketTypeConfigUpdateCache[key] = cache
		socketTypeConfigUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q socketTypeConfigQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q socketTypeConfigQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q socketTypeConfigQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q socketTypeConfigQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for socket_type_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for socket_type_configs")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SocketTypeConfigSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SocketTypeConfigSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SocketTypeConfigSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SocketTypeConfigSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), socketTypeConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"socket_type_configs\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, socketTypeConfigPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in socketTypeConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all socketTypeConfig")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SocketTypeConfig) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SocketTypeConfig) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SocketTypeConfig) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SocketTypeConfig) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no socket_type_configs provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(socketTypeConfigColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	socketTypeConfigUpsertCacheMut.RLock()
	cache, cached := socketTypeConfigUpsertCache[key]
	socketTypeConfigUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			socketTypeConfigAllColumns,
			socketTypeConfigColumnsWithDefault,
			socketTypeConfigColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			socketTypeConfigAllColumns,
			socketTypeConfigPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert socket_type_configs, could not build update column list")
		}

		ret := strmangle.SetComplement(socketTypeConfigAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(socketTypeConfigPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert socket_type_configs, could not build conflict column list")
			}

			conflict = make([]string, len(socketTypeConfigPrimaryKeyColumns))
			copy(conflict, socketTypeConfigPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"socket_type_configs\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert socket_type_configs")
	}

	if !cached {
		socketTypeConfigUpsertCacheMut.Lock()
		socketTypeConfigUpsertCache[key] = cache
		socketTypeConfigUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single SocketTypeConfig record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SocketTypeConfig) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single SocketTypeConfig record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SocketTypeConfig) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single SocketTypeConfig record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SocketTypeConfig) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single SocketTypeConfig record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SocketTypeConfig) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no SocketTypeConfig provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), socketTypeConfigPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"socket_type_configs\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"socket_type_configs\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(socketTypeConfigType, socketTypeConfigMapping, append(wl, socketTypeConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from socket_type_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for socket_type_configs")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q socketTypeConfigQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q socketTypeConfigQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q socketTypeConfigQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q socketTypeConfigQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no socketTypeConfigQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from socket_type_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for socket_type_configs")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o SocketTypeConfigSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SocketTypeConfigSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SocketTypeConfigSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SocketTypeConfigSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(socketTypeConfigBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), socketTypeConfigPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"socket_type_configs\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, socketTypeConfigPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), socketTypeConfigPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"socket_type_configs\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, socketTypeConfigPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from socketTypeConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for socket_type_configs")
	}

	if len(socketTypeConfigAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SocketTypeConfig) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no SocketTypeConfig provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SocketTypeConfig) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SocketTypeConfig) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SocketTypeConfig) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSocketTypeConfig(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SocketTypeConfigSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty SocketTypeConfigSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SocketTypeConfigSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SocketTypeConfigSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SocketTypeConfigSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SocketTypeConfigSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), socketTypeConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"socket_type_configs\".* FROM \"game_config\".\"socket_type_configs\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, socketTypeConfigPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in SocketTypeConfigSlice")
	}

	*o = slice

	return nil
}

// SocketTypeConfigExistsG checks if the SocketTypeConfig row exists.
func SocketTypeConfigExistsG(ctx context.Context, iD string) (bool, error) {
	return SocketTypeConfigExists(ctx, boil.GetContextDB(), iD)
}

// SocketTypeConfigExistsP checks if the SocketTypeConfig row exists. Panics on error.
func SocketTypeConfigExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := SocketTypeConfigExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SocketTypeConfigExistsGP checks if the SocketTypeConfig row exists. Panics on error.
func SocketTypeConfigExistsGP(ctx context.Context, iD string) bool {
	e, err := SocketTypeConfigExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SocketTypeConfigExists checks if the SocketTypeConfig row exists.
func SocketTypeConfigExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"socket_type_configs\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if socket_type_configs exists")
	}

	return exists, nil
}

// Exists checks if the SocketTypeConfig row exists.
func (o *SocketTypeConfig) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SocketTypeConfigExists(ctx, exec, o.ID)
}
