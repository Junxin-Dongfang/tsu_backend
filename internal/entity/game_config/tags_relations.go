// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TagsRelation is an object representing the database table.
type TagsRelation struct {
	ID         string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	TagID      string    `boil:"tag_id" json:"tag_id" toml:"tag_id" yaml:"tag_id"`
	EntityType string    `boil:"entity_type" json:"entity_type" toml:"entity_type" yaml:"entity_type"`
	EntityID   string    `boil:"entity_id" json:"entity_id" toml:"entity_id" yaml:"entity_id"`
	CreatedAt  time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt  null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *tagsRelationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L tagsRelationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TagsRelationColumns = struct {
	ID         string
	TagID      string
	EntityType string
	EntityID   string
	CreatedAt  string
	UpdatedAt  string
	DeletedAt  string
}{
	ID:         "id",
	TagID:      "tag_id",
	EntityType: "entity_type",
	EntityID:   "entity_id",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
	DeletedAt:  "deleted_at",
}

var TagsRelationTableColumns = struct {
	ID         string
	TagID      string
	EntityType string
	EntityID   string
	CreatedAt  string
	UpdatedAt  string
	DeletedAt  string
}{
	ID:         "tags_relations.id",
	TagID:      "tags_relations.tag_id",
	EntityType: "tags_relations.entity_type",
	EntityID:   "tags_relations.entity_id",
	CreatedAt:  "tags_relations.created_at",
	UpdatedAt:  "tags_relations.updated_at",
	DeletedAt:  "tags_relations.deleted_at",
}

// Generated where

var TagsRelationWhere = struct {
	ID         whereHelperstring
	TagID      whereHelperstring
	EntityType whereHelperstring
	EntityID   whereHelperstring
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
	DeletedAt  whereHelpernull_Time
}{
	ID:         whereHelperstring{field: "\"game_config\".\"tags_relations\".\"id\""},
	TagID:      whereHelperstring{field: "\"game_config\".\"tags_relations\".\"tag_id\""},
	EntityType: whereHelperstring{field: "\"game_config\".\"tags_relations\".\"entity_type\""},
	EntityID:   whereHelperstring{field: "\"game_config\".\"tags_relations\".\"entity_id\""},
	CreatedAt:  whereHelpertime_Time{field: "\"game_config\".\"tags_relations\".\"created_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"game_config\".\"tags_relations\".\"updated_at\""},
	DeletedAt:  whereHelpernull_Time{field: "\"game_config\".\"tags_relations\".\"deleted_at\""},
}

// TagsRelationRels is where relationship names are stored.
var TagsRelationRels = struct {
	Tag string
}{
	Tag: "Tag",
}

// tagsRelationR is where relationships are stored.
type tagsRelationR struct {
	Tag *Tag `boil:"Tag" json:"Tag" toml:"Tag" yaml:"Tag"`
}

// NewStruct creates a new relationship struct
func (*tagsRelationR) NewStruct() *tagsRelationR {
	return &tagsRelationR{}
}

func (o *TagsRelation) GetTag() *Tag {
	if o == nil {
		return nil
	}

	return o.R.GetTag()
}

func (r *tagsRelationR) GetTag() *Tag {
	if r == nil {
		return nil
	}

	return r.Tag
}

// tagsRelationL is where Load methods for each relationship are stored.
type tagsRelationL struct{}

var (
	tagsRelationAllColumns            = []string{"id", "tag_id", "entity_type", "entity_id", "created_at", "updated_at", "deleted_at"}
	tagsRelationColumnsWithoutDefault = []string{"tag_id", "entity_type", "entity_id"}
	tagsRelationColumnsWithDefault    = []string{"id", "created_at", "updated_at", "deleted_at"}
	tagsRelationPrimaryKeyColumns     = []string{"id"}
	tagsRelationGeneratedColumns      = []string{}
)

type (
	// TagsRelationSlice is an alias for a slice of pointers to TagsRelation.
	// This should almost always be used instead of []TagsRelation.
	TagsRelationSlice []*TagsRelation
	// TagsRelationHook is the signature for custom TagsRelation hook methods
	TagsRelationHook func(context.Context, boil.ContextExecutor, *TagsRelation) error

	tagsRelationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	tagsRelationType                 = reflect.TypeOf(&TagsRelation{})
	tagsRelationMapping              = queries.MakeStructMapping(tagsRelationType)
	tagsRelationPrimaryKeyMapping, _ = queries.BindMapping(tagsRelationType, tagsRelationMapping, tagsRelationPrimaryKeyColumns)
	tagsRelationInsertCacheMut       sync.RWMutex
	tagsRelationInsertCache          = make(map[string]insertCache)
	tagsRelationUpdateCacheMut       sync.RWMutex
	tagsRelationUpdateCache          = make(map[string]updateCache)
	tagsRelationUpsertCacheMut       sync.RWMutex
	tagsRelationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var tagsRelationAfterSelectMu sync.Mutex
var tagsRelationAfterSelectHooks []TagsRelationHook

var tagsRelationBeforeInsertMu sync.Mutex
var tagsRelationBeforeInsertHooks []TagsRelationHook
var tagsRelationAfterInsertMu sync.Mutex
var tagsRelationAfterInsertHooks []TagsRelationHook

var tagsRelationBeforeUpdateMu sync.Mutex
var tagsRelationBeforeUpdateHooks []TagsRelationHook
var tagsRelationAfterUpdateMu sync.Mutex
var tagsRelationAfterUpdateHooks []TagsRelationHook

var tagsRelationBeforeDeleteMu sync.Mutex
var tagsRelationBeforeDeleteHooks []TagsRelationHook
var tagsRelationAfterDeleteMu sync.Mutex
var tagsRelationAfterDeleteHooks []TagsRelationHook

var tagsRelationBeforeUpsertMu sync.Mutex
var tagsRelationBeforeUpsertHooks []TagsRelationHook
var tagsRelationAfterUpsertMu sync.Mutex
var tagsRelationAfterUpsertHooks []TagsRelationHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TagsRelation) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TagsRelation) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TagsRelation) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TagsRelation) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TagsRelation) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TagsRelation) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TagsRelation) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TagsRelation) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TagsRelation) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range tagsRelationAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTagsRelationHook registers your hook function for all future operations.
func AddTagsRelationHook(hookPoint boil.HookPoint, tagsRelationHook TagsRelationHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		tagsRelationAfterSelectMu.Lock()
		tagsRelationAfterSelectHooks = append(tagsRelationAfterSelectHooks, tagsRelationHook)
		tagsRelationAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		tagsRelationBeforeInsertMu.Lock()
		tagsRelationBeforeInsertHooks = append(tagsRelationBeforeInsertHooks, tagsRelationHook)
		tagsRelationBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		tagsRelationAfterInsertMu.Lock()
		tagsRelationAfterInsertHooks = append(tagsRelationAfterInsertHooks, tagsRelationHook)
		tagsRelationAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		tagsRelationBeforeUpdateMu.Lock()
		tagsRelationBeforeUpdateHooks = append(tagsRelationBeforeUpdateHooks, tagsRelationHook)
		tagsRelationBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		tagsRelationAfterUpdateMu.Lock()
		tagsRelationAfterUpdateHooks = append(tagsRelationAfterUpdateHooks, tagsRelationHook)
		tagsRelationAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		tagsRelationBeforeDeleteMu.Lock()
		tagsRelationBeforeDeleteHooks = append(tagsRelationBeforeDeleteHooks, tagsRelationHook)
		tagsRelationBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		tagsRelationAfterDeleteMu.Lock()
		tagsRelationAfterDeleteHooks = append(tagsRelationAfterDeleteHooks, tagsRelationHook)
		tagsRelationAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		tagsRelationBeforeUpsertMu.Lock()
		tagsRelationBeforeUpsertHooks = append(tagsRelationBeforeUpsertHooks, tagsRelationHook)
		tagsRelationBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		tagsRelationAfterUpsertMu.Lock()
		tagsRelationAfterUpsertHooks = append(tagsRelationAfterUpsertHooks, tagsRelationHook)
		tagsRelationAfterUpsertMu.Unlock()
	}
}

// OneG returns a single tagsRelation record from the query using the global executor.
func (q tagsRelationQuery) OneG(ctx context.Context) (*TagsRelation, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single tagsRelation record from the query using the global executor, and panics on error.
func (q tagsRelationQuery) OneGP(ctx context.Context) *TagsRelation {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single tagsRelation record from the query, and panics on error.
func (q tagsRelationQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *TagsRelation {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single tagsRelation record from the query.
func (q tagsRelationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TagsRelation, error) {
	o := &TagsRelation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for tags_relations")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TagsRelation records from the query using the global executor.
func (q tagsRelationQuery) AllG(ctx context.Context) (TagsRelationSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all TagsRelation records from the query using the global executor, and panics on error.
func (q tagsRelationQuery) AllGP(ctx context.Context) TagsRelationSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all TagsRelation records from the query, and panics on error.
func (q tagsRelationQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TagsRelationSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TagsRelation records from the query.
func (q tagsRelationQuery) All(ctx context.Context, exec boil.ContextExecutor) (TagsRelationSlice, error) {
	var o []*TagsRelation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to TagsRelation slice")
	}

	if len(tagsRelationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TagsRelation records in the query using the global executor
func (q tagsRelationQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all TagsRelation records in the query using the global executor, and panics on error.
func (q tagsRelationQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all TagsRelation records in the query, and panics on error.
func (q tagsRelationQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TagsRelation records in the query.
func (q tagsRelationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count tags_relations rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q tagsRelationQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q tagsRelationQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q tagsRelationQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q tagsRelationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if tags_relations exists")
	}

	return count > 0, nil
}

// Tag pointed to by the foreign key.
func (o *TagsRelation) Tag(mods ...qm.QueryMod) tagQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TagID),
	}

	queryMods = append(queryMods, mods...)

	return Tags(queryMods...)
}

// LoadTag allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (tagsRelationL) LoadTag(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTagsRelation interface{}, mods queries.Applicator) error {
	var slice []*TagsRelation
	var object *TagsRelation

	if singular {
		var ok bool
		object, ok = maybeTagsRelation.(*TagsRelation)
		if !ok {
			object = new(TagsRelation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTagsRelation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTagsRelation))
			}
		}
	} else {
		s, ok := maybeTagsRelation.(*[]*TagsRelation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTagsRelation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTagsRelation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &tagsRelationR{}
		}
		args[object.TagID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &tagsRelationR{}
			}

			args[obj.TagID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.tags`),
		qm.WhereIn(`game_config.tags.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.tags.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Tag")
	}

	var resultSlice []*Tag
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Tag")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tags")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tags")
	}

	if len(tagAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Tag = foreign
		if foreign.R == nil {
			foreign.R = &tagR{}
		}
		foreign.R.TagsRelations = append(foreign.R.TagsRelations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TagID == foreign.ID {
				local.R.Tag = foreign
				if foreign.R == nil {
					foreign.R = &tagR{}
				}
				foreign.R.TagsRelations = append(foreign.R.TagsRelations, local)
				break
			}
		}
	}

	return nil
}

// SetTagG of the tagsRelation to the related item.
// Sets o.R.Tag to related.
// Adds o to related.R.TagsRelations.
// Uses the global database handle.
func (o *TagsRelation) SetTagG(ctx context.Context, insert bool, related *Tag) error {
	return o.SetTag(ctx, boil.GetContextDB(), insert, related)
}

// SetTagP of the tagsRelation to the related item.
// Sets o.R.Tag to related.
// Adds o to related.R.TagsRelations.
// Panics on error.
func (o *TagsRelation) SetTagP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tag) {
	if err := o.SetTag(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTagGP of the tagsRelation to the related item.
// Sets o.R.Tag to related.
// Adds o to related.R.TagsRelations.
// Uses the global database handle and panics on error.
func (o *TagsRelation) SetTagGP(ctx context.Context, insert bool, related *Tag) {
	if err := o.SetTag(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTag of the tagsRelation to the related item.
// Sets o.R.Tag to related.
// Adds o to related.R.TagsRelations.
func (o *TagsRelation) SetTag(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Tag) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"tags_relations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"tag_id"}),
		strmangle.WhereClause("\"", "\"", 2, tagsRelationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TagID = related.ID
	if o.R == nil {
		o.R = &tagsRelationR{
			Tag: related,
		}
	} else {
		o.R.Tag = related
	}

	if related.R == nil {
		related.R = &tagR{
			TagsRelations: TagsRelationSlice{o},
		}
	} else {
		related.R.TagsRelations = append(related.R.TagsRelations, o)
	}

	return nil
}

// TagsRelations retrieves all the records using an executor.
func TagsRelations(mods ...qm.QueryMod) tagsRelationQuery {
	mods = append(mods, qm.From("\"game_config\".\"tags_relations\""), qmhelper.WhereIsNull("\"game_config\".\"tags_relations\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"tags_relations\".*"})
	}

	return tagsRelationQuery{q}
}

// FindTagsRelationG retrieves a single record by ID.
func FindTagsRelationG(ctx context.Context, iD string, selectCols ...string) (*TagsRelation, error) {
	return FindTagsRelation(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTagsRelationP retrieves a single record by ID with an executor, and panics on error.
func FindTagsRelationP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *TagsRelation {
	retobj, err := FindTagsRelation(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTagsRelationGP retrieves a single record by ID, and panics on error.
func FindTagsRelationGP(ctx context.Context, iD string, selectCols ...string) *TagsRelation {
	retobj, err := FindTagsRelation(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTagsRelation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTagsRelation(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TagsRelation, error) {
	tagsRelationObj := &TagsRelation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"tags_relations\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, tagsRelationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from tags_relations")
	}

	if err = tagsRelationObj.doAfterSelectHooks(ctx, exec); err != nil {
		return tagsRelationObj, err
	}

	return tagsRelationObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TagsRelation) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TagsRelation) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TagsRelation) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TagsRelation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no tags_relations provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tagsRelationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	tagsRelationInsertCacheMut.RLock()
	cache, cached := tagsRelationInsertCache[key]
	tagsRelationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			tagsRelationAllColumns,
			tagsRelationColumnsWithDefault,
			tagsRelationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(tagsRelationType, tagsRelationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(tagsRelationType, tagsRelationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"tags_relations\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"tags_relations\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into tags_relations")
	}

	if !cached {
		tagsRelationInsertCacheMut.Lock()
		tagsRelationInsertCache[key] = cache
		tagsRelationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single TagsRelation record using the global executor.
// See Update for more documentation.
func (o *TagsRelation) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the TagsRelation, and panics on error.
// See Update for more documentation.
func (o *TagsRelation) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single TagsRelation record using the global executor. Panics on error.
// See Update for more documentation.
func (o *TagsRelation) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the TagsRelation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TagsRelation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	tagsRelationUpdateCacheMut.RLock()
	cache, cached := tagsRelationUpdateCache[key]
	tagsRelationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			tagsRelationAllColumns,
			tagsRelationPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update tags_relations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"tags_relations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, tagsRelationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(tagsRelationType, tagsRelationMapping, append(wl, tagsRelationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update tags_relations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for tags_relations")
	}

	if !cached {
		tagsRelationUpdateCacheMut.Lock()
		tagsRelationUpdateCache[key] = cache
		tagsRelationUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q tagsRelationQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q tagsRelationQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q tagsRelationQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q tagsRelationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for tags_relations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for tags_relations")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TagsRelationSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TagsRelationSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TagsRelationSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TagsRelationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tagsRelationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"tags_relations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, tagsRelationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in tagsRelation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all tagsRelation")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TagsRelation) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TagsRelation) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TagsRelation) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TagsRelation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no tags_relations provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(tagsRelationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	tagsRelationUpsertCacheMut.RLock()
	cache, cached := tagsRelationUpsertCache[key]
	tagsRelationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			tagsRelationAllColumns,
			tagsRelationColumnsWithDefault,
			tagsRelationColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			tagsRelationAllColumns,
			tagsRelationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert tags_relations, could not build update column list")
		}

		ret := strmangle.SetComplement(tagsRelationAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(tagsRelationPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert tags_relations, could not build conflict column list")
			}

			conflict = make([]string, len(tagsRelationPrimaryKeyColumns))
			copy(conflict, tagsRelationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"tags_relations\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(tagsRelationType, tagsRelationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(tagsRelationType, tagsRelationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert tags_relations")
	}

	if !cached {
		tagsRelationUpsertCacheMut.Lock()
		tagsRelationUpsertCache[key] = cache
		tagsRelationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single TagsRelation record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TagsRelation) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single TagsRelation record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TagsRelation) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single TagsRelation record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TagsRelation) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single TagsRelation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TagsRelation) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no TagsRelation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), tagsRelationPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"tags_relations\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"tags_relations\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(tagsRelationType, tagsRelationMapping, append(wl, tagsRelationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from tags_relations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for tags_relations")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q tagsRelationQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q tagsRelationQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q tagsRelationQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q tagsRelationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no tagsRelationQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from tags_relations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for tags_relations")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TagsRelationSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TagsRelationSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TagsRelationSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TagsRelationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(tagsRelationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tagsRelationPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"tags_relations\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tagsRelationPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tagsRelationPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"tags_relations\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, tagsRelationPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from tagsRelation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for tags_relations")
	}

	if len(tagsRelationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TagsRelation) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no TagsRelation provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TagsRelation) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TagsRelation) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TagsRelation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTagsRelation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TagsRelationSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty TagsRelationSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TagsRelationSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TagsRelationSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TagsRelationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TagsRelationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), tagsRelationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"tags_relations\".* FROM \"game_config\".\"tags_relations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, tagsRelationPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in TagsRelationSlice")
	}

	*o = slice

	return nil
}

// TagsRelationExistsG checks if the TagsRelation row exists.
func TagsRelationExistsG(ctx context.Context, iD string) (bool, error) {
	return TagsRelationExists(ctx, boil.GetContextDB(), iD)
}

// TagsRelationExistsP checks if the TagsRelation row exists. Panics on error.
func TagsRelationExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TagsRelationExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TagsRelationExistsGP checks if the TagsRelation row exists. Panics on error.
func TagsRelationExistsGP(ctx context.Context, iD string) bool {
	e, err := TagsRelationExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TagsRelationExists checks if the TagsRelation row exists.
func TagsRelationExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"tags_relations\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if tags_relations exists")
	}

	return exists, nil
}

// Exists checks if the TagsRelation row exists.
func (o *TagsRelation) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TagsRelationExists(ctx, exec, o.ID)
}
