// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_config

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/sqlboiler/v4/types"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorldDropConfig is an object representing the database table.
type WorldDropConfig struct {
	ID              string   `boil:"id" json:"id" toml:"id" yaml:"id"`
	ItemID          string   `boil:"item_id" json:"item_id" toml:"item_id" yaml:"item_id"`
	TotalDropLimit  null.Int `boil:"total_drop_limit" json:"total_drop_limit,omitempty" toml:"total_drop_limit" yaml:"total_drop_limit,omitempty"`
	DailyDropLimit  null.Int `boil:"daily_drop_limit" json:"daily_drop_limit,omitempty" toml:"daily_drop_limit" yaml:"daily_drop_limit,omitempty"`
	HourlyDropLimit null.Int `boil:"hourly_drop_limit" json:"hourly_drop_limit,omitempty" toml:"hourly_drop_limit" yaml:"hourly_drop_limit,omitempty"`
	MinDropInterval null.Int `boil:"min_drop_interval" json:"min_drop_interval,omitempty" toml:"min_drop_interval" yaml:"min_drop_interval,omitempty"`
	MaxDropInterval null.Int `boil:"max_drop_interval" json:"max_drop_interval,omitempty" toml:"max_drop_interval" yaml:"max_drop_interval,omitempty"`
	// 触发条件 - JSON格式,定义何时可以掉落此物品
	TriggerConditions null.JSON     `boil:"trigger_conditions" json:"trigger_conditions,omitempty" toml:"trigger_conditions" yaml:"trigger_conditions,omitempty"`
	BaseDropRate      types.Decimal `boil:"base_drop_rate" json:"base_drop_rate" toml:"base_drop_rate" yaml:"base_drop_rate"`
	// 掉落概率修正因子 - JSON格式,根据不同条件修正掉落概率
	DropRateModifiers null.JSON `boil:"drop_rate_modifiers" json:"drop_rate_modifiers,omitempty" toml:"drop_rate_modifiers" yaml:"drop_rate_modifiers,omitempty"`
	IsActive          bool      `boil:"is_active" json:"is_active" toml:"is_active" yaml:"is_active"`
	CreatedAt         time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt         time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt         null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *worldDropConfigR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L worldDropConfigL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorldDropConfigColumns = struct {
	ID                string
	ItemID            string
	TotalDropLimit    string
	DailyDropLimit    string
	HourlyDropLimit   string
	MinDropInterval   string
	MaxDropInterval   string
	TriggerConditions string
	BaseDropRate      string
	DropRateModifiers string
	IsActive          string
	CreatedAt         string
	UpdatedAt         string
	DeletedAt         string
}{
	ID:                "id",
	ItemID:            "item_id",
	TotalDropLimit:    "total_drop_limit",
	DailyDropLimit:    "daily_drop_limit",
	HourlyDropLimit:   "hourly_drop_limit",
	MinDropInterval:   "min_drop_interval",
	MaxDropInterval:   "max_drop_interval",
	TriggerConditions: "trigger_conditions",
	BaseDropRate:      "base_drop_rate",
	DropRateModifiers: "drop_rate_modifiers",
	IsActive:          "is_active",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
	DeletedAt:         "deleted_at",
}

var WorldDropConfigTableColumns = struct {
	ID                string
	ItemID            string
	TotalDropLimit    string
	DailyDropLimit    string
	HourlyDropLimit   string
	MinDropInterval   string
	MaxDropInterval   string
	TriggerConditions string
	BaseDropRate      string
	DropRateModifiers string
	IsActive          string
	CreatedAt         string
	UpdatedAt         string
	DeletedAt         string
}{
	ID:                "world_drop_configs.id",
	ItemID:            "world_drop_configs.item_id",
	TotalDropLimit:    "world_drop_configs.total_drop_limit",
	DailyDropLimit:    "world_drop_configs.daily_drop_limit",
	HourlyDropLimit:   "world_drop_configs.hourly_drop_limit",
	MinDropInterval:   "world_drop_configs.min_drop_interval",
	MaxDropInterval:   "world_drop_configs.max_drop_interval",
	TriggerConditions: "world_drop_configs.trigger_conditions",
	BaseDropRate:      "world_drop_configs.base_drop_rate",
	DropRateModifiers: "world_drop_configs.drop_rate_modifiers",
	IsActive:          "world_drop_configs.is_active",
	CreatedAt:         "world_drop_configs.created_at",
	UpdatedAt:         "world_drop_configs.updated_at",
	DeletedAt:         "world_drop_configs.deleted_at",
}

// Generated where

var WorldDropConfigWhere = struct {
	ID                whereHelperstring
	ItemID            whereHelperstring
	TotalDropLimit    whereHelpernull_Int
	DailyDropLimit    whereHelpernull_Int
	HourlyDropLimit   whereHelpernull_Int
	MinDropInterval   whereHelpernull_Int
	MaxDropInterval   whereHelpernull_Int
	TriggerConditions whereHelpernull_JSON
	BaseDropRate      whereHelpertypes_Decimal
	DropRateModifiers whereHelpernull_JSON
	IsActive          whereHelperbool
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
	DeletedAt         whereHelpernull_Time
}{
	ID:                whereHelperstring{field: "\"game_config\".\"world_drop_configs\".\"id\""},
	ItemID:            whereHelperstring{field: "\"game_config\".\"world_drop_configs\".\"item_id\""},
	TotalDropLimit:    whereHelpernull_Int{field: "\"game_config\".\"world_drop_configs\".\"total_drop_limit\""},
	DailyDropLimit:    whereHelpernull_Int{field: "\"game_config\".\"world_drop_configs\".\"daily_drop_limit\""},
	HourlyDropLimit:   whereHelpernull_Int{field: "\"game_config\".\"world_drop_configs\".\"hourly_drop_limit\""},
	MinDropInterval:   whereHelpernull_Int{field: "\"game_config\".\"world_drop_configs\".\"min_drop_interval\""},
	MaxDropInterval:   whereHelpernull_Int{field: "\"game_config\".\"world_drop_configs\".\"max_drop_interval\""},
	TriggerConditions: whereHelpernull_JSON{field: "\"game_config\".\"world_drop_configs\".\"trigger_conditions\""},
	BaseDropRate:      whereHelpertypes_Decimal{field: "\"game_config\".\"world_drop_configs\".\"base_drop_rate\""},
	DropRateModifiers: whereHelpernull_JSON{field: "\"game_config\".\"world_drop_configs\".\"drop_rate_modifiers\""},
	IsActive:          whereHelperbool{field: "\"game_config\".\"world_drop_configs\".\"is_active\""},
	CreatedAt:         whereHelpertime_Time{field: "\"game_config\".\"world_drop_configs\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"game_config\".\"world_drop_configs\".\"updated_at\""},
	DeletedAt:         whereHelpernull_Time{field: "\"game_config\".\"world_drop_configs\".\"deleted_at\""},
}

// WorldDropConfigRels is where relationship names are stored.
var WorldDropConfigRels = struct {
	Item           string
	WorldDropItems string
}{
	Item:           "Item",
	WorldDropItems: "WorldDropItems",
}

// worldDropConfigR is where relationships are stored.
type worldDropConfigR struct {
	Item           *Item              `boil:"Item" json:"Item" toml:"Item" yaml:"Item"`
	WorldDropItems WorldDropItemSlice `boil:"WorldDropItems" json:"WorldDropItems" toml:"WorldDropItems" yaml:"WorldDropItems"`
}

// NewStruct creates a new relationship struct
func (*worldDropConfigR) NewStruct() *worldDropConfigR {
	return &worldDropConfigR{}
}

func (o *WorldDropConfig) GetItem() *Item {
	if o == nil {
		return nil
	}

	return o.R.GetItem()
}

func (r *worldDropConfigR) GetItem() *Item {
	if r == nil {
		return nil
	}

	return r.Item
}

func (o *WorldDropConfig) GetWorldDropItems() WorldDropItemSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWorldDropItems()
}

func (r *worldDropConfigR) GetWorldDropItems() WorldDropItemSlice {
	if r == nil {
		return nil
	}

	return r.WorldDropItems
}

// worldDropConfigL is where Load methods for each relationship are stored.
type worldDropConfigL struct{}

var (
	worldDropConfigAllColumns            = []string{"id", "item_id", "total_drop_limit", "daily_drop_limit", "hourly_drop_limit", "min_drop_interval", "max_drop_interval", "trigger_conditions", "base_drop_rate", "drop_rate_modifiers", "is_active", "created_at", "updated_at", "deleted_at"}
	worldDropConfigColumnsWithoutDefault = []string{"item_id", "base_drop_rate"}
	worldDropConfigColumnsWithDefault    = []string{"id", "total_drop_limit", "daily_drop_limit", "hourly_drop_limit", "min_drop_interval", "max_drop_interval", "trigger_conditions", "drop_rate_modifiers", "is_active", "created_at", "updated_at", "deleted_at"}
	worldDropConfigPrimaryKeyColumns     = []string{"id"}
	worldDropConfigGeneratedColumns      = []string{}
)

type (
	// WorldDropConfigSlice is an alias for a slice of pointers to WorldDropConfig.
	// This should almost always be used instead of []WorldDropConfig.
	WorldDropConfigSlice []*WorldDropConfig
	// WorldDropConfigHook is the signature for custom WorldDropConfig hook methods
	WorldDropConfigHook func(context.Context, boil.ContextExecutor, *WorldDropConfig) error

	worldDropConfigQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	worldDropConfigType                 = reflect.TypeOf(&WorldDropConfig{})
	worldDropConfigMapping              = queries.MakeStructMapping(worldDropConfigType)
	worldDropConfigPrimaryKeyMapping, _ = queries.BindMapping(worldDropConfigType, worldDropConfigMapping, worldDropConfigPrimaryKeyColumns)
	worldDropConfigInsertCacheMut       sync.RWMutex
	worldDropConfigInsertCache          = make(map[string]insertCache)
	worldDropConfigUpdateCacheMut       sync.RWMutex
	worldDropConfigUpdateCache          = make(map[string]updateCache)
	worldDropConfigUpsertCacheMut       sync.RWMutex
	worldDropConfigUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var worldDropConfigAfterSelectMu sync.Mutex
var worldDropConfigAfterSelectHooks []WorldDropConfigHook

var worldDropConfigBeforeInsertMu sync.Mutex
var worldDropConfigBeforeInsertHooks []WorldDropConfigHook
var worldDropConfigAfterInsertMu sync.Mutex
var worldDropConfigAfterInsertHooks []WorldDropConfigHook

var worldDropConfigBeforeUpdateMu sync.Mutex
var worldDropConfigBeforeUpdateHooks []WorldDropConfigHook
var worldDropConfigAfterUpdateMu sync.Mutex
var worldDropConfigAfterUpdateHooks []WorldDropConfigHook

var worldDropConfigBeforeDeleteMu sync.Mutex
var worldDropConfigBeforeDeleteHooks []WorldDropConfigHook
var worldDropConfigAfterDeleteMu sync.Mutex
var worldDropConfigAfterDeleteHooks []WorldDropConfigHook

var worldDropConfigBeforeUpsertMu sync.Mutex
var worldDropConfigBeforeUpsertHooks []WorldDropConfigHook
var worldDropConfigAfterUpsertMu sync.Mutex
var worldDropConfigAfterUpsertHooks []WorldDropConfigHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorldDropConfig) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorldDropConfig) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorldDropConfig) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorldDropConfig) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorldDropConfig) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorldDropConfig) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorldDropConfig) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorldDropConfig) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorldDropConfig) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropConfigAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorldDropConfigHook registers your hook function for all future operations.
func AddWorldDropConfigHook(hookPoint boil.HookPoint, worldDropConfigHook WorldDropConfigHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		worldDropConfigAfterSelectMu.Lock()
		worldDropConfigAfterSelectHooks = append(worldDropConfigAfterSelectHooks, worldDropConfigHook)
		worldDropConfigAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		worldDropConfigBeforeInsertMu.Lock()
		worldDropConfigBeforeInsertHooks = append(worldDropConfigBeforeInsertHooks, worldDropConfigHook)
		worldDropConfigBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		worldDropConfigAfterInsertMu.Lock()
		worldDropConfigAfterInsertHooks = append(worldDropConfigAfterInsertHooks, worldDropConfigHook)
		worldDropConfigAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		worldDropConfigBeforeUpdateMu.Lock()
		worldDropConfigBeforeUpdateHooks = append(worldDropConfigBeforeUpdateHooks, worldDropConfigHook)
		worldDropConfigBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		worldDropConfigAfterUpdateMu.Lock()
		worldDropConfigAfterUpdateHooks = append(worldDropConfigAfterUpdateHooks, worldDropConfigHook)
		worldDropConfigAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		worldDropConfigBeforeDeleteMu.Lock()
		worldDropConfigBeforeDeleteHooks = append(worldDropConfigBeforeDeleteHooks, worldDropConfigHook)
		worldDropConfigBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		worldDropConfigAfterDeleteMu.Lock()
		worldDropConfigAfterDeleteHooks = append(worldDropConfigAfterDeleteHooks, worldDropConfigHook)
		worldDropConfigAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		worldDropConfigBeforeUpsertMu.Lock()
		worldDropConfigBeforeUpsertHooks = append(worldDropConfigBeforeUpsertHooks, worldDropConfigHook)
		worldDropConfigBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		worldDropConfigAfterUpsertMu.Lock()
		worldDropConfigAfterUpsertHooks = append(worldDropConfigAfterUpsertHooks, worldDropConfigHook)
		worldDropConfigAfterUpsertMu.Unlock()
	}
}

// OneG returns a single worldDropConfig record from the query using the global executor.
func (q worldDropConfigQuery) OneG(ctx context.Context) (*WorldDropConfig, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single worldDropConfig record from the query using the global executor, and panics on error.
func (q worldDropConfigQuery) OneGP(ctx context.Context) *WorldDropConfig {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single worldDropConfig record from the query, and panics on error.
func (q worldDropConfigQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *WorldDropConfig {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single worldDropConfig record from the query.
func (q worldDropConfigQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorldDropConfig, error) {
	o := &WorldDropConfig{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: failed to execute a one query for world_drop_configs")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all WorldDropConfig records from the query using the global executor.
func (q worldDropConfigQuery) AllG(ctx context.Context) (WorldDropConfigSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all WorldDropConfig records from the query using the global executor, and panics on error.
func (q worldDropConfigQuery) AllGP(ctx context.Context) WorldDropConfigSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all WorldDropConfig records from the query, and panics on error.
func (q worldDropConfigQuery) AllP(ctx context.Context, exec boil.ContextExecutor) WorldDropConfigSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all WorldDropConfig records from the query.
func (q worldDropConfigQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorldDropConfigSlice, error) {
	var o []*WorldDropConfig

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_config: failed to assign all query results to WorldDropConfig slice")
	}

	if len(worldDropConfigAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all WorldDropConfig records in the query using the global executor
func (q worldDropConfigQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all WorldDropConfig records in the query using the global executor, and panics on error.
func (q worldDropConfigQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all WorldDropConfig records in the query, and panics on error.
func (q worldDropConfigQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all WorldDropConfig records in the query.
func (q worldDropConfigQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to count world_drop_configs rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q worldDropConfigQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q worldDropConfigQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q worldDropConfigQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q worldDropConfigQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_config: failed to check if world_drop_configs exists")
	}

	return count > 0, nil
}

// Item pointed to by the foreign key.
func (o *WorldDropConfig) Item(mods ...qm.QueryMod) itemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ItemID),
	}

	queryMods = append(queryMods, mods...)

	return Items(queryMods...)
}

// WorldDropItems retrieves all the world_drop_item's WorldDropItems with an executor.
func (o *WorldDropConfig) WorldDropItems(mods ...qm.QueryMod) worldDropItemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_config\".\"world_drop_items\".\"world_drop_config_id\"=?", o.ID),
	)

	return WorldDropItems(queryMods...)
}

// LoadItem allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (worldDropConfigL) LoadItem(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorldDropConfig interface{}, mods queries.Applicator) error {
	var slice []*WorldDropConfig
	var object *WorldDropConfig

	if singular {
		var ok bool
		object, ok = maybeWorldDropConfig.(*WorldDropConfig)
		if !ok {
			object = new(WorldDropConfig)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorldDropConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorldDropConfig))
			}
		}
	} else {
		s, ok := maybeWorldDropConfig.(*[]*WorldDropConfig)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorldDropConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorldDropConfig))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &worldDropConfigR{}
		}
		args[object.ItemID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &worldDropConfigR{}
			}

			args[obj.ItemID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.items`),
		qm.WhereIn(`game_config.items.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.items.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Item")
	}

	var resultSlice []*Item
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Item")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for items")
	}

	if len(itemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Item = foreign
		if foreign.R == nil {
			foreign.R = &itemR{}
		}
		foreign.R.WorldDropConfigs = append(foreign.R.WorldDropConfigs, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ItemID == foreign.ID {
				local.R.Item = foreign
				if foreign.R == nil {
					foreign.R = &itemR{}
				}
				foreign.R.WorldDropConfigs = append(foreign.R.WorldDropConfigs, local)
				break
			}
		}
	}

	return nil
}

// LoadWorldDropItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (worldDropConfigL) LoadWorldDropItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeWorldDropConfig interface{}, mods queries.Applicator) error {
	var slice []*WorldDropConfig
	var object *WorldDropConfig

	if singular {
		var ok bool
		object, ok = maybeWorldDropConfig.(*WorldDropConfig)
		if !ok {
			object = new(WorldDropConfig)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeWorldDropConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeWorldDropConfig))
			}
		}
	} else {
		s, ok := maybeWorldDropConfig.(*[]*WorldDropConfig)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeWorldDropConfig)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeWorldDropConfig))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &worldDropConfigR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &worldDropConfigR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_config.world_drop_items`),
		qm.WhereIn(`game_config.world_drop_items.world_drop_config_id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_config.world_drop_items.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load world_drop_items")
	}

	var resultSlice []*WorldDropItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice world_drop_items")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on world_drop_items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for world_drop_items")
	}

	if len(worldDropItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WorldDropItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &worldDropItemR{}
			}
			foreign.R.WorldDropConfig = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WorldDropConfigID {
				local.R.WorldDropItems = append(local.R.WorldDropItems, foreign)
				if foreign.R == nil {
					foreign.R = &worldDropItemR{}
				}
				foreign.R.WorldDropConfig = local
				break
			}
		}
	}

	return nil
}

// SetItemG of the worldDropConfig to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.WorldDropConfigs.
// Uses the global database handle.
func (o *WorldDropConfig) SetItemG(ctx context.Context, insert bool, related *Item) error {
	return o.SetItem(ctx, boil.GetContextDB(), insert, related)
}

// SetItemP of the worldDropConfig to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.WorldDropConfigs.
// Panics on error.
func (o *WorldDropConfig) SetItemP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Item) {
	if err := o.SetItem(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetItemGP of the worldDropConfig to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.WorldDropConfigs.
// Uses the global database handle and panics on error.
func (o *WorldDropConfig) SetItemGP(ctx context.Context, insert bool, related *Item) {
	if err := o.SetItem(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetItem of the worldDropConfig to the related item.
// Sets o.R.Item to related.
// Adds o to related.R.WorldDropConfigs.
func (o *WorldDropConfig) SetItem(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Item) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_config\".\"world_drop_configs\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"item_id"}),
		strmangle.WhereClause("\"", "\"", 2, worldDropConfigPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ItemID = related.ID
	if o.R == nil {
		o.R = &worldDropConfigR{
			Item: related,
		}
	} else {
		o.R.Item = related
	}

	if related.R == nil {
		related.R = &itemR{
			WorldDropConfigs: WorldDropConfigSlice{o},
		}
	} else {
		related.R.WorldDropConfigs = append(related.R.WorldDropConfigs, o)
	}

	return nil
}

// AddWorldDropItemsG adds the given related objects to the existing relationships
// of the world_drop_config, optionally inserting them as new records.
// Appends related to o.R.WorldDropItems.
// Sets related.R.WorldDropConfig appropriately.
// Uses the global database handle.
func (o *WorldDropConfig) AddWorldDropItemsG(ctx context.Context, insert bool, related ...*WorldDropItem) error {
	return o.AddWorldDropItems(ctx, boil.GetContextDB(), insert, related...)
}

// AddWorldDropItemsP adds the given related objects to the existing relationships
// of the world_drop_config, optionally inserting them as new records.
// Appends related to o.R.WorldDropItems.
// Sets related.R.WorldDropConfig appropriately.
// Panics on error.
func (o *WorldDropConfig) AddWorldDropItemsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorldDropItem) {
	if err := o.AddWorldDropItems(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWorldDropItemsGP adds the given related objects to the existing relationships
// of the world_drop_config, optionally inserting them as new records.
// Appends related to o.R.WorldDropItems.
// Sets related.R.WorldDropConfig appropriately.
// Uses the global database handle and panics on error.
func (o *WorldDropConfig) AddWorldDropItemsGP(ctx context.Context, insert bool, related ...*WorldDropItem) {
	if err := o.AddWorldDropItems(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWorldDropItems adds the given related objects to the existing relationships
// of the world_drop_config, optionally inserting them as new records.
// Appends related to o.R.WorldDropItems.
// Sets related.R.WorldDropConfig appropriately.
func (o *WorldDropConfig) AddWorldDropItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*WorldDropItem) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WorldDropConfigID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_config\".\"world_drop_items\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"world_drop_config_id"}),
				strmangle.WhereClause("\"", "\"", 2, worldDropItemPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WorldDropConfigID = o.ID
		}
	}

	if o.R == nil {
		o.R = &worldDropConfigR{
			WorldDropItems: related,
		}
	} else {
		o.R.WorldDropItems = append(o.R.WorldDropItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &worldDropItemR{
				WorldDropConfig: o,
			}
		} else {
			rel.R.WorldDropConfig = o
		}
	}
	return nil
}

// WorldDropConfigs retrieves all the records using an executor.
func WorldDropConfigs(mods ...qm.QueryMod) worldDropConfigQuery {
	mods = append(mods, qm.From("\"game_config\".\"world_drop_configs\""), qmhelper.WhereIsNull("\"game_config\".\"world_drop_configs\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_config\".\"world_drop_configs\".*"})
	}

	return worldDropConfigQuery{q}
}

// FindWorldDropConfigG retrieves a single record by ID.
func FindWorldDropConfigG(ctx context.Context, iD string, selectCols ...string) (*WorldDropConfig, error) {
	return FindWorldDropConfig(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindWorldDropConfigP retrieves a single record by ID with an executor, and panics on error.
func FindWorldDropConfigP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *WorldDropConfig {
	retobj, err := FindWorldDropConfig(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindWorldDropConfigGP retrieves a single record by ID, and panics on error.
func FindWorldDropConfigGP(ctx context.Context, iD string, selectCols ...string) *WorldDropConfig {
	retobj, err := FindWorldDropConfig(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindWorldDropConfig retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorldDropConfig(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorldDropConfig, error) {
	worldDropConfigObj := &WorldDropConfig{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_config\".\"world_drop_configs\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, worldDropConfigObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_config: unable to select from world_drop_configs")
	}

	if err = worldDropConfigObj.doAfterSelectHooks(ctx, exec); err != nil {
		return worldDropConfigObj, err
	}

	return worldDropConfigObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *WorldDropConfig) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *WorldDropConfig) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *WorldDropConfig) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorldDropConfig) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_config: no world_drop_configs provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(worldDropConfigColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	worldDropConfigInsertCacheMut.RLock()
	cache, cached := worldDropConfigInsertCache[key]
	worldDropConfigInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			worldDropConfigAllColumns,
			worldDropConfigColumnsWithDefault,
			worldDropConfigColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(worldDropConfigType, worldDropConfigMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(worldDropConfigType, worldDropConfigMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_config\".\"world_drop_configs\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_config\".\"world_drop_configs\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_config: unable to insert into world_drop_configs")
	}

	if !cached {
		worldDropConfigInsertCacheMut.Lock()
		worldDropConfigInsertCache[key] = cache
		worldDropConfigInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single WorldDropConfig record using the global executor.
// See Update for more documentation.
func (o *WorldDropConfig) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the WorldDropConfig, and panics on error.
// See Update for more documentation.
func (o *WorldDropConfig) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single WorldDropConfig record using the global executor. Panics on error.
// See Update for more documentation.
func (o *WorldDropConfig) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the WorldDropConfig.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorldDropConfig) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	worldDropConfigUpdateCacheMut.RLock()
	cache, cached := worldDropConfigUpdateCache[key]
	worldDropConfigUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			worldDropConfigAllColumns,
			worldDropConfigPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_config: unable to update world_drop_configs, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_config\".\"world_drop_configs\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, worldDropConfigPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(worldDropConfigType, worldDropConfigMapping, append(wl, worldDropConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update world_drop_configs row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by update for world_drop_configs")
	}

	if !cached {
		worldDropConfigUpdateCacheMut.Lock()
		worldDropConfigUpdateCache[key] = cache
		worldDropConfigUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q worldDropConfigQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q worldDropConfigQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q worldDropConfigQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q worldDropConfigQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all for world_drop_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected for world_drop_configs")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o WorldDropConfigSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o WorldDropConfigSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o WorldDropConfigSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorldDropConfigSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_config: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_config\".\"world_drop_configs\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, worldDropConfigPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to update all in worldDropConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to retrieve rows affected all in update all worldDropConfig")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *WorldDropConfig) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *WorldDropConfig) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *WorldDropConfig) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorldDropConfig) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_config: no world_drop_configs provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(worldDropConfigColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	worldDropConfigUpsertCacheMut.RLock()
	cache, cached := worldDropConfigUpsertCache[key]
	worldDropConfigUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			worldDropConfigAllColumns,
			worldDropConfigColumnsWithDefault,
			worldDropConfigColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			worldDropConfigAllColumns,
			worldDropConfigPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_config: unable to upsert world_drop_configs, could not build update column list")
		}

		ret := strmangle.SetComplement(worldDropConfigAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(worldDropConfigPrimaryKeyColumns) == 0 {
				return errors.New("game_config: unable to upsert world_drop_configs, could not build conflict column list")
			}

			conflict = make([]string, len(worldDropConfigPrimaryKeyColumns))
			copy(conflict, worldDropConfigPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_config\".\"world_drop_configs\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(worldDropConfigType, worldDropConfigMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(worldDropConfigType, worldDropConfigMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_config: unable to upsert world_drop_configs")
	}

	if !cached {
		worldDropConfigUpsertCacheMut.Lock()
		worldDropConfigUpsertCache[key] = cache
		worldDropConfigUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single WorldDropConfig record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *WorldDropConfig) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single WorldDropConfig record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *WorldDropConfig) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single WorldDropConfig record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *WorldDropConfig) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single WorldDropConfig record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorldDropConfig) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_config: no WorldDropConfig provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), worldDropConfigPrimaryKeyMapping)
		sql = "DELETE FROM \"game_config\".\"world_drop_configs\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"world_drop_configs\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(worldDropConfigType, worldDropConfigMapping, append(wl, worldDropConfigPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete from world_drop_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by delete for world_drop_configs")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q worldDropConfigQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q worldDropConfigQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q worldDropConfigQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q worldDropConfigQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_config: no worldDropConfigQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from world_drop_configs")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for world_drop_configs")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o WorldDropConfigSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o WorldDropConfigSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o WorldDropConfigSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorldDropConfigSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(worldDropConfigBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropConfigPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_config\".\"world_drop_configs\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, worldDropConfigPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropConfigPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_config\".\"world_drop_configs\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, worldDropConfigPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_config: unable to delete all from worldDropConfig slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_config: failed to get rows affected by deleteall for world_drop_configs")
	}

	if len(worldDropConfigAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *WorldDropConfig) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: no WorldDropConfig provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *WorldDropConfig) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *WorldDropConfig) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorldDropConfig) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorldDropConfig(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorldDropConfigSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_config: empty WorldDropConfigSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *WorldDropConfigSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *WorldDropConfigSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorldDropConfigSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorldDropConfigSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropConfigPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_config\".\"world_drop_configs\".* FROM \"game_config\".\"world_drop_configs\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, worldDropConfigPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_config: unable to reload all in WorldDropConfigSlice")
	}

	*o = slice

	return nil
}

// WorldDropConfigExistsG checks if the WorldDropConfig row exists.
func WorldDropConfigExistsG(ctx context.Context, iD string) (bool, error) {
	return WorldDropConfigExists(ctx, boil.GetContextDB(), iD)
}

// WorldDropConfigExistsP checks if the WorldDropConfig row exists. Panics on error.
func WorldDropConfigExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := WorldDropConfigExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// WorldDropConfigExistsGP checks if the WorldDropConfig row exists. Panics on error.
func WorldDropConfigExistsGP(ctx context.Context, iD string) bool {
	e, err := WorldDropConfigExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// WorldDropConfigExists checks if the WorldDropConfig row exists.
func WorldDropConfigExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_config\".\"world_drop_configs\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_config: unable to check if world_drop_configs exists")
	}

	return exists, nil
}

// Exists checks if the WorldDropConfig row exists.
func (o *WorldDropConfig) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorldDropConfigExists(ctx, exec, o.ID)
}
