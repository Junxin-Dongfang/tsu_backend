// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// CurrentHeroContext is an object representing the database table.
type CurrentHeroContext struct {
	UserID     string    `boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	HeroID     string    `boil:"hero_id" json:"hero_id" toml:"hero_id" yaml:"hero_id"`
	SwitchedAt time.Time `boil:"switched_at" json:"switched_at" toml:"switched_at" yaml:"switched_at"`
	UpdatedAt  time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *currentHeroContextR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L currentHeroContextL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var CurrentHeroContextColumns = struct {
	UserID     string
	HeroID     string
	SwitchedAt string
	UpdatedAt  string
}{
	UserID:     "user_id",
	HeroID:     "hero_id",
	SwitchedAt: "switched_at",
	UpdatedAt:  "updated_at",
}

var CurrentHeroContextTableColumns = struct {
	UserID     string
	HeroID     string
	SwitchedAt string
	UpdatedAt  string
}{
	UserID:     "current_hero_contexts.user_id",
	HeroID:     "current_hero_contexts.hero_id",
	SwitchedAt: "current_hero_contexts.switched_at",
	UpdatedAt:  "current_hero_contexts.updated_at",
}

// Generated where

var CurrentHeroContextWhere = struct {
	UserID     whereHelperstring
	HeroID     whereHelperstring
	SwitchedAt whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
}{
	UserID:     whereHelperstring{field: "\"game_runtime\".\"current_hero_contexts\".\"user_id\""},
	HeroID:     whereHelperstring{field: "\"game_runtime\".\"current_hero_contexts\".\"hero_id\""},
	SwitchedAt: whereHelpertime_Time{field: "\"game_runtime\".\"current_hero_contexts\".\"switched_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"game_runtime\".\"current_hero_contexts\".\"updated_at\""},
}

// CurrentHeroContextRels is where relationship names are stored.
var CurrentHeroContextRels = struct {
	Hero string
}{
	Hero: "Hero",
}

// currentHeroContextR is where relationships are stored.
type currentHeroContextR struct {
	Hero *Hero `boil:"Hero" json:"Hero" toml:"Hero" yaml:"Hero"`
}

// NewStruct creates a new relationship struct
func (*currentHeroContextR) NewStruct() *currentHeroContextR {
	return &currentHeroContextR{}
}

func (o *CurrentHeroContext) GetHero() *Hero {
	if o == nil {
		return nil
	}

	return o.R.GetHero()
}

func (r *currentHeroContextR) GetHero() *Hero {
	if r == nil {
		return nil
	}

	return r.Hero
}

// currentHeroContextL is where Load methods for each relationship are stored.
type currentHeroContextL struct{}

var (
	currentHeroContextAllColumns            = []string{"user_id", "hero_id", "switched_at", "updated_at"}
	currentHeroContextColumnsWithoutDefault = []string{"user_id", "hero_id"}
	currentHeroContextColumnsWithDefault    = []string{"switched_at", "updated_at"}
	currentHeroContextPrimaryKeyColumns     = []string{"user_id"}
	currentHeroContextGeneratedColumns      = []string{}
)

type (
	// CurrentHeroContextSlice is an alias for a slice of pointers to CurrentHeroContext.
	// This should almost always be used instead of []CurrentHeroContext.
	CurrentHeroContextSlice []*CurrentHeroContext
	// CurrentHeroContextHook is the signature for custom CurrentHeroContext hook methods
	CurrentHeroContextHook func(context.Context, boil.ContextExecutor, *CurrentHeroContext) error

	currentHeroContextQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	currentHeroContextType                 = reflect.TypeOf(&CurrentHeroContext{})
	currentHeroContextMapping              = queries.MakeStructMapping(currentHeroContextType)
	currentHeroContextPrimaryKeyMapping, _ = queries.BindMapping(currentHeroContextType, currentHeroContextMapping, currentHeroContextPrimaryKeyColumns)
	currentHeroContextInsertCacheMut       sync.RWMutex
	currentHeroContextInsertCache          = make(map[string]insertCache)
	currentHeroContextUpdateCacheMut       sync.RWMutex
	currentHeroContextUpdateCache          = make(map[string]updateCache)
	currentHeroContextUpsertCacheMut       sync.RWMutex
	currentHeroContextUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var currentHeroContextAfterSelectMu sync.Mutex
var currentHeroContextAfterSelectHooks []CurrentHeroContextHook

var currentHeroContextBeforeInsertMu sync.Mutex
var currentHeroContextBeforeInsertHooks []CurrentHeroContextHook
var currentHeroContextAfterInsertMu sync.Mutex
var currentHeroContextAfterInsertHooks []CurrentHeroContextHook

var currentHeroContextBeforeUpdateMu sync.Mutex
var currentHeroContextBeforeUpdateHooks []CurrentHeroContextHook
var currentHeroContextAfterUpdateMu sync.Mutex
var currentHeroContextAfterUpdateHooks []CurrentHeroContextHook

var currentHeroContextBeforeDeleteMu sync.Mutex
var currentHeroContextBeforeDeleteHooks []CurrentHeroContextHook
var currentHeroContextAfterDeleteMu sync.Mutex
var currentHeroContextAfterDeleteHooks []CurrentHeroContextHook

var currentHeroContextBeforeUpsertMu sync.Mutex
var currentHeroContextBeforeUpsertHooks []CurrentHeroContextHook
var currentHeroContextAfterUpsertMu sync.Mutex
var currentHeroContextAfterUpsertHooks []CurrentHeroContextHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *CurrentHeroContext) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *CurrentHeroContext) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *CurrentHeroContext) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *CurrentHeroContext) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *CurrentHeroContext) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *CurrentHeroContext) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *CurrentHeroContext) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *CurrentHeroContext) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *CurrentHeroContext) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range currentHeroContextAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddCurrentHeroContextHook registers your hook function for all future operations.
func AddCurrentHeroContextHook(hookPoint boil.HookPoint, currentHeroContextHook CurrentHeroContextHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		currentHeroContextAfterSelectMu.Lock()
		currentHeroContextAfterSelectHooks = append(currentHeroContextAfterSelectHooks, currentHeroContextHook)
		currentHeroContextAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		currentHeroContextBeforeInsertMu.Lock()
		currentHeroContextBeforeInsertHooks = append(currentHeroContextBeforeInsertHooks, currentHeroContextHook)
		currentHeroContextBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		currentHeroContextAfterInsertMu.Lock()
		currentHeroContextAfterInsertHooks = append(currentHeroContextAfterInsertHooks, currentHeroContextHook)
		currentHeroContextAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		currentHeroContextBeforeUpdateMu.Lock()
		currentHeroContextBeforeUpdateHooks = append(currentHeroContextBeforeUpdateHooks, currentHeroContextHook)
		currentHeroContextBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		currentHeroContextAfterUpdateMu.Lock()
		currentHeroContextAfterUpdateHooks = append(currentHeroContextAfterUpdateHooks, currentHeroContextHook)
		currentHeroContextAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		currentHeroContextBeforeDeleteMu.Lock()
		currentHeroContextBeforeDeleteHooks = append(currentHeroContextBeforeDeleteHooks, currentHeroContextHook)
		currentHeroContextBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		currentHeroContextAfterDeleteMu.Lock()
		currentHeroContextAfterDeleteHooks = append(currentHeroContextAfterDeleteHooks, currentHeroContextHook)
		currentHeroContextAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		currentHeroContextBeforeUpsertMu.Lock()
		currentHeroContextBeforeUpsertHooks = append(currentHeroContextBeforeUpsertHooks, currentHeroContextHook)
		currentHeroContextBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		currentHeroContextAfterUpsertMu.Lock()
		currentHeroContextAfterUpsertHooks = append(currentHeroContextAfterUpsertHooks, currentHeroContextHook)
		currentHeroContextAfterUpsertMu.Unlock()
	}
}

// OneG returns a single currentHeroContext record from the query using the global executor.
func (q currentHeroContextQuery) OneG(ctx context.Context) (*CurrentHeroContext, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single currentHeroContext record from the query using the global executor, and panics on error.
func (q currentHeroContextQuery) OneGP(ctx context.Context) *CurrentHeroContext {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single currentHeroContext record from the query, and panics on error.
func (q currentHeroContextQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *CurrentHeroContext {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single currentHeroContext record from the query.
func (q currentHeroContextQuery) One(ctx context.Context, exec boil.ContextExecutor) (*CurrentHeroContext, error) {
	o := &CurrentHeroContext{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for current_hero_contexts")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all CurrentHeroContext records from the query using the global executor.
func (q currentHeroContextQuery) AllG(ctx context.Context) (CurrentHeroContextSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all CurrentHeroContext records from the query using the global executor, and panics on error.
func (q currentHeroContextQuery) AllGP(ctx context.Context) CurrentHeroContextSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all CurrentHeroContext records from the query, and panics on error.
func (q currentHeroContextQuery) AllP(ctx context.Context, exec boil.ContextExecutor) CurrentHeroContextSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all CurrentHeroContext records from the query.
func (q currentHeroContextQuery) All(ctx context.Context, exec boil.ContextExecutor) (CurrentHeroContextSlice, error) {
	var o []*CurrentHeroContext

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to CurrentHeroContext slice")
	}

	if len(currentHeroContextAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all CurrentHeroContext records in the query using the global executor
func (q currentHeroContextQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all CurrentHeroContext records in the query using the global executor, and panics on error.
func (q currentHeroContextQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all CurrentHeroContext records in the query, and panics on error.
func (q currentHeroContextQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all CurrentHeroContext records in the query.
func (q currentHeroContextQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count current_hero_contexts rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q currentHeroContextQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q currentHeroContextQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q currentHeroContextQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q currentHeroContextQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if current_hero_contexts exists")
	}

	return count > 0, nil
}

// Hero pointed to by the foreign key.
func (o *CurrentHeroContext) Hero(mods ...qm.QueryMod) heroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.HeroID),
	}

	queryMods = append(queryMods, mods...)

	return Heroes(queryMods...)
}

// LoadHero allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (currentHeroContextL) LoadHero(ctx context.Context, e boil.ContextExecutor, singular bool, maybeCurrentHeroContext interface{}, mods queries.Applicator) error {
	var slice []*CurrentHeroContext
	var object *CurrentHeroContext

	if singular {
		var ok bool
		object, ok = maybeCurrentHeroContext.(*CurrentHeroContext)
		if !ok {
			object = new(CurrentHeroContext)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeCurrentHeroContext)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeCurrentHeroContext))
			}
		}
	} else {
		s, ok := maybeCurrentHeroContext.(*[]*CurrentHeroContext)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeCurrentHeroContext)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeCurrentHeroContext))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &currentHeroContextR{}
		}
		args[object.HeroID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &currentHeroContextR{}
			}

			args[obj.HeroID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.heroes`),
		qm.WhereIn(`game_runtime.heroes.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.heroes.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Hero")
	}

	var resultSlice []*Hero
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Hero")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for heroes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for heroes")
	}

	if len(heroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Hero = foreign
		if foreign.R == nil {
			foreign.R = &heroR{}
		}
		foreign.R.CurrentHeroContexts = append(foreign.R.CurrentHeroContexts, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.HeroID == foreign.ID {
				local.R.Hero = foreign
				if foreign.R == nil {
					foreign.R = &heroR{}
				}
				foreign.R.CurrentHeroContexts = append(foreign.R.CurrentHeroContexts, local)
				break
			}
		}
	}

	return nil
}

// SetHeroG of the currentHeroContext to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.CurrentHeroContexts.
// Uses the global database handle.
func (o *CurrentHeroContext) SetHeroG(ctx context.Context, insert bool, related *Hero) error {
	return o.SetHero(ctx, boil.GetContextDB(), insert, related)
}

// SetHeroP of the currentHeroContext to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.CurrentHeroContexts.
// Panics on error.
func (o *CurrentHeroContext) SetHeroP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) {
	if err := o.SetHero(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetHeroGP of the currentHeroContext to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.CurrentHeroContexts.
// Uses the global database handle and panics on error.
func (o *CurrentHeroContext) SetHeroGP(ctx context.Context, insert bool, related *Hero) {
	if err := o.SetHero(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetHero of the currentHeroContext to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.CurrentHeroContexts.
func (o *CurrentHeroContext) SetHero(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"current_hero_contexts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"hero_id"}),
		strmangle.WhereClause("\"", "\"", 2, currentHeroContextPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.UserID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.HeroID = related.ID
	if o.R == nil {
		o.R = &currentHeroContextR{
			Hero: related,
		}
	} else {
		o.R.Hero = related
	}

	if related.R == nil {
		related.R = &heroR{
			CurrentHeroContexts: CurrentHeroContextSlice{o},
		}
	} else {
		related.R.CurrentHeroContexts = append(related.R.CurrentHeroContexts, o)
	}

	return nil
}

// CurrentHeroContexts retrieves all the records using an executor.
func CurrentHeroContexts(mods ...qm.QueryMod) currentHeroContextQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"current_hero_contexts\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"current_hero_contexts\".*"})
	}

	return currentHeroContextQuery{q}
}

// FindCurrentHeroContextG retrieves a single record by ID.
func FindCurrentHeroContextG(ctx context.Context, userID string, selectCols ...string) (*CurrentHeroContext, error) {
	return FindCurrentHeroContext(ctx, boil.GetContextDB(), userID, selectCols...)
}

// FindCurrentHeroContextP retrieves a single record by ID with an executor, and panics on error.
func FindCurrentHeroContextP(ctx context.Context, exec boil.ContextExecutor, userID string, selectCols ...string) *CurrentHeroContext {
	retobj, err := FindCurrentHeroContext(ctx, exec, userID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindCurrentHeroContextGP retrieves a single record by ID, and panics on error.
func FindCurrentHeroContextGP(ctx context.Context, userID string, selectCols ...string) *CurrentHeroContext {
	retobj, err := FindCurrentHeroContext(ctx, boil.GetContextDB(), userID, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindCurrentHeroContext retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindCurrentHeroContext(ctx context.Context, exec boil.ContextExecutor, userID string, selectCols ...string) (*CurrentHeroContext, error) {
	currentHeroContextObj := &CurrentHeroContext{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"current_hero_contexts\" where \"user_id\"=$1", sel,
	)

	q := queries.Raw(query, userID)

	err := q.Bind(ctx, exec, currentHeroContextObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from current_hero_contexts")
	}

	if err = currentHeroContextObj.doAfterSelectHooks(ctx, exec); err != nil {
		return currentHeroContextObj, err
	}

	return currentHeroContextObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *CurrentHeroContext) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *CurrentHeroContext) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *CurrentHeroContext) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *CurrentHeroContext) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no current_hero_contexts provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(currentHeroContextColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	currentHeroContextInsertCacheMut.RLock()
	cache, cached := currentHeroContextInsertCache[key]
	currentHeroContextInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			currentHeroContextAllColumns,
			currentHeroContextColumnsWithDefault,
			currentHeroContextColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(currentHeroContextType, currentHeroContextMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(currentHeroContextType, currentHeroContextMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"current_hero_contexts\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"current_hero_contexts\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into current_hero_contexts")
	}

	if !cached {
		currentHeroContextInsertCacheMut.Lock()
		currentHeroContextInsertCache[key] = cache
		currentHeroContextInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single CurrentHeroContext record using the global executor.
// See Update for more documentation.
func (o *CurrentHeroContext) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the CurrentHeroContext, and panics on error.
// See Update for more documentation.
func (o *CurrentHeroContext) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single CurrentHeroContext record using the global executor. Panics on error.
// See Update for more documentation.
func (o *CurrentHeroContext) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the CurrentHeroContext.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *CurrentHeroContext) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	currentHeroContextUpdateCacheMut.RLock()
	cache, cached := currentHeroContextUpdateCache[key]
	currentHeroContextUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			currentHeroContextAllColumns,
			currentHeroContextPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update current_hero_contexts, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"current_hero_contexts\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, currentHeroContextPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(currentHeroContextType, currentHeroContextMapping, append(wl, currentHeroContextPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update current_hero_contexts row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for current_hero_contexts")
	}

	if !cached {
		currentHeroContextUpdateCacheMut.Lock()
		currentHeroContextUpdateCache[key] = cache
		currentHeroContextUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q currentHeroContextQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q currentHeroContextQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q currentHeroContextQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q currentHeroContextQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for current_hero_contexts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for current_hero_contexts")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o CurrentHeroContextSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o CurrentHeroContextSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o CurrentHeroContextSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o CurrentHeroContextSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), currentHeroContextPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"current_hero_contexts\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, currentHeroContextPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in currentHeroContext slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all currentHeroContext")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *CurrentHeroContext) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *CurrentHeroContext) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *CurrentHeroContext) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *CurrentHeroContext) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no current_hero_contexts provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(currentHeroContextColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	currentHeroContextUpsertCacheMut.RLock()
	cache, cached := currentHeroContextUpsertCache[key]
	currentHeroContextUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			currentHeroContextAllColumns,
			currentHeroContextColumnsWithDefault,
			currentHeroContextColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			currentHeroContextAllColumns,
			currentHeroContextPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert current_hero_contexts, could not build update column list")
		}

		ret := strmangle.SetComplement(currentHeroContextAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(currentHeroContextPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert current_hero_contexts, could not build conflict column list")
			}

			conflict = make([]string, len(currentHeroContextPrimaryKeyColumns))
			copy(conflict, currentHeroContextPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"current_hero_contexts\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(currentHeroContextType, currentHeroContextMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(currentHeroContextType, currentHeroContextMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert current_hero_contexts")
	}

	if !cached {
		currentHeroContextUpsertCacheMut.Lock()
		currentHeroContextUpsertCache[key] = cache
		currentHeroContextUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single CurrentHeroContext record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *CurrentHeroContext) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single CurrentHeroContext record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *CurrentHeroContext) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single CurrentHeroContext record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *CurrentHeroContext) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single CurrentHeroContext record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *CurrentHeroContext) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no CurrentHeroContext provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), currentHeroContextPrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"current_hero_contexts\" WHERE \"user_id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from current_hero_contexts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for current_hero_contexts")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q currentHeroContextQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q currentHeroContextQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q currentHeroContextQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q currentHeroContextQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no currentHeroContextQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from current_hero_contexts")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for current_hero_contexts")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o CurrentHeroContextSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o CurrentHeroContextSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o CurrentHeroContextSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o CurrentHeroContextSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(currentHeroContextBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), currentHeroContextPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"current_hero_contexts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, currentHeroContextPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from currentHeroContext slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for current_hero_contexts")
	}

	if len(currentHeroContextAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *CurrentHeroContext) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no CurrentHeroContext provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *CurrentHeroContext) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *CurrentHeroContext) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *CurrentHeroContext) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindCurrentHeroContext(ctx, exec, o.UserID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CurrentHeroContextSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty CurrentHeroContextSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *CurrentHeroContextSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *CurrentHeroContextSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *CurrentHeroContextSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := CurrentHeroContextSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), currentHeroContextPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"current_hero_contexts\".* FROM \"game_runtime\".\"current_hero_contexts\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, currentHeroContextPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in CurrentHeroContextSlice")
	}

	*o = slice

	return nil
}

// CurrentHeroContextExistsG checks if the CurrentHeroContext row exists.
func CurrentHeroContextExistsG(ctx context.Context, userID string) (bool, error) {
	return CurrentHeroContextExists(ctx, boil.GetContextDB(), userID)
}

// CurrentHeroContextExistsP checks if the CurrentHeroContext row exists. Panics on error.
func CurrentHeroContextExistsP(ctx context.Context, exec boil.ContextExecutor, userID string) bool {
	e, err := CurrentHeroContextExists(ctx, exec, userID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// CurrentHeroContextExistsGP checks if the CurrentHeroContext row exists. Panics on error.
func CurrentHeroContextExistsGP(ctx context.Context, userID string) bool {
	e, err := CurrentHeroContextExists(ctx, boil.GetContextDB(), userID)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// CurrentHeroContextExists checks if the CurrentHeroContext row exists.
func CurrentHeroContextExists(ctx context.Context, exec boil.ContextExecutor, userID string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"current_hero_contexts\" where \"user_id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, userID)
	}
	row := exec.QueryRowContext(ctx, sql, userID)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if current_hero_contexts exists")
	}

	return exists, nil
}

// Exists checks if the CurrentHeroContext row exists.
func (o *CurrentHeroContext) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return CurrentHeroContextExists(ctx, exec, o.UserID)
}
