// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// HeroClassHistory is an object representing the database table.
type HeroClassHistory struct {
	ID         string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	HeroID     string    `boil:"hero_id" json:"hero_id" toml:"hero_id" yaml:"hero_id"`
	ClassID    string    `boil:"class_id" json:"class_id" toml:"class_id" yaml:"class_id"`
	IsCurrent  bool      `boil:"is_current" json:"is_current" toml:"is_current" yaml:"is_current"`
	AcquiredAt time.Time `boil:"acquired_at" json:"acquired_at" toml:"acquired_at" yaml:"acquired_at"`
	// 获得方式：initial(初始)、advancement(进阶-保留技能池)、transfer(转职-放弃旧技能池)
	AcquisitionType string    `boil:"acquisition_type" json:"acquisition_type" toml:"acquisition_type" yaml:"acquisition_type"`
	CreatedAt       null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt       null.Time `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`

	R *heroClassHistoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L heroClassHistoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HeroClassHistoryColumns = struct {
	ID              string
	HeroID          string
	ClassID         string
	IsCurrent       string
	AcquiredAt      string
	AcquisitionType string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "id",
	HeroID:          "hero_id",
	ClassID:         "class_id",
	IsCurrent:       "is_current",
	AcquiredAt:      "acquired_at",
	AcquisitionType: "acquisition_type",
	CreatedAt:       "created_at",
	UpdatedAt:       "updated_at",
}

var HeroClassHistoryTableColumns = struct {
	ID              string
	HeroID          string
	ClassID         string
	IsCurrent       string
	AcquiredAt      string
	AcquisitionType string
	CreatedAt       string
	UpdatedAt       string
}{
	ID:              "hero_class_history.id",
	HeroID:          "hero_class_history.hero_id",
	ClassID:         "hero_class_history.class_id",
	IsCurrent:       "hero_class_history.is_current",
	AcquiredAt:      "hero_class_history.acquired_at",
	AcquisitionType: "hero_class_history.acquisition_type",
	CreatedAt:       "hero_class_history.created_at",
	UpdatedAt:       "hero_class_history.updated_at",
}

// Generated where

type whereHelperbool struct{ field string }

func (w whereHelperbool) EQ(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.EQ, x) }
func (w whereHelperbool) NEQ(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.NEQ, x) }
func (w whereHelperbool) LT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.LT, x) }
func (w whereHelperbool) LTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.LTE, x) }
func (w whereHelperbool) GT(x bool) qm.QueryMod  { return qmhelper.Where(w.field, qmhelper.GT, x) }
func (w whereHelperbool) GTE(x bool) qm.QueryMod { return qmhelper.Where(w.field, qmhelper.GTE, x) }

var HeroClassHistoryWhere = struct {
	ID              whereHelperstring
	HeroID          whereHelperstring
	ClassID         whereHelperstring
	IsCurrent       whereHelperbool
	AcquiredAt      whereHelpertime_Time
	AcquisitionType whereHelperstring
	CreatedAt       whereHelpernull_Time
	UpdatedAt       whereHelpernull_Time
}{
	ID:              whereHelperstring{field: "\"game_runtime\".\"hero_class_history\".\"id\""},
	HeroID:          whereHelperstring{field: "\"game_runtime\".\"hero_class_history\".\"hero_id\""},
	ClassID:         whereHelperstring{field: "\"game_runtime\".\"hero_class_history\".\"class_id\""},
	IsCurrent:       whereHelperbool{field: "\"game_runtime\".\"hero_class_history\".\"is_current\""},
	AcquiredAt:      whereHelpertime_Time{field: "\"game_runtime\".\"hero_class_history\".\"acquired_at\""},
	AcquisitionType: whereHelperstring{field: "\"game_runtime\".\"hero_class_history\".\"acquisition_type\""},
	CreatedAt:       whereHelpernull_Time{field: "\"game_runtime\".\"hero_class_history\".\"created_at\""},
	UpdatedAt:       whereHelpernull_Time{field: "\"game_runtime\".\"hero_class_history\".\"updated_at\""},
}

// HeroClassHistoryRels is where relationship names are stored.
var HeroClassHistoryRels = struct {
	Hero string
}{
	Hero: "Hero",
}

// heroClassHistoryR is where relationships are stored.
type heroClassHistoryR struct {
	Hero *Hero `boil:"Hero" json:"Hero" toml:"Hero" yaml:"Hero"`
}

// NewStruct creates a new relationship struct
func (*heroClassHistoryR) NewStruct() *heroClassHistoryR {
	return &heroClassHistoryR{}
}

func (o *HeroClassHistory) GetHero() *Hero {
	if o == nil {
		return nil
	}

	return o.R.GetHero()
}

func (r *heroClassHistoryR) GetHero() *Hero {
	if r == nil {
		return nil
	}

	return r.Hero
}

// heroClassHistoryL is where Load methods for each relationship are stored.
type heroClassHistoryL struct{}

var (
	heroClassHistoryAllColumns            = []string{"id", "hero_id", "class_id", "is_current", "acquired_at", "acquisition_type", "created_at", "updated_at"}
	heroClassHistoryColumnsWithoutDefault = []string{"hero_id", "class_id"}
	heroClassHistoryColumnsWithDefault    = []string{"id", "is_current", "acquired_at", "acquisition_type", "created_at", "updated_at"}
	heroClassHistoryPrimaryKeyColumns     = []string{"id"}
	heroClassHistoryGeneratedColumns      = []string{}
)

type (
	// HeroClassHistorySlice is an alias for a slice of pointers to HeroClassHistory.
	// This should almost always be used instead of []HeroClassHistory.
	HeroClassHistorySlice []*HeroClassHistory
	// HeroClassHistoryHook is the signature for custom HeroClassHistory hook methods
	HeroClassHistoryHook func(context.Context, boil.ContextExecutor, *HeroClassHistory) error

	heroClassHistoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	heroClassHistoryType                 = reflect.TypeOf(&HeroClassHistory{})
	heroClassHistoryMapping              = queries.MakeStructMapping(heroClassHistoryType)
	heroClassHistoryPrimaryKeyMapping, _ = queries.BindMapping(heroClassHistoryType, heroClassHistoryMapping, heroClassHistoryPrimaryKeyColumns)
	heroClassHistoryInsertCacheMut       sync.RWMutex
	heroClassHistoryInsertCache          = make(map[string]insertCache)
	heroClassHistoryUpdateCacheMut       sync.RWMutex
	heroClassHistoryUpdateCache          = make(map[string]updateCache)
	heroClassHistoryUpsertCacheMut       sync.RWMutex
	heroClassHistoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var heroClassHistoryAfterSelectMu sync.Mutex
var heroClassHistoryAfterSelectHooks []HeroClassHistoryHook

var heroClassHistoryBeforeInsertMu sync.Mutex
var heroClassHistoryBeforeInsertHooks []HeroClassHistoryHook
var heroClassHistoryAfterInsertMu sync.Mutex
var heroClassHistoryAfterInsertHooks []HeroClassHistoryHook

var heroClassHistoryBeforeUpdateMu sync.Mutex
var heroClassHistoryBeforeUpdateHooks []HeroClassHistoryHook
var heroClassHistoryAfterUpdateMu sync.Mutex
var heroClassHistoryAfterUpdateHooks []HeroClassHistoryHook

var heroClassHistoryBeforeDeleteMu sync.Mutex
var heroClassHistoryBeforeDeleteHooks []HeroClassHistoryHook
var heroClassHistoryAfterDeleteMu sync.Mutex
var heroClassHistoryAfterDeleteHooks []HeroClassHistoryHook

var heroClassHistoryBeforeUpsertMu sync.Mutex
var heroClassHistoryBeforeUpsertHooks []HeroClassHistoryHook
var heroClassHistoryAfterUpsertMu sync.Mutex
var heroClassHistoryAfterUpsertHooks []HeroClassHistoryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *HeroClassHistory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *HeroClassHistory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *HeroClassHistory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *HeroClassHistory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *HeroClassHistory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *HeroClassHistory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *HeroClassHistory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *HeroClassHistory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *HeroClassHistory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range heroClassHistoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddHeroClassHistoryHook registers your hook function for all future operations.
func AddHeroClassHistoryHook(hookPoint boil.HookPoint, heroClassHistoryHook HeroClassHistoryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		heroClassHistoryAfterSelectMu.Lock()
		heroClassHistoryAfterSelectHooks = append(heroClassHistoryAfterSelectHooks, heroClassHistoryHook)
		heroClassHistoryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		heroClassHistoryBeforeInsertMu.Lock()
		heroClassHistoryBeforeInsertHooks = append(heroClassHistoryBeforeInsertHooks, heroClassHistoryHook)
		heroClassHistoryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		heroClassHistoryAfterInsertMu.Lock()
		heroClassHistoryAfterInsertHooks = append(heroClassHistoryAfterInsertHooks, heroClassHistoryHook)
		heroClassHistoryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		heroClassHistoryBeforeUpdateMu.Lock()
		heroClassHistoryBeforeUpdateHooks = append(heroClassHistoryBeforeUpdateHooks, heroClassHistoryHook)
		heroClassHistoryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		heroClassHistoryAfterUpdateMu.Lock()
		heroClassHistoryAfterUpdateHooks = append(heroClassHistoryAfterUpdateHooks, heroClassHistoryHook)
		heroClassHistoryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		heroClassHistoryBeforeDeleteMu.Lock()
		heroClassHistoryBeforeDeleteHooks = append(heroClassHistoryBeforeDeleteHooks, heroClassHistoryHook)
		heroClassHistoryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		heroClassHistoryAfterDeleteMu.Lock()
		heroClassHistoryAfterDeleteHooks = append(heroClassHistoryAfterDeleteHooks, heroClassHistoryHook)
		heroClassHistoryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		heroClassHistoryBeforeUpsertMu.Lock()
		heroClassHistoryBeforeUpsertHooks = append(heroClassHistoryBeforeUpsertHooks, heroClassHistoryHook)
		heroClassHistoryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		heroClassHistoryAfterUpsertMu.Lock()
		heroClassHistoryAfterUpsertHooks = append(heroClassHistoryAfterUpsertHooks, heroClassHistoryHook)
		heroClassHistoryAfterUpsertMu.Unlock()
	}
}

// OneG returns a single heroClassHistory record from the query using the global executor.
func (q heroClassHistoryQuery) OneG(ctx context.Context) (*HeroClassHistory, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single heroClassHistory record from the query using the global executor, and panics on error.
func (q heroClassHistoryQuery) OneGP(ctx context.Context) *HeroClassHistory {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single heroClassHistory record from the query, and panics on error.
func (q heroClassHistoryQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *HeroClassHistory {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single heroClassHistory record from the query.
func (q heroClassHistoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*HeroClassHistory, error) {
	o := &HeroClassHistory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for hero_class_history")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all HeroClassHistory records from the query using the global executor.
func (q heroClassHistoryQuery) AllG(ctx context.Context) (HeroClassHistorySlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all HeroClassHistory records from the query using the global executor, and panics on error.
func (q heroClassHistoryQuery) AllGP(ctx context.Context) HeroClassHistorySlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all HeroClassHistory records from the query, and panics on error.
func (q heroClassHistoryQuery) AllP(ctx context.Context, exec boil.ContextExecutor) HeroClassHistorySlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all HeroClassHistory records from the query.
func (q heroClassHistoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (HeroClassHistorySlice, error) {
	var o []*HeroClassHistory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to HeroClassHistory slice")
	}

	if len(heroClassHistoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all HeroClassHistory records in the query using the global executor
func (q heroClassHistoryQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all HeroClassHistory records in the query using the global executor, and panics on error.
func (q heroClassHistoryQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all HeroClassHistory records in the query, and panics on error.
func (q heroClassHistoryQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all HeroClassHistory records in the query.
func (q heroClassHistoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count hero_class_history rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q heroClassHistoryQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q heroClassHistoryQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q heroClassHistoryQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q heroClassHistoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if hero_class_history exists")
	}

	return count > 0, nil
}

// Hero pointed to by the foreign key.
func (o *HeroClassHistory) Hero(mods ...qm.QueryMod) heroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.HeroID),
	}

	queryMods = append(queryMods, mods...)

	return Heroes(queryMods...)
}

// LoadHero allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (heroClassHistoryL) LoadHero(ctx context.Context, e boil.ContextExecutor, singular bool, maybeHeroClassHistory interface{}, mods queries.Applicator) error {
	var slice []*HeroClassHistory
	var object *HeroClassHistory

	if singular {
		var ok bool
		object, ok = maybeHeroClassHistory.(*HeroClassHistory)
		if !ok {
			object = new(HeroClassHistory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeHeroClassHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeHeroClassHistory))
			}
		}
	} else {
		s, ok := maybeHeroClassHistory.(*[]*HeroClassHistory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeHeroClassHistory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeHeroClassHistory))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &heroClassHistoryR{}
		}
		args[object.HeroID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &heroClassHistoryR{}
			}

			args[obj.HeroID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.heroes`),
		qm.WhereIn(`game_runtime.heroes.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.heroes.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Hero")
	}

	var resultSlice []*Hero
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Hero")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for heroes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for heroes")
	}

	if len(heroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Hero = foreign
		if foreign.R == nil {
			foreign.R = &heroR{}
		}
		foreign.R.HeroClassHistories = append(foreign.R.HeroClassHistories, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.HeroID == foreign.ID {
				local.R.Hero = foreign
				if foreign.R == nil {
					foreign.R = &heroR{}
				}
				foreign.R.HeroClassHistories = append(foreign.R.HeroClassHistories, local)
				break
			}
		}
	}

	return nil
}

// SetHeroG of the heroClassHistory to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.HeroClassHistories.
// Uses the global database handle.
func (o *HeroClassHistory) SetHeroG(ctx context.Context, insert bool, related *Hero) error {
	return o.SetHero(ctx, boil.GetContextDB(), insert, related)
}

// SetHeroP of the heroClassHistory to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.HeroClassHistories.
// Panics on error.
func (o *HeroClassHistory) SetHeroP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) {
	if err := o.SetHero(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetHeroGP of the heroClassHistory to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.HeroClassHistories.
// Uses the global database handle and panics on error.
func (o *HeroClassHistory) SetHeroGP(ctx context.Context, insert bool, related *Hero) {
	if err := o.SetHero(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetHero of the heroClassHistory to the related item.
// Sets o.R.Hero to related.
// Adds o to related.R.HeroClassHistories.
func (o *HeroClassHistory) SetHero(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"hero_class_history\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"hero_id"}),
		strmangle.WhereClause("\"", "\"", 2, heroClassHistoryPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.HeroID = related.ID
	if o.R == nil {
		o.R = &heroClassHistoryR{
			Hero: related,
		}
	} else {
		o.R.Hero = related
	}

	if related.R == nil {
		related.R = &heroR{
			HeroClassHistories: HeroClassHistorySlice{o},
		}
	} else {
		related.R.HeroClassHistories = append(related.R.HeroClassHistories, o)
	}

	return nil
}

// HeroClassHistories retrieves all the records using an executor.
func HeroClassHistories(mods ...qm.QueryMod) heroClassHistoryQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"hero_class_history\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"hero_class_history\".*"})
	}

	return heroClassHistoryQuery{q}
}

// FindHeroClassHistoryG retrieves a single record by ID.
func FindHeroClassHistoryG(ctx context.Context, iD string, selectCols ...string) (*HeroClassHistory, error) {
	return FindHeroClassHistory(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindHeroClassHistoryP retrieves a single record by ID with an executor, and panics on error.
func FindHeroClassHistoryP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *HeroClassHistory {
	retobj, err := FindHeroClassHistory(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindHeroClassHistoryGP retrieves a single record by ID, and panics on error.
func FindHeroClassHistoryGP(ctx context.Context, iD string, selectCols ...string) *HeroClassHistory {
	retobj, err := FindHeroClassHistory(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindHeroClassHistory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHeroClassHistory(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*HeroClassHistory, error) {
	heroClassHistoryObj := &HeroClassHistory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"hero_class_history\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, heroClassHistoryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from hero_class_history")
	}

	if err = heroClassHistoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return heroClassHistoryObj, err
	}

	return heroClassHistoryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *HeroClassHistory) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *HeroClassHistory) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *HeroClassHistory) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *HeroClassHistory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no hero_class_history provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(heroClassHistoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	heroClassHistoryInsertCacheMut.RLock()
	cache, cached := heroClassHistoryInsertCache[key]
	heroClassHistoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			heroClassHistoryAllColumns,
			heroClassHistoryColumnsWithDefault,
			heroClassHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(heroClassHistoryType, heroClassHistoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(heroClassHistoryType, heroClassHistoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"hero_class_history\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"hero_class_history\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into hero_class_history")
	}

	if !cached {
		heroClassHistoryInsertCacheMut.Lock()
		heroClassHistoryInsertCache[key] = cache
		heroClassHistoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single HeroClassHistory record using the global executor.
// See Update for more documentation.
func (o *HeroClassHistory) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the HeroClassHistory, and panics on error.
// See Update for more documentation.
func (o *HeroClassHistory) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single HeroClassHistory record using the global executor. Panics on error.
// See Update for more documentation.
func (o *HeroClassHistory) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the HeroClassHistory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *HeroClassHistory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	heroClassHistoryUpdateCacheMut.RLock()
	cache, cached := heroClassHistoryUpdateCache[key]
	heroClassHistoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			heroClassHistoryAllColumns,
			heroClassHistoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update hero_class_history, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"hero_class_history\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, heroClassHistoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(heroClassHistoryType, heroClassHistoryMapping, append(wl, heroClassHistoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update hero_class_history row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for hero_class_history")
	}

	if !cached {
		heroClassHistoryUpdateCacheMut.Lock()
		heroClassHistoryUpdateCache[key] = cache
		heroClassHistoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q heroClassHistoryQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q heroClassHistoryQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q heroClassHistoryQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q heroClassHistoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for hero_class_history")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for hero_class_history")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o HeroClassHistorySlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o HeroClassHistorySlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o HeroClassHistorySlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HeroClassHistorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), heroClassHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"hero_class_history\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, heroClassHistoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in heroClassHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all heroClassHistory")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *HeroClassHistory) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *HeroClassHistory) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *HeroClassHistory) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *HeroClassHistory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no hero_class_history provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(heroClassHistoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	heroClassHistoryUpsertCacheMut.RLock()
	cache, cached := heroClassHistoryUpsertCache[key]
	heroClassHistoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			heroClassHistoryAllColumns,
			heroClassHistoryColumnsWithDefault,
			heroClassHistoryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			heroClassHistoryAllColumns,
			heroClassHistoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert hero_class_history, could not build update column list")
		}

		ret := strmangle.SetComplement(heroClassHistoryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(heroClassHistoryPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert hero_class_history, could not build conflict column list")
			}

			conflict = make([]string, len(heroClassHistoryPrimaryKeyColumns))
			copy(conflict, heroClassHistoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"hero_class_history\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(heroClassHistoryType, heroClassHistoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(heroClassHistoryType, heroClassHistoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert hero_class_history")
	}

	if !cached {
		heroClassHistoryUpsertCacheMut.Lock()
		heroClassHistoryUpsertCache[key] = cache
		heroClassHistoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single HeroClassHistory record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *HeroClassHistory) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single HeroClassHistory record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *HeroClassHistory) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single HeroClassHistory record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *HeroClassHistory) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single HeroClassHistory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *HeroClassHistory) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no HeroClassHistory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), heroClassHistoryPrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"hero_class_history\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from hero_class_history")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for hero_class_history")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q heroClassHistoryQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q heroClassHistoryQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q heroClassHistoryQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q heroClassHistoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no heroClassHistoryQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from hero_class_history")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for hero_class_history")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o HeroClassHistorySlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o HeroClassHistorySlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o HeroClassHistorySlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HeroClassHistorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(heroClassHistoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), heroClassHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"hero_class_history\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, heroClassHistoryPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from heroClassHistory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for hero_class_history")
	}

	if len(heroClassHistoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *HeroClassHistory) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no HeroClassHistory provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *HeroClassHistory) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *HeroClassHistory) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *HeroClassHistory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindHeroClassHistory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HeroClassHistorySlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty HeroClassHistorySlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *HeroClassHistorySlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *HeroClassHistorySlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HeroClassHistorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HeroClassHistorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), heroClassHistoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"hero_class_history\".* FROM \"game_runtime\".\"hero_class_history\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, heroClassHistoryPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in HeroClassHistorySlice")
	}

	*o = slice

	return nil
}

// HeroClassHistoryExistsG checks if the HeroClassHistory row exists.
func HeroClassHistoryExistsG(ctx context.Context, iD string) (bool, error) {
	return HeroClassHistoryExists(ctx, boil.GetContextDB(), iD)
}

// HeroClassHistoryExistsP checks if the HeroClassHistory row exists. Panics on error.
func HeroClassHistoryExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := HeroClassHistoryExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// HeroClassHistoryExistsGP checks if the HeroClassHistory row exists. Panics on error.
func HeroClassHistoryExistsGP(ctx context.Context, iD string) bool {
	e, err := HeroClassHistoryExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// HeroClassHistoryExists checks if the HeroClassHistory row exists.
func HeroClassHistoryExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"hero_class_history\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if hero_class_history exists")
	}

	return exists, nil
}

// Exists checks if the HeroClassHistory row exists.
func (o *HeroClassHistory) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return HeroClassHistoryExists(ctx, exec, o.ID)
}
