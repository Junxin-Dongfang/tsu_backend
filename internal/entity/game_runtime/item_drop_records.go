// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// ItemDropRecord is an object representing the database table.
type ItemDropRecord struct {
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 物品实例ID - 关联到 game_runtime.player_items 表
	ItemInstanceID string `boil:"item_instance_id" json:"item_instance_id" toml:"item_instance_id" yaml:"item_instance_id"`
	// 物品配置ID - 关联到 game_config.items 表
	ItemConfigID string      `boil:"item_config_id" json:"item_config_id" toml:"item_config_id" yaml:"item_config_id"`
	DropSource   string      `boil:"drop_source" json:"drop_source" toml:"drop_source" yaml:"drop_source"`
	SourceID     null.String `boil:"source_id" json:"source_id,omitempty" toml:"source_id" yaml:"source_id,omitempty"`
	ReceiverID   string      `boil:"receiver_id" json:"receiver_id" toml:"receiver_id" yaml:"receiver_id"`
	TeamID       null.String `boil:"team_id" json:"team_id,omitempty" toml:"team_id" yaml:"team_id,omitempty"`
	PlayerLevel  null.Int16  `boil:"player_level" json:"player_level,omitempty" toml:"player_level" yaml:"player_level,omitempty"`
	ItemQuality  null.String `boil:"item_quality" json:"item_quality,omitempty" toml:"item_quality" yaml:"item_quality,omitempty"`
	DroppedAt    time.Time   `boil:"dropped_at" json:"dropped_at" toml:"dropped_at" yaml:"dropped_at"`

	R *itemDropRecordR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L itemDropRecordL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ItemDropRecordColumns = struct {
	ID             string
	ItemInstanceID string
	ItemConfigID   string
	DropSource     string
	SourceID       string
	ReceiverID     string
	TeamID         string
	PlayerLevel    string
	ItemQuality    string
	DroppedAt      string
}{
	ID:             "id",
	ItemInstanceID: "item_instance_id",
	ItemConfigID:   "item_config_id",
	DropSource:     "drop_source",
	SourceID:       "source_id",
	ReceiverID:     "receiver_id",
	TeamID:         "team_id",
	PlayerLevel:    "player_level",
	ItemQuality:    "item_quality",
	DroppedAt:      "dropped_at",
}

var ItemDropRecordTableColumns = struct {
	ID             string
	ItemInstanceID string
	ItemConfigID   string
	DropSource     string
	SourceID       string
	ReceiverID     string
	TeamID         string
	PlayerLevel    string
	ItemQuality    string
	DroppedAt      string
}{
	ID:             "item_drop_records.id",
	ItemInstanceID: "item_drop_records.item_instance_id",
	ItemConfigID:   "item_drop_records.item_config_id",
	DropSource:     "item_drop_records.drop_source",
	SourceID:       "item_drop_records.source_id",
	ReceiverID:     "item_drop_records.receiver_id",
	TeamID:         "item_drop_records.team_id",
	PlayerLevel:    "item_drop_records.player_level",
	ItemQuality:    "item_drop_records.item_quality",
	DroppedAt:      "item_drop_records.dropped_at",
}

// Generated where

var ItemDropRecordWhere = struct {
	ID             whereHelperstring
	ItemInstanceID whereHelperstring
	ItemConfigID   whereHelperstring
	DropSource     whereHelperstring
	SourceID       whereHelpernull_String
	ReceiverID     whereHelperstring
	TeamID         whereHelpernull_String
	PlayerLevel    whereHelpernull_Int16
	ItemQuality    whereHelpernull_String
	DroppedAt      whereHelpertime_Time
}{
	ID:             whereHelperstring{field: "\"game_runtime\".\"item_drop_records\".\"id\""},
	ItemInstanceID: whereHelperstring{field: "\"game_runtime\".\"item_drop_records\".\"item_instance_id\""},
	ItemConfigID:   whereHelperstring{field: "\"game_runtime\".\"item_drop_records\".\"item_config_id\""},
	DropSource:     whereHelperstring{field: "\"game_runtime\".\"item_drop_records\".\"drop_source\""},
	SourceID:       whereHelpernull_String{field: "\"game_runtime\".\"item_drop_records\".\"source_id\""},
	ReceiverID:     whereHelperstring{field: "\"game_runtime\".\"item_drop_records\".\"receiver_id\""},
	TeamID:         whereHelpernull_String{field: "\"game_runtime\".\"item_drop_records\".\"team_id\""},
	PlayerLevel:    whereHelpernull_Int16{field: "\"game_runtime\".\"item_drop_records\".\"player_level\""},
	ItemQuality:    whereHelpernull_String{field: "\"game_runtime\".\"item_drop_records\".\"item_quality\""},
	DroppedAt:      whereHelpertime_Time{field: "\"game_runtime\".\"item_drop_records\".\"dropped_at\""},
}

// ItemDropRecordRels is where relationship names are stored.
var ItemDropRecordRels = struct {
	ItemInstance string
}{
	ItemInstance: "ItemInstance",
}

// itemDropRecordR is where relationships are stored.
type itemDropRecordR struct {
	ItemInstance *PlayerItem `boil:"ItemInstance" json:"ItemInstance" toml:"ItemInstance" yaml:"ItemInstance"`
}

// NewStruct creates a new relationship struct
func (*itemDropRecordR) NewStruct() *itemDropRecordR {
	return &itemDropRecordR{}
}

func (o *ItemDropRecord) GetItemInstance() *PlayerItem {
	if o == nil {
		return nil
	}

	return o.R.GetItemInstance()
}

func (r *itemDropRecordR) GetItemInstance() *PlayerItem {
	if r == nil {
		return nil
	}

	return r.ItemInstance
}

// itemDropRecordL is where Load methods for each relationship are stored.
type itemDropRecordL struct{}

var (
	itemDropRecordAllColumns            = []string{"id", "item_instance_id", "item_config_id", "drop_source", "source_id", "receiver_id", "team_id", "player_level", "item_quality", "dropped_at"}
	itemDropRecordColumnsWithoutDefault = []string{"item_instance_id", "item_config_id", "drop_source", "receiver_id"}
	itemDropRecordColumnsWithDefault    = []string{"id", "source_id", "team_id", "player_level", "item_quality", "dropped_at"}
	itemDropRecordPrimaryKeyColumns     = []string{"id"}
	itemDropRecordGeneratedColumns      = []string{}
)

type (
	// ItemDropRecordSlice is an alias for a slice of pointers to ItemDropRecord.
	// This should almost always be used instead of []ItemDropRecord.
	ItemDropRecordSlice []*ItemDropRecord
	// ItemDropRecordHook is the signature for custom ItemDropRecord hook methods
	ItemDropRecordHook func(context.Context, boil.ContextExecutor, *ItemDropRecord) error

	itemDropRecordQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	itemDropRecordType                 = reflect.TypeOf(&ItemDropRecord{})
	itemDropRecordMapping              = queries.MakeStructMapping(itemDropRecordType)
	itemDropRecordPrimaryKeyMapping, _ = queries.BindMapping(itemDropRecordType, itemDropRecordMapping, itemDropRecordPrimaryKeyColumns)
	itemDropRecordInsertCacheMut       sync.RWMutex
	itemDropRecordInsertCache          = make(map[string]insertCache)
	itemDropRecordUpdateCacheMut       sync.RWMutex
	itemDropRecordUpdateCache          = make(map[string]updateCache)
	itemDropRecordUpsertCacheMut       sync.RWMutex
	itemDropRecordUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var itemDropRecordAfterSelectMu sync.Mutex
var itemDropRecordAfterSelectHooks []ItemDropRecordHook

var itemDropRecordBeforeInsertMu sync.Mutex
var itemDropRecordBeforeInsertHooks []ItemDropRecordHook
var itemDropRecordAfterInsertMu sync.Mutex
var itemDropRecordAfterInsertHooks []ItemDropRecordHook

var itemDropRecordBeforeUpdateMu sync.Mutex
var itemDropRecordBeforeUpdateHooks []ItemDropRecordHook
var itemDropRecordAfterUpdateMu sync.Mutex
var itemDropRecordAfterUpdateHooks []ItemDropRecordHook

var itemDropRecordBeforeDeleteMu sync.Mutex
var itemDropRecordBeforeDeleteHooks []ItemDropRecordHook
var itemDropRecordAfterDeleteMu sync.Mutex
var itemDropRecordAfterDeleteHooks []ItemDropRecordHook

var itemDropRecordBeforeUpsertMu sync.Mutex
var itemDropRecordBeforeUpsertHooks []ItemDropRecordHook
var itemDropRecordAfterUpsertMu sync.Mutex
var itemDropRecordAfterUpsertHooks []ItemDropRecordHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *ItemDropRecord) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *ItemDropRecord) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *ItemDropRecord) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *ItemDropRecord) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *ItemDropRecord) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *ItemDropRecord) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *ItemDropRecord) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *ItemDropRecord) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *ItemDropRecord) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range itemDropRecordAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddItemDropRecordHook registers your hook function for all future operations.
func AddItemDropRecordHook(hookPoint boil.HookPoint, itemDropRecordHook ItemDropRecordHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		itemDropRecordAfterSelectMu.Lock()
		itemDropRecordAfterSelectHooks = append(itemDropRecordAfterSelectHooks, itemDropRecordHook)
		itemDropRecordAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		itemDropRecordBeforeInsertMu.Lock()
		itemDropRecordBeforeInsertHooks = append(itemDropRecordBeforeInsertHooks, itemDropRecordHook)
		itemDropRecordBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		itemDropRecordAfterInsertMu.Lock()
		itemDropRecordAfterInsertHooks = append(itemDropRecordAfterInsertHooks, itemDropRecordHook)
		itemDropRecordAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		itemDropRecordBeforeUpdateMu.Lock()
		itemDropRecordBeforeUpdateHooks = append(itemDropRecordBeforeUpdateHooks, itemDropRecordHook)
		itemDropRecordBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		itemDropRecordAfterUpdateMu.Lock()
		itemDropRecordAfterUpdateHooks = append(itemDropRecordAfterUpdateHooks, itemDropRecordHook)
		itemDropRecordAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		itemDropRecordBeforeDeleteMu.Lock()
		itemDropRecordBeforeDeleteHooks = append(itemDropRecordBeforeDeleteHooks, itemDropRecordHook)
		itemDropRecordBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		itemDropRecordAfterDeleteMu.Lock()
		itemDropRecordAfterDeleteHooks = append(itemDropRecordAfterDeleteHooks, itemDropRecordHook)
		itemDropRecordAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		itemDropRecordBeforeUpsertMu.Lock()
		itemDropRecordBeforeUpsertHooks = append(itemDropRecordBeforeUpsertHooks, itemDropRecordHook)
		itemDropRecordBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		itemDropRecordAfterUpsertMu.Lock()
		itemDropRecordAfterUpsertHooks = append(itemDropRecordAfterUpsertHooks, itemDropRecordHook)
		itemDropRecordAfterUpsertMu.Unlock()
	}
}

// OneG returns a single itemDropRecord record from the query using the global executor.
func (q itemDropRecordQuery) OneG(ctx context.Context) (*ItemDropRecord, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single itemDropRecord record from the query using the global executor, and panics on error.
func (q itemDropRecordQuery) OneGP(ctx context.Context) *ItemDropRecord {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single itemDropRecord record from the query, and panics on error.
func (q itemDropRecordQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *ItemDropRecord {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single itemDropRecord record from the query.
func (q itemDropRecordQuery) One(ctx context.Context, exec boil.ContextExecutor) (*ItemDropRecord, error) {
	o := &ItemDropRecord{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for item_drop_records")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all ItemDropRecord records from the query using the global executor.
func (q itemDropRecordQuery) AllG(ctx context.Context) (ItemDropRecordSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all ItemDropRecord records from the query using the global executor, and panics on error.
func (q itemDropRecordQuery) AllGP(ctx context.Context) ItemDropRecordSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all ItemDropRecord records from the query, and panics on error.
func (q itemDropRecordQuery) AllP(ctx context.Context, exec boil.ContextExecutor) ItemDropRecordSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all ItemDropRecord records from the query.
func (q itemDropRecordQuery) All(ctx context.Context, exec boil.ContextExecutor) (ItemDropRecordSlice, error) {
	var o []*ItemDropRecord

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to ItemDropRecord slice")
	}

	if len(itemDropRecordAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all ItemDropRecord records in the query using the global executor
func (q itemDropRecordQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all ItemDropRecord records in the query using the global executor, and panics on error.
func (q itemDropRecordQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all ItemDropRecord records in the query, and panics on error.
func (q itemDropRecordQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all ItemDropRecord records in the query.
func (q itemDropRecordQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count item_drop_records rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q itemDropRecordQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q itemDropRecordQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q itemDropRecordQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q itemDropRecordQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if item_drop_records exists")
	}

	return count > 0, nil
}

// ItemInstance pointed to by the foreign key.
func (o *ItemDropRecord) ItemInstance(mods ...qm.QueryMod) playerItemQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ItemInstanceID),
	}

	queryMods = append(queryMods, mods...)

	return PlayerItems(queryMods...)
}

// LoadItemInstance allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (itemDropRecordL) LoadItemInstance(ctx context.Context, e boil.ContextExecutor, singular bool, maybeItemDropRecord interface{}, mods queries.Applicator) error {
	var slice []*ItemDropRecord
	var object *ItemDropRecord

	if singular {
		var ok bool
		object, ok = maybeItemDropRecord.(*ItemDropRecord)
		if !ok {
			object = new(ItemDropRecord)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeItemDropRecord)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeItemDropRecord))
			}
		}
	} else {
		s, ok := maybeItemDropRecord.(*[]*ItemDropRecord)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeItemDropRecord)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeItemDropRecord))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &itemDropRecordR{}
		}
		args[object.ItemInstanceID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &itemDropRecordR{}
			}

			args[obj.ItemInstanceID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.player_items`),
		qm.WhereIn(`game_runtime.player_items.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.player_items.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load PlayerItem")
	}

	var resultSlice []*PlayerItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice PlayerItem")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for player_items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for player_items")
	}

	if len(playerItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ItemInstance = foreign
		if foreign.R == nil {
			foreign.R = &playerItemR{}
		}
		foreign.R.ItemInstanceItemDropRecords = append(foreign.R.ItemInstanceItemDropRecords, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ItemInstanceID == foreign.ID {
				local.R.ItemInstance = foreign
				if foreign.R == nil {
					foreign.R = &playerItemR{}
				}
				foreign.R.ItemInstanceItemDropRecords = append(foreign.R.ItemInstanceItemDropRecords, local)
				break
			}
		}
	}

	return nil
}

// SetItemInstanceG of the itemDropRecord to the related item.
// Sets o.R.ItemInstance to related.
// Adds o to related.R.ItemInstanceItemDropRecords.
// Uses the global database handle.
func (o *ItemDropRecord) SetItemInstanceG(ctx context.Context, insert bool, related *PlayerItem) error {
	return o.SetItemInstance(ctx, boil.GetContextDB(), insert, related)
}

// SetItemInstanceP of the itemDropRecord to the related item.
// Sets o.R.ItemInstance to related.
// Adds o to related.R.ItemInstanceItemDropRecords.
// Panics on error.
func (o *ItemDropRecord) SetItemInstanceP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PlayerItem) {
	if err := o.SetItemInstance(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetItemInstanceGP of the itemDropRecord to the related item.
// Sets o.R.ItemInstance to related.
// Adds o to related.R.ItemInstanceItemDropRecords.
// Uses the global database handle and panics on error.
func (o *ItemDropRecord) SetItemInstanceGP(ctx context.Context, insert bool, related *PlayerItem) {
	if err := o.SetItemInstance(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetItemInstance of the itemDropRecord to the related item.
// Sets o.R.ItemInstance to related.
// Adds o to related.R.ItemInstanceItemDropRecords.
func (o *ItemDropRecord) SetItemInstance(ctx context.Context, exec boil.ContextExecutor, insert bool, related *PlayerItem) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"item_drop_records\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"item_instance_id"}),
		strmangle.WhereClause("\"", "\"", 2, itemDropRecordPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ItemInstanceID = related.ID
	if o.R == nil {
		o.R = &itemDropRecordR{
			ItemInstance: related,
		}
	} else {
		o.R.ItemInstance = related
	}

	if related.R == nil {
		related.R = &playerItemR{
			ItemInstanceItemDropRecords: ItemDropRecordSlice{o},
		}
	} else {
		related.R.ItemInstanceItemDropRecords = append(related.R.ItemInstanceItemDropRecords, o)
	}

	return nil
}

// ItemDropRecords retrieves all the records using an executor.
func ItemDropRecords(mods ...qm.QueryMod) itemDropRecordQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"item_drop_records\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"item_drop_records\".*"})
	}

	return itemDropRecordQuery{q}
}

// FindItemDropRecordG retrieves a single record by ID.
func FindItemDropRecordG(ctx context.Context, iD string, selectCols ...string) (*ItemDropRecord, error) {
	return FindItemDropRecord(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindItemDropRecordP retrieves a single record by ID with an executor, and panics on error.
func FindItemDropRecordP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *ItemDropRecord {
	retobj, err := FindItemDropRecord(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindItemDropRecordGP retrieves a single record by ID, and panics on error.
func FindItemDropRecordGP(ctx context.Context, iD string, selectCols ...string) *ItemDropRecord {
	retobj, err := FindItemDropRecord(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindItemDropRecord retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindItemDropRecord(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*ItemDropRecord, error) {
	itemDropRecordObj := &ItemDropRecord{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"item_drop_records\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, itemDropRecordObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from item_drop_records")
	}

	if err = itemDropRecordObj.doAfterSelectHooks(ctx, exec); err != nil {
		return itemDropRecordObj, err
	}

	return itemDropRecordObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *ItemDropRecord) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *ItemDropRecord) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *ItemDropRecord) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *ItemDropRecord) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no item_drop_records provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(itemDropRecordColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	itemDropRecordInsertCacheMut.RLock()
	cache, cached := itemDropRecordInsertCache[key]
	itemDropRecordInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			itemDropRecordAllColumns,
			itemDropRecordColumnsWithDefault,
			itemDropRecordColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(itemDropRecordType, itemDropRecordMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(itemDropRecordType, itemDropRecordMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"item_drop_records\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"item_drop_records\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into item_drop_records")
	}

	if !cached {
		itemDropRecordInsertCacheMut.Lock()
		itemDropRecordInsertCache[key] = cache
		itemDropRecordInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single ItemDropRecord record using the global executor.
// See Update for more documentation.
func (o *ItemDropRecord) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the ItemDropRecord, and panics on error.
// See Update for more documentation.
func (o *ItemDropRecord) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single ItemDropRecord record using the global executor. Panics on error.
// See Update for more documentation.
func (o *ItemDropRecord) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the ItemDropRecord.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *ItemDropRecord) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	itemDropRecordUpdateCacheMut.RLock()
	cache, cached := itemDropRecordUpdateCache[key]
	itemDropRecordUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			itemDropRecordAllColumns,
			itemDropRecordPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update item_drop_records, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"item_drop_records\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, itemDropRecordPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(itemDropRecordType, itemDropRecordMapping, append(wl, itemDropRecordPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update item_drop_records row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for item_drop_records")
	}

	if !cached {
		itemDropRecordUpdateCacheMut.Lock()
		itemDropRecordUpdateCache[key] = cache
		itemDropRecordUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q itemDropRecordQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q itemDropRecordQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q itemDropRecordQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q itemDropRecordQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for item_drop_records")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for item_drop_records")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o ItemDropRecordSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o ItemDropRecordSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o ItemDropRecordSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ItemDropRecordSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), itemDropRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"item_drop_records\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, itemDropRecordPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in itemDropRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all itemDropRecord")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *ItemDropRecord) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *ItemDropRecord) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *ItemDropRecord) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *ItemDropRecord) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no item_drop_records provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(itemDropRecordColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	itemDropRecordUpsertCacheMut.RLock()
	cache, cached := itemDropRecordUpsertCache[key]
	itemDropRecordUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			itemDropRecordAllColumns,
			itemDropRecordColumnsWithDefault,
			itemDropRecordColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			itemDropRecordAllColumns,
			itemDropRecordPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert item_drop_records, could not build update column list")
		}

		ret := strmangle.SetComplement(itemDropRecordAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(itemDropRecordPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert item_drop_records, could not build conflict column list")
			}

			conflict = make([]string, len(itemDropRecordPrimaryKeyColumns))
			copy(conflict, itemDropRecordPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"item_drop_records\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(itemDropRecordType, itemDropRecordMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(itemDropRecordType, itemDropRecordMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert item_drop_records")
	}

	if !cached {
		itemDropRecordUpsertCacheMut.Lock()
		itemDropRecordUpsertCache[key] = cache
		itemDropRecordUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single ItemDropRecord record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *ItemDropRecord) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single ItemDropRecord record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ItemDropRecord) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single ItemDropRecord record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *ItemDropRecord) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single ItemDropRecord record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *ItemDropRecord) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no ItemDropRecord provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), itemDropRecordPrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"item_drop_records\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from item_drop_records")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for item_drop_records")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q itemDropRecordQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q itemDropRecordQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q itemDropRecordQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q itemDropRecordQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no itemDropRecordQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from item_drop_records")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for item_drop_records")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o ItemDropRecordSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o ItemDropRecordSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o ItemDropRecordSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ItemDropRecordSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(itemDropRecordBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), itemDropRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"item_drop_records\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, itemDropRecordPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from itemDropRecord slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for item_drop_records")
	}

	if len(itemDropRecordAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *ItemDropRecord) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no ItemDropRecord provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *ItemDropRecord) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *ItemDropRecord) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *ItemDropRecord) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindItemDropRecord(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ItemDropRecordSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty ItemDropRecordSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ItemDropRecordSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *ItemDropRecordSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ItemDropRecordSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ItemDropRecordSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), itemDropRecordPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"item_drop_records\".* FROM \"game_runtime\".\"item_drop_records\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, itemDropRecordPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in ItemDropRecordSlice")
	}

	*o = slice

	return nil
}

// ItemDropRecordExistsG checks if the ItemDropRecord row exists.
func ItemDropRecordExistsG(ctx context.Context, iD string) (bool, error) {
	return ItemDropRecordExists(ctx, boil.GetContextDB(), iD)
}

// ItemDropRecordExistsP checks if the ItemDropRecord row exists. Panics on error.
func ItemDropRecordExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := ItemDropRecordExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ItemDropRecordExistsGP checks if the ItemDropRecord row exists. Panics on error.
func ItemDropRecordExistsGP(ctx context.Context, iD string) bool {
	e, err := ItemDropRecordExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ItemDropRecordExists checks if the ItemDropRecord row exists.
func ItemDropRecordExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"item_drop_records\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if item_drop_records exists")
	}

	return exists, nil
}

// Exists checks if the ItemDropRecord row exists.
func (o *ItemDropRecord) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return ItemDropRecordExists(ctx, exec, o.ID)
}
