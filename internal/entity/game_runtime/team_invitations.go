// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TeamInvitation is an object representing the database table.
type TeamInvitation struct {
	// 邀请ID
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 团队ID
	TeamID string `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	// 邀请人英雄ID
	InviterHeroID string `boil:"inviter_hero_id" json:"inviter_hero_id" toml:"inviter_hero_id" yaml:"inviter_hero_id"`
	// 被邀请人英雄ID
	InviteeHeroID string `boil:"invitee_hero_id" json:"invitee_hero_id" toml:"invitee_hero_id" yaml:"invitee_hero_id"`
	// 邀请留言
	Message null.String `boil:"message" json:"message,omitempty" toml:"message" yaml:"message,omitempty"`
	// 状态
	Status string `boil:"status" json:"status" toml:"status" yaml:"status"`
	// 审批人英雄ID
	ApprovedByHeroID null.String `boil:"approved_by_hero_id" json:"approved_by_hero_id,omitempty" toml:"approved_by_hero_id" yaml:"approved_by_hero_id,omitempty"`
	// 审批时间
	ApprovedAt null.Time `boil:"approved_at" json:"approved_at,omitempty" toml:"approved_at" yaml:"approved_at,omitempty"`
	// 被邀请人响应时间
	RespondedAt null.Time `boil:"responded_at" json:"responded_at,omitempty" toml:"responded_at" yaml:"responded_at,omitempty"`
	// 过期时间
	ExpiresAt time.Time `boil:"expires_at" json:"expires_at" toml:"expires_at" yaml:"expires_at"`
	// 创建时间
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *teamInvitationR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L teamInvitationL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TeamInvitationColumns = struct {
	ID               string
	TeamID           string
	InviterHeroID    string
	InviteeHeroID    string
	Message          string
	Status           string
	ApprovedByHeroID string
	ApprovedAt       string
	RespondedAt      string
	ExpiresAt        string
	CreatedAt        string
}{
	ID:               "id",
	TeamID:           "team_id",
	InviterHeroID:    "inviter_hero_id",
	InviteeHeroID:    "invitee_hero_id",
	Message:          "message",
	Status:           "status",
	ApprovedByHeroID: "approved_by_hero_id",
	ApprovedAt:       "approved_at",
	RespondedAt:      "responded_at",
	ExpiresAt:        "expires_at",
	CreatedAt:        "created_at",
}

var TeamInvitationTableColumns = struct {
	ID               string
	TeamID           string
	InviterHeroID    string
	InviteeHeroID    string
	Message          string
	Status           string
	ApprovedByHeroID string
	ApprovedAt       string
	RespondedAt      string
	ExpiresAt        string
	CreatedAt        string
}{
	ID:               "team_invitations.id",
	TeamID:           "team_invitations.team_id",
	InviterHeroID:    "team_invitations.inviter_hero_id",
	InviteeHeroID:    "team_invitations.invitee_hero_id",
	Message:          "team_invitations.message",
	Status:           "team_invitations.status",
	ApprovedByHeroID: "team_invitations.approved_by_hero_id",
	ApprovedAt:       "team_invitations.approved_at",
	RespondedAt:      "team_invitations.responded_at",
	ExpiresAt:        "team_invitations.expires_at",
	CreatedAt:        "team_invitations.created_at",
}

// Generated where

var TeamInvitationWhere = struct {
	ID               whereHelperstring
	TeamID           whereHelperstring
	InviterHeroID    whereHelperstring
	InviteeHeroID    whereHelperstring
	Message          whereHelpernull_String
	Status           whereHelperstring
	ApprovedByHeroID whereHelpernull_String
	ApprovedAt       whereHelpernull_Time
	RespondedAt      whereHelpernull_Time
	ExpiresAt        whereHelpertime_Time
	CreatedAt        whereHelpertime_Time
}{
	ID:               whereHelperstring{field: "\"game_runtime\".\"team_invitations\".\"id\""},
	TeamID:           whereHelperstring{field: "\"game_runtime\".\"team_invitations\".\"team_id\""},
	InviterHeroID:    whereHelperstring{field: "\"game_runtime\".\"team_invitations\".\"inviter_hero_id\""},
	InviteeHeroID:    whereHelperstring{field: "\"game_runtime\".\"team_invitations\".\"invitee_hero_id\""},
	Message:          whereHelpernull_String{field: "\"game_runtime\".\"team_invitations\".\"message\""},
	Status:           whereHelperstring{field: "\"game_runtime\".\"team_invitations\".\"status\""},
	ApprovedByHeroID: whereHelpernull_String{field: "\"game_runtime\".\"team_invitations\".\"approved_by_hero_id\""},
	ApprovedAt:       whereHelpernull_Time{field: "\"game_runtime\".\"team_invitations\".\"approved_at\""},
	RespondedAt:      whereHelpernull_Time{field: "\"game_runtime\".\"team_invitations\".\"responded_at\""},
	ExpiresAt:        whereHelpertime_Time{field: "\"game_runtime\".\"team_invitations\".\"expires_at\""},
	CreatedAt:        whereHelpertime_Time{field: "\"game_runtime\".\"team_invitations\".\"created_at\""},
}

// TeamInvitationRels is where relationship names are stored.
var TeamInvitationRels = struct {
	ApprovedByHero string
	InviteeHero    string
	InviterHero    string
	Team           string
}{
	ApprovedByHero: "ApprovedByHero",
	InviteeHero:    "InviteeHero",
	InviterHero:    "InviterHero",
	Team:           "Team",
}

// teamInvitationR is where relationships are stored.
type teamInvitationR struct {
	ApprovedByHero *Hero `boil:"ApprovedByHero" json:"ApprovedByHero" toml:"ApprovedByHero" yaml:"ApprovedByHero"`
	InviteeHero    *Hero `boil:"InviteeHero" json:"InviteeHero" toml:"InviteeHero" yaml:"InviteeHero"`
	InviterHero    *Hero `boil:"InviterHero" json:"InviterHero" toml:"InviterHero" yaml:"InviterHero"`
	Team           *Team `boil:"Team" json:"Team" toml:"Team" yaml:"Team"`
}

// NewStruct creates a new relationship struct
func (*teamInvitationR) NewStruct() *teamInvitationR {
	return &teamInvitationR{}
}

func (o *TeamInvitation) GetApprovedByHero() *Hero {
	if o == nil {
		return nil
	}

	return o.R.GetApprovedByHero()
}

func (r *teamInvitationR) GetApprovedByHero() *Hero {
	if r == nil {
		return nil
	}

	return r.ApprovedByHero
}

func (o *TeamInvitation) GetInviteeHero() *Hero {
	if o == nil {
		return nil
	}

	return o.R.GetInviteeHero()
}

func (r *teamInvitationR) GetInviteeHero() *Hero {
	if r == nil {
		return nil
	}

	return r.InviteeHero
}

func (o *TeamInvitation) GetInviterHero() *Hero {
	if o == nil {
		return nil
	}

	return o.R.GetInviterHero()
}

func (r *teamInvitationR) GetInviterHero() *Hero {
	if r == nil {
		return nil
	}

	return r.InviterHero
}

func (o *TeamInvitation) GetTeam() *Team {
	if o == nil {
		return nil
	}

	return o.R.GetTeam()
}

func (r *teamInvitationR) GetTeam() *Team {
	if r == nil {
		return nil
	}

	return r.Team
}

// teamInvitationL is where Load methods for each relationship are stored.
type teamInvitationL struct{}

var (
	teamInvitationAllColumns            = []string{"id", "team_id", "inviter_hero_id", "invitee_hero_id", "message", "status", "approved_by_hero_id", "approved_at", "responded_at", "expires_at", "created_at"}
	teamInvitationColumnsWithoutDefault = []string{"team_id", "inviter_hero_id", "invitee_hero_id"}
	teamInvitationColumnsWithDefault    = []string{"id", "message", "status", "approved_by_hero_id", "approved_at", "responded_at", "expires_at", "created_at"}
	teamInvitationPrimaryKeyColumns     = []string{"id"}
	teamInvitationGeneratedColumns      = []string{}
)

type (
	// TeamInvitationSlice is an alias for a slice of pointers to TeamInvitation.
	// This should almost always be used instead of []TeamInvitation.
	TeamInvitationSlice []*TeamInvitation
	// TeamInvitationHook is the signature for custom TeamInvitation hook methods
	TeamInvitationHook func(context.Context, boil.ContextExecutor, *TeamInvitation) error

	teamInvitationQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	teamInvitationType                 = reflect.TypeOf(&TeamInvitation{})
	teamInvitationMapping              = queries.MakeStructMapping(teamInvitationType)
	teamInvitationPrimaryKeyMapping, _ = queries.BindMapping(teamInvitationType, teamInvitationMapping, teamInvitationPrimaryKeyColumns)
	teamInvitationInsertCacheMut       sync.RWMutex
	teamInvitationInsertCache          = make(map[string]insertCache)
	teamInvitationUpdateCacheMut       sync.RWMutex
	teamInvitationUpdateCache          = make(map[string]updateCache)
	teamInvitationUpsertCacheMut       sync.RWMutex
	teamInvitationUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var teamInvitationAfterSelectMu sync.Mutex
var teamInvitationAfterSelectHooks []TeamInvitationHook

var teamInvitationBeforeInsertMu sync.Mutex
var teamInvitationBeforeInsertHooks []TeamInvitationHook
var teamInvitationAfterInsertMu sync.Mutex
var teamInvitationAfterInsertHooks []TeamInvitationHook

var teamInvitationBeforeUpdateMu sync.Mutex
var teamInvitationBeforeUpdateHooks []TeamInvitationHook
var teamInvitationAfterUpdateMu sync.Mutex
var teamInvitationAfterUpdateHooks []TeamInvitationHook

var teamInvitationBeforeDeleteMu sync.Mutex
var teamInvitationBeforeDeleteHooks []TeamInvitationHook
var teamInvitationAfterDeleteMu sync.Mutex
var teamInvitationAfterDeleteHooks []TeamInvitationHook

var teamInvitationBeforeUpsertMu sync.Mutex
var teamInvitationBeforeUpsertHooks []TeamInvitationHook
var teamInvitationAfterUpsertMu sync.Mutex
var teamInvitationAfterUpsertHooks []TeamInvitationHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TeamInvitation) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TeamInvitation) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TeamInvitation) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TeamInvitation) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TeamInvitation) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TeamInvitation) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TeamInvitation) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TeamInvitation) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TeamInvitation) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamInvitationAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTeamInvitationHook registers your hook function for all future operations.
func AddTeamInvitationHook(hookPoint boil.HookPoint, teamInvitationHook TeamInvitationHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		teamInvitationAfterSelectMu.Lock()
		teamInvitationAfterSelectHooks = append(teamInvitationAfterSelectHooks, teamInvitationHook)
		teamInvitationAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		teamInvitationBeforeInsertMu.Lock()
		teamInvitationBeforeInsertHooks = append(teamInvitationBeforeInsertHooks, teamInvitationHook)
		teamInvitationBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		teamInvitationAfterInsertMu.Lock()
		teamInvitationAfterInsertHooks = append(teamInvitationAfterInsertHooks, teamInvitationHook)
		teamInvitationAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		teamInvitationBeforeUpdateMu.Lock()
		teamInvitationBeforeUpdateHooks = append(teamInvitationBeforeUpdateHooks, teamInvitationHook)
		teamInvitationBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		teamInvitationAfterUpdateMu.Lock()
		teamInvitationAfterUpdateHooks = append(teamInvitationAfterUpdateHooks, teamInvitationHook)
		teamInvitationAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		teamInvitationBeforeDeleteMu.Lock()
		teamInvitationBeforeDeleteHooks = append(teamInvitationBeforeDeleteHooks, teamInvitationHook)
		teamInvitationBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		teamInvitationAfterDeleteMu.Lock()
		teamInvitationAfterDeleteHooks = append(teamInvitationAfterDeleteHooks, teamInvitationHook)
		teamInvitationAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		teamInvitationBeforeUpsertMu.Lock()
		teamInvitationBeforeUpsertHooks = append(teamInvitationBeforeUpsertHooks, teamInvitationHook)
		teamInvitationBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		teamInvitationAfterUpsertMu.Lock()
		teamInvitationAfterUpsertHooks = append(teamInvitationAfterUpsertHooks, teamInvitationHook)
		teamInvitationAfterUpsertMu.Unlock()
	}
}

// OneG returns a single teamInvitation record from the query using the global executor.
func (q teamInvitationQuery) OneG(ctx context.Context) (*TeamInvitation, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single teamInvitation record from the query using the global executor, and panics on error.
func (q teamInvitationQuery) OneGP(ctx context.Context) *TeamInvitation {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single teamInvitation record from the query, and panics on error.
func (q teamInvitationQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *TeamInvitation {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single teamInvitation record from the query.
func (q teamInvitationQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TeamInvitation, error) {
	o := &TeamInvitation{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for team_invitations")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TeamInvitation records from the query using the global executor.
func (q teamInvitationQuery) AllG(ctx context.Context) (TeamInvitationSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all TeamInvitation records from the query using the global executor, and panics on error.
func (q teamInvitationQuery) AllGP(ctx context.Context) TeamInvitationSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all TeamInvitation records from the query, and panics on error.
func (q teamInvitationQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TeamInvitationSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TeamInvitation records from the query.
func (q teamInvitationQuery) All(ctx context.Context, exec boil.ContextExecutor) (TeamInvitationSlice, error) {
	var o []*TeamInvitation

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to TeamInvitation slice")
	}

	if len(teamInvitationAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TeamInvitation records in the query using the global executor
func (q teamInvitationQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all TeamInvitation records in the query using the global executor, and panics on error.
func (q teamInvitationQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all TeamInvitation records in the query, and panics on error.
func (q teamInvitationQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TeamInvitation records in the query.
func (q teamInvitationQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count team_invitations rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q teamInvitationQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q teamInvitationQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q teamInvitationQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q teamInvitationQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if team_invitations exists")
	}

	return count > 0, nil
}

// ApprovedByHero pointed to by the foreign key.
func (o *TeamInvitation) ApprovedByHero(mods ...qm.QueryMod) heroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ApprovedByHeroID),
	}

	queryMods = append(queryMods, mods...)

	return Heroes(queryMods...)
}

// InviteeHero pointed to by the foreign key.
func (o *TeamInvitation) InviteeHero(mods ...qm.QueryMod) heroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.InviteeHeroID),
	}

	queryMods = append(queryMods, mods...)

	return Heroes(queryMods...)
}

// InviterHero pointed to by the foreign key.
func (o *TeamInvitation) InviterHero(mods ...qm.QueryMod) heroQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.InviterHeroID),
	}

	queryMods = append(queryMods, mods...)

	return Heroes(queryMods...)
}

// Team pointed to by the foreign key.
func (o *TeamInvitation) Team(mods ...qm.QueryMod) teamQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TeamID),
	}

	queryMods = append(queryMods, mods...)

	return Teams(queryMods...)
}

// LoadApprovedByHero allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (teamInvitationL) LoadApprovedByHero(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamInvitation interface{}, mods queries.Applicator) error {
	var slice []*TeamInvitation
	var object *TeamInvitation

	if singular {
		var ok bool
		object, ok = maybeTeamInvitation.(*TeamInvitation)
		if !ok {
			object = new(TeamInvitation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamInvitation))
			}
		}
	} else {
		s, ok := maybeTeamInvitation.(*[]*TeamInvitation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamInvitation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamInvitationR{}
		}
		if !queries.IsNil(object.ApprovedByHeroID) {
			args[object.ApprovedByHeroID] = struct{}{}
		}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamInvitationR{}
			}

			if !queries.IsNil(obj.ApprovedByHeroID) {
				args[obj.ApprovedByHeroID] = struct{}{}
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.heroes`),
		qm.WhereIn(`game_runtime.heroes.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.heroes.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Hero")
	}

	var resultSlice []*Hero
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Hero")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for heroes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for heroes")
	}

	if len(heroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ApprovedByHero = foreign
		if foreign.R == nil {
			foreign.R = &heroR{}
		}
		foreign.R.ApprovedByHeroTeamInvitations = append(foreign.R.ApprovedByHeroTeamInvitations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ApprovedByHeroID, foreign.ID) {
				local.R.ApprovedByHero = foreign
				if foreign.R == nil {
					foreign.R = &heroR{}
				}
				foreign.R.ApprovedByHeroTeamInvitations = append(foreign.R.ApprovedByHeroTeamInvitations, local)
				break
			}
		}
	}

	return nil
}

// LoadInviteeHero allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (teamInvitationL) LoadInviteeHero(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamInvitation interface{}, mods queries.Applicator) error {
	var slice []*TeamInvitation
	var object *TeamInvitation

	if singular {
		var ok bool
		object, ok = maybeTeamInvitation.(*TeamInvitation)
		if !ok {
			object = new(TeamInvitation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamInvitation))
			}
		}
	} else {
		s, ok := maybeTeamInvitation.(*[]*TeamInvitation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamInvitation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamInvitationR{}
		}
		args[object.InviteeHeroID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamInvitationR{}
			}

			args[obj.InviteeHeroID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.heroes`),
		qm.WhereIn(`game_runtime.heroes.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.heroes.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Hero")
	}

	var resultSlice []*Hero
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Hero")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for heroes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for heroes")
	}

	if len(heroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.InviteeHero = foreign
		if foreign.R == nil {
			foreign.R = &heroR{}
		}
		foreign.R.InviteeHeroTeamInvitations = append(foreign.R.InviteeHeroTeamInvitations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.InviteeHeroID == foreign.ID {
				local.R.InviteeHero = foreign
				if foreign.R == nil {
					foreign.R = &heroR{}
				}
				foreign.R.InviteeHeroTeamInvitations = append(foreign.R.InviteeHeroTeamInvitations, local)
				break
			}
		}
	}

	return nil
}

// LoadInviterHero allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (teamInvitationL) LoadInviterHero(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamInvitation interface{}, mods queries.Applicator) error {
	var slice []*TeamInvitation
	var object *TeamInvitation

	if singular {
		var ok bool
		object, ok = maybeTeamInvitation.(*TeamInvitation)
		if !ok {
			object = new(TeamInvitation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamInvitation))
			}
		}
	} else {
		s, ok := maybeTeamInvitation.(*[]*TeamInvitation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamInvitation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamInvitationR{}
		}
		args[object.InviterHeroID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamInvitationR{}
			}

			args[obj.InviterHeroID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.heroes`),
		qm.WhereIn(`game_runtime.heroes.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.heroes.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Hero")
	}

	var resultSlice []*Hero
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Hero")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for heroes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for heroes")
	}

	if len(heroAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.InviterHero = foreign
		if foreign.R == nil {
			foreign.R = &heroR{}
		}
		foreign.R.InviterHeroTeamInvitations = append(foreign.R.InviterHeroTeamInvitations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.InviterHeroID == foreign.ID {
				local.R.InviterHero = foreign
				if foreign.R == nil {
					foreign.R = &heroR{}
				}
				foreign.R.InviterHeroTeamInvitations = append(foreign.R.InviterHeroTeamInvitations, local)
				break
			}
		}
	}

	return nil
}

// LoadTeam allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (teamInvitationL) LoadTeam(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamInvitation interface{}, mods queries.Applicator) error {
	var slice []*TeamInvitation
	var object *TeamInvitation

	if singular {
		var ok bool
		object, ok = maybeTeamInvitation.(*TeamInvitation)
		if !ok {
			object = new(TeamInvitation)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamInvitation))
			}
		}
	} else {
		s, ok := maybeTeamInvitation.(*[]*TeamInvitation)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamInvitation)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamInvitation))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamInvitationR{}
		}
		args[object.TeamID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamInvitationR{}
			}

			args[obj.TeamID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.teams`),
		qm.WhereIn(`game_runtime.teams.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.teams.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Team")
	}

	var resultSlice []*Team
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Team")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for teams")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for teams")
	}

	if len(teamAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Team = foreign
		if foreign.R == nil {
			foreign.R = &teamR{}
		}
		foreign.R.TeamInvitations = append(foreign.R.TeamInvitations, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TeamID == foreign.ID {
				local.R.Team = foreign
				if foreign.R == nil {
					foreign.R = &teamR{}
				}
				foreign.R.TeamInvitations = append(foreign.R.TeamInvitations, local)
				break
			}
		}
	}

	return nil
}

// SetApprovedByHeroG of the teamInvitation to the related item.
// Sets o.R.ApprovedByHero to related.
// Adds o to related.R.ApprovedByHeroTeamInvitations.
// Uses the global database handle.
func (o *TeamInvitation) SetApprovedByHeroG(ctx context.Context, insert bool, related *Hero) error {
	return o.SetApprovedByHero(ctx, boil.GetContextDB(), insert, related)
}

// SetApprovedByHeroP of the teamInvitation to the related item.
// Sets o.R.ApprovedByHero to related.
// Adds o to related.R.ApprovedByHeroTeamInvitations.
// Panics on error.
func (o *TeamInvitation) SetApprovedByHeroP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) {
	if err := o.SetApprovedByHero(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetApprovedByHeroGP of the teamInvitation to the related item.
// Sets o.R.ApprovedByHero to related.
// Adds o to related.R.ApprovedByHeroTeamInvitations.
// Uses the global database handle and panics on error.
func (o *TeamInvitation) SetApprovedByHeroGP(ctx context.Context, insert bool, related *Hero) {
	if err := o.SetApprovedByHero(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetApprovedByHero of the teamInvitation to the related item.
// Sets o.R.ApprovedByHero to related.
// Adds o to related.R.ApprovedByHeroTeamInvitations.
func (o *TeamInvitation) SetApprovedByHero(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"approved_by_hero_id"}),
		strmangle.WhereClause("\"", "\"", 2, teamInvitationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ApprovedByHeroID, related.ID)
	if o.R == nil {
		o.R = &teamInvitationR{
			ApprovedByHero: related,
		}
	} else {
		o.R.ApprovedByHero = related
	}

	if related.R == nil {
		related.R = &heroR{
			ApprovedByHeroTeamInvitations: TeamInvitationSlice{o},
		}
	} else {
		related.R.ApprovedByHeroTeamInvitations = append(related.R.ApprovedByHeroTeamInvitations, o)
	}

	return nil
}

// RemoveApprovedByHeroG relationship.
// Sets o.R.ApprovedByHero to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle.
func (o *TeamInvitation) RemoveApprovedByHeroG(ctx context.Context, related *Hero) error {
	return o.RemoveApprovedByHero(ctx, boil.GetContextDB(), related)
}

// RemoveApprovedByHeroP relationship.
// Sets o.R.ApprovedByHero to nil.
// Removes o from all passed in related items' relationships struct.
// Panics on error.
func (o *TeamInvitation) RemoveApprovedByHeroP(ctx context.Context, exec boil.ContextExecutor, related *Hero) {
	if err := o.RemoveApprovedByHero(ctx, exec, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveApprovedByHeroGP relationship.
// Sets o.R.ApprovedByHero to nil.
// Removes o from all passed in related items' relationships struct.
// Uses the global database handle and panics on error.
func (o *TeamInvitation) RemoveApprovedByHeroGP(ctx context.Context, related *Hero) {
	if err := o.RemoveApprovedByHero(ctx, boil.GetContextDB(), related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveApprovedByHero relationship.
// Sets o.R.ApprovedByHero to nil.
// Removes o from all passed in related items' relationships struct.
func (o *TeamInvitation) RemoveApprovedByHero(ctx context.Context, exec boil.ContextExecutor, related *Hero) error {
	var err error

	queries.SetScanner(&o.ApprovedByHeroID, nil)
	if _, err = o.Update(ctx, exec, boil.Whitelist("approved_by_hero_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ApprovedByHero = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ApprovedByHeroTeamInvitations {
		if queries.Equal(o.ApprovedByHeroID, ri.ApprovedByHeroID) {
			continue
		}

		ln := len(related.R.ApprovedByHeroTeamInvitations)
		if ln > 1 && i < ln-1 {
			related.R.ApprovedByHeroTeamInvitations[i] = related.R.ApprovedByHeroTeamInvitations[ln-1]
		}
		related.R.ApprovedByHeroTeamInvitations = related.R.ApprovedByHeroTeamInvitations[:ln-1]
		break
	}
	return nil
}

// SetInviteeHeroG of the teamInvitation to the related item.
// Sets o.R.InviteeHero to related.
// Adds o to related.R.InviteeHeroTeamInvitations.
// Uses the global database handle.
func (o *TeamInvitation) SetInviteeHeroG(ctx context.Context, insert bool, related *Hero) error {
	return o.SetInviteeHero(ctx, boil.GetContextDB(), insert, related)
}

// SetInviteeHeroP of the teamInvitation to the related item.
// Sets o.R.InviteeHero to related.
// Adds o to related.R.InviteeHeroTeamInvitations.
// Panics on error.
func (o *TeamInvitation) SetInviteeHeroP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) {
	if err := o.SetInviteeHero(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetInviteeHeroGP of the teamInvitation to the related item.
// Sets o.R.InviteeHero to related.
// Adds o to related.R.InviteeHeroTeamInvitations.
// Uses the global database handle and panics on error.
func (o *TeamInvitation) SetInviteeHeroGP(ctx context.Context, insert bool, related *Hero) {
	if err := o.SetInviteeHero(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetInviteeHero of the teamInvitation to the related item.
// Sets o.R.InviteeHero to related.
// Adds o to related.R.InviteeHeroTeamInvitations.
func (o *TeamInvitation) SetInviteeHero(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"invitee_hero_id"}),
		strmangle.WhereClause("\"", "\"", 2, teamInvitationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.InviteeHeroID = related.ID
	if o.R == nil {
		o.R = &teamInvitationR{
			InviteeHero: related,
		}
	} else {
		o.R.InviteeHero = related
	}

	if related.R == nil {
		related.R = &heroR{
			InviteeHeroTeamInvitations: TeamInvitationSlice{o},
		}
	} else {
		related.R.InviteeHeroTeamInvitations = append(related.R.InviteeHeroTeamInvitations, o)
	}

	return nil
}

// SetInviterHeroG of the teamInvitation to the related item.
// Sets o.R.InviterHero to related.
// Adds o to related.R.InviterHeroTeamInvitations.
// Uses the global database handle.
func (o *TeamInvitation) SetInviterHeroG(ctx context.Context, insert bool, related *Hero) error {
	return o.SetInviterHero(ctx, boil.GetContextDB(), insert, related)
}

// SetInviterHeroP of the teamInvitation to the related item.
// Sets o.R.InviterHero to related.
// Adds o to related.R.InviterHeroTeamInvitations.
// Panics on error.
func (o *TeamInvitation) SetInviterHeroP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) {
	if err := o.SetInviterHero(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetInviterHeroGP of the teamInvitation to the related item.
// Sets o.R.InviterHero to related.
// Adds o to related.R.InviterHeroTeamInvitations.
// Uses the global database handle and panics on error.
func (o *TeamInvitation) SetInviterHeroGP(ctx context.Context, insert bool, related *Hero) {
	if err := o.SetInviterHero(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetInviterHero of the teamInvitation to the related item.
// Sets o.R.InviterHero to related.
// Adds o to related.R.InviterHeroTeamInvitations.
func (o *TeamInvitation) SetInviterHero(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Hero) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"inviter_hero_id"}),
		strmangle.WhereClause("\"", "\"", 2, teamInvitationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.InviterHeroID = related.ID
	if o.R == nil {
		o.R = &teamInvitationR{
			InviterHero: related,
		}
	} else {
		o.R.InviterHero = related
	}

	if related.R == nil {
		related.R = &heroR{
			InviterHeroTeamInvitations: TeamInvitationSlice{o},
		}
	} else {
		related.R.InviterHeroTeamInvitations = append(related.R.InviterHeroTeamInvitations, o)
	}

	return nil
}

// SetTeamG of the teamInvitation to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamInvitations.
// Uses the global database handle.
func (o *TeamInvitation) SetTeamG(ctx context.Context, insert bool, related *Team) error {
	return o.SetTeam(ctx, boil.GetContextDB(), insert, related)
}

// SetTeamP of the teamInvitation to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamInvitations.
// Panics on error.
func (o *TeamInvitation) SetTeamP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Team) {
	if err := o.SetTeam(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTeamGP of the teamInvitation to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamInvitations.
// Uses the global database handle and panics on error.
func (o *TeamInvitation) SetTeamGP(ctx context.Context, insert bool, related *Team) {
	if err := o.SetTeam(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTeam of the teamInvitation to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamInvitations.
func (o *TeamInvitation) SetTeam(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Team) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
		strmangle.WhereClause("\"", "\"", 2, teamInvitationPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TeamID = related.ID
	if o.R == nil {
		o.R = &teamInvitationR{
			Team: related,
		}
	} else {
		o.R.Team = related
	}

	if related.R == nil {
		related.R = &teamR{
			TeamInvitations: TeamInvitationSlice{o},
		}
	} else {
		related.R.TeamInvitations = append(related.R.TeamInvitations, o)
	}

	return nil
}

// TeamInvitations retrieves all the records using an executor.
func TeamInvitations(mods ...qm.QueryMod) teamInvitationQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"team_invitations\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"team_invitations\".*"})
	}

	return teamInvitationQuery{q}
}

// FindTeamInvitationG retrieves a single record by ID.
func FindTeamInvitationG(ctx context.Context, iD string, selectCols ...string) (*TeamInvitation, error) {
	return FindTeamInvitation(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTeamInvitationP retrieves a single record by ID with an executor, and panics on error.
func FindTeamInvitationP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *TeamInvitation {
	retobj, err := FindTeamInvitation(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamInvitationGP retrieves a single record by ID, and panics on error.
func FindTeamInvitationGP(ctx context.Context, iD string, selectCols ...string) *TeamInvitation {
	retobj, err := FindTeamInvitation(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamInvitation retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTeamInvitation(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TeamInvitation, error) {
	teamInvitationObj := &TeamInvitation{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"team_invitations\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, teamInvitationObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from team_invitations")
	}

	if err = teamInvitationObj.doAfterSelectHooks(ctx, exec); err != nil {
		return teamInvitationObj, err
	}

	return teamInvitationObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TeamInvitation) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TeamInvitation) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TeamInvitation) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TeamInvitation) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no team_invitations provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamInvitationColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	teamInvitationInsertCacheMut.RLock()
	cache, cached := teamInvitationInsertCache[key]
	teamInvitationInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			teamInvitationAllColumns,
			teamInvitationColumnsWithDefault,
			teamInvitationColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(teamInvitationType, teamInvitationMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(teamInvitationType, teamInvitationMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"team_invitations\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"team_invitations\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into team_invitations")
	}

	if !cached {
		teamInvitationInsertCacheMut.Lock()
		teamInvitationInsertCache[key] = cache
		teamInvitationInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single TeamInvitation record using the global executor.
// See Update for more documentation.
func (o *TeamInvitation) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the TeamInvitation, and panics on error.
// See Update for more documentation.
func (o *TeamInvitation) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single TeamInvitation record using the global executor. Panics on error.
// See Update for more documentation.
func (o *TeamInvitation) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the TeamInvitation.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TeamInvitation) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	teamInvitationUpdateCacheMut.RLock()
	cache, cached := teamInvitationUpdateCache[key]
	teamInvitationUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			teamInvitationAllColumns,
			teamInvitationPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update team_invitations, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, teamInvitationPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(teamInvitationType, teamInvitationMapping, append(wl, teamInvitationPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update team_invitations row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for team_invitations")
	}

	if !cached {
		teamInvitationUpdateCacheMut.Lock()
		teamInvitationUpdateCache[key] = cache
		teamInvitationUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q teamInvitationQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q teamInvitationQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q teamInvitationQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q teamInvitationQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for team_invitations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for team_invitations")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TeamInvitationSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TeamInvitationSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TeamInvitationSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TeamInvitationSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamInvitationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, teamInvitationPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in teamInvitation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all teamInvitation")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TeamInvitation) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TeamInvitation) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TeamInvitation) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TeamInvitation) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no team_invitations provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamInvitationColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	teamInvitationUpsertCacheMut.RLock()
	cache, cached := teamInvitationUpsertCache[key]
	teamInvitationUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			teamInvitationAllColumns,
			teamInvitationColumnsWithDefault,
			teamInvitationColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			teamInvitationAllColumns,
			teamInvitationPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert team_invitations, could not build update column list")
		}

		ret := strmangle.SetComplement(teamInvitationAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(teamInvitationPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert team_invitations, could not build conflict column list")
			}

			conflict = make([]string, len(teamInvitationPrimaryKeyColumns))
			copy(conflict, teamInvitationPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"team_invitations\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(teamInvitationType, teamInvitationMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(teamInvitationType, teamInvitationMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert team_invitations")
	}

	if !cached {
		teamInvitationUpsertCacheMut.Lock()
		teamInvitationUpsertCache[key] = cache
		teamInvitationUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single TeamInvitation record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TeamInvitation) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single TeamInvitation record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TeamInvitation) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single TeamInvitation record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TeamInvitation) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single TeamInvitation record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TeamInvitation) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no TeamInvitation provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), teamInvitationPrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"team_invitations\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from team_invitations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for team_invitations")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q teamInvitationQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q teamInvitationQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q teamInvitationQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q teamInvitationQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no teamInvitationQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from team_invitations")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for team_invitations")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TeamInvitationSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TeamInvitationSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TeamInvitationSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TeamInvitationSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(teamInvitationBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamInvitationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"team_invitations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamInvitationPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from teamInvitation slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for team_invitations")
	}

	if len(teamInvitationAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TeamInvitation) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no TeamInvitation provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TeamInvitation) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TeamInvitation) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TeamInvitation) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTeamInvitation(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamInvitationSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty TeamInvitationSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamInvitationSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamInvitationSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamInvitationSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TeamInvitationSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamInvitationPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"team_invitations\".* FROM \"game_runtime\".\"team_invitations\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamInvitationPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in TeamInvitationSlice")
	}

	*o = slice

	return nil
}

// TeamInvitationExistsG checks if the TeamInvitation row exists.
func TeamInvitationExistsG(ctx context.Context, iD string) (bool, error) {
	return TeamInvitationExists(ctx, boil.GetContextDB(), iD)
}

// TeamInvitationExistsP checks if the TeamInvitation row exists. Panics on error.
func TeamInvitationExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TeamInvitationExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamInvitationExistsGP checks if the TeamInvitation row exists. Panics on error.
func TeamInvitationExistsGP(ctx context.Context, iD string) bool {
	e, err := TeamInvitationExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamInvitationExists checks if the TeamInvitation row exists.
func TeamInvitationExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"team_invitations\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if team_invitations exists")
	}

	return exists, nil
}

// Exists checks if the TeamInvitation row exists.
func (o *TeamInvitation) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TeamInvitationExists(ctx, exec, o.ID)
}
