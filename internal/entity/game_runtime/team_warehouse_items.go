// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TeamWarehouseItem is an object representing the database table.
type TeamWarehouseItem struct {
	// 物品记录ID
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 仓库ID
	WarehouseID string `boil:"warehouse_id" json:"warehouse_id" toml:"warehouse_id" yaml:"warehouse_id"`
	// 物品配置ID
	ItemID string `boil:"item_id" json:"item_id" toml:"item_id" yaml:"item_id"`
	// 物品类型
	ItemType string `boil:"item_type" json:"item_type" toml:"item_type" yaml:"item_type"`
	// 数量
	Quantity int `boil:"quantity" json:"quantity" toml:"quantity" yaml:"quantity"`
	// 来源地城ID
	SourceDungeonID null.String `boil:"source_dungeon_id" json:"source_dungeon_id,omitempty" toml:"source_dungeon_id" yaml:"source_dungeon_id,omitempty"`
	// 获得时间
	ObtainedAt time.Time `boil:"obtained_at" json:"obtained_at" toml:"obtained_at" yaml:"obtained_at"`

	R *teamWarehouseItemR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L teamWarehouseItemL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TeamWarehouseItemColumns = struct {
	ID              string
	WarehouseID     string
	ItemID          string
	ItemType        string
	Quantity        string
	SourceDungeonID string
	ObtainedAt      string
}{
	ID:              "id",
	WarehouseID:     "warehouse_id",
	ItemID:          "item_id",
	ItemType:        "item_type",
	Quantity:        "quantity",
	SourceDungeonID: "source_dungeon_id",
	ObtainedAt:      "obtained_at",
}

var TeamWarehouseItemTableColumns = struct {
	ID              string
	WarehouseID     string
	ItemID          string
	ItemType        string
	Quantity        string
	SourceDungeonID string
	ObtainedAt      string
}{
	ID:              "team_warehouse_items.id",
	WarehouseID:     "team_warehouse_items.warehouse_id",
	ItemID:          "team_warehouse_items.item_id",
	ItemType:        "team_warehouse_items.item_type",
	Quantity:        "team_warehouse_items.quantity",
	SourceDungeonID: "team_warehouse_items.source_dungeon_id",
	ObtainedAt:      "team_warehouse_items.obtained_at",
}

// Generated where

var TeamWarehouseItemWhere = struct {
	ID              whereHelperstring
	WarehouseID     whereHelperstring
	ItemID          whereHelperstring
	ItemType        whereHelperstring
	Quantity        whereHelperint
	SourceDungeonID whereHelpernull_String
	ObtainedAt      whereHelpertime_Time
}{
	ID:              whereHelperstring{field: "\"game_runtime\".\"team_warehouse_items\".\"id\""},
	WarehouseID:     whereHelperstring{field: "\"game_runtime\".\"team_warehouse_items\".\"warehouse_id\""},
	ItemID:          whereHelperstring{field: "\"game_runtime\".\"team_warehouse_items\".\"item_id\""},
	ItemType:        whereHelperstring{field: "\"game_runtime\".\"team_warehouse_items\".\"item_type\""},
	Quantity:        whereHelperint{field: "\"game_runtime\".\"team_warehouse_items\".\"quantity\""},
	SourceDungeonID: whereHelpernull_String{field: "\"game_runtime\".\"team_warehouse_items\".\"source_dungeon_id\""},
	ObtainedAt:      whereHelpertime_Time{field: "\"game_runtime\".\"team_warehouse_items\".\"obtained_at\""},
}

// TeamWarehouseItemRels is where relationship names are stored.
var TeamWarehouseItemRels = struct {
	Warehouse string
}{
	Warehouse: "Warehouse",
}

// teamWarehouseItemR is where relationships are stored.
type teamWarehouseItemR struct {
	Warehouse *TeamWarehouse `boil:"Warehouse" json:"Warehouse" toml:"Warehouse" yaml:"Warehouse"`
}

// NewStruct creates a new relationship struct
func (*teamWarehouseItemR) NewStruct() *teamWarehouseItemR {
	return &teamWarehouseItemR{}
}

func (o *TeamWarehouseItem) GetWarehouse() *TeamWarehouse {
	if o == nil {
		return nil
	}

	return o.R.GetWarehouse()
}

func (r *teamWarehouseItemR) GetWarehouse() *TeamWarehouse {
	if r == nil {
		return nil
	}

	return r.Warehouse
}

// teamWarehouseItemL is where Load methods for each relationship are stored.
type teamWarehouseItemL struct{}

var (
	teamWarehouseItemAllColumns            = []string{"id", "warehouse_id", "item_id", "item_type", "quantity", "source_dungeon_id", "obtained_at"}
	teamWarehouseItemColumnsWithoutDefault = []string{"warehouse_id", "item_id", "item_type"}
	teamWarehouseItemColumnsWithDefault    = []string{"id", "quantity", "source_dungeon_id", "obtained_at"}
	teamWarehouseItemPrimaryKeyColumns     = []string{"id"}
	teamWarehouseItemGeneratedColumns      = []string{}
)

type (
	// TeamWarehouseItemSlice is an alias for a slice of pointers to TeamWarehouseItem.
	// This should almost always be used instead of []TeamWarehouseItem.
	TeamWarehouseItemSlice []*TeamWarehouseItem
	// TeamWarehouseItemHook is the signature for custom TeamWarehouseItem hook methods
	TeamWarehouseItemHook func(context.Context, boil.ContextExecutor, *TeamWarehouseItem) error

	teamWarehouseItemQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	teamWarehouseItemType                 = reflect.TypeOf(&TeamWarehouseItem{})
	teamWarehouseItemMapping              = queries.MakeStructMapping(teamWarehouseItemType)
	teamWarehouseItemPrimaryKeyMapping, _ = queries.BindMapping(teamWarehouseItemType, teamWarehouseItemMapping, teamWarehouseItemPrimaryKeyColumns)
	teamWarehouseItemInsertCacheMut       sync.RWMutex
	teamWarehouseItemInsertCache          = make(map[string]insertCache)
	teamWarehouseItemUpdateCacheMut       sync.RWMutex
	teamWarehouseItemUpdateCache          = make(map[string]updateCache)
	teamWarehouseItemUpsertCacheMut       sync.RWMutex
	teamWarehouseItemUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var teamWarehouseItemAfterSelectMu sync.Mutex
var teamWarehouseItemAfterSelectHooks []TeamWarehouseItemHook

var teamWarehouseItemBeforeInsertMu sync.Mutex
var teamWarehouseItemBeforeInsertHooks []TeamWarehouseItemHook
var teamWarehouseItemAfterInsertMu sync.Mutex
var teamWarehouseItemAfterInsertHooks []TeamWarehouseItemHook

var teamWarehouseItemBeforeUpdateMu sync.Mutex
var teamWarehouseItemBeforeUpdateHooks []TeamWarehouseItemHook
var teamWarehouseItemAfterUpdateMu sync.Mutex
var teamWarehouseItemAfterUpdateHooks []TeamWarehouseItemHook

var teamWarehouseItemBeforeDeleteMu sync.Mutex
var teamWarehouseItemBeforeDeleteHooks []TeamWarehouseItemHook
var teamWarehouseItemAfterDeleteMu sync.Mutex
var teamWarehouseItemAfterDeleteHooks []TeamWarehouseItemHook

var teamWarehouseItemBeforeUpsertMu sync.Mutex
var teamWarehouseItemBeforeUpsertHooks []TeamWarehouseItemHook
var teamWarehouseItemAfterUpsertMu sync.Mutex
var teamWarehouseItemAfterUpsertHooks []TeamWarehouseItemHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TeamWarehouseItem) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TeamWarehouseItem) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TeamWarehouseItem) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TeamWarehouseItem) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TeamWarehouseItem) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TeamWarehouseItem) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TeamWarehouseItem) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TeamWarehouseItem) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TeamWarehouseItem) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseItemAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTeamWarehouseItemHook registers your hook function for all future operations.
func AddTeamWarehouseItemHook(hookPoint boil.HookPoint, teamWarehouseItemHook TeamWarehouseItemHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		teamWarehouseItemAfterSelectMu.Lock()
		teamWarehouseItemAfterSelectHooks = append(teamWarehouseItemAfterSelectHooks, teamWarehouseItemHook)
		teamWarehouseItemAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		teamWarehouseItemBeforeInsertMu.Lock()
		teamWarehouseItemBeforeInsertHooks = append(teamWarehouseItemBeforeInsertHooks, teamWarehouseItemHook)
		teamWarehouseItemBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		teamWarehouseItemAfterInsertMu.Lock()
		teamWarehouseItemAfterInsertHooks = append(teamWarehouseItemAfterInsertHooks, teamWarehouseItemHook)
		teamWarehouseItemAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		teamWarehouseItemBeforeUpdateMu.Lock()
		teamWarehouseItemBeforeUpdateHooks = append(teamWarehouseItemBeforeUpdateHooks, teamWarehouseItemHook)
		teamWarehouseItemBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		teamWarehouseItemAfterUpdateMu.Lock()
		teamWarehouseItemAfterUpdateHooks = append(teamWarehouseItemAfterUpdateHooks, teamWarehouseItemHook)
		teamWarehouseItemAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		teamWarehouseItemBeforeDeleteMu.Lock()
		teamWarehouseItemBeforeDeleteHooks = append(teamWarehouseItemBeforeDeleteHooks, teamWarehouseItemHook)
		teamWarehouseItemBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		teamWarehouseItemAfterDeleteMu.Lock()
		teamWarehouseItemAfterDeleteHooks = append(teamWarehouseItemAfterDeleteHooks, teamWarehouseItemHook)
		teamWarehouseItemAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		teamWarehouseItemBeforeUpsertMu.Lock()
		teamWarehouseItemBeforeUpsertHooks = append(teamWarehouseItemBeforeUpsertHooks, teamWarehouseItemHook)
		teamWarehouseItemBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		teamWarehouseItemAfterUpsertMu.Lock()
		teamWarehouseItemAfterUpsertHooks = append(teamWarehouseItemAfterUpsertHooks, teamWarehouseItemHook)
		teamWarehouseItemAfterUpsertMu.Unlock()
	}
}

// OneG returns a single teamWarehouseItem record from the query using the global executor.
func (q teamWarehouseItemQuery) OneG(ctx context.Context) (*TeamWarehouseItem, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single teamWarehouseItem record from the query using the global executor, and panics on error.
func (q teamWarehouseItemQuery) OneGP(ctx context.Context) *TeamWarehouseItem {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single teamWarehouseItem record from the query, and panics on error.
func (q teamWarehouseItemQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *TeamWarehouseItem {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single teamWarehouseItem record from the query.
func (q teamWarehouseItemQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TeamWarehouseItem, error) {
	o := &TeamWarehouseItem{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for team_warehouse_items")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TeamWarehouseItem records from the query using the global executor.
func (q teamWarehouseItemQuery) AllG(ctx context.Context) (TeamWarehouseItemSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all TeamWarehouseItem records from the query using the global executor, and panics on error.
func (q teamWarehouseItemQuery) AllGP(ctx context.Context) TeamWarehouseItemSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all TeamWarehouseItem records from the query, and panics on error.
func (q teamWarehouseItemQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TeamWarehouseItemSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TeamWarehouseItem records from the query.
func (q teamWarehouseItemQuery) All(ctx context.Context, exec boil.ContextExecutor) (TeamWarehouseItemSlice, error) {
	var o []*TeamWarehouseItem

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to TeamWarehouseItem slice")
	}

	if len(teamWarehouseItemAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TeamWarehouseItem records in the query using the global executor
func (q teamWarehouseItemQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all TeamWarehouseItem records in the query using the global executor, and panics on error.
func (q teamWarehouseItemQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all TeamWarehouseItem records in the query, and panics on error.
func (q teamWarehouseItemQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TeamWarehouseItem records in the query.
func (q teamWarehouseItemQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count team_warehouse_items rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q teamWarehouseItemQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q teamWarehouseItemQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q teamWarehouseItemQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q teamWarehouseItemQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if team_warehouse_items exists")
	}

	return count > 0, nil
}

// Warehouse pointed to by the foreign key.
func (o *TeamWarehouseItem) Warehouse(mods ...qm.QueryMod) teamWarehouseQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.WarehouseID),
	}

	queryMods = append(queryMods, mods...)

	return TeamWarehouses(queryMods...)
}

// LoadWarehouse allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (teamWarehouseItemL) LoadWarehouse(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamWarehouseItem interface{}, mods queries.Applicator) error {
	var slice []*TeamWarehouseItem
	var object *TeamWarehouseItem

	if singular {
		var ok bool
		object, ok = maybeTeamWarehouseItem.(*TeamWarehouseItem)
		if !ok {
			object = new(TeamWarehouseItem)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamWarehouseItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamWarehouseItem))
			}
		}
	} else {
		s, ok := maybeTeamWarehouseItem.(*[]*TeamWarehouseItem)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamWarehouseItem)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamWarehouseItem))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamWarehouseItemR{}
		}
		args[object.WarehouseID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamWarehouseItemR{}
			}

			args[obj.WarehouseID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_warehouses`),
		qm.WhereIn(`game_runtime.team_warehouses.id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TeamWarehouse")
	}

	var resultSlice []*TeamWarehouse
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TeamWarehouse")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for team_warehouses")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_warehouses")
	}

	if len(teamWarehouseAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Warehouse = foreign
		if foreign.R == nil {
			foreign.R = &teamWarehouseR{}
		}
		foreign.R.WarehouseTeamWarehouseItems = append(foreign.R.WarehouseTeamWarehouseItems, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.WarehouseID == foreign.ID {
				local.R.Warehouse = foreign
				if foreign.R == nil {
					foreign.R = &teamWarehouseR{}
				}
				foreign.R.WarehouseTeamWarehouseItems = append(foreign.R.WarehouseTeamWarehouseItems, local)
				break
			}
		}
	}

	return nil
}

// SetWarehouseG of the teamWarehouseItem to the related item.
// Sets o.R.Warehouse to related.
// Adds o to related.R.WarehouseTeamWarehouseItems.
// Uses the global database handle.
func (o *TeamWarehouseItem) SetWarehouseG(ctx context.Context, insert bool, related *TeamWarehouse) error {
	return o.SetWarehouse(ctx, boil.GetContextDB(), insert, related)
}

// SetWarehouseP of the teamWarehouseItem to the related item.
// Sets o.R.Warehouse to related.
// Adds o to related.R.WarehouseTeamWarehouseItems.
// Panics on error.
func (o *TeamWarehouseItem) SetWarehouseP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TeamWarehouse) {
	if err := o.SetWarehouse(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetWarehouseGP of the teamWarehouseItem to the related item.
// Sets o.R.Warehouse to related.
// Adds o to related.R.WarehouseTeamWarehouseItems.
// Uses the global database handle and panics on error.
func (o *TeamWarehouseItem) SetWarehouseGP(ctx context.Context, insert bool, related *TeamWarehouse) {
	if err := o.SetWarehouse(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetWarehouse of the teamWarehouseItem to the related item.
// Sets o.R.Warehouse to related.
// Adds o to related.R.WarehouseTeamWarehouseItems.
func (o *TeamWarehouseItem) SetWarehouse(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TeamWarehouse) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"team_warehouse_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"warehouse_id"}),
		strmangle.WhereClause("\"", "\"", 2, teamWarehouseItemPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.WarehouseID = related.ID
	if o.R == nil {
		o.R = &teamWarehouseItemR{
			Warehouse: related,
		}
	} else {
		o.R.Warehouse = related
	}

	if related.R == nil {
		related.R = &teamWarehouseR{
			WarehouseTeamWarehouseItems: TeamWarehouseItemSlice{o},
		}
	} else {
		related.R.WarehouseTeamWarehouseItems = append(related.R.WarehouseTeamWarehouseItems, o)
	}

	return nil
}

// TeamWarehouseItems retrieves all the records using an executor.
func TeamWarehouseItems(mods ...qm.QueryMod) teamWarehouseItemQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"team_warehouse_items\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"team_warehouse_items\".*"})
	}

	return teamWarehouseItemQuery{q}
}

// FindTeamWarehouseItemG retrieves a single record by ID.
func FindTeamWarehouseItemG(ctx context.Context, iD string, selectCols ...string) (*TeamWarehouseItem, error) {
	return FindTeamWarehouseItem(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTeamWarehouseItemP retrieves a single record by ID with an executor, and panics on error.
func FindTeamWarehouseItemP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *TeamWarehouseItem {
	retobj, err := FindTeamWarehouseItem(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamWarehouseItemGP retrieves a single record by ID, and panics on error.
func FindTeamWarehouseItemGP(ctx context.Context, iD string, selectCols ...string) *TeamWarehouseItem {
	retobj, err := FindTeamWarehouseItem(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamWarehouseItem retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTeamWarehouseItem(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TeamWarehouseItem, error) {
	teamWarehouseItemObj := &TeamWarehouseItem{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"team_warehouse_items\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, teamWarehouseItemObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from team_warehouse_items")
	}

	if err = teamWarehouseItemObj.doAfterSelectHooks(ctx, exec); err != nil {
		return teamWarehouseItemObj, err
	}

	return teamWarehouseItemObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TeamWarehouseItem) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TeamWarehouseItem) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TeamWarehouseItem) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TeamWarehouseItem) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no team_warehouse_items provided for insertion")
	}

	var err error

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamWarehouseItemColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	teamWarehouseItemInsertCacheMut.RLock()
	cache, cached := teamWarehouseItemInsertCache[key]
	teamWarehouseItemInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			teamWarehouseItemAllColumns,
			teamWarehouseItemColumnsWithDefault,
			teamWarehouseItemColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(teamWarehouseItemType, teamWarehouseItemMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(teamWarehouseItemType, teamWarehouseItemMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"team_warehouse_items\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"team_warehouse_items\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into team_warehouse_items")
	}

	if !cached {
		teamWarehouseItemInsertCacheMut.Lock()
		teamWarehouseItemInsertCache[key] = cache
		teamWarehouseItemInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single TeamWarehouseItem record using the global executor.
// See Update for more documentation.
func (o *TeamWarehouseItem) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the TeamWarehouseItem, and panics on error.
// See Update for more documentation.
func (o *TeamWarehouseItem) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single TeamWarehouseItem record using the global executor. Panics on error.
// See Update for more documentation.
func (o *TeamWarehouseItem) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the TeamWarehouseItem.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TeamWarehouseItem) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	teamWarehouseItemUpdateCacheMut.RLock()
	cache, cached := teamWarehouseItemUpdateCache[key]
	teamWarehouseItemUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			teamWarehouseItemAllColumns,
			teamWarehouseItemPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update team_warehouse_items, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"team_warehouse_items\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, teamWarehouseItemPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(teamWarehouseItemType, teamWarehouseItemMapping, append(wl, teamWarehouseItemPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update team_warehouse_items row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for team_warehouse_items")
	}

	if !cached {
		teamWarehouseItemUpdateCacheMut.Lock()
		teamWarehouseItemUpdateCache[key] = cache
		teamWarehouseItemUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q teamWarehouseItemQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q teamWarehouseItemQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q teamWarehouseItemQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q teamWarehouseItemQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for team_warehouse_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for team_warehouse_items")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TeamWarehouseItemSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TeamWarehouseItemSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TeamWarehouseItemSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TeamWarehouseItemSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamWarehouseItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"team_warehouse_items\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, teamWarehouseItemPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in teamWarehouseItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all teamWarehouseItem")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TeamWarehouseItem) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TeamWarehouseItem) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TeamWarehouseItem) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TeamWarehouseItem) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no team_warehouse_items provided for upsert")
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamWarehouseItemColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	teamWarehouseItemUpsertCacheMut.RLock()
	cache, cached := teamWarehouseItemUpsertCache[key]
	teamWarehouseItemUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			teamWarehouseItemAllColumns,
			teamWarehouseItemColumnsWithDefault,
			teamWarehouseItemColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			teamWarehouseItemAllColumns,
			teamWarehouseItemPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert team_warehouse_items, could not build update column list")
		}

		ret := strmangle.SetComplement(teamWarehouseItemAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(teamWarehouseItemPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert team_warehouse_items, could not build conflict column list")
			}

			conflict = make([]string, len(teamWarehouseItemPrimaryKeyColumns))
			copy(conflict, teamWarehouseItemPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"team_warehouse_items\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(teamWarehouseItemType, teamWarehouseItemMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(teamWarehouseItemType, teamWarehouseItemMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert team_warehouse_items")
	}

	if !cached {
		teamWarehouseItemUpsertCacheMut.Lock()
		teamWarehouseItemUpsertCache[key] = cache
		teamWarehouseItemUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single TeamWarehouseItem record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TeamWarehouseItem) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single TeamWarehouseItem record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TeamWarehouseItem) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single TeamWarehouseItem record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TeamWarehouseItem) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single TeamWarehouseItem record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TeamWarehouseItem) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no TeamWarehouseItem provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), teamWarehouseItemPrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"team_warehouse_items\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from team_warehouse_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for team_warehouse_items")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q teamWarehouseItemQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q teamWarehouseItemQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q teamWarehouseItemQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q teamWarehouseItemQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no teamWarehouseItemQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from team_warehouse_items")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for team_warehouse_items")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TeamWarehouseItemSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TeamWarehouseItemSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TeamWarehouseItemSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TeamWarehouseItemSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(teamWarehouseItemBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamWarehouseItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"team_warehouse_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamWarehouseItemPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from teamWarehouseItem slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for team_warehouse_items")
	}

	if len(teamWarehouseItemAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TeamWarehouseItem) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no TeamWarehouseItem provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TeamWarehouseItem) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TeamWarehouseItem) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TeamWarehouseItem) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTeamWarehouseItem(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamWarehouseItemSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty TeamWarehouseItemSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamWarehouseItemSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamWarehouseItemSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamWarehouseItemSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TeamWarehouseItemSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamWarehouseItemPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"team_warehouse_items\".* FROM \"game_runtime\".\"team_warehouse_items\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamWarehouseItemPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in TeamWarehouseItemSlice")
	}

	*o = slice

	return nil
}

// TeamWarehouseItemExistsG checks if the TeamWarehouseItem row exists.
func TeamWarehouseItemExistsG(ctx context.Context, iD string) (bool, error) {
	return TeamWarehouseItemExists(ctx, boil.GetContextDB(), iD)
}

// TeamWarehouseItemExistsP checks if the TeamWarehouseItem row exists. Panics on error.
func TeamWarehouseItemExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TeamWarehouseItemExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamWarehouseItemExistsGP checks if the TeamWarehouseItem row exists. Panics on error.
func TeamWarehouseItemExistsGP(ctx context.Context, iD string) bool {
	e, err := TeamWarehouseItemExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamWarehouseItemExists checks if the TeamWarehouseItem row exists.
func TeamWarehouseItemExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"team_warehouse_items\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if team_warehouse_items exists")
	}

	return exists, nil
}

// Exists checks if the TeamWarehouseItem row exists.
func (o *TeamWarehouseItem) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TeamWarehouseItemExists(ctx, exec, o.ID)
}
