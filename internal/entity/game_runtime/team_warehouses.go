// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// TeamWarehouse is an object representing the database table.
type TeamWarehouse struct {
	// 仓库ID
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 团队ID
	TeamID string `boil:"team_id" json:"team_id" toml:"team_id" yaml:"team_id"`
	// 金币数量
	GoldAmount int64 `boil:"gold_amount" json:"gold_amount" toml:"gold_amount" yaml:"gold_amount"`
	// 创建时间
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	// 更新时间
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *teamWarehouseR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L teamWarehouseL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TeamWarehouseColumns = struct {
	ID         string
	TeamID     string
	GoldAmount string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "id",
	TeamID:     "team_id",
	GoldAmount: "gold_amount",
	CreatedAt:  "created_at",
	UpdatedAt:  "updated_at",
}

var TeamWarehouseTableColumns = struct {
	ID         string
	TeamID     string
	GoldAmount string
	CreatedAt  string
	UpdatedAt  string
}{
	ID:         "team_warehouses.id",
	TeamID:     "team_warehouses.team_id",
	GoldAmount: "team_warehouses.gold_amount",
	CreatedAt:  "team_warehouses.created_at",
	UpdatedAt:  "team_warehouses.updated_at",
}

// Generated where

var TeamWarehouseWhere = struct {
	ID         whereHelperstring
	TeamID     whereHelperstring
	GoldAmount whereHelperint64
	CreatedAt  whereHelpertime_Time
	UpdatedAt  whereHelpertime_Time
}{
	ID:         whereHelperstring{field: "\"game_runtime\".\"team_warehouses\".\"id\""},
	TeamID:     whereHelperstring{field: "\"game_runtime\".\"team_warehouses\".\"team_id\""},
	GoldAmount: whereHelperint64{field: "\"game_runtime\".\"team_warehouses\".\"gold_amount\""},
	CreatedAt:  whereHelpertime_Time{field: "\"game_runtime\".\"team_warehouses\".\"created_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"game_runtime\".\"team_warehouses\".\"updated_at\""},
}

// TeamWarehouseRels is where relationship names are stored.
var TeamWarehouseRels = struct {
	Team                                   string
	WarehouseTeamLootDistributionHistories string
	WarehouseTeamWarehouseItems            string
	WarehouseTeamWarehouseLootLogs         string
}{
	Team:                                   "Team",
	WarehouseTeamLootDistributionHistories: "WarehouseTeamLootDistributionHistories",
	WarehouseTeamWarehouseItems:            "WarehouseTeamWarehouseItems",
	WarehouseTeamWarehouseLootLogs:         "WarehouseTeamWarehouseLootLogs",
}

// teamWarehouseR is where relationships are stored.
type teamWarehouseR struct {
	Team                                   *Team                            `boil:"Team" json:"Team" toml:"Team" yaml:"Team"`
	WarehouseTeamLootDistributionHistories TeamLootDistributionHistorySlice `boil:"WarehouseTeamLootDistributionHistories" json:"WarehouseTeamLootDistributionHistories" toml:"WarehouseTeamLootDistributionHistories" yaml:"WarehouseTeamLootDistributionHistories"`
	WarehouseTeamWarehouseItems            TeamWarehouseItemSlice           `boil:"WarehouseTeamWarehouseItems" json:"WarehouseTeamWarehouseItems" toml:"WarehouseTeamWarehouseItems" yaml:"WarehouseTeamWarehouseItems"`
	WarehouseTeamWarehouseLootLogs         TeamWarehouseLootLogSlice        `boil:"WarehouseTeamWarehouseLootLogs" json:"WarehouseTeamWarehouseLootLogs" toml:"WarehouseTeamWarehouseLootLogs" yaml:"WarehouseTeamWarehouseLootLogs"`
}

// NewStruct creates a new relationship struct
func (*teamWarehouseR) NewStruct() *teamWarehouseR {
	return &teamWarehouseR{}
}

func (o *TeamWarehouse) GetTeam() *Team {
	if o == nil {
		return nil
	}

	return o.R.GetTeam()
}

func (r *teamWarehouseR) GetTeam() *Team {
	if r == nil {
		return nil
	}

	return r.Team
}

func (o *TeamWarehouse) GetWarehouseTeamLootDistributionHistories() TeamLootDistributionHistorySlice {
	if o == nil {
		return nil
	}

	return o.R.GetWarehouseTeamLootDistributionHistories()
}

func (r *teamWarehouseR) GetWarehouseTeamLootDistributionHistories() TeamLootDistributionHistorySlice {
	if r == nil {
		return nil
	}

	return r.WarehouseTeamLootDistributionHistories
}

func (o *TeamWarehouse) GetWarehouseTeamWarehouseItems() TeamWarehouseItemSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWarehouseTeamWarehouseItems()
}

func (r *teamWarehouseR) GetWarehouseTeamWarehouseItems() TeamWarehouseItemSlice {
	if r == nil {
		return nil
	}

	return r.WarehouseTeamWarehouseItems
}

func (o *TeamWarehouse) GetWarehouseTeamWarehouseLootLogs() TeamWarehouseLootLogSlice {
	if o == nil {
		return nil
	}

	return o.R.GetWarehouseTeamWarehouseLootLogs()
}

func (r *teamWarehouseR) GetWarehouseTeamWarehouseLootLogs() TeamWarehouseLootLogSlice {
	if r == nil {
		return nil
	}

	return r.WarehouseTeamWarehouseLootLogs
}

// teamWarehouseL is where Load methods for each relationship are stored.
type teamWarehouseL struct{}

var (
	teamWarehouseAllColumns            = []string{"id", "team_id", "gold_amount", "created_at", "updated_at"}
	teamWarehouseColumnsWithoutDefault = []string{"team_id"}
	teamWarehouseColumnsWithDefault    = []string{"id", "gold_amount", "created_at", "updated_at"}
	teamWarehousePrimaryKeyColumns     = []string{"id"}
	teamWarehouseGeneratedColumns      = []string{}
)

type (
	// TeamWarehouseSlice is an alias for a slice of pointers to TeamWarehouse.
	// This should almost always be used instead of []TeamWarehouse.
	TeamWarehouseSlice []*TeamWarehouse
	// TeamWarehouseHook is the signature for custom TeamWarehouse hook methods
	TeamWarehouseHook func(context.Context, boil.ContextExecutor, *TeamWarehouse) error

	teamWarehouseQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	teamWarehouseType                 = reflect.TypeOf(&TeamWarehouse{})
	teamWarehouseMapping              = queries.MakeStructMapping(teamWarehouseType)
	teamWarehousePrimaryKeyMapping, _ = queries.BindMapping(teamWarehouseType, teamWarehouseMapping, teamWarehousePrimaryKeyColumns)
	teamWarehouseInsertCacheMut       sync.RWMutex
	teamWarehouseInsertCache          = make(map[string]insertCache)
	teamWarehouseUpdateCacheMut       sync.RWMutex
	teamWarehouseUpdateCache          = make(map[string]updateCache)
	teamWarehouseUpsertCacheMut       sync.RWMutex
	teamWarehouseUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var teamWarehouseAfterSelectMu sync.Mutex
var teamWarehouseAfterSelectHooks []TeamWarehouseHook

var teamWarehouseBeforeInsertMu sync.Mutex
var teamWarehouseBeforeInsertHooks []TeamWarehouseHook
var teamWarehouseAfterInsertMu sync.Mutex
var teamWarehouseAfterInsertHooks []TeamWarehouseHook

var teamWarehouseBeforeUpdateMu sync.Mutex
var teamWarehouseBeforeUpdateHooks []TeamWarehouseHook
var teamWarehouseAfterUpdateMu sync.Mutex
var teamWarehouseAfterUpdateHooks []TeamWarehouseHook

var teamWarehouseBeforeDeleteMu sync.Mutex
var teamWarehouseBeforeDeleteHooks []TeamWarehouseHook
var teamWarehouseAfterDeleteMu sync.Mutex
var teamWarehouseAfterDeleteHooks []TeamWarehouseHook

var teamWarehouseBeforeUpsertMu sync.Mutex
var teamWarehouseBeforeUpsertHooks []TeamWarehouseHook
var teamWarehouseAfterUpsertMu sync.Mutex
var teamWarehouseAfterUpsertHooks []TeamWarehouseHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TeamWarehouse) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TeamWarehouse) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TeamWarehouse) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TeamWarehouse) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TeamWarehouse) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TeamWarehouse) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TeamWarehouse) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TeamWarehouse) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TeamWarehouse) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamWarehouseAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTeamWarehouseHook registers your hook function for all future operations.
func AddTeamWarehouseHook(hookPoint boil.HookPoint, teamWarehouseHook TeamWarehouseHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		teamWarehouseAfterSelectMu.Lock()
		teamWarehouseAfterSelectHooks = append(teamWarehouseAfterSelectHooks, teamWarehouseHook)
		teamWarehouseAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		teamWarehouseBeforeInsertMu.Lock()
		teamWarehouseBeforeInsertHooks = append(teamWarehouseBeforeInsertHooks, teamWarehouseHook)
		teamWarehouseBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		teamWarehouseAfterInsertMu.Lock()
		teamWarehouseAfterInsertHooks = append(teamWarehouseAfterInsertHooks, teamWarehouseHook)
		teamWarehouseAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		teamWarehouseBeforeUpdateMu.Lock()
		teamWarehouseBeforeUpdateHooks = append(teamWarehouseBeforeUpdateHooks, teamWarehouseHook)
		teamWarehouseBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		teamWarehouseAfterUpdateMu.Lock()
		teamWarehouseAfterUpdateHooks = append(teamWarehouseAfterUpdateHooks, teamWarehouseHook)
		teamWarehouseAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		teamWarehouseBeforeDeleteMu.Lock()
		teamWarehouseBeforeDeleteHooks = append(teamWarehouseBeforeDeleteHooks, teamWarehouseHook)
		teamWarehouseBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		teamWarehouseAfterDeleteMu.Lock()
		teamWarehouseAfterDeleteHooks = append(teamWarehouseAfterDeleteHooks, teamWarehouseHook)
		teamWarehouseAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		teamWarehouseBeforeUpsertMu.Lock()
		teamWarehouseBeforeUpsertHooks = append(teamWarehouseBeforeUpsertHooks, teamWarehouseHook)
		teamWarehouseBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		teamWarehouseAfterUpsertMu.Lock()
		teamWarehouseAfterUpsertHooks = append(teamWarehouseAfterUpsertHooks, teamWarehouseHook)
		teamWarehouseAfterUpsertMu.Unlock()
	}
}

// OneG returns a single teamWarehouse record from the query using the global executor.
func (q teamWarehouseQuery) OneG(ctx context.Context) (*TeamWarehouse, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single teamWarehouse record from the query using the global executor, and panics on error.
func (q teamWarehouseQuery) OneGP(ctx context.Context) *TeamWarehouse {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single teamWarehouse record from the query, and panics on error.
func (q teamWarehouseQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *TeamWarehouse {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single teamWarehouse record from the query.
func (q teamWarehouseQuery) One(ctx context.Context, exec boil.ContextExecutor) (*TeamWarehouse, error) {
	o := &TeamWarehouse{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for team_warehouses")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all TeamWarehouse records from the query using the global executor.
func (q teamWarehouseQuery) AllG(ctx context.Context) (TeamWarehouseSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all TeamWarehouse records from the query using the global executor, and panics on error.
func (q teamWarehouseQuery) AllGP(ctx context.Context) TeamWarehouseSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all TeamWarehouse records from the query, and panics on error.
func (q teamWarehouseQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TeamWarehouseSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all TeamWarehouse records from the query.
func (q teamWarehouseQuery) All(ctx context.Context, exec boil.ContextExecutor) (TeamWarehouseSlice, error) {
	var o []*TeamWarehouse

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to TeamWarehouse slice")
	}

	if len(teamWarehouseAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all TeamWarehouse records in the query using the global executor
func (q teamWarehouseQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all TeamWarehouse records in the query using the global executor, and panics on error.
func (q teamWarehouseQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all TeamWarehouse records in the query, and panics on error.
func (q teamWarehouseQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all TeamWarehouse records in the query.
func (q teamWarehouseQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count team_warehouses rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q teamWarehouseQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q teamWarehouseQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q teamWarehouseQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q teamWarehouseQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if team_warehouses exists")
	}

	return count > 0, nil
}

// Team pointed to by the foreign key.
func (o *TeamWarehouse) Team(mods ...qm.QueryMod) teamQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TeamID),
	}

	queryMods = append(queryMods, mods...)

	return Teams(queryMods...)
}

// WarehouseTeamLootDistributionHistories retrieves all the team_loot_distribution_history's TeamLootDistributionHistories with an executor via warehouse_id column.
func (o *TeamWarehouse) WarehouseTeamLootDistributionHistories(mods ...qm.QueryMod) teamLootDistributionHistoryQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_loot_distribution_history\".\"warehouse_id\"=?", o.ID),
	)

	return TeamLootDistributionHistories(queryMods...)
}

// WarehouseTeamWarehouseItems retrieves all the team_warehouse_item's TeamWarehouseItems with an executor via warehouse_id column.
func (o *TeamWarehouse) WarehouseTeamWarehouseItems(mods ...qm.QueryMod) teamWarehouseItemQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_warehouse_items\".\"warehouse_id\"=?", o.ID),
	)

	return TeamWarehouseItems(queryMods...)
}

// WarehouseTeamWarehouseLootLogs retrieves all the team_warehouse_loot_log's TeamWarehouseLootLogs with an executor via warehouse_id column.
func (o *TeamWarehouse) WarehouseTeamWarehouseLootLogs(mods ...qm.QueryMod) teamWarehouseLootLogQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_warehouse_loot_log\".\"warehouse_id\"=?", o.ID),
	)

	return TeamWarehouseLootLogs(queryMods...)
}

// LoadTeam allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (teamWarehouseL) LoadTeam(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamWarehouse interface{}, mods queries.Applicator) error {
	var slice []*TeamWarehouse
	var object *TeamWarehouse

	if singular {
		var ok bool
		object, ok = maybeTeamWarehouse.(*TeamWarehouse)
		if !ok {
			object = new(TeamWarehouse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamWarehouse))
			}
		}
	} else {
		s, ok := maybeTeamWarehouse.(*[]*TeamWarehouse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamWarehouse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamWarehouseR{}
		}
		args[object.TeamID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamWarehouseR{}
			}

			args[obj.TeamID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.teams`),
		qm.WhereIn(`game_runtime.teams.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`game_runtime.teams.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Team")
	}

	var resultSlice []*Team
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Team")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for teams")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for teams")
	}

	if len(teamAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Team = foreign
		if foreign.R == nil {
			foreign.R = &teamR{}
		}
		foreign.R.TeamWarehouse = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TeamID == foreign.ID {
				local.R.Team = foreign
				if foreign.R == nil {
					foreign.R = &teamR{}
				}
				foreign.R.TeamWarehouse = local
				break
			}
		}
	}

	return nil
}

// LoadWarehouseTeamLootDistributionHistories allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamWarehouseL) LoadWarehouseTeamLootDistributionHistories(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamWarehouse interface{}, mods queries.Applicator) error {
	var slice []*TeamWarehouse
	var object *TeamWarehouse

	if singular {
		var ok bool
		object, ok = maybeTeamWarehouse.(*TeamWarehouse)
		if !ok {
			object = new(TeamWarehouse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamWarehouse))
			}
		}
	} else {
		s, ok := maybeTeamWarehouse.(*[]*TeamWarehouse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamWarehouse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamWarehouseR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamWarehouseR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_loot_distribution_history`),
		qm.WhereIn(`game_runtime.team_loot_distribution_history.warehouse_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_loot_distribution_history")
	}

	var resultSlice []*TeamLootDistributionHistory
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_loot_distribution_history")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_loot_distribution_history")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_loot_distribution_history")
	}

	if len(teamLootDistributionHistoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WarehouseTeamLootDistributionHistories = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamLootDistributionHistoryR{}
			}
			foreign.R.Warehouse = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WarehouseID {
				local.R.WarehouseTeamLootDistributionHistories = append(local.R.WarehouseTeamLootDistributionHistories, foreign)
				if foreign.R == nil {
					foreign.R = &teamLootDistributionHistoryR{}
				}
				foreign.R.Warehouse = local
				break
			}
		}
	}

	return nil
}

// LoadWarehouseTeamWarehouseItems allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamWarehouseL) LoadWarehouseTeamWarehouseItems(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamWarehouse interface{}, mods queries.Applicator) error {
	var slice []*TeamWarehouse
	var object *TeamWarehouse

	if singular {
		var ok bool
		object, ok = maybeTeamWarehouse.(*TeamWarehouse)
		if !ok {
			object = new(TeamWarehouse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamWarehouse))
			}
		}
	} else {
		s, ok := maybeTeamWarehouse.(*[]*TeamWarehouse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamWarehouse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamWarehouseR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamWarehouseR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_warehouse_items`),
		qm.WhereIn(`game_runtime.team_warehouse_items.warehouse_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_warehouse_items")
	}

	var resultSlice []*TeamWarehouseItem
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_warehouse_items")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_warehouse_items")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_warehouse_items")
	}

	if len(teamWarehouseItemAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WarehouseTeamWarehouseItems = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamWarehouseItemR{}
			}
			foreign.R.Warehouse = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WarehouseID {
				local.R.WarehouseTeamWarehouseItems = append(local.R.WarehouseTeamWarehouseItems, foreign)
				if foreign.R == nil {
					foreign.R = &teamWarehouseItemR{}
				}
				foreign.R.Warehouse = local
				break
			}
		}
	}

	return nil
}

// LoadWarehouseTeamWarehouseLootLogs allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamWarehouseL) LoadWarehouseTeamWarehouseLootLogs(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeamWarehouse interface{}, mods queries.Applicator) error {
	var slice []*TeamWarehouse
	var object *TeamWarehouse

	if singular {
		var ok bool
		object, ok = maybeTeamWarehouse.(*TeamWarehouse)
		if !ok {
			object = new(TeamWarehouse)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeamWarehouse))
			}
		}
	} else {
		s, ok := maybeTeamWarehouse.(*[]*TeamWarehouse)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeamWarehouse)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeamWarehouse))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamWarehouseR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamWarehouseR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_warehouse_loot_log`),
		qm.WhereIn(`game_runtime.team_warehouse_loot_log.warehouse_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_warehouse_loot_log")
	}

	var resultSlice []*TeamWarehouseLootLog
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_warehouse_loot_log")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_warehouse_loot_log")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_warehouse_loot_log")
	}

	if len(teamWarehouseLootLogAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WarehouseTeamWarehouseLootLogs = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamWarehouseLootLogR{}
			}
			foreign.R.Warehouse = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.WarehouseID {
				local.R.WarehouseTeamWarehouseLootLogs = append(local.R.WarehouseTeamWarehouseLootLogs, foreign)
				if foreign.R == nil {
					foreign.R = &teamWarehouseLootLogR{}
				}
				foreign.R.Warehouse = local
				break
			}
		}
	}

	return nil
}

// SetTeamG of the teamWarehouse to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamWarehouse.
// Uses the global database handle.
func (o *TeamWarehouse) SetTeamG(ctx context.Context, insert bool, related *Team) error {
	return o.SetTeam(ctx, boil.GetContextDB(), insert, related)
}

// SetTeamP of the teamWarehouse to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamWarehouse.
// Panics on error.
func (o *TeamWarehouse) SetTeamP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Team) {
	if err := o.SetTeam(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTeamGP of the teamWarehouse to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamWarehouse.
// Uses the global database handle and panics on error.
func (o *TeamWarehouse) SetTeamGP(ctx context.Context, insert bool, related *Team) {
	if err := o.SetTeam(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTeam of the teamWarehouse to the related item.
// Sets o.R.Team to related.
// Adds o to related.R.TeamWarehouse.
func (o *TeamWarehouse) SetTeam(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Team) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"game_runtime\".\"team_warehouses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
		strmangle.WhereClause("\"", "\"", 2, teamWarehousePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TeamID = related.ID
	if o.R == nil {
		o.R = &teamWarehouseR{
			Team: related,
		}
	} else {
		o.R.Team = related
	}

	if related.R == nil {
		related.R = &teamR{
			TeamWarehouse: o,
		}
	} else {
		related.R.TeamWarehouse = o
	}

	return nil
}

// AddWarehouseTeamLootDistributionHistoriesG adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamLootDistributionHistories.
// Sets related.R.Warehouse appropriately.
// Uses the global database handle.
func (o *TeamWarehouse) AddWarehouseTeamLootDistributionHistoriesG(ctx context.Context, insert bool, related ...*TeamLootDistributionHistory) error {
	return o.AddWarehouseTeamLootDistributionHistories(ctx, boil.GetContextDB(), insert, related...)
}

// AddWarehouseTeamLootDistributionHistoriesP adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamLootDistributionHistories.
// Sets related.R.Warehouse appropriately.
// Panics on error.
func (o *TeamWarehouse) AddWarehouseTeamLootDistributionHistoriesP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamLootDistributionHistory) {
	if err := o.AddWarehouseTeamLootDistributionHistories(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWarehouseTeamLootDistributionHistoriesGP adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamLootDistributionHistories.
// Sets related.R.Warehouse appropriately.
// Uses the global database handle and panics on error.
func (o *TeamWarehouse) AddWarehouseTeamLootDistributionHistoriesGP(ctx context.Context, insert bool, related ...*TeamLootDistributionHistory) {
	if err := o.AddWarehouseTeamLootDistributionHistories(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWarehouseTeamLootDistributionHistories adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamLootDistributionHistories.
// Sets related.R.Warehouse appropriately.
func (o *TeamWarehouse) AddWarehouseTeamLootDistributionHistories(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamLootDistributionHistory) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WarehouseID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_loot_distribution_history\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"warehouse_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamLootDistributionHistoryPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WarehouseID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamWarehouseR{
			WarehouseTeamLootDistributionHistories: related,
		}
	} else {
		o.R.WarehouseTeamLootDistributionHistories = append(o.R.WarehouseTeamLootDistributionHistories, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamLootDistributionHistoryR{
				Warehouse: o,
			}
		} else {
			rel.R.Warehouse = o
		}
	}
	return nil
}

// AddWarehouseTeamWarehouseItemsG adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseItems.
// Sets related.R.Warehouse appropriately.
// Uses the global database handle.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseItemsG(ctx context.Context, insert bool, related ...*TeamWarehouseItem) error {
	return o.AddWarehouseTeamWarehouseItems(ctx, boil.GetContextDB(), insert, related...)
}

// AddWarehouseTeamWarehouseItemsP adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseItems.
// Sets related.R.Warehouse appropriately.
// Panics on error.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseItemsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamWarehouseItem) {
	if err := o.AddWarehouseTeamWarehouseItems(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWarehouseTeamWarehouseItemsGP adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseItems.
// Sets related.R.Warehouse appropriately.
// Uses the global database handle and panics on error.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseItemsGP(ctx context.Context, insert bool, related ...*TeamWarehouseItem) {
	if err := o.AddWarehouseTeamWarehouseItems(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWarehouseTeamWarehouseItems adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseItems.
// Sets related.R.Warehouse appropriately.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseItems(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamWarehouseItem) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WarehouseID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_warehouse_items\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"warehouse_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamWarehouseItemPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WarehouseID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamWarehouseR{
			WarehouseTeamWarehouseItems: related,
		}
	} else {
		o.R.WarehouseTeamWarehouseItems = append(o.R.WarehouseTeamWarehouseItems, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamWarehouseItemR{
				Warehouse: o,
			}
		} else {
			rel.R.Warehouse = o
		}
	}
	return nil
}

// AddWarehouseTeamWarehouseLootLogsG adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseLootLogs.
// Sets related.R.Warehouse appropriately.
// Uses the global database handle.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseLootLogsG(ctx context.Context, insert bool, related ...*TeamWarehouseLootLog) error {
	return o.AddWarehouseTeamWarehouseLootLogs(ctx, boil.GetContextDB(), insert, related...)
}

// AddWarehouseTeamWarehouseLootLogsP adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseLootLogs.
// Sets related.R.Warehouse appropriately.
// Panics on error.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseLootLogsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamWarehouseLootLog) {
	if err := o.AddWarehouseTeamWarehouseLootLogs(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWarehouseTeamWarehouseLootLogsGP adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseLootLogs.
// Sets related.R.Warehouse appropriately.
// Uses the global database handle and panics on error.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseLootLogsGP(ctx context.Context, insert bool, related ...*TeamWarehouseLootLog) {
	if err := o.AddWarehouseTeamWarehouseLootLogs(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddWarehouseTeamWarehouseLootLogs adds the given related objects to the existing relationships
// of the team_warehouse, optionally inserting them as new records.
// Appends related to o.R.WarehouseTeamWarehouseLootLogs.
// Sets related.R.Warehouse appropriately.
func (o *TeamWarehouse) AddWarehouseTeamWarehouseLootLogs(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamWarehouseLootLog) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.WarehouseID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_warehouse_loot_log\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"warehouse_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamWarehouseLootLogPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.WarehouseID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamWarehouseR{
			WarehouseTeamWarehouseLootLogs: related,
		}
	} else {
		o.R.WarehouseTeamWarehouseLootLogs = append(o.R.WarehouseTeamWarehouseLootLogs, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamWarehouseLootLogR{
				Warehouse: o,
			}
		} else {
			rel.R.Warehouse = o
		}
	}
	return nil
}

// TeamWarehouses retrieves all the records using an executor.
func TeamWarehouses(mods ...qm.QueryMod) teamWarehouseQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"team_warehouses\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"team_warehouses\".*"})
	}

	return teamWarehouseQuery{q}
}

// FindTeamWarehouseG retrieves a single record by ID.
func FindTeamWarehouseG(ctx context.Context, iD string, selectCols ...string) (*TeamWarehouse, error) {
	return FindTeamWarehouse(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTeamWarehouseP retrieves a single record by ID with an executor, and panics on error.
func FindTeamWarehouseP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *TeamWarehouse {
	retobj, err := FindTeamWarehouse(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamWarehouseGP retrieves a single record by ID, and panics on error.
func FindTeamWarehouseGP(ctx context.Context, iD string, selectCols ...string) *TeamWarehouse {
	retobj, err := FindTeamWarehouse(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamWarehouse retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTeamWarehouse(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*TeamWarehouse, error) {
	teamWarehouseObj := &TeamWarehouse{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"team_warehouses\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, teamWarehouseObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from team_warehouses")
	}

	if err = teamWarehouseObj.doAfterSelectHooks(ctx, exec); err != nil {
		return teamWarehouseObj, err
	}

	return teamWarehouseObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *TeamWarehouse) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *TeamWarehouse) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *TeamWarehouse) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TeamWarehouse) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no team_warehouses provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamWarehouseColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	teamWarehouseInsertCacheMut.RLock()
	cache, cached := teamWarehouseInsertCache[key]
	teamWarehouseInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			teamWarehouseAllColumns,
			teamWarehouseColumnsWithDefault,
			teamWarehouseColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(teamWarehouseType, teamWarehouseMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(teamWarehouseType, teamWarehouseMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"team_warehouses\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"team_warehouses\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into team_warehouses")
	}

	if !cached {
		teamWarehouseInsertCacheMut.Lock()
		teamWarehouseInsertCache[key] = cache
		teamWarehouseInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single TeamWarehouse record using the global executor.
// See Update for more documentation.
func (o *TeamWarehouse) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the TeamWarehouse, and panics on error.
// See Update for more documentation.
func (o *TeamWarehouse) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single TeamWarehouse record using the global executor. Panics on error.
// See Update for more documentation.
func (o *TeamWarehouse) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the TeamWarehouse.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TeamWarehouse) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	teamWarehouseUpdateCacheMut.RLock()
	cache, cached := teamWarehouseUpdateCache[key]
	teamWarehouseUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			teamWarehouseAllColumns,
			teamWarehousePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update team_warehouses, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"team_warehouses\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, teamWarehousePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(teamWarehouseType, teamWarehouseMapping, append(wl, teamWarehousePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update team_warehouses row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for team_warehouses")
	}

	if !cached {
		teamWarehouseUpdateCacheMut.Lock()
		teamWarehouseUpdateCache[key] = cache
		teamWarehouseUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q teamWarehouseQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q teamWarehouseQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q teamWarehouseQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q teamWarehouseQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for team_warehouses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for team_warehouses")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TeamWarehouseSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TeamWarehouseSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TeamWarehouseSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TeamWarehouseSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamWarehousePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"team_warehouses\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, teamWarehousePrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in teamWarehouse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all teamWarehouse")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *TeamWarehouse) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *TeamWarehouse) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *TeamWarehouse) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TeamWarehouse) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no team_warehouses provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamWarehouseColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	teamWarehouseUpsertCacheMut.RLock()
	cache, cached := teamWarehouseUpsertCache[key]
	teamWarehouseUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			teamWarehouseAllColumns,
			teamWarehouseColumnsWithDefault,
			teamWarehouseColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			teamWarehouseAllColumns,
			teamWarehousePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert team_warehouses, could not build update column list")
		}

		ret := strmangle.SetComplement(teamWarehouseAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(teamWarehousePrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert team_warehouses, could not build conflict column list")
			}

			conflict = make([]string, len(teamWarehousePrimaryKeyColumns))
			copy(conflict, teamWarehousePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"team_warehouses\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(teamWarehouseType, teamWarehouseMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(teamWarehouseType, teamWarehouseMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert team_warehouses")
	}

	if !cached {
		teamWarehouseUpsertCacheMut.Lock()
		teamWarehouseUpsertCache[key] = cache
		teamWarehouseUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single TeamWarehouse record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *TeamWarehouse) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single TeamWarehouse record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TeamWarehouse) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single TeamWarehouse record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *TeamWarehouse) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single TeamWarehouse record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TeamWarehouse) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no TeamWarehouse provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), teamWarehousePrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"team_warehouses\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from team_warehouses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for team_warehouses")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q teamWarehouseQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q teamWarehouseQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q teamWarehouseQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q teamWarehouseQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no teamWarehouseQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from team_warehouses")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for team_warehouses")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TeamWarehouseSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TeamWarehouseSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TeamWarehouseSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TeamWarehouseSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(teamWarehouseBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamWarehousePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"team_warehouses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamWarehousePrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from teamWarehouse slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for team_warehouses")
	}

	if len(teamWarehouseAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *TeamWarehouse) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no TeamWarehouse provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *TeamWarehouse) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *TeamWarehouse) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TeamWarehouse) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTeamWarehouse(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamWarehouseSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty TeamWarehouseSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamWarehouseSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamWarehouseSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamWarehouseSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TeamWarehouseSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamWarehousePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"team_warehouses\".* FROM \"game_runtime\".\"team_warehouses\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamWarehousePrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in TeamWarehouseSlice")
	}

	*o = slice

	return nil
}

// TeamWarehouseExistsG checks if the TeamWarehouse row exists.
func TeamWarehouseExistsG(ctx context.Context, iD string) (bool, error) {
	return TeamWarehouseExists(ctx, boil.GetContextDB(), iD)
}

// TeamWarehouseExistsP checks if the TeamWarehouse row exists. Panics on error.
func TeamWarehouseExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TeamWarehouseExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamWarehouseExistsGP checks if the TeamWarehouse row exists. Panics on error.
func TeamWarehouseExistsGP(ctx context.Context, iD string) bool {
	e, err := TeamWarehouseExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamWarehouseExists checks if the TeamWarehouse row exists.
func TeamWarehouseExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"team_warehouses\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if team_warehouses exists")
	}

	return exists, nil
}

// Exists checks if the TeamWarehouse row exists.
func (o *TeamWarehouse) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TeamWarehouseExists(ctx, exec, o.ID)
}
