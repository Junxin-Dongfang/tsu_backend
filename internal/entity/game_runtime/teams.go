// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// Team is an object representing the database table.
type Team struct {
	// 团队ID
	ID string `boil:"id" json:"id" toml:"id" yaml:"id"`
	// 团队名称
	Name string `boil:"name" json:"name" toml:"name" yaml:"name"`
	// 团队描述
	Description null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	// 最大成员数
	MaxMembers int `boil:"max_members" json:"max_members" toml:"max_members" yaml:"max_members"`
	// 当前队长的英雄ID
	LeaderHeroID string `boil:"leader_hero_id" json:"leader_hero_id" toml:"leader_hero_id" yaml:"leader_hero_id"`
	// 创建时间
	CreatedAt time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	// 更新时间
	UpdatedAt time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	// 软删除时间
	DeletedAt null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *teamR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L teamL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TeamColumns = struct {
	ID           string
	Name         string
	Description  string
	MaxMembers   string
	LeaderHeroID string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "id",
	Name:         "name",
	Description:  "description",
	MaxMembers:   "max_members",
	LeaderHeroID: "leader_hero_id",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
}

var TeamTableColumns = struct {
	ID           string
	Name         string
	Description  string
	MaxMembers   string
	LeaderHeroID string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "teams.id",
	Name:         "teams.name",
	Description:  "teams.description",
	MaxMembers:   "teams.max_members",
	LeaderHeroID: "teams.leader_hero_id",
	CreatedAt:    "teams.created_at",
	UpdatedAt:    "teams.updated_at",
	DeletedAt:    "teams.deleted_at",
}

// Generated where

var TeamWhere = struct {
	ID           whereHelperstring
	Name         whereHelperstring
	Description  whereHelpernull_String
	MaxMembers   whereHelperint
	LeaderHeroID whereHelperstring
	CreatedAt    whereHelpertime_Time
	UpdatedAt    whereHelpertime_Time
	DeletedAt    whereHelpernull_Time
}{
	ID:           whereHelperstring{field: "\"game_runtime\".\"teams\".\"id\""},
	Name:         whereHelperstring{field: "\"game_runtime\".\"teams\".\"name\""},
	Description:  whereHelpernull_String{field: "\"game_runtime\".\"teams\".\"description\""},
	MaxMembers:   whereHelperint{field: "\"game_runtime\".\"teams\".\"max_members\""},
	LeaderHeroID: whereHelperstring{field: "\"game_runtime\".\"teams\".\"leader_hero_id\""},
	CreatedAt:    whereHelpertime_Time{field: "\"game_runtime\".\"teams\".\"created_at\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"game_runtime\".\"teams\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"game_runtime\".\"teams\".\"deleted_at\""},
}

// TeamRels is where relationship names are stored.
var TeamRels = struct {
	TeamWarehouse                 string
	TeamInvitations               string
	TeamJoinRequests              string
	TeamKickedRecords             string
	TeamLootDistributionHistories string
	TeamMembers                   string
}{
	TeamWarehouse:                 "TeamWarehouse",
	TeamInvitations:               "TeamInvitations",
	TeamJoinRequests:              "TeamJoinRequests",
	TeamKickedRecords:             "TeamKickedRecords",
	TeamLootDistributionHistories: "TeamLootDistributionHistories",
	TeamMembers:                   "TeamMembers",
}

// teamR is where relationships are stored.
type teamR struct {
	TeamWarehouse                 *TeamWarehouse                   `boil:"TeamWarehouse" json:"TeamWarehouse" toml:"TeamWarehouse" yaml:"TeamWarehouse"`
	TeamInvitations               TeamInvitationSlice              `boil:"TeamInvitations" json:"TeamInvitations" toml:"TeamInvitations" yaml:"TeamInvitations"`
	TeamJoinRequests              TeamJoinRequestSlice             `boil:"TeamJoinRequests" json:"TeamJoinRequests" toml:"TeamJoinRequests" yaml:"TeamJoinRequests"`
	TeamKickedRecords             TeamKickedRecordSlice            `boil:"TeamKickedRecords" json:"TeamKickedRecords" toml:"TeamKickedRecords" yaml:"TeamKickedRecords"`
	TeamLootDistributionHistories TeamLootDistributionHistorySlice `boil:"TeamLootDistributionHistories" json:"TeamLootDistributionHistories" toml:"TeamLootDistributionHistories" yaml:"TeamLootDistributionHistories"`
	TeamMembers                   TeamMemberSlice                  `boil:"TeamMembers" json:"TeamMembers" toml:"TeamMembers" yaml:"TeamMembers"`
}

// NewStruct creates a new relationship struct
func (*teamR) NewStruct() *teamR {
	return &teamR{}
}

func (o *Team) GetTeamWarehouse() *TeamWarehouse {
	if o == nil {
		return nil
	}

	return o.R.GetTeamWarehouse()
}

func (r *teamR) GetTeamWarehouse() *TeamWarehouse {
	if r == nil {
		return nil
	}

	return r.TeamWarehouse
}

func (o *Team) GetTeamInvitations() TeamInvitationSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTeamInvitations()
}

func (r *teamR) GetTeamInvitations() TeamInvitationSlice {
	if r == nil {
		return nil
	}

	return r.TeamInvitations
}

func (o *Team) GetTeamJoinRequests() TeamJoinRequestSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTeamJoinRequests()
}

func (r *teamR) GetTeamJoinRequests() TeamJoinRequestSlice {
	if r == nil {
		return nil
	}

	return r.TeamJoinRequests
}

func (o *Team) GetTeamKickedRecords() TeamKickedRecordSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTeamKickedRecords()
}

func (r *teamR) GetTeamKickedRecords() TeamKickedRecordSlice {
	if r == nil {
		return nil
	}

	return r.TeamKickedRecords
}

func (o *Team) GetTeamLootDistributionHistories() TeamLootDistributionHistorySlice {
	if o == nil {
		return nil
	}

	return o.R.GetTeamLootDistributionHistories()
}

func (r *teamR) GetTeamLootDistributionHistories() TeamLootDistributionHistorySlice {
	if r == nil {
		return nil
	}

	return r.TeamLootDistributionHistories
}

func (o *Team) GetTeamMembers() TeamMemberSlice {
	if o == nil {
		return nil
	}

	return o.R.GetTeamMembers()
}

func (r *teamR) GetTeamMembers() TeamMemberSlice {
	if r == nil {
		return nil
	}

	return r.TeamMembers
}

// teamL is where Load methods for each relationship are stored.
type teamL struct{}

var (
	teamAllColumns            = []string{"id", "name", "description", "max_members", "leader_hero_id", "created_at", "updated_at", "deleted_at"}
	teamColumnsWithoutDefault = []string{"name", "leader_hero_id"}
	teamColumnsWithDefault    = []string{"id", "description", "max_members", "created_at", "updated_at", "deleted_at"}
	teamPrimaryKeyColumns     = []string{"id"}
	teamGeneratedColumns      = []string{}
)

type (
	// TeamSlice is an alias for a slice of pointers to Team.
	// This should almost always be used instead of []Team.
	TeamSlice []*Team
	// TeamHook is the signature for custom Team hook methods
	TeamHook func(context.Context, boil.ContextExecutor, *Team) error

	teamQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	teamType                 = reflect.TypeOf(&Team{})
	teamMapping              = queries.MakeStructMapping(teamType)
	teamPrimaryKeyMapping, _ = queries.BindMapping(teamType, teamMapping, teamPrimaryKeyColumns)
	teamInsertCacheMut       sync.RWMutex
	teamInsertCache          = make(map[string]insertCache)
	teamUpdateCacheMut       sync.RWMutex
	teamUpdateCache          = make(map[string]updateCache)
	teamUpsertCacheMut       sync.RWMutex
	teamUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var teamAfterSelectMu sync.Mutex
var teamAfterSelectHooks []TeamHook

var teamBeforeInsertMu sync.Mutex
var teamBeforeInsertHooks []TeamHook
var teamAfterInsertMu sync.Mutex
var teamAfterInsertHooks []TeamHook

var teamBeforeUpdateMu sync.Mutex
var teamBeforeUpdateHooks []TeamHook
var teamAfterUpdateMu sync.Mutex
var teamAfterUpdateHooks []TeamHook

var teamBeforeDeleteMu sync.Mutex
var teamBeforeDeleteHooks []TeamHook
var teamAfterDeleteMu sync.Mutex
var teamAfterDeleteHooks []TeamHook

var teamBeforeUpsertMu sync.Mutex
var teamBeforeUpsertHooks []TeamHook
var teamAfterUpsertMu sync.Mutex
var teamAfterUpsertHooks []TeamHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Team) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Team) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Team) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Team) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Team) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Team) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Team) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Team) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Team) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range teamAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTeamHook registers your hook function for all future operations.
func AddTeamHook(hookPoint boil.HookPoint, teamHook TeamHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		teamAfterSelectMu.Lock()
		teamAfterSelectHooks = append(teamAfterSelectHooks, teamHook)
		teamAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		teamBeforeInsertMu.Lock()
		teamBeforeInsertHooks = append(teamBeforeInsertHooks, teamHook)
		teamBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		teamAfterInsertMu.Lock()
		teamAfterInsertHooks = append(teamAfterInsertHooks, teamHook)
		teamAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		teamBeforeUpdateMu.Lock()
		teamBeforeUpdateHooks = append(teamBeforeUpdateHooks, teamHook)
		teamBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		teamAfterUpdateMu.Lock()
		teamAfterUpdateHooks = append(teamAfterUpdateHooks, teamHook)
		teamAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		teamBeforeDeleteMu.Lock()
		teamBeforeDeleteHooks = append(teamBeforeDeleteHooks, teamHook)
		teamBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		teamAfterDeleteMu.Lock()
		teamAfterDeleteHooks = append(teamAfterDeleteHooks, teamHook)
		teamAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		teamBeforeUpsertMu.Lock()
		teamBeforeUpsertHooks = append(teamBeforeUpsertHooks, teamHook)
		teamBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		teamAfterUpsertMu.Lock()
		teamAfterUpsertHooks = append(teamAfterUpsertHooks, teamHook)
		teamAfterUpsertMu.Unlock()
	}
}

// OneG returns a single team record from the query using the global executor.
func (q teamQuery) OneG(ctx context.Context) (*Team, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single team record from the query using the global executor, and panics on error.
func (q teamQuery) OneGP(ctx context.Context) *Team {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single team record from the query, and panics on error.
func (q teamQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *Team {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single team record from the query.
func (q teamQuery) One(ctx context.Context, exec boil.ContextExecutor) (*Team, error) {
	o := &Team{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for teams")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all Team records from the query using the global executor.
func (q teamQuery) AllG(ctx context.Context) (TeamSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all Team records from the query using the global executor, and panics on error.
func (q teamQuery) AllGP(ctx context.Context) TeamSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all Team records from the query, and panics on error.
func (q teamQuery) AllP(ctx context.Context, exec boil.ContextExecutor) TeamSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all Team records from the query.
func (q teamQuery) All(ctx context.Context, exec boil.ContextExecutor) (TeamSlice, error) {
	var o []*Team

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to Team slice")
	}

	if len(teamAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all Team records in the query using the global executor
func (q teamQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all Team records in the query using the global executor, and panics on error.
func (q teamQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all Team records in the query, and panics on error.
func (q teamQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all Team records in the query.
func (q teamQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count teams rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q teamQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q teamQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q teamQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q teamQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if teams exists")
	}

	return count > 0, nil
}

// TeamWarehouse pointed to by the foreign key.
func (o *Team) TeamWarehouse(mods ...qm.QueryMod) teamWarehouseQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"team_id\" = ?", o.ID),
	}

	queryMods = append(queryMods, mods...)

	return TeamWarehouses(queryMods...)
}

// TeamInvitations retrieves all the team_invitation's TeamInvitations with an executor.
func (o *Team) TeamInvitations(mods ...qm.QueryMod) teamInvitationQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_invitations\".\"team_id\"=?", o.ID),
	)

	return TeamInvitations(queryMods...)
}

// TeamJoinRequests retrieves all the team_join_request's TeamJoinRequests with an executor.
func (o *Team) TeamJoinRequests(mods ...qm.QueryMod) teamJoinRequestQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_join_requests\".\"team_id\"=?", o.ID),
	)

	return TeamJoinRequests(queryMods...)
}

// TeamKickedRecords retrieves all the team_kicked_record's TeamKickedRecords with an executor.
func (o *Team) TeamKickedRecords(mods ...qm.QueryMod) teamKickedRecordQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_kicked_records\".\"team_id\"=?", o.ID),
	)

	return TeamKickedRecords(queryMods...)
}

// TeamLootDistributionHistories retrieves all the team_loot_distribution_history's TeamLootDistributionHistories with an executor.
func (o *Team) TeamLootDistributionHistories(mods ...qm.QueryMod) teamLootDistributionHistoryQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_loot_distribution_history\".\"team_id\"=?", o.ID),
	)

	return TeamLootDistributionHistories(queryMods...)
}

// TeamMembers retrieves all the team_member's TeamMembers with an executor.
func (o *Team) TeamMembers(mods ...qm.QueryMod) teamMemberQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"game_runtime\".\"team_members\".\"team_id\"=?", o.ID),
	)

	return TeamMembers(queryMods...)
}

// LoadTeamWarehouse allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-1 relationship.
func (teamL) LoadTeamWarehouse(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeam interface{}, mods queries.Applicator) error {
	var slice []*Team
	var object *Team

	if singular {
		var ok bool
		object, ok = maybeTeam.(*Team)
		if !ok {
			object = new(Team)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeam))
			}
		}
	} else {
		s, ok := maybeTeam.(*[]*Team)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeam))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamR{}
			}

			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_warehouses`),
		qm.WhereIn(`game_runtime.team_warehouses.team_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TeamWarehouse")
	}

	var resultSlice []*TeamWarehouse
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TeamWarehouse")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for team_warehouses")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_warehouses")
	}

	if len(teamWarehouseAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TeamWarehouse = foreign
		if foreign.R == nil {
			foreign.R = &teamWarehouseR{}
		}
		foreign.R.Team = object
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ID == foreign.TeamID {
				local.R.TeamWarehouse = foreign
				if foreign.R == nil {
					foreign.R = &teamWarehouseR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// LoadTeamInvitations allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamL) LoadTeamInvitations(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeam interface{}, mods queries.Applicator) error {
	var slice []*Team
	var object *Team

	if singular {
		var ok bool
		object, ok = maybeTeam.(*Team)
		if !ok {
			object = new(Team)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeam))
			}
		}
	} else {
		s, ok := maybeTeam.(*[]*Team)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeam))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_invitations`),
		qm.WhereIn(`game_runtime.team_invitations.team_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_invitations")
	}

	var resultSlice []*TeamInvitation
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_invitations")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_invitations")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_invitations")
	}

	if len(teamInvitationAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamInvitations = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamInvitationR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.TeamID {
				local.R.TeamInvitations = append(local.R.TeamInvitations, foreign)
				if foreign.R == nil {
					foreign.R = &teamInvitationR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// LoadTeamJoinRequests allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamL) LoadTeamJoinRequests(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeam interface{}, mods queries.Applicator) error {
	var slice []*Team
	var object *Team

	if singular {
		var ok bool
		object, ok = maybeTeam.(*Team)
		if !ok {
			object = new(Team)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeam))
			}
		}
	} else {
		s, ok := maybeTeam.(*[]*Team)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeam))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_join_requests`),
		qm.WhereIn(`game_runtime.team_join_requests.team_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_join_requests")
	}

	var resultSlice []*TeamJoinRequest
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_join_requests")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_join_requests")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_join_requests")
	}

	if len(teamJoinRequestAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamJoinRequests = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamJoinRequestR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.TeamID {
				local.R.TeamJoinRequests = append(local.R.TeamJoinRequests, foreign)
				if foreign.R == nil {
					foreign.R = &teamJoinRequestR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// LoadTeamKickedRecords allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamL) LoadTeamKickedRecords(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeam interface{}, mods queries.Applicator) error {
	var slice []*Team
	var object *Team

	if singular {
		var ok bool
		object, ok = maybeTeam.(*Team)
		if !ok {
			object = new(Team)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeam))
			}
		}
	} else {
		s, ok := maybeTeam.(*[]*Team)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeam))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_kicked_records`),
		qm.WhereIn(`game_runtime.team_kicked_records.team_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_kicked_records")
	}

	var resultSlice []*TeamKickedRecord
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_kicked_records")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_kicked_records")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_kicked_records")
	}

	if len(teamKickedRecordAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamKickedRecords = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamKickedRecordR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.TeamID {
				local.R.TeamKickedRecords = append(local.R.TeamKickedRecords, foreign)
				if foreign.R == nil {
					foreign.R = &teamKickedRecordR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// LoadTeamLootDistributionHistories allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamL) LoadTeamLootDistributionHistories(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeam interface{}, mods queries.Applicator) error {
	var slice []*Team
	var object *Team

	if singular {
		var ok bool
		object, ok = maybeTeam.(*Team)
		if !ok {
			object = new(Team)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeam))
			}
		}
	} else {
		s, ok := maybeTeam.(*[]*Team)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeam))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_loot_distribution_history`),
		qm.WhereIn(`game_runtime.team_loot_distribution_history.team_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_loot_distribution_history")
	}

	var resultSlice []*TeamLootDistributionHistory
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_loot_distribution_history")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_loot_distribution_history")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_loot_distribution_history")
	}

	if len(teamLootDistributionHistoryAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamLootDistributionHistories = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamLootDistributionHistoryR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.TeamID {
				local.R.TeamLootDistributionHistories = append(local.R.TeamLootDistributionHistories, foreign)
				if foreign.R == nil {
					foreign.R = &teamLootDistributionHistoryR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// LoadTeamMembers allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (teamL) LoadTeamMembers(ctx context.Context, e boil.ContextExecutor, singular bool, maybeTeam interface{}, mods queries.Applicator) error {
	var slice []*Team
	var object *Team

	if singular {
		var ok bool
		object, ok = maybeTeam.(*Team)
		if !ok {
			object = new(Team)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeTeam))
			}
		}
	} else {
		s, ok := maybeTeam.(*[]*Team)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeTeam)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeTeam))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &teamR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &teamR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`game_runtime.team_members`),
		qm.WhereIn(`game_runtime.team_members.team_id in ?`, argsSlice...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load team_members")
	}

	var resultSlice []*TeamMember
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice team_members")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on team_members")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for team_members")
	}

	if len(teamMemberAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.TeamMembers = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &teamMemberR{}
			}
			foreign.R.Team = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.TeamID {
				local.R.TeamMembers = append(local.R.TeamMembers, foreign)
				if foreign.R == nil {
					foreign.R = &teamMemberR{}
				}
				foreign.R.Team = local
				break
			}
		}
	}

	return nil
}

// SetTeamWarehouseG of the team to the related item.
// Sets o.R.TeamWarehouse to related.
// Adds o to related.R.Team.
// Uses the global database handle.
func (o *Team) SetTeamWarehouseG(ctx context.Context, insert bool, related *TeamWarehouse) error {
	return o.SetTeamWarehouse(ctx, boil.GetContextDB(), insert, related)
}

// SetTeamWarehouseP of the team to the related item.
// Sets o.R.TeamWarehouse to related.
// Adds o to related.R.Team.
// Panics on error.
func (o *Team) SetTeamWarehouseP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TeamWarehouse) {
	if err := o.SetTeamWarehouse(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTeamWarehouseGP of the team to the related item.
// Sets o.R.TeamWarehouse to related.
// Adds o to related.R.Team.
// Uses the global database handle and panics on error.
func (o *Team) SetTeamWarehouseGP(ctx context.Context, insert bool, related *TeamWarehouse) {
	if err := o.SetTeamWarehouse(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetTeamWarehouse of the team to the related item.
// Sets o.R.TeamWarehouse to related.
// Adds o to related.R.Team.
func (o *Team) SetTeamWarehouse(ctx context.Context, exec boil.ContextExecutor, insert bool, related *TeamWarehouse) error {
	var err error

	if insert {
		related.TeamID = o.ID

		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	} else {
		updateQuery := fmt.Sprintf(
			"UPDATE \"game_runtime\".\"team_warehouses\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
			strmangle.WhereClause("\"", "\"", 2, teamWarehousePrimaryKeyColumns),
		)
		values := []interface{}{o.ID, related.ID}

		if boil.IsDebug(ctx) {
			writer := boil.DebugWriterFrom(ctx)
			fmt.Fprintln(writer, updateQuery)
			fmt.Fprintln(writer, values)
		}
		if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
			return errors.Wrap(err, "failed to update foreign table")
		}

		related.TeamID = o.ID
	}

	if o.R == nil {
		o.R = &teamR{
			TeamWarehouse: related,
		}
	} else {
		o.R.TeamWarehouse = related
	}

	if related.R == nil {
		related.R = &teamWarehouseR{
			Team: o,
		}
	} else {
		related.R.Team = o
	}
	return nil
}

// AddTeamInvitationsG adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamInvitations.
// Sets related.R.Team appropriately.
// Uses the global database handle.
func (o *Team) AddTeamInvitationsG(ctx context.Context, insert bool, related ...*TeamInvitation) error {
	return o.AddTeamInvitations(ctx, boil.GetContextDB(), insert, related...)
}

// AddTeamInvitationsP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamInvitations.
// Sets related.R.Team appropriately.
// Panics on error.
func (o *Team) AddTeamInvitationsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamInvitation) {
	if err := o.AddTeamInvitations(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamInvitationsGP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamInvitations.
// Sets related.R.Team appropriately.
// Uses the global database handle and panics on error.
func (o *Team) AddTeamInvitationsGP(ctx context.Context, insert bool, related ...*TeamInvitation) {
	if err := o.AddTeamInvitations(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamInvitations adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamInvitations.
// Sets related.R.Team appropriately.
func (o *Team) AddTeamInvitations(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamInvitation) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TeamID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_invitations\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamInvitationPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TeamID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamR{
			TeamInvitations: related,
		}
	} else {
		o.R.TeamInvitations = append(o.R.TeamInvitations, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamInvitationR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// AddTeamJoinRequestsG adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamJoinRequests.
// Sets related.R.Team appropriately.
// Uses the global database handle.
func (o *Team) AddTeamJoinRequestsG(ctx context.Context, insert bool, related ...*TeamJoinRequest) error {
	return o.AddTeamJoinRequests(ctx, boil.GetContextDB(), insert, related...)
}

// AddTeamJoinRequestsP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamJoinRequests.
// Sets related.R.Team appropriately.
// Panics on error.
func (o *Team) AddTeamJoinRequestsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamJoinRequest) {
	if err := o.AddTeamJoinRequests(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamJoinRequestsGP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamJoinRequests.
// Sets related.R.Team appropriately.
// Uses the global database handle and panics on error.
func (o *Team) AddTeamJoinRequestsGP(ctx context.Context, insert bool, related ...*TeamJoinRequest) {
	if err := o.AddTeamJoinRequests(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamJoinRequests adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamJoinRequests.
// Sets related.R.Team appropriately.
func (o *Team) AddTeamJoinRequests(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamJoinRequest) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TeamID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_join_requests\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamJoinRequestPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TeamID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamR{
			TeamJoinRequests: related,
		}
	} else {
		o.R.TeamJoinRequests = append(o.R.TeamJoinRequests, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamJoinRequestR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// AddTeamKickedRecordsG adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamKickedRecords.
// Sets related.R.Team appropriately.
// Uses the global database handle.
func (o *Team) AddTeamKickedRecordsG(ctx context.Context, insert bool, related ...*TeamKickedRecord) error {
	return o.AddTeamKickedRecords(ctx, boil.GetContextDB(), insert, related...)
}

// AddTeamKickedRecordsP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamKickedRecords.
// Sets related.R.Team appropriately.
// Panics on error.
func (o *Team) AddTeamKickedRecordsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamKickedRecord) {
	if err := o.AddTeamKickedRecords(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamKickedRecordsGP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamKickedRecords.
// Sets related.R.Team appropriately.
// Uses the global database handle and panics on error.
func (o *Team) AddTeamKickedRecordsGP(ctx context.Context, insert bool, related ...*TeamKickedRecord) {
	if err := o.AddTeamKickedRecords(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamKickedRecords adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamKickedRecords.
// Sets related.R.Team appropriately.
func (o *Team) AddTeamKickedRecords(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamKickedRecord) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TeamID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_kicked_records\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamKickedRecordPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TeamID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamR{
			TeamKickedRecords: related,
		}
	} else {
		o.R.TeamKickedRecords = append(o.R.TeamKickedRecords, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamKickedRecordR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// AddTeamLootDistributionHistoriesG adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamLootDistributionHistories.
// Sets related.R.Team appropriately.
// Uses the global database handle.
func (o *Team) AddTeamLootDistributionHistoriesG(ctx context.Context, insert bool, related ...*TeamLootDistributionHistory) error {
	return o.AddTeamLootDistributionHistories(ctx, boil.GetContextDB(), insert, related...)
}

// AddTeamLootDistributionHistoriesP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamLootDistributionHistories.
// Sets related.R.Team appropriately.
// Panics on error.
func (o *Team) AddTeamLootDistributionHistoriesP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamLootDistributionHistory) {
	if err := o.AddTeamLootDistributionHistories(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamLootDistributionHistoriesGP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamLootDistributionHistories.
// Sets related.R.Team appropriately.
// Uses the global database handle and panics on error.
func (o *Team) AddTeamLootDistributionHistoriesGP(ctx context.Context, insert bool, related ...*TeamLootDistributionHistory) {
	if err := o.AddTeamLootDistributionHistories(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamLootDistributionHistories adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamLootDistributionHistories.
// Sets related.R.Team appropriately.
func (o *Team) AddTeamLootDistributionHistories(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamLootDistributionHistory) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TeamID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_loot_distribution_history\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamLootDistributionHistoryPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TeamID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamR{
			TeamLootDistributionHistories: related,
		}
	} else {
		o.R.TeamLootDistributionHistories = append(o.R.TeamLootDistributionHistories, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamLootDistributionHistoryR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// AddTeamMembersG adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamMembers.
// Sets related.R.Team appropriately.
// Uses the global database handle.
func (o *Team) AddTeamMembersG(ctx context.Context, insert bool, related ...*TeamMember) error {
	return o.AddTeamMembers(ctx, boil.GetContextDB(), insert, related...)
}

// AddTeamMembersP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamMembers.
// Sets related.R.Team appropriately.
// Panics on error.
func (o *Team) AddTeamMembersP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamMember) {
	if err := o.AddTeamMembers(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamMembersGP adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamMembers.
// Sets related.R.Team appropriately.
// Uses the global database handle and panics on error.
func (o *Team) AddTeamMembersGP(ctx context.Context, insert bool, related ...*TeamMember) {
	if err := o.AddTeamMembers(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddTeamMembers adds the given related objects to the existing relationships
// of the team, optionally inserting them as new records.
// Appends related to o.R.TeamMembers.
// Sets related.R.Team appropriately.
func (o *Team) AddTeamMembers(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*TeamMember) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.TeamID = o.ID
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"game_runtime\".\"team_members\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"team_id"}),
				strmangle.WhereClause("\"", "\"", 2, teamMemberPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.TeamID = o.ID
		}
	}

	if o.R == nil {
		o.R = &teamR{
			TeamMembers: related,
		}
	} else {
		o.R.TeamMembers = append(o.R.TeamMembers, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &teamMemberR{
				Team: o,
			}
		} else {
			rel.R.Team = o
		}
	}
	return nil
}

// Teams retrieves all the records using an executor.
func Teams(mods ...qm.QueryMod) teamQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"teams\""), qmhelper.WhereIsNull("\"game_runtime\".\"teams\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"teams\".*"})
	}

	return teamQuery{q}
}

// FindTeamG retrieves a single record by ID.
func FindTeamG(ctx context.Context, iD string, selectCols ...string) (*Team, error) {
	return FindTeam(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindTeamP retrieves a single record by ID with an executor, and panics on error.
func FindTeamP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *Team {
	retobj, err := FindTeam(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeamGP retrieves a single record by ID, and panics on error.
func FindTeamGP(ctx context.Context, iD string, selectCols ...string) *Team {
	retobj, err := FindTeam(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindTeam retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTeam(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*Team, error) {
	teamObj := &Team{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"teams\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, teamObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from teams")
	}

	if err = teamObj.doAfterSelectHooks(ctx, exec); err != nil {
		return teamObj, err
	}

	return teamObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *Team) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *Team) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *Team) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Team) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no teams provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	teamInsertCacheMut.RLock()
	cache, cached := teamInsertCache[key]
	teamInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			teamAllColumns,
			teamColumnsWithDefault,
			teamColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(teamType, teamMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(teamType, teamMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"teams\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"teams\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into teams")
	}

	if !cached {
		teamInsertCacheMut.Lock()
		teamInsertCache[key] = cache
		teamInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single Team record using the global executor.
// See Update for more documentation.
func (o *Team) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the Team, and panics on error.
// See Update for more documentation.
func (o *Team) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single Team record using the global executor. Panics on error.
// See Update for more documentation.
func (o *Team) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the Team.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Team) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	teamUpdateCacheMut.RLock()
	cache, cached := teamUpdateCache[key]
	teamUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			teamAllColumns,
			teamPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update teams, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"teams\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, teamPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(teamType, teamMapping, append(wl, teamPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update teams row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for teams")
	}

	if !cached {
		teamUpdateCacheMut.Lock()
		teamUpdateCache[key] = cache
		teamUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q teamQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q teamQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q teamQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q teamQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for teams")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o TeamSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o TeamSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o TeamSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TeamSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"teams\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, teamPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in team slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all team")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *Team) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *Team) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *Team) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Team) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no teams provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(teamColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	teamUpsertCacheMut.RLock()
	cache, cached := teamUpsertCache[key]
	teamUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			teamAllColumns,
			teamColumnsWithDefault,
			teamColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			teamAllColumns,
			teamPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert teams, could not build update column list")
		}

		ret := strmangle.SetComplement(teamAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(teamPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert teams, could not build conflict column list")
			}

			conflict = make([]string, len(teamPrimaryKeyColumns))
			copy(conflict, teamPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"teams\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(teamType, teamMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(teamType, teamMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert teams")
	}

	if !cached {
		teamUpsertCacheMut.Lock()
		teamUpsertCache[key] = cache
		teamUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single Team record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *Team) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single Team record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *Team) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single Team record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *Team) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single Team record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Team) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no Team provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), teamPrimaryKeyMapping)
		sql = "DELETE FROM \"game_runtime\".\"teams\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_runtime\".\"teams\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(teamType, teamMapping, append(wl, teamPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for teams")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q teamQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q teamQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q teamQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q teamQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no teamQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from teams")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for teams")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o TeamSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o TeamSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o TeamSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TeamSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(teamBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"game_runtime\".\"teams\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"game_runtime\".\"teams\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, teamPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from team slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for teams")
	}

	if len(teamAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *Team) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no Team provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *Team) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *Team) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Team) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindTeam(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty TeamSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *TeamSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TeamSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TeamSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), teamPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"teams\".* FROM \"game_runtime\".\"teams\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, teamPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in TeamSlice")
	}

	*o = slice

	return nil
}

// TeamExistsG checks if the Team row exists.
func TeamExistsG(ctx context.Context, iD string) (bool, error) {
	return TeamExists(ctx, boil.GetContextDB(), iD)
}

// TeamExistsP checks if the Team row exists. Panics on error.
func TeamExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := TeamExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamExistsGP checks if the Team row exists. Panics on error.
func TeamExistsGP(ctx context.Context, iD string) bool {
	e, err := TeamExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// TeamExists checks if the Team row exists.
func TeamExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"teams\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if teams exists")
	}

	return exists, nil
}

// Exists checks if the Team row exists.
func (o *Team) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return TeamExists(ctx, exec, o.ID)
}
