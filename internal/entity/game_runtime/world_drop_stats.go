// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package game_runtime

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// WorldDropStat is an object representing the database table.
type WorldDropStat struct {
	ID                string `boil:"id" json:"id" toml:"id" yaml:"id"`
	WorldDropConfigID string `boil:"world_drop_config_id" json:"world_drop_config_id" toml:"world_drop_config_id" yaml:"world_drop_config_id"`
	ItemID            string `boil:"item_id" json:"item_id" toml:"item_id" yaml:"item_id"`
	// 总掉落数量 - 用于限制全局掉落总数
	TotalDropped int `boil:"total_dropped" json:"total_dropped" toml:"total_dropped" yaml:"total_dropped"`
	// 上次掉落时间 - 用于限制掉落间隔
	LastDropAt    null.Time `boil:"last_drop_at" json:"last_drop_at,omitempty" toml:"last_drop_at" yaml:"last_drop_at,omitempty"`
	DailyDropped  int       `boil:"daily_dropped" json:"daily_dropped" toml:"daily_dropped" yaml:"daily_dropped"`
	DailyResetAt  null.Time `boil:"daily_reset_at" json:"daily_reset_at,omitempty" toml:"daily_reset_at" yaml:"daily_reset_at,omitempty"`
	HourlyDropped int       `boil:"hourly_dropped" json:"hourly_dropped" toml:"hourly_dropped" yaml:"hourly_dropped"`
	HourlyResetAt null.Time `boil:"hourly_reset_at" json:"hourly_reset_at,omitempty" toml:"hourly_reset_at" yaml:"hourly_reset_at,omitempty"`
	CreatedAt     time.Time `boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt     time.Time `boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`

	R *worldDropStatR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L worldDropStatL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var WorldDropStatColumns = struct {
	ID                string
	WorldDropConfigID string
	ItemID            string
	TotalDropped      string
	LastDropAt        string
	DailyDropped      string
	DailyResetAt      string
	HourlyDropped     string
	HourlyResetAt     string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "id",
	WorldDropConfigID: "world_drop_config_id",
	ItemID:            "item_id",
	TotalDropped:      "total_dropped",
	LastDropAt:        "last_drop_at",
	DailyDropped:      "daily_dropped",
	DailyResetAt:      "daily_reset_at",
	HourlyDropped:     "hourly_dropped",
	HourlyResetAt:     "hourly_reset_at",
	CreatedAt:         "created_at",
	UpdatedAt:         "updated_at",
}

var WorldDropStatTableColumns = struct {
	ID                string
	WorldDropConfigID string
	ItemID            string
	TotalDropped      string
	LastDropAt        string
	DailyDropped      string
	DailyResetAt      string
	HourlyDropped     string
	HourlyResetAt     string
	CreatedAt         string
	UpdatedAt         string
}{
	ID:                "world_drop_stats.id",
	WorldDropConfigID: "world_drop_stats.world_drop_config_id",
	ItemID:            "world_drop_stats.item_id",
	TotalDropped:      "world_drop_stats.total_dropped",
	LastDropAt:        "world_drop_stats.last_drop_at",
	DailyDropped:      "world_drop_stats.daily_dropped",
	DailyResetAt:      "world_drop_stats.daily_reset_at",
	HourlyDropped:     "world_drop_stats.hourly_dropped",
	HourlyResetAt:     "world_drop_stats.hourly_reset_at",
	CreatedAt:         "world_drop_stats.created_at",
	UpdatedAt:         "world_drop_stats.updated_at",
}

// Generated where

var WorldDropStatWhere = struct {
	ID                whereHelperstring
	WorldDropConfigID whereHelperstring
	ItemID            whereHelperstring
	TotalDropped      whereHelperint
	LastDropAt        whereHelpernull_Time
	DailyDropped      whereHelperint
	DailyResetAt      whereHelpernull_Time
	HourlyDropped     whereHelperint
	HourlyResetAt     whereHelpernull_Time
	CreatedAt         whereHelpertime_Time
	UpdatedAt         whereHelpertime_Time
}{
	ID:                whereHelperstring{field: "\"game_runtime\".\"world_drop_stats\".\"id\""},
	WorldDropConfigID: whereHelperstring{field: "\"game_runtime\".\"world_drop_stats\".\"world_drop_config_id\""},
	ItemID:            whereHelperstring{field: "\"game_runtime\".\"world_drop_stats\".\"item_id\""},
	TotalDropped:      whereHelperint{field: "\"game_runtime\".\"world_drop_stats\".\"total_dropped\""},
	LastDropAt:        whereHelpernull_Time{field: "\"game_runtime\".\"world_drop_stats\".\"last_drop_at\""},
	DailyDropped:      whereHelperint{field: "\"game_runtime\".\"world_drop_stats\".\"daily_dropped\""},
	DailyResetAt:      whereHelpernull_Time{field: "\"game_runtime\".\"world_drop_stats\".\"daily_reset_at\""},
	HourlyDropped:     whereHelperint{field: "\"game_runtime\".\"world_drop_stats\".\"hourly_dropped\""},
	HourlyResetAt:     whereHelpernull_Time{field: "\"game_runtime\".\"world_drop_stats\".\"hourly_reset_at\""},
	CreatedAt:         whereHelpertime_Time{field: "\"game_runtime\".\"world_drop_stats\".\"created_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"game_runtime\".\"world_drop_stats\".\"updated_at\""},
}

// WorldDropStatRels is where relationship names are stored.
var WorldDropStatRels = struct {
}{}

// worldDropStatR is where relationships are stored.
type worldDropStatR struct {
}

// NewStruct creates a new relationship struct
func (*worldDropStatR) NewStruct() *worldDropStatR {
	return &worldDropStatR{}
}

// worldDropStatL is where Load methods for each relationship are stored.
type worldDropStatL struct{}

var (
	worldDropStatAllColumns            = []string{"id", "world_drop_config_id", "item_id", "total_dropped", "last_drop_at", "daily_dropped", "daily_reset_at", "hourly_dropped", "hourly_reset_at", "created_at", "updated_at"}
	worldDropStatColumnsWithoutDefault = []string{"world_drop_config_id", "item_id"}
	worldDropStatColumnsWithDefault    = []string{"id", "total_dropped", "last_drop_at", "daily_dropped", "daily_reset_at", "hourly_dropped", "hourly_reset_at", "created_at", "updated_at"}
	worldDropStatPrimaryKeyColumns     = []string{"id"}
	worldDropStatGeneratedColumns      = []string{}
)

type (
	// WorldDropStatSlice is an alias for a slice of pointers to WorldDropStat.
	// This should almost always be used instead of []WorldDropStat.
	WorldDropStatSlice []*WorldDropStat
	// WorldDropStatHook is the signature for custom WorldDropStat hook methods
	WorldDropStatHook func(context.Context, boil.ContextExecutor, *WorldDropStat) error

	worldDropStatQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	worldDropStatType                 = reflect.TypeOf(&WorldDropStat{})
	worldDropStatMapping              = queries.MakeStructMapping(worldDropStatType)
	worldDropStatPrimaryKeyMapping, _ = queries.BindMapping(worldDropStatType, worldDropStatMapping, worldDropStatPrimaryKeyColumns)
	worldDropStatInsertCacheMut       sync.RWMutex
	worldDropStatInsertCache          = make(map[string]insertCache)
	worldDropStatUpdateCacheMut       sync.RWMutex
	worldDropStatUpdateCache          = make(map[string]updateCache)
	worldDropStatUpsertCacheMut       sync.RWMutex
	worldDropStatUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var worldDropStatAfterSelectMu sync.Mutex
var worldDropStatAfterSelectHooks []WorldDropStatHook

var worldDropStatBeforeInsertMu sync.Mutex
var worldDropStatBeforeInsertHooks []WorldDropStatHook
var worldDropStatAfterInsertMu sync.Mutex
var worldDropStatAfterInsertHooks []WorldDropStatHook

var worldDropStatBeforeUpdateMu sync.Mutex
var worldDropStatBeforeUpdateHooks []WorldDropStatHook
var worldDropStatAfterUpdateMu sync.Mutex
var worldDropStatAfterUpdateHooks []WorldDropStatHook

var worldDropStatBeforeDeleteMu sync.Mutex
var worldDropStatBeforeDeleteHooks []WorldDropStatHook
var worldDropStatAfterDeleteMu sync.Mutex
var worldDropStatAfterDeleteHooks []WorldDropStatHook

var worldDropStatBeforeUpsertMu sync.Mutex
var worldDropStatBeforeUpsertHooks []WorldDropStatHook
var worldDropStatAfterUpsertMu sync.Mutex
var worldDropStatAfterUpsertHooks []WorldDropStatHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *WorldDropStat) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *WorldDropStat) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *WorldDropStat) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *WorldDropStat) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *WorldDropStat) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *WorldDropStat) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *WorldDropStat) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *WorldDropStat) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *WorldDropStat) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range worldDropStatAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddWorldDropStatHook registers your hook function for all future operations.
func AddWorldDropStatHook(hookPoint boil.HookPoint, worldDropStatHook WorldDropStatHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		worldDropStatAfterSelectMu.Lock()
		worldDropStatAfterSelectHooks = append(worldDropStatAfterSelectHooks, worldDropStatHook)
		worldDropStatAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		worldDropStatBeforeInsertMu.Lock()
		worldDropStatBeforeInsertHooks = append(worldDropStatBeforeInsertHooks, worldDropStatHook)
		worldDropStatBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		worldDropStatAfterInsertMu.Lock()
		worldDropStatAfterInsertHooks = append(worldDropStatAfterInsertHooks, worldDropStatHook)
		worldDropStatAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		worldDropStatBeforeUpdateMu.Lock()
		worldDropStatBeforeUpdateHooks = append(worldDropStatBeforeUpdateHooks, worldDropStatHook)
		worldDropStatBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		worldDropStatAfterUpdateMu.Lock()
		worldDropStatAfterUpdateHooks = append(worldDropStatAfterUpdateHooks, worldDropStatHook)
		worldDropStatAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		worldDropStatBeforeDeleteMu.Lock()
		worldDropStatBeforeDeleteHooks = append(worldDropStatBeforeDeleteHooks, worldDropStatHook)
		worldDropStatBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		worldDropStatAfterDeleteMu.Lock()
		worldDropStatAfterDeleteHooks = append(worldDropStatAfterDeleteHooks, worldDropStatHook)
		worldDropStatAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		worldDropStatBeforeUpsertMu.Lock()
		worldDropStatBeforeUpsertHooks = append(worldDropStatBeforeUpsertHooks, worldDropStatHook)
		worldDropStatBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		worldDropStatAfterUpsertMu.Lock()
		worldDropStatAfterUpsertHooks = append(worldDropStatAfterUpsertHooks, worldDropStatHook)
		worldDropStatAfterUpsertMu.Unlock()
	}
}

// OneG returns a single worldDropStat record from the query using the global executor.
func (q worldDropStatQuery) OneG(ctx context.Context) (*WorldDropStat, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single worldDropStat record from the query using the global executor, and panics on error.
func (q worldDropStatQuery) OneGP(ctx context.Context) *WorldDropStat {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single worldDropStat record from the query, and panics on error.
func (q worldDropStatQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *WorldDropStat {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single worldDropStat record from the query.
func (q worldDropStatQuery) One(ctx context.Context, exec boil.ContextExecutor) (*WorldDropStat, error) {
	o := &WorldDropStat{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: failed to execute a one query for world_drop_stats")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all WorldDropStat records from the query using the global executor.
func (q worldDropStatQuery) AllG(ctx context.Context) (WorldDropStatSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all WorldDropStat records from the query using the global executor, and panics on error.
func (q worldDropStatQuery) AllGP(ctx context.Context) WorldDropStatSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all WorldDropStat records from the query, and panics on error.
func (q worldDropStatQuery) AllP(ctx context.Context, exec boil.ContextExecutor) WorldDropStatSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all WorldDropStat records from the query.
func (q worldDropStatQuery) All(ctx context.Context, exec boil.ContextExecutor) (WorldDropStatSlice, error) {
	var o []*WorldDropStat

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "game_runtime: failed to assign all query results to WorldDropStat slice")
	}

	if len(worldDropStatAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all WorldDropStat records in the query using the global executor
func (q worldDropStatQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all WorldDropStat records in the query using the global executor, and panics on error.
func (q worldDropStatQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all WorldDropStat records in the query, and panics on error.
func (q worldDropStatQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all WorldDropStat records in the query.
func (q worldDropStatQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to count world_drop_stats rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q worldDropStatQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q worldDropStatQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q worldDropStatQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q worldDropStatQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: failed to check if world_drop_stats exists")
	}

	return count > 0, nil
}

// WorldDropStats retrieves all the records using an executor.
func WorldDropStats(mods ...qm.QueryMod) worldDropStatQuery {
	mods = append(mods, qm.From("\"game_runtime\".\"world_drop_stats\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"game_runtime\".\"world_drop_stats\".*"})
	}

	return worldDropStatQuery{q}
}

// FindWorldDropStatG retrieves a single record by ID.
func FindWorldDropStatG(ctx context.Context, iD string, selectCols ...string) (*WorldDropStat, error) {
	return FindWorldDropStat(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindWorldDropStatP retrieves a single record by ID with an executor, and panics on error.
func FindWorldDropStatP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *WorldDropStat {
	retobj, err := FindWorldDropStat(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindWorldDropStatGP retrieves a single record by ID, and panics on error.
func FindWorldDropStatGP(ctx context.Context, iD string, selectCols ...string) *WorldDropStat {
	retobj, err := FindWorldDropStat(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindWorldDropStat retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindWorldDropStat(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*WorldDropStat, error) {
	worldDropStatObj := &WorldDropStat{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"game_runtime\".\"world_drop_stats\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, worldDropStatObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "game_runtime: unable to select from world_drop_stats")
	}

	if err = worldDropStatObj.doAfterSelectHooks(ctx, exec); err != nil {
		return worldDropStatObj, err
	}

	return worldDropStatObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *WorldDropStat) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *WorldDropStat) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *WorldDropStat) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *WorldDropStat) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("game_runtime: no world_drop_stats provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		if o.UpdatedAt.IsZero() {
			o.UpdatedAt = currTime
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(worldDropStatColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	worldDropStatInsertCacheMut.RLock()
	cache, cached := worldDropStatInsertCache[key]
	worldDropStatInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			worldDropStatAllColumns,
			worldDropStatColumnsWithDefault,
			worldDropStatColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(worldDropStatType, worldDropStatMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(worldDropStatType, worldDropStatMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"game_runtime\".\"world_drop_stats\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"game_runtime\".\"world_drop_stats\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to insert into world_drop_stats")
	}

	if !cached {
		worldDropStatInsertCacheMut.Lock()
		worldDropStatInsertCache[key] = cache
		worldDropStatInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single WorldDropStat record using the global executor.
// See Update for more documentation.
func (o *WorldDropStat) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the WorldDropStat, and panics on error.
// See Update for more documentation.
func (o *WorldDropStat) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single WorldDropStat record using the global executor. Panics on error.
// See Update for more documentation.
func (o *WorldDropStat) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the WorldDropStat.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *WorldDropStat) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		o.UpdatedAt = currTime
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	worldDropStatUpdateCacheMut.RLock()
	cache, cached := worldDropStatUpdateCache[key]
	worldDropStatUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			worldDropStatAllColumns,
			worldDropStatPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("game_runtime: unable to update world_drop_stats, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"game_runtime\".\"world_drop_stats\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, worldDropStatPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(worldDropStatType, worldDropStatMapping, append(wl, worldDropStatPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update world_drop_stats row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by update for world_drop_stats")
	}

	if !cached {
		worldDropStatUpdateCacheMut.Lock()
		worldDropStatUpdateCache[key] = cache
		worldDropStatUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q worldDropStatQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q worldDropStatQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q worldDropStatQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q worldDropStatQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all for world_drop_stats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected for world_drop_stats")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o WorldDropStatSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o WorldDropStatSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o WorldDropStatSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o WorldDropStatSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("game_runtime: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropStatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"game_runtime\".\"world_drop_stats\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, worldDropStatPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to update all in worldDropStat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to retrieve rows affected all in update all worldDropStat")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *WorldDropStat) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *WorldDropStat) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *WorldDropStat) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *WorldDropStat) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("game_runtime: no world_drop_stats provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if o.CreatedAt.IsZero() {
			o.CreatedAt = currTime
		}
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(worldDropStatColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	worldDropStatUpsertCacheMut.RLock()
	cache, cached := worldDropStatUpsertCache[key]
	worldDropStatUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			worldDropStatAllColumns,
			worldDropStatColumnsWithDefault,
			worldDropStatColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			worldDropStatAllColumns,
			worldDropStatPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("game_runtime: unable to upsert world_drop_stats, could not build update column list")
		}

		ret := strmangle.SetComplement(worldDropStatAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(worldDropStatPrimaryKeyColumns) == 0 {
				return errors.New("game_runtime: unable to upsert world_drop_stats, could not build conflict column list")
			}

			conflict = make([]string, len(worldDropStatPrimaryKeyColumns))
			copy(conflict, worldDropStatPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"game_runtime\".\"world_drop_stats\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(worldDropStatType, worldDropStatMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(worldDropStatType, worldDropStatMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to upsert world_drop_stats")
	}

	if !cached {
		worldDropStatUpsertCacheMut.Lock()
		worldDropStatUpsertCache[key] = cache
		worldDropStatUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single WorldDropStat record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *WorldDropStat) DeleteG(ctx context.Context) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB())
}

// DeleteP deletes a single WorldDropStat record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *WorldDropStat) DeleteP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.Delete(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single WorldDropStat record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *WorldDropStat) DeleteGP(ctx context.Context) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single WorldDropStat record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *WorldDropStat) Delete(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if o == nil {
		return 0, errors.New("game_runtime: no WorldDropStat provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), worldDropStatPrimaryKeyMapping)
	sql := "DELETE FROM \"game_runtime\".\"world_drop_stats\" WHERE \"id\"=$1"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete from world_drop_stats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by delete for world_drop_stats")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q worldDropStatQuery) DeleteAllG(ctx context.Context) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows, and panics on error.
func (q worldDropStatQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q worldDropStatQuery) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q worldDropStatQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("game_runtime: no worldDropStatQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from world_drop_stats")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for world_drop_stats")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o WorldDropStatSlice) DeleteAllG(ctx context.Context) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB())
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o WorldDropStatSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o WorldDropStatSlice) DeleteAllGP(ctx context.Context) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o WorldDropStatSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(worldDropStatBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropStatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"game_runtime\".\"world_drop_stats\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, worldDropStatPrimaryKeyColumns, len(o))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: unable to delete all from worldDropStat slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "game_runtime: failed to get rows affected by deleteall for world_drop_stats")
	}

	if len(worldDropStatAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *WorldDropStat) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: no WorldDropStat provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *WorldDropStat) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *WorldDropStat) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *WorldDropStat) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindWorldDropStat(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorldDropStatSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("game_runtime: empty WorldDropStatSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *WorldDropStatSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *WorldDropStatSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *WorldDropStatSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := WorldDropStatSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), worldDropStatPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"game_runtime\".\"world_drop_stats\".* FROM \"game_runtime\".\"world_drop_stats\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, worldDropStatPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "game_runtime: unable to reload all in WorldDropStatSlice")
	}

	*o = slice

	return nil
}

// WorldDropStatExistsG checks if the WorldDropStat row exists.
func WorldDropStatExistsG(ctx context.Context, iD string) (bool, error) {
	return WorldDropStatExists(ctx, boil.GetContextDB(), iD)
}

// WorldDropStatExistsP checks if the WorldDropStat row exists. Panics on error.
func WorldDropStatExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := WorldDropStatExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// WorldDropStatExistsGP checks if the WorldDropStat row exists. Panics on error.
func WorldDropStatExistsGP(ctx context.Context, iD string) bool {
	e, err := WorldDropStatExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// WorldDropStatExists checks if the WorldDropStat row exists.
func WorldDropStatExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"game_runtime\".\"world_drop_stats\" where \"id\"=$1 limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "game_runtime: unable to check if world_drop_stats exists")
	}

	return exists, nil
}

// Exists checks if the WorldDropStat row exists.
func (o *WorldDropStat) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return WorldDropStatExists(ctx, exec, o.ID)
}
