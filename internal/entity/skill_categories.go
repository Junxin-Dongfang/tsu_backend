// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// SkillCategory is an object representing the database table.
type SkillCategory struct {
	ID           string      `boil:"id" json:"id" toml:"id" yaml:"id"`
	CategoryCode string      `boil:"category_code" json:"category_code" toml:"category_code" yaml:"category_code"`
	CategoryName string      `boil:"category_name" json:"category_name" toml:"category_name" yaml:"category_name"`
	Description  null.String `boil:"description" json:"description,omitempty" toml:"description" yaml:"description,omitempty"`
	Icon         null.String `boil:"icon" json:"icon,omitempty" toml:"icon" yaml:"icon,omitempty"`
	Color        null.String `boil:"color" json:"color,omitempty" toml:"color" yaml:"color,omitempty"`
	DisplayOrder null.Int    `boil:"display_order" json:"display_order,omitempty" toml:"display_order" yaml:"display_order,omitempty"`
	IsActive     null.Bool   `boil:"is_active" json:"is_active,omitempty" toml:"is_active" yaml:"is_active,omitempty"`
	CreatedAt    null.Time   `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	UpdatedAt    null.Time   `boil:"updated_at" json:"updated_at,omitempty" toml:"updated_at" yaml:"updated_at,omitempty"`
	DeletedAt    null.Time   `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *skillCategoryR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L skillCategoryL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SkillCategoryColumns = struct {
	ID           string
	CategoryCode string
	CategoryName string
	Description  string
	Icon         string
	Color        string
	DisplayOrder string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "id",
	CategoryCode: "category_code",
	CategoryName: "category_name",
	Description:  "description",
	Icon:         "icon",
	Color:        "color",
	DisplayOrder: "display_order",
	IsActive:     "is_active",
	CreatedAt:    "created_at",
	UpdatedAt:    "updated_at",
	DeletedAt:    "deleted_at",
}

var SkillCategoryTableColumns = struct {
	ID           string
	CategoryCode string
	CategoryName string
	Description  string
	Icon         string
	Color        string
	DisplayOrder string
	IsActive     string
	CreatedAt    string
	UpdatedAt    string
	DeletedAt    string
}{
	ID:           "skill_categories.id",
	CategoryCode: "skill_categories.category_code",
	CategoryName: "skill_categories.category_name",
	Description:  "skill_categories.description",
	Icon:         "skill_categories.icon",
	Color:        "skill_categories.color",
	DisplayOrder: "skill_categories.display_order",
	IsActive:     "skill_categories.is_active",
	CreatedAt:    "skill_categories.created_at",
	UpdatedAt:    "skill_categories.updated_at",
	DeletedAt:    "skill_categories.deleted_at",
}

// Generated where

var SkillCategoryWhere = struct {
	ID           whereHelperstring
	CategoryCode whereHelperstring
	CategoryName whereHelperstring
	Description  whereHelpernull_String
	Icon         whereHelpernull_String
	Color        whereHelpernull_String
	DisplayOrder whereHelpernull_Int
	IsActive     whereHelpernull_Bool
	CreatedAt    whereHelpernull_Time
	UpdatedAt    whereHelpernull_Time
	DeletedAt    whereHelpernull_Time
}{
	ID:           whereHelperstring{field: "\"skill_categories\".\"id\""},
	CategoryCode: whereHelperstring{field: "\"skill_categories\".\"category_code\""},
	CategoryName: whereHelperstring{field: "\"skill_categories\".\"category_name\""},
	Description:  whereHelpernull_String{field: "\"skill_categories\".\"description\""},
	Icon:         whereHelpernull_String{field: "\"skill_categories\".\"icon\""},
	Color:        whereHelpernull_String{field: "\"skill_categories\".\"color\""},
	DisplayOrder: whereHelpernull_Int{field: "\"skill_categories\".\"display_order\""},
	IsActive:     whereHelpernull_Bool{field: "\"skill_categories\".\"is_active\""},
	CreatedAt:    whereHelpernull_Time{field: "\"skill_categories\".\"created_at\""},
	UpdatedAt:    whereHelpernull_Time{field: "\"skill_categories\".\"updated_at\""},
	DeletedAt:    whereHelpernull_Time{field: "\"skill_categories\".\"deleted_at\""},
}

// SkillCategoryRels is where relationship names are stored.
var SkillCategoryRels = struct {
	CategorySkills string
}{
	CategorySkills: "CategorySkills",
}

// skillCategoryR is where relationships are stored.
type skillCategoryR struct {
	CategorySkills SkillSlice `boil:"CategorySkills" json:"CategorySkills" toml:"CategorySkills" yaml:"CategorySkills"`
}

// NewStruct creates a new relationship struct
func (*skillCategoryR) NewStruct() *skillCategoryR {
	return &skillCategoryR{}
}

func (o *SkillCategory) GetCategorySkills() SkillSlice {
	if o == nil {
		return nil
	}

	return o.R.GetCategorySkills()
}

func (r *skillCategoryR) GetCategorySkills() SkillSlice {
	if r == nil {
		return nil
	}

	return r.CategorySkills
}

// skillCategoryL is where Load methods for each relationship are stored.
type skillCategoryL struct{}

var (
	skillCategoryAllColumns            = []string{"id", "category_code", "category_name", "description", "icon", "color", "display_order", "is_active", "created_at", "updated_at", "deleted_at"}
	skillCategoryColumnsWithoutDefault = []string{"category_code", "category_name"}
	skillCategoryColumnsWithDefault    = []string{"id", "description", "icon", "color", "display_order", "is_active", "created_at", "updated_at", "deleted_at"}
	skillCategoryPrimaryKeyColumns     = []string{"id"}
	skillCategoryGeneratedColumns      = []string{}
)

type (
	// SkillCategorySlice is an alias for a slice of pointers to SkillCategory.
	// This should almost always be used instead of []SkillCategory.
	SkillCategorySlice []*SkillCategory
	// SkillCategoryHook is the signature for custom SkillCategory hook methods
	SkillCategoryHook func(context.Context, boil.ContextExecutor, *SkillCategory) error

	skillCategoryQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	skillCategoryType                 = reflect.TypeOf(&SkillCategory{})
	skillCategoryMapping              = queries.MakeStructMapping(skillCategoryType)
	skillCategoryPrimaryKeyMapping, _ = queries.BindMapping(skillCategoryType, skillCategoryMapping, skillCategoryPrimaryKeyColumns)
	skillCategoryInsertCacheMut       sync.RWMutex
	skillCategoryInsertCache          = make(map[string]insertCache)
	skillCategoryUpdateCacheMut       sync.RWMutex
	skillCategoryUpdateCache          = make(map[string]updateCache)
	skillCategoryUpsertCacheMut       sync.RWMutex
	skillCategoryUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var skillCategoryAfterSelectMu sync.Mutex
var skillCategoryAfterSelectHooks []SkillCategoryHook

var skillCategoryBeforeInsertMu sync.Mutex
var skillCategoryBeforeInsertHooks []SkillCategoryHook
var skillCategoryAfterInsertMu sync.Mutex
var skillCategoryAfterInsertHooks []SkillCategoryHook

var skillCategoryBeforeUpdateMu sync.Mutex
var skillCategoryBeforeUpdateHooks []SkillCategoryHook
var skillCategoryAfterUpdateMu sync.Mutex
var skillCategoryAfterUpdateHooks []SkillCategoryHook

var skillCategoryBeforeDeleteMu sync.Mutex
var skillCategoryBeforeDeleteHooks []SkillCategoryHook
var skillCategoryAfterDeleteMu sync.Mutex
var skillCategoryAfterDeleteHooks []SkillCategoryHook

var skillCategoryBeforeUpsertMu sync.Mutex
var skillCategoryBeforeUpsertHooks []SkillCategoryHook
var skillCategoryAfterUpsertMu sync.Mutex
var skillCategoryAfterUpsertHooks []SkillCategoryHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SkillCategory) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SkillCategory) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SkillCategory) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SkillCategory) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SkillCategory) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SkillCategory) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SkillCategory) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SkillCategory) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SkillCategory) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillCategoryAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSkillCategoryHook registers your hook function for all future operations.
func AddSkillCategoryHook(hookPoint boil.HookPoint, skillCategoryHook SkillCategoryHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		skillCategoryAfterSelectMu.Lock()
		skillCategoryAfterSelectHooks = append(skillCategoryAfterSelectHooks, skillCategoryHook)
		skillCategoryAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		skillCategoryBeforeInsertMu.Lock()
		skillCategoryBeforeInsertHooks = append(skillCategoryBeforeInsertHooks, skillCategoryHook)
		skillCategoryBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		skillCategoryAfterInsertMu.Lock()
		skillCategoryAfterInsertHooks = append(skillCategoryAfterInsertHooks, skillCategoryHook)
		skillCategoryAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		skillCategoryBeforeUpdateMu.Lock()
		skillCategoryBeforeUpdateHooks = append(skillCategoryBeforeUpdateHooks, skillCategoryHook)
		skillCategoryBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		skillCategoryAfterUpdateMu.Lock()
		skillCategoryAfterUpdateHooks = append(skillCategoryAfterUpdateHooks, skillCategoryHook)
		skillCategoryAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		skillCategoryBeforeDeleteMu.Lock()
		skillCategoryBeforeDeleteHooks = append(skillCategoryBeforeDeleteHooks, skillCategoryHook)
		skillCategoryBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		skillCategoryAfterDeleteMu.Lock()
		skillCategoryAfterDeleteHooks = append(skillCategoryAfterDeleteHooks, skillCategoryHook)
		skillCategoryAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		skillCategoryBeforeUpsertMu.Lock()
		skillCategoryBeforeUpsertHooks = append(skillCategoryBeforeUpsertHooks, skillCategoryHook)
		skillCategoryBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		skillCategoryAfterUpsertMu.Lock()
		skillCategoryAfterUpsertHooks = append(skillCategoryAfterUpsertHooks, skillCategoryHook)
		skillCategoryAfterUpsertMu.Unlock()
	}
}

// OneG returns a single skillCategory record from the query using the global executor.
func (q skillCategoryQuery) OneG(ctx context.Context) (*SkillCategory, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single skillCategory record from the query using the global executor, and panics on error.
func (q skillCategoryQuery) OneGP(ctx context.Context) *SkillCategory {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single skillCategory record from the query, and panics on error.
func (q skillCategoryQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *SkillCategory {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single skillCategory record from the query.
func (q skillCategoryQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SkillCategory, error) {
	o := &SkillCategory{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for skill_categories")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all SkillCategory records from the query using the global executor.
func (q skillCategoryQuery) AllG(ctx context.Context) (SkillCategorySlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all SkillCategory records from the query using the global executor, and panics on error.
func (q skillCategoryQuery) AllGP(ctx context.Context) SkillCategorySlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all SkillCategory records from the query, and panics on error.
func (q skillCategoryQuery) AllP(ctx context.Context, exec boil.ContextExecutor) SkillCategorySlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SkillCategory records from the query.
func (q skillCategoryQuery) All(ctx context.Context, exec boil.ContextExecutor) (SkillCategorySlice, error) {
	var o []*SkillCategory

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to SkillCategory slice")
	}

	if len(skillCategoryAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all SkillCategory records in the query using the global executor
func (q skillCategoryQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all SkillCategory records in the query using the global executor, and panics on error.
func (q skillCategoryQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all SkillCategory records in the query, and panics on error.
func (q skillCategoryQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SkillCategory records in the query.
func (q skillCategoryQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count skill_categories rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q skillCategoryQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q skillCategoryQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q skillCategoryQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q skillCategoryQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if skill_categories exists")
	}

	return count > 0, nil
}

// CategorySkills retrieves all the skill's Skills with an executor via category_id column.
func (o *SkillCategory) CategorySkills(mods ...qm.QueryMod) skillQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"skills\".\"category_id\"=?", o.ID),
	)

	return Skills(queryMods...)
}

// LoadCategorySkills allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (skillCategoryL) LoadCategorySkills(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSkillCategory interface{}, mods queries.Applicator) error {
	var slice []*SkillCategory
	var object *SkillCategory

	if singular {
		var ok bool
		object, ok = maybeSkillCategory.(*SkillCategory)
		if !ok {
			object = new(SkillCategory)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSkillCategory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSkillCategory))
			}
		}
	} else {
		s, ok := maybeSkillCategory.(*[]*SkillCategory)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSkillCategory)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSkillCategory))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &skillCategoryR{}
		}
		args[object.ID] = struct{}{}
	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &skillCategoryR{}
			}
			args[obj.ID] = struct{}{}
		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`skills`),
		qm.WhereIn(`skills.category_id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`skills.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load skills")
	}

	var resultSlice []*Skill
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice skills")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on skills")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for skills")
	}

	if len(skillAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.CategorySkills = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &skillR{}
			}
			foreign.R.Category = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.CategoryID) {
				local.R.CategorySkills = append(local.R.CategorySkills, foreign)
				if foreign.R == nil {
					foreign.R = &skillR{}
				}
				foreign.R.Category = local
				break
			}
		}
	}

	return nil
}

// AddCategorySkillsG adds the given related objects to the existing relationships
// of the skill_category, optionally inserting them as new records.
// Appends related to o.R.CategorySkills.
// Sets related.R.Category appropriately.
// Uses the global database handle.
func (o *SkillCategory) AddCategorySkillsG(ctx context.Context, insert bool, related ...*Skill) error {
	return o.AddCategorySkills(ctx, boil.GetContextDB(), insert, related...)
}

// AddCategorySkillsP adds the given related objects to the existing relationships
// of the skill_category, optionally inserting them as new records.
// Appends related to o.R.CategorySkills.
// Sets related.R.Category appropriately.
// Panics on error.
func (o *SkillCategory) AddCategorySkillsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Skill) {
	if err := o.AddCategorySkills(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddCategorySkillsGP adds the given related objects to the existing relationships
// of the skill_category, optionally inserting them as new records.
// Appends related to o.R.CategorySkills.
// Sets related.R.Category appropriately.
// Uses the global database handle and panics on error.
func (o *SkillCategory) AddCategorySkillsGP(ctx context.Context, insert bool, related ...*Skill) {
	if err := o.AddCategorySkills(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// AddCategorySkills adds the given related objects to the existing relationships
// of the skill_category, optionally inserting them as new records.
// Appends related to o.R.CategorySkills.
// Sets related.R.Category appropriately.
func (o *SkillCategory) AddCategorySkills(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Skill) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.CategoryID, o.ID)
			if err = rel.Insert(ctx, exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"skills\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"category_id"}),
				strmangle.WhereClause("\"", "\"", 2, skillPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.IsDebug(ctx) {
				writer := boil.DebugWriterFrom(ctx)
				fmt.Fprintln(writer, updateQuery)
				fmt.Fprintln(writer, values)
			}
			if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.CategoryID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &skillCategoryR{
			CategorySkills: related,
		}
	} else {
		o.R.CategorySkills = append(o.R.CategorySkills, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &skillR{
				Category: o,
			}
		} else {
			rel.R.Category = o
		}
	}
	return nil
}

// SetCategorySkillsG removes all previously related items of the
// skill_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Category's CategorySkills accordingly.
// Replaces o.R.CategorySkills with related.
// Sets related.R.Category's CategorySkills accordingly.
// Uses the global database handle.
func (o *SkillCategory) SetCategorySkillsG(ctx context.Context, insert bool, related ...*Skill) error {
	return o.SetCategorySkills(ctx, boil.GetContextDB(), insert, related...)
}

// SetCategorySkillsP removes all previously related items of the
// skill_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Category's CategorySkills accordingly.
// Replaces o.R.CategorySkills with related.
// Sets related.R.Category's CategorySkills accordingly.
// Panics on error.
func (o *SkillCategory) SetCategorySkillsP(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Skill) {
	if err := o.SetCategorySkills(ctx, exec, insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetCategorySkillsGP removes all previously related items of the
// skill_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Category's CategorySkills accordingly.
// Replaces o.R.CategorySkills with related.
// Sets related.R.Category's CategorySkills accordingly.
// Uses the global database handle and panics on error.
func (o *SkillCategory) SetCategorySkillsGP(ctx context.Context, insert bool, related ...*Skill) {
	if err := o.SetCategorySkills(ctx, boil.GetContextDB(), insert, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetCategorySkills removes all previously related items of the
// skill_category replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.Category's CategorySkills accordingly.
// Replaces o.R.CategorySkills with related.
// Sets related.R.Category's CategorySkills accordingly.
func (o *SkillCategory) SetCategorySkills(ctx context.Context, exec boil.ContextExecutor, insert bool, related ...*Skill) error {
	query := "update \"skills\" set \"category_id\" = null where \"category_id\" = $1"
	values := []interface{}{o.ID}
	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, query)
		fmt.Fprintln(writer, values)
	}
	_, err := exec.ExecContext(ctx, query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.CategorySkills {
			queries.SetScanner(&rel.CategoryID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.Category = nil
		}
		o.R.CategorySkills = nil
	}

	return o.AddCategorySkills(ctx, exec, insert, related...)
}

// RemoveCategorySkillsG relationships from objects passed in.
// Removes related items from R.CategorySkills (uses pointer comparison, removal does not keep order)
// Sets related.R.Category.
// Uses the global database handle.
func (o *SkillCategory) RemoveCategorySkillsG(ctx context.Context, related ...*Skill) error {
	return o.RemoveCategorySkills(ctx, boil.GetContextDB(), related...)
}

// RemoveCategorySkillsP relationships from objects passed in.
// Removes related items from R.CategorySkills (uses pointer comparison, removal does not keep order)
// Sets related.R.Category.
// Panics on error.
func (o *SkillCategory) RemoveCategorySkillsP(ctx context.Context, exec boil.ContextExecutor, related ...*Skill) {
	if err := o.RemoveCategorySkills(ctx, exec, related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveCategorySkillsGP relationships from objects passed in.
// Removes related items from R.CategorySkills (uses pointer comparison, removal does not keep order)
// Sets related.R.Category.
// Uses the global database handle and panics on error.
func (o *SkillCategory) RemoveCategorySkillsGP(ctx context.Context, related ...*Skill) {
	if err := o.RemoveCategorySkills(ctx, boil.GetContextDB(), related...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// RemoveCategorySkills relationships from objects passed in.
// Removes related items from R.CategorySkills (uses pointer comparison, removal does not keep order)
// Sets related.R.Category.
func (o *SkillCategory) RemoveCategorySkills(ctx context.Context, exec boil.ContextExecutor, related ...*Skill) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.CategoryID, nil)
		if rel.R != nil {
			rel.R.Category = nil
		}
		if _, err = rel.Update(ctx, exec, boil.Whitelist("category_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.CategorySkills {
			if rel != ri {
				continue
			}

			ln := len(o.R.CategorySkills)
			if ln > 1 && i < ln-1 {
				o.R.CategorySkills[i] = o.R.CategorySkills[ln-1]
			}
			o.R.CategorySkills = o.R.CategorySkills[:ln-1]
			break
		}
	}

	return nil
}

// SkillCategories retrieves all the records using an executor.
func SkillCategories(mods ...qm.QueryMod) skillCategoryQuery {
	mods = append(mods, qm.From("\"skill_categories\""), qmhelper.WhereIsNull("\"skill_categories\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"skill_categories\".*"})
	}

	return skillCategoryQuery{q}
}

// FindSkillCategoryG retrieves a single record by ID.
func FindSkillCategoryG(ctx context.Context, iD string, selectCols ...string) (*SkillCategory, error) {
	return FindSkillCategory(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindSkillCategoryP retrieves a single record by ID with an executor, and panics on error.
func FindSkillCategoryP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *SkillCategory {
	retobj, err := FindSkillCategory(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSkillCategoryGP retrieves a single record by ID, and panics on error.
func FindSkillCategoryGP(ctx context.Context, iD string, selectCols ...string) *SkillCategory {
	retobj, err := FindSkillCategory(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSkillCategory retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSkillCategory(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SkillCategory, error) {
	skillCategoryObj := &SkillCategory{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"skill_categories\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, skillCategoryObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from skill_categories")
	}

	if err = skillCategoryObj.doAfterSelectHooks(ctx, exec); err != nil {
		return skillCategoryObj, err
	}

	return skillCategoryObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SkillCategory) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SkillCategory) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SkillCategory) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SkillCategory) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no skill_categories provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		if queries.MustTime(o.UpdatedAt).IsZero() {
			queries.SetScanner(&o.UpdatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(skillCategoryColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	skillCategoryInsertCacheMut.RLock()
	cache, cached := skillCategoryInsertCache[key]
	skillCategoryInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			skillCategoryAllColumns,
			skillCategoryColumnsWithDefault,
			skillCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(skillCategoryType, skillCategoryMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(skillCategoryType, skillCategoryMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"skill_categories\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"skill_categories\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into skill_categories")
	}

	if !cached {
		skillCategoryInsertCacheMut.Lock()
		skillCategoryInsertCache[key] = cache
		skillCategoryInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single SkillCategory record using the global executor.
// See Update for more documentation.
func (o *SkillCategory) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the SkillCategory, and panics on error.
// See Update for more documentation.
func (o *SkillCategory) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single SkillCategory record using the global executor. Panics on error.
// See Update for more documentation.
func (o *SkillCategory) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the SkillCategory.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SkillCategory) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	skillCategoryUpdateCacheMut.RLock()
	cache, cached := skillCategoryUpdateCache[key]
	skillCategoryUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			skillCategoryAllColumns,
			skillCategoryPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update skill_categories, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"skill_categories\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, skillCategoryPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(skillCategoryType, skillCategoryMapping, append(wl, skillCategoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update skill_categories row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for skill_categories")
	}

	if !cached {
		skillCategoryUpdateCacheMut.Lock()
		skillCategoryUpdateCache[key] = cache
		skillCategoryUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q skillCategoryQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q skillCategoryQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q skillCategoryQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q skillCategoryQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for skill_categories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for skill_categories")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SkillCategorySlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SkillCategorySlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SkillCategorySlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SkillCategorySlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"skill_categories\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, skillCategoryPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in skillCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all skillCategory")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SkillCategory) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SkillCategory) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SkillCategory) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SkillCategory) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no skill_categories provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
		queries.SetScanner(&o.UpdatedAt, currTime)
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(skillCategoryColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	skillCategoryUpsertCacheMut.RLock()
	cache, cached := skillCategoryUpsertCache[key]
	skillCategoryUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			skillCategoryAllColumns,
			skillCategoryColumnsWithDefault,
			skillCategoryColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			skillCategoryAllColumns,
			skillCategoryPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert skill_categories, could not build update column list")
		}

		ret := strmangle.SetComplement(skillCategoryAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(skillCategoryPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert skill_categories, could not build conflict column list")
			}

			conflict = make([]string, len(skillCategoryPrimaryKeyColumns))
			copy(conflict, skillCategoryPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"skill_categories\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(skillCategoryType, skillCategoryMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(skillCategoryType, skillCategoryMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert skill_categories")
	}

	if !cached {
		skillCategoryUpsertCacheMut.Lock()
		skillCategoryUpsertCache[key] = cache
		skillCategoryUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single SkillCategory record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SkillCategory) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single SkillCategory record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SkillCategory) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single SkillCategory record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SkillCategory) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single SkillCategory record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SkillCategory) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no SkillCategory provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), skillCategoryPrimaryKeyMapping)
		sql = "DELETE FROM \"skill_categories\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"skill_categories\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(skillCategoryType, skillCategoryMapping, append(wl, skillCategoryPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from skill_categories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for skill_categories")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q skillCategoryQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q skillCategoryQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q skillCategoryQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q skillCategoryQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no skillCategoryQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from skill_categories")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for skill_categories")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o SkillCategorySlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SkillCategorySlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SkillCategorySlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SkillCategorySlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(skillCategoryBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillCategoryPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"skill_categories\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, skillCategoryPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillCategoryPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"skill_categories\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, skillCategoryPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from skillCategory slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for skill_categories")
	}

	if len(skillCategoryAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SkillCategory) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("entity: no SkillCategory provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SkillCategory) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SkillCategory) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SkillCategory) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSkillCategory(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SkillCategorySlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("entity: empty SkillCategorySlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SkillCategorySlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SkillCategorySlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SkillCategorySlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SkillCategorySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillCategoryPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"skill_categories\".* FROM \"skill_categories\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, skillCategoryPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in SkillCategorySlice")
	}

	*o = slice

	return nil
}

// SkillCategoryExistsG checks if the SkillCategory row exists.
func SkillCategoryExistsG(ctx context.Context, iD string) (bool, error) {
	return SkillCategoryExists(ctx, boil.GetContextDB(), iD)
}

// SkillCategoryExistsP checks if the SkillCategory row exists. Panics on error.
func SkillCategoryExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := SkillCategoryExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SkillCategoryExistsGP checks if the SkillCategory row exists. Panics on error.
func SkillCategoryExistsGP(ctx context.Context, iD string) bool {
	e, err := SkillCategoryExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SkillCategoryExists checks if the SkillCategory row exists.
func SkillCategoryExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"skill_categories\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if skill_categories exists")
	}

	return exists, nil
}

// Exists checks if the SkillCategory row exists.
func (o *SkillCategory) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SkillCategoryExists(ctx, exec, o.ID)
}
