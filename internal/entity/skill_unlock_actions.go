// Code generated by SQLBoiler 4.19.5 (https://github.com/aarondl/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package entity

import (
	"context"
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/aarondl/null/v8"
	"github.com/aarondl/sqlboiler/v4/boil"
	"github.com/aarondl/sqlboiler/v4/queries"
	"github.com/aarondl/sqlboiler/v4/queries/qm"
	"github.com/aarondl/sqlboiler/v4/queries/qmhelper"
	"github.com/aarondl/strmangle"
	"github.com/friendsofgo/errors"
)

// SkillUnlockAction is an object representing the database table.
type SkillUnlockAction struct {
	ID          string    `boil:"id" json:"id" toml:"id" yaml:"id"`
	SkillID     string    `boil:"skill_id" json:"skill_id" toml:"skill_id" yaml:"skill_id"`
	ActionID    string    `boil:"action_id" json:"action_id" toml:"action_id" yaml:"action_id"`
	UnlockLevel int       `boil:"unlock_level" json:"unlock_level" toml:"unlock_level" yaml:"unlock_level"`
	IsDefault   null.Bool `boil:"is_default" json:"is_default,omitempty" toml:"is_default" yaml:"is_default,omitempty"`
	CreatedAt   null.Time `boil:"created_at" json:"created_at,omitempty" toml:"created_at" yaml:"created_at,omitempty"`
	DeletedAt   null.Time `boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *skillUnlockActionR `boil:"-" json:"-" toml:"-" yaml:"-"`
	L skillUnlockActionL  `boil:"-" json:"-" toml:"-" yaml:"-"`
}

var SkillUnlockActionColumns = struct {
	ID          string
	SkillID     string
	ActionID    string
	UnlockLevel string
	IsDefault   string
	CreatedAt   string
	DeletedAt   string
}{
	ID:          "id",
	SkillID:     "skill_id",
	ActionID:    "action_id",
	UnlockLevel: "unlock_level",
	IsDefault:   "is_default",
	CreatedAt:   "created_at",
	DeletedAt:   "deleted_at",
}

var SkillUnlockActionTableColumns = struct {
	ID          string
	SkillID     string
	ActionID    string
	UnlockLevel string
	IsDefault   string
	CreatedAt   string
	DeletedAt   string
}{
	ID:          "skill_unlock_actions.id",
	SkillID:     "skill_unlock_actions.skill_id",
	ActionID:    "skill_unlock_actions.action_id",
	UnlockLevel: "skill_unlock_actions.unlock_level",
	IsDefault:   "skill_unlock_actions.is_default",
	CreatedAt:   "skill_unlock_actions.created_at",
	DeletedAt:   "skill_unlock_actions.deleted_at",
}

// Generated where

var SkillUnlockActionWhere = struct {
	ID          whereHelperstring
	SkillID     whereHelperstring
	ActionID    whereHelperstring
	UnlockLevel whereHelperint
	IsDefault   whereHelpernull_Bool
	CreatedAt   whereHelpernull_Time
	DeletedAt   whereHelpernull_Time
}{
	ID:          whereHelperstring{field: "\"skill_unlock_actions\".\"id\""},
	SkillID:     whereHelperstring{field: "\"skill_unlock_actions\".\"skill_id\""},
	ActionID:    whereHelperstring{field: "\"skill_unlock_actions\".\"action_id\""},
	UnlockLevel: whereHelperint{field: "\"skill_unlock_actions\".\"unlock_level\""},
	IsDefault:   whereHelpernull_Bool{field: "\"skill_unlock_actions\".\"is_default\""},
	CreatedAt:   whereHelpernull_Time{field: "\"skill_unlock_actions\".\"created_at\""},
	DeletedAt:   whereHelpernull_Time{field: "\"skill_unlock_actions\".\"deleted_at\""},
}

// SkillUnlockActionRels is where relationship names are stored.
var SkillUnlockActionRels = struct {
	Action string
	Skill  string
}{
	Action: "Action",
	Skill:  "Skill",
}

// skillUnlockActionR is where relationships are stored.
type skillUnlockActionR struct {
	Action *Action `boil:"Action" json:"Action" toml:"Action" yaml:"Action"`
	Skill  *Skill  `boil:"Skill" json:"Skill" toml:"Skill" yaml:"Skill"`
}

// NewStruct creates a new relationship struct
func (*skillUnlockActionR) NewStruct() *skillUnlockActionR {
	return &skillUnlockActionR{}
}

func (o *SkillUnlockAction) GetAction() *Action {
	if o == nil {
		return nil
	}

	return o.R.GetAction()
}

func (r *skillUnlockActionR) GetAction() *Action {
	if r == nil {
		return nil
	}

	return r.Action
}

func (o *SkillUnlockAction) GetSkill() *Skill {
	if o == nil {
		return nil
	}

	return o.R.GetSkill()
}

func (r *skillUnlockActionR) GetSkill() *Skill {
	if r == nil {
		return nil
	}

	return r.Skill
}

// skillUnlockActionL is where Load methods for each relationship are stored.
type skillUnlockActionL struct{}

var (
	skillUnlockActionAllColumns            = []string{"id", "skill_id", "action_id", "unlock_level", "is_default", "created_at", "deleted_at"}
	skillUnlockActionColumnsWithoutDefault = []string{"skill_id", "action_id"}
	skillUnlockActionColumnsWithDefault    = []string{"id", "unlock_level", "is_default", "created_at", "deleted_at"}
	skillUnlockActionPrimaryKeyColumns     = []string{"id"}
	skillUnlockActionGeneratedColumns      = []string{}
)

type (
	// SkillUnlockActionSlice is an alias for a slice of pointers to SkillUnlockAction.
	// This should almost always be used instead of []SkillUnlockAction.
	SkillUnlockActionSlice []*SkillUnlockAction
	// SkillUnlockActionHook is the signature for custom SkillUnlockAction hook methods
	SkillUnlockActionHook func(context.Context, boil.ContextExecutor, *SkillUnlockAction) error

	skillUnlockActionQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	skillUnlockActionType                 = reflect.TypeOf(&SkillUnlockAction{})
	skillUnlockActionMapping              = queries.MakeStructMapping(skillUnlockActionType)
	skillUnlockActionPrimaryKeyMapping, _ = queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, skillUnlockActionPrimaryKeyColumns)
	skillUnlockActionInsertCacheMut       sync.RWMutex
	skillUnlockActionInsertCache          = make(map[string]insertCache)
	skillUnlockActionUpdateCacheMut       sync.RWMutex
	skillUnlockActionUpdateCache          = make(map[string]updateCache)
	skillUnlockActionUpsertCacheMut       sync.RWMutex
	skillUnlockActionUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var skillUnlockActionAfterSelectMu sync.Mutex
var skillUnlockActionAfterSelectHooks []SkillUnlockActionHook

var skillUnlockActionBeforeInsertMu sync.Mutex
var skillUnlockActionBeforeInsertHooks []SkillUnlockActionHook
var skillUnlockActionAfterInsertMu sync.Mutex
var skillUnlockActionAfterInsertHooks []SkillUnlockActionHook

var skillUnlockActionBeforeUpdateMu sync.Mutex
var skillUnlockActionBeforeUpdateHooks []SkillUnlockActionHook
var skillUnlockActionAfterUpdateMu sync.Mutex
var skillUnlockActionAfterUpdateHooks []SkillUnlockActionHook

var skillUnlockActionBeforeDeleteMu sync.Mutex
var skillUnlockActionBeforeDeleteHooks []SkillUnlockActionHook
var skillUnlockActionAfterDeleteMu sync.Mutex
var skillUnlockActionAfterDeleteHooks []SkillUnlockActionHook

var skillUnlockActionBeforeUpsertMu sync.Mutex
var skillUnlockActionBeforeUpsertHooks []SkillUnlockActionHook
var skillUnlockActionAfterUpsertMu sync.Mutex
var skillUnlockActionAfterUpsertHooks []SkillUnlockActionHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *SkillUnlockAction) doAfterSelectHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionAfterSelectHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *SkillUnlockAction) doBeforeInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionBeforeInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *SkillUnlockAction) doAfterInsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionAfterInsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *SkillUnlockAction) doBeforeUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionBeforeUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *SkillUnlockAction) doAfterUpdateHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionAfterUpdateHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *SkillUnlockAction) doBeforeDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionBeforeDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *SkillUnlockAction) doAfterDeleteHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionAfterDeleteHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *SkillUnlockAction) doBeforeUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionBeforeUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *SkillUnlockAction) doAfterUpsertHooks(ctx context.Context, exec boil.ContextExecutor) (err error) {
	if boil.HooksAreSkipped(ctx) {
		return nil
	}

	for _, hook := range skillUnlockActionAfterUpsertHooks {
		if err := hook(ctx, exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddSkillUnlockActionHook registers your hook function for all future operations.
func AddSkillUnlockActionHook(hookPoint boil.HookPoint, skillUnlockActionHook SkillUnlockActionHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		skillUnlockActionAfterSelectMu.Lock()
		skillUnlockActionAfterSelectHooks = append(skillUnlockActionAfterSelectHooks, skillUnlockActionHook)
		skillUnlockActionAfterSelectMu.Unlock()
	case boil.BeforeInsertHook:
		skillUnlockActionBeforeInsertMu.Lock()
		skillUnlockActionBeforeInsertHooks = append(skillUnlockActionBeforeInsertHooks, skillUnlockActionHook)
		skillUnlockActionBeforeInsertMu.Unlock()
	case boil.AfterInsertHook:
		skillUnlockActionAfterInsertMu.Lock()
		skillUnlockActionAfterInsertHooks = append(skillUnlockActionAfterInsertHooks, skillUnlockActionHook)
		skillUnlockActionAfterInsertMu.Unlock()
	case boil.BeforeUpdateHook:
		skillUnlockActionBeforeUpdateMu.Lock()
		skillUnlockActionBeforeUpdateHooks = append(skillUnlockActionBeforeUpdateHooks, skillUnlockActionHook)
		skillUnlockActionBeforeUpdateMu.Unlock()
	case boil.AfterUpdateHook:
		skillUnlockActionAfterUpdateMu.Lock()
		skillUnlockActionAfterUpdateHooks = append(skillUnlockActionAfterUpdateHooks, skillUnlockActionHook)
		skillUnlockActionAfterUpdateMu.Unlock()
	case boil.BeforeDeleteHook:
		skillUnlockActionBeforeDeleteMu.Lock()
		skillUnlockActionBeforeDeleteHooks = append(skillUnlockActionBeforeDeleteHooks, skillUnlockActionHook)
		skillUnlockActionBeforeDeleteMu.Unlock()
	case boil.AfterDeleteHook:
		skillUnlockActionAfterDeleteMu.Lock()
		skillUnlockActionAfterDeleteHooks = append(skillUnlockActionAfterDeleteHooks, skillUnlockActionHook)
		skillUnlockActionAfterDeleteMu.Unlock()
	case boil.BeforeUpsertHook:
		skillUnlockActionBeforeUpsertMu.Lock()
		skillUnlockActionBeforeUpsertHooks = append(skillUnlockActionBeforeUpsertHooks, skillUnlockActionHook)
		skillUnlockActionBeforeUpsertMu.Unlock()
	case boil.AfterUpsertHook:
		skillUnlockActionAfterUpsertMu.Lock()
		skillUnlockActionAfterUpsertHooks = append(skillUnlockActionAfterUpsertHooks, skillUnlockActionHook)
		skillUnlockActionAfterUpsertMu.Unlock()
	}
}

// OneG returns a single skillUnlockAction record from the query using the global executor.
func (q skillUnlockActionQuery) OneG(ctx context.Context) (*SkillUnlockAction, error) {
	return q.One(ctx, boil.GetContextDB())
}

// OneGP returns a single skillUnlockAction record from the query using the global executor, and panics on error.
func (q skillUnlockActionQuery) OneGP(ctx context.Context) *SkillUnlockAction {
	o, err := q.One(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// OneP returns a single skillUnlockAction record from the query, and panics on error.
func (q skillUnlockActionQuery) OneP(ctx context.Context, exec boil.ContextExecutor) *SkillUnlockAction {
	o, err := q.One(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// One returns a single skillUnlockAction record from the query.
func (q skillUnlockActionQuery) One(ctx context.Context, exec boil.ContextExecutor) (*SkillUnlockAction, error) {
	o := &SkillUnlockAction{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(ctx, exec, o)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: failed to execute a one query for skill_unlock_actions")
	}

	if err := o.doAfterSelectHooks(ctx, exec); err != nil {
		return o, err
	}

	return o, nil
}

// AllG returns all SkillUnlockAction records from the query using the global executor.
func (q skillUnlockActionQuery) AllG(ctx context.Context) (SkillUnlockActionSlice, error) {
	return q.All(ctx, boil.GetContextDB())
}

// AllGP returns all SkillUnlockAction records from the query using the global executor, and panics on error.
func (q skillUnlockActionQuery) AllGP(ctx context.Context) SkillUnlockActionSlice {
	o, err := q.All(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// AllP returns all SkillUnlockAction records from the query, and panics on error.
func (q skillUnlockActionQuery) AllP(ctx context.Context, exec boil.ContextExecutor) SkillUnlockActionSlice {
	o, err := q.All(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return o
}

// All returns all SkillUnlockAction records from the query.
func (q skillUnlockActionQuery) All(ctx context.Context, exec boil.ContextExecutor) (SkillUnlockActionSlice, error) {
	var o []*SkillUnlockAction

	err := q.Bind(ctx, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "entity: failed to assign all query results to SkillUnlockAction slice")
	}

	if len(skillUnlockActionAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(ctx, exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// CountG returns the count of all SkillUnlockAction records in the query using the global executor
func (q skillUnlockActionQuery) CountG(ctx context.Context) (int64, error) {
	return q.Count(ctx, boil.GetContextDB())
}

// CountGP returns the count of all SkillUnlockAction records in the query using the global executor, and panics on error.
func (q skillUnlockActionQuery) CountGP(ctx context.Context) int64 {
	c, err := q.Count(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// CountP returns the count of all SkillUnlockAction records in the query, and panics on error.
func (q skillUnlockActionQuery) CountP(ctx context.Context, exec boil.ContextExecutor) int64 {
	c, err := q.Count(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return c
}

// Count returns the count of all SkillUnlockAction records in the query.
func (q skillUnlockActionQuery) Count(ctx context.Context, exec boil.ContextExecutor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to count skill_unlock_actions rows")
	}

	return count, nil
}

// ExistsG checks if the row exists in the table using the global executor.
func (q skillUnlockActionQuery) ExistsG(ctx context.Context) (bool, error) {
	return q.Exists(ctx, boil.GetContextDB())
}

// ExistsGP checks if the row exists in the table using the global executor, and panics on error.
func (q skillUnlockActionQuery) ExistsGP(ctx context.Context) bool {
	e, err := q.Exists(ctx, boil.GetContextDB())
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// ExistsP checks if the row exists in the table, and panics on error.
func (q skillUnlockActionQuery) ExistsP(ctx context.Context, exec boil.ContextExecutor) bool {
	e, err := q.Exists(ctx, exec)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// Exists checks if the row exists in the table.
func (q skillUnlockActionQuery) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRowContext(ctx, exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "entity: failed to check if skill_unlock_actions exists")
	}

	return count > 0, nil
}

// Action pointed to by the foreign key.
func (o *SkillUnlockAction) Action(mods ...qm.QueryMod) actionQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ActionID),
	}

	queryMods = append(queryMods, mods...)

	return Actions(queryMods...)
}

// Skill pointed to by the foreign key.
func (o *SkillUnlockAction) Skill(mods ...qm.QueryMod) skillQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.SkillID),
	}

	queryMods = append(queryMods, mods...)

	return Skills(queryMods...)
}

// LoadAction allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (skillUnlockActionL) LoadAction(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSkillUnlockAction interface{}, mods queries.Applicator) error {
	var slice []*SkillUnlockAction
	var object *SkillUnlockAction

	if singular {
		var ok bool
		object, ok = maybeSkillUnlockAction.(*SkillUnlockAction)
		if !ok {
			object = new(SkillUnlockAction)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSkillUnlockAction)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSkillUnlockAction))
			}
		}
	} else {
		s, ok := maybeSkillUnlockAction.(*[]*SkillUnlockAction)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSkillUnlockAction)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSkillUnlockAction))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &skillUnlockActionR{}
		}
		args[object.ActionID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &skillUnlockActionR{}
			}

			args[obj.ActionID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`actions`),
		qm.WhereIn(`actions.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`actions.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Action")
	}

	var resultSlice []*Action
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Action")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for actions")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for actions")
	}

	if len(actionAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Action = foreign
		if foreign.R == nil {
			foreign.R = &actionR{}
		}
		foreign.R.SkillUnlockActions = append(foreign.R.SkillUnlockActions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ActionID == foreign.ID {
				local.R.Action = foreign
				if foreign.R == nil {
					foreign.R = &actionR{}
				}
				foreign.R.SkillUnlockActions = append(foreign.R.SkillUnlockActions, local)
				break
			}
		}
	}

	return nil
}

// LoadSkill allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (skillUnlockActionL) LoadSkill(ctx context.Context, e boil.ContextExecutor, singular bool, maybeSkillUnlockAction interface{}, mods queries.Applicator) error {
	var slice []*SkillUnlockAction
	var object *SkillUnlockAction

	if singular {
		var ok bool
		object, ok = maybeSkillUnlockAction.(*SkillUnlockAction)
		if !ok {
			object = new(SkillUnlockAction)
			ok = queries.SetFromEmbeddedStruct(&object, &maybeSkillUnlockAction)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", object, maybeSkillUnlockAction))
			}
		}
	} else {
		s, ok := maybeSkillUnlockAction.(*[]*SkillUnlockAction)
		if ok {
			slice = *s
		} else {
			ok = queries.SetFromEmbeddedStruct(&slice, maybeSkillUnlockAction)
			if !ok {
				return errors.New(fmt.Sprintf("failed to set %T from embedded struct %T", slice, maybeSkillUnlockAction))
			}
		}
	}

	args := make(map[interface{}]struct{})
	if singular {
		if object.R == nil {
			object.R = &skillUnlockActionR{}
		}
		args[object.SkillID] = struct{}{}

	} else {
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &skillUnlockActionR{}
			}

			args[obj.SkillID] = struct{}{}

		}
	}

	if len(args) == 0 {
		return nil
	}

	argsSlice := make([]interface{}, len(args))
	i := 0
	for arg := range args {
		argsSlice[i] = arg
		i++
	}

	query := NewQuery(
		qm.From(`skills`),
		qm.WhereIn(`skills.id in ?`, argsSlice...),
		qmhelper.WhereIsNull(`skills.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.QueryContext(ctx, e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Skill")
	}

	var resultSlice []*Skill
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Skill")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for skills")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for skills")
	}

	if len(skillAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(ctx, e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Skill = foreign
		if foreign.R == nil {
			foreign.R = &skillR{}
		}
		foreign.R.SkillUnlockActions = append(foreign.R.SkillUnlockActions, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.SkillID == foreign.ID {
				local.R.Skill = foreign
				if foreign.R == nil {
					foreign.R = &skillR{}
				}
				foreign.R.SkillUnlockActions = append(foreign.R.SkillUnlockActions, local)
				break
			}
		}
	}

	return nil
}

// SetActionG of the skillUnlockAction to the related item.
// Sets o.R.Action to related.
// Adds o to related.R.SkillUnlockActions.
// Uses the global database handle.
func (o *SkillUnlockAction) SetActionG(ctx context.Context, insert bool, related *Action) error {
	return o.SetAction(ctx, boil.GetContextDB(), insert, related)
}

// SetActionP of the skillUnlockAction to the related item.
// Sets o.R.Action to related.
// Adds o to related.R.SkillUnlockActions.
// Panics on error.
func (o *SkillUnlockAction) SetActionP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Action) {
	if err := o.SetAction(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetActionGP of the skillUnlockAction to the related item.
// Sets o.R.Action to related.
// Adds o to related.R.SkillUnlockActions.
// Uses the global database handle and panics on error.
func (o *SkillUnlockAction) SetActionGP(ctx context.Context, insert bool, related *Action) {
	if err := o.SetAction(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetAction of the skillUnlockAction to the related item.
// Sets o.R.Action to related.
// Adds o to related.R.SkillUnlockActions.
func (o *SkillUnlockAction) SetAction(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Action) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"skill_unlock_actions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"action_id"}),
		strmangle.WhereClause("\"", "\"", 2, skillUnlockActionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ActionID = related.ID
	if o.R == nil {
		o.R = &skillUnlockActionR{
			Action: related,
		}
	} else {
		o.R.Action = related
	}

	if related.R == nil {
		related.R = &actionR{
			SkillUnlockActions: SkillUnlockActionSlice{o},
		}
	} else {
		related.R.SkillUnlockActions = append(related.R.SkillUnlockActions, o)
	}

	return nil
}

// SetSkillG of the skillUnlockAction to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.SkillUnlockActions.
// Uses the global database handle.
func (o *SkillUnlockAction) SetSkillG(ctx context.Context, insert bool, related *Skill) error {
	return o.SetSkill(ctx, boil.GetContextDB(), insert, related)
}

// SetSkillP of the skillUnlockAction to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.SkillUnlockActions.
// Panics on error.
func (o *SkillUnlockAction) SetSkillP(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Skill) {
	if err := o.SetSkill(ctx, exec, insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSkillGP of the skillUnlockAction to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.SkillUnlockActions.
// Uses the global database handle and panics on error.
func (o *SkillUnlockAction) SetSkillGP(ctx context.Context, insert bool, related *Skill) {
	if err := o.SetSkill(ctx, boil.GetContextDB(), insert, related); err != nil {
		panic(boil.WrapErr(err))
	}
}

// SetSkill of the skillUnlockAction to the related item.
// Sets o.R.Skill to related.
// Adds o to related.R.SkillUnlockActions.
func (o *SkillUnlockAction) SetSkill(ctx context.Context, exec boil.ContextExecutor, insert bool, related *Skill) error {
	var err error
	if insert {
		if err = related.Insert(ctx, exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"skill_unlock_actions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"skill_id"}),
		strmangle.WhereClause("\"", "\"", 2, skillUnlockActionPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, updateQuery)
		fmt.Fprintln(writer, values)
	}
	if _, err = exec.ExecContext(ctx, updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.SkillID = related.ID
	if o.R == nil {
		o.R = &skillUnlockActionR{
			Skill: related,
		}
	} else {
		o.R.Skill = related
	}

	if related.R == nil {
		related.R = &skillR{
			SkillUnlockActions: SkillUnlockActionSlice{o},
		}
	} else {
		related.R.SkillUnlockActions = append(related.R.SkillUnlockActions, o)
	}

	return nil
}

// SkillUnlockActions retrieves all the records using an executor.
func SkillUnlockActions(mods ...qm.QueryMod) skillUnlockActionQuery {
	mods = append(mods, qm.From("\"skill_unlock_actions\""), qmhelper.WhereIsNull("\"skill_unlock_actions\".\"deleted_at\""))
	q := NewQuery(mods...)
	if len(queries.GetSelect(q)) == 0 {
		queries.SetSelect(q, []string{"\"skill_unlock_actions\".*"})
	}

	return skillUnlockActionQuery{q}
}

// FindSkillUnlockActionG retrieves a single record by ID.
func FindSkillUnlockActionG(ctx context.Context, iD string, selectCols ...string) (*SkillUnlockAction, error) {
	return FindSkillUnlockAction(ctx, boil.GetContextDB(), iD, selectCols...)
}

// FindSkillUnlockActionP retrieves a single record by ID with an executor, and panics on error.
func FindSkillUnlockActionP(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) *SkillUnlockAction {
	retobj, err := FindSkillUnlockAction(ctx, exec, iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSkillUnlockActionGP retrieves a single record by ID, and panics on error.
func FindSkillUnlockActionGP(ctx context.Context, iD string, selectCols ...string) *SkillUnlockAction {
	retobj, err := FindSkillUnlockAction(ctx, boil.GetContextDB(), iD, selectCols...)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return retobj
}

// FindSkillUnlockAction retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindSkillUnlockAction(ctx context.Context, exec boil.ContextExecutor, iD string, selectCols ...string) (*SkillUnlockAction, error) {
	skillUnlockActionObj := &SkillUnlockAction{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"skill_unlock_actions\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(ctx, exec, skillUnlockActionObj)
	if err != nil {
		if errors.Is(err, sql.ErrNoRows) {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "entity: unable to select from skill_unlock_actions")
	}

	if err = skillUnlockActionObj.doAfterSelectHooks(ctx, exec); err != nil {
		return skillUnlockActionObj, err
	}

	return skillUnlockActionObj, nil
}

// InsertG a single record. See Insert for whitelist behavior description.
func (o *SkillUnlockAction) InsertG(ctx context.Context, columns boil.Columns) error {
	return o.Insert(ctx, boil.GetContextDB(), columns)
}

// InsertP a single record using an executor, and panics on error. See Insert
// for whitelist behavior description.
func (o *SkillUnlockAction) InsertP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) {
	if err := o.Insert(ctx, exec, columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// InsertGP a single record, and panics on error. See Insert for whitelist
// behavior description.
func (o *SkillUnlockAction) InsertGP(ctx context.Context, columns boil.Columns) {
	if err := o.Insert(ctx, boil.GetContextDB(), columns); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *SkillUnlockAction) Insert(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) error {
	if o == nil {
		return errors.New("entity: no skill_unlock_actions provided for insertion")
	}

	var err error
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeInsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(skillUnlockActionColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	skillUnlockActionInsertCacheMut.RLock()
	cache, cached := skillUnlockActionInsertCache[key]
	skillUnlockActionInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			skillUnlockActionAllColumns,
			skillUnlockActionColumnsWithDefault,
			skillUnlockActionColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"skill_unlock_actions\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"skill_unlock_actions\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "entity: unable to insert into skill_unlock_actions")
	}

	if !cached {
		skillUnlockActionInsertCacheMut.Lock()
		skillUnlockActionInsertCache[key] = cache
		skillUnlockActionInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(ctx, exec)
}

// UpdateG a single SkillUnlockAction record using the global executor.
// See Update for more documentation.
func (o *SkillUnlockAction) UpdateG(ctx context.Context, columns boil.Columns) (int64, error) {
	return o.Update(ctx, boil.GetContextDB(), columns)
}

// UpdateP uses an executor to update the SkillUnlockAction, and panics on error.
// See Update for more documentation.
func (o *SkillUnlockAction) UpdateP(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, exec, columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateGP a single SkillUnlockAction record using the global executor. Panics on error.
// See Update for more documentation.
func (o *SkillUnlockAction) UpdateGP(ctx context.Context, columns boil.Columns) int64 {
	rowsAff, err := o.Update(ctx, boil.GetContextDB(), columns)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Update uses an executor to update the SkillUnlockAction.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *SkillUnlockAction) Update(ctx context.Context, exec boil.ContextExecutor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(ctx, exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	skillUnlockActionUpdateCacheMut.RLock()
	cache, cached := skillUnlockActionUpdateCache[key]
	skillUnlockActionUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			skillUnlockActionAllColumns,
			skillUnlockActionPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("entity: unable to update skill_unlock_actions, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"skill_unlock_actions\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, skillUnlockActionPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, append(wl, skillUnlockActionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, values)
	}
	var result sql.Result
	result, err = exec.ExecContext(ctx, cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update skill_unlock_actions row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by update for skill_unlock_actions")
	}

	if !cached {
		skillUnlockActionUpdateCacheMut.Lock()
		skillUnlockActionUpdateCache[key] = cache
		skillUnlockActionUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(ctx, exec)
}

// UpdateAllP updates all rows with matching column names, and panics on error.
func (q skillUnlockActionQuery) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllG updates all rows with the specified column values.
func (q skillUnlockActionQuery) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return q.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (q skillUnlockActionQuery) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := q.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values.
func (q skillUnlockActionQuery) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all for skill_unlock_actions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected for skill_unlock_actions")
	}

	return rowsAff, nil
}

// UpdateAllG updates all rows with the specified column values.
func (o SkillUnlockActionSlice) UpdateAllG(ctx context.Context, cols M) (int64, error) {
	return o.UpdateAll(ctx, boil.GetContextDB(), cols)
}

// UpdateAllGP updates all rows with the specified column values, and panics on error.
func (o SkillUnlockActionSlice) UpdateAllGP(ctx context.Context, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, boil.GetContextDB(), cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAllP updates all rows with the specified column values, and panics on error.
func (o SkillUnlockActionSlice) UpdateAllP(ctx context.Context, exec boil.ContextExecutor, cols M) int64 {
	rowsAff, err := o.UpdateAll(ctx, exec, cols)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o SkillUnlockActionSlice) UpdateAll(ctx context.Context, exec boil.ContextExecutor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("entity: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillUnlockActionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"skill_unlock_actions\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, skillUnlockActionPrimaryKeyColumns, len(o)))

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to update all in skillUnlockAction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to retrieve rows affected all in update all skillUnlockAction")
	}
	return rowsAff, nil
}

// UpsertG attempts an insert, and does an update or ignore on conflict.
func (o *SkillUnlockAction) UpsertG(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	return o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...)
}

// UpsertGP attempts an insert, and does an update or ignore on conflict. Panics on error.
func (o *SkillUnlockAction) UpsertGP(ctx context.Context, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, boil.GetContextDB(), updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// UpsertP attempts an insert using an executor, and does an update or ignore on conflict.
// UpsertP panics on error.
func (o *SkillUnlockAction) UpsertP(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) {
	if err := o.Upsert(ctx, exec, updateOnConflict, conflictColumns, updateColumns, insertColumns, opts...); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *SkillUnlockAction) Upsert(ctx context.Context, exec boil.ContextExecutor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns, opts ...UpsertOptionFunc) error {
	if o == nil {
		return errors.New("entity: no skill_unlock_actions provided for upsert")
	}
	if !boil.TimestampsAreSkipped(ctx) {
		currTime := time.Now().In(boil.GetLocation())

		if queries.MustTime(o.CreatedAt).IsZero() {
			queries.SetScanner(&o.CreatedAt, currTime)
		}
	}

	if err := o.doBeforeUpsertHooks(ctx, exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(skillUnlockActionColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	skillUnlockActionUpsertCacheMut.RLock()
	cache, cached := skillUnlockActionUpsertCache[key]
	skillUnlockActionUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, _ := insertColumns.InsertColumnSet(
			skillUnlockActionAllColumns,
			skillUnlockActionColumnsWithDefault,
			skillUnlockActionColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			skillUnlockActionAllColumns,
			skillUnlockActionPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("entity: unable to upsert skill_unlock_actions, could not build update column list")
		}

		ret := strmangle.SetComplement(skillUnlockActionAllColumns, strmangle.SetIntersect(insert, update))

		conflict := conflictColumns
		if len(conflict) == 0 && updateOnConflict && len(update) != 0 {
			if len(skillUnlockActionPrimaryKeyColumns) == 0 {
				return errors.New("entity: unable to upsert skill_unlock_actions, could not build conflict column list")
			}

			conflict = make([]string, len(skillUnlockActionPrimaryKeyColumns))
			copy(conflict, skillUnlockActionPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"skill_unlock_actions\"", updateOnConflict, ret, update, conflict, insert, opts...)

		cache.valueMapping, err = queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, cache.query)
		fmt.Fprintln(writer, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRowContext(ctx, cache.query, vals...).Scan(returns...)
		if errors.Is(err, sql.ErrNoRows) {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.ExecContext(ctx, cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "entity: unable to upsert skill_unlock_actions")
	}

	if !cached {
		skillUnlockActionUpsertCacheMut.Lock()
		skillUnlockActionUpsertCache[key] = cache
		skillUnlockActionUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(ctx, exec)
}

// DeleteG deletes a single SkillUnlockAction record.
// DeleteG will match against the primary key column to find the record to delete.
func (o *SkillUnlockAction) DeleteG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.Delete(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteP deletes a single SkillUnlockAction record with an executor.
// DeleteP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SkillUnlockAction) DeleteP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteGP deletes a single SkillUnlockAction record.
// DeleteGP will match against the primary key column to find the record to delete.
// Panics on error.
func (o *SkillUnlockAction) DeleteGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.Delete(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// Delete deletes a single SkillUnlockAction record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *SkillUnlockAction) Delete(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("entity: no SkillUnlockAction provided for delete")
	}

	if err := o.doBeforeDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), skillUnlockActionPrimaryKeyMapping)
		sql = "DELETE FROM \"skill_unlock_actions\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"skill_unlock_actions\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(skillUnlockActionType, skillUnlockActionMapping, append(wl, skillUnlockActionPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args...)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete from skill_unlock_actions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by delete for skill_unlock_actions")
	}

	if err := o.doAfterDeleteHooks(ctx, exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

func (q skillUnlockActionQuery) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows, and panics on error.
func (q skillUnlockActionQuery) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows, and panics on error.
func (q skillUnlockActionQuery) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := q.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all matching rows.
func (q skillUnlockActionQuery) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("entity: no skillUnlockActionQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.ExecContext(ctx, exec)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from skill_unlock_actions")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for skill_unlock_actions")
	}

	return rowsAff, nil
}

// DeleteAllG deletes all rows in the slice.
func (o SkillUnlockActionSlice) DeleteAllG(ctx context.Context, hardDelete bool) (int64, error) {
	return o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
}

// DeleteAllP deletes all rows in the slice, using an executor, and panics on error.
func (o SkillUnlockActionSlice) DeleteAllP(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, exec, hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAllGP deletes all rows in the slice, and panics on error.
func (o SkillUnlockActionSlice) DeleteAllGP(ctx context.Context, hardDelete bool) int64 {
	rowsAff, err := o.DeleteAll(ctx, boil.GetContextDB(), hardDelete)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return rowsAff
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o SkillUnlockActionSlice) DeleteAll(ctx context.Context, exec boil.ContextExecutor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(skillUnlockActionBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillUnlockActionPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"skill_unlock_actions\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, skillUnlockActionPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillUnlockActionPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"skill_unlock_actions\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, skillUnlockActionPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, args)
	}
	result, err := exec.ExecContext(ctx, sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "entity: unable to delete all from skillUnlockAction slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "entity: failed to get rows affected by deleteall for skill_unlock_actions")
	}

	if len(skillUnlockActionAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(ctx, exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// ReloadG refetches the object from the database using the primary keys.
func (o *SkillUnlockAction) ReloadG(ctx context.Context) error {
	if o == nil {
		return errors.New("entity: no SkillUnlockAction provided for reload")
	}

	return o.Reload(ctx, boil.GetContextDB())
}

// ReloadP refetches the object from the database with an executor. Panics on error.
func (o *SkillUnlockAction) ReloadP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.Reload(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadGP refetches the object from the database and panics on error.
func (o *SkillUnlockAction) ReloadGP(ctx context.Context) {
	if err := o.Reload(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *SkillUnlockAction) Reload(ctx context.Context, exec boil.ContextExecutor) error {
	ret, err := FindSkillUnlockAction(ctx, exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAllG refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SkillUnlockActionSlice) ReloadAllG(ctx context.Context) error {
	if o == nil {
		return errors.New("entity: empty SkillUnlockActionSlice provided for reload all")
	}

	return o.ReloadAll(ctx, boil.GetContextDB())
}

// ReloadAllP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SkillUnlockActionSlice) ReloadAllP(ctx context.Context, exec boil.ContextExecutor) {
	if err := o.ReloadAll(ctx, exec); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAllGP refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
// Panics on error.
func (o *SkillUnlockActionSlice) ReloadAllGP(ctx context.Context) {
	if err := o.ReloadAll(ctx, boil.GetContextDB()); err != nil {
		panic(boil.WrapErr(err))
	}
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *SkillUnlockActionSlice) ReloadAll(ctx context.Context, exec boil.ContextExecutor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := SkillUnlockActionSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), skillUnlockActionPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"skill_unlock_actions\".* FROM \"skill_unlock_actions\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, skillUnlockActionPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(ctx, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "entity: unable to reload all in SkillUnlockActionSlice")
	}

	*o = slice

	return nil
}

// SkillUnlockActionExistsG checks if the SkillUnlockAction row exists.
func SkillUnlockActionExistsG(ctx context.Context, iD string) (bool, error) {
	return SkillUnlockActionExists(ctx, boil.GetContextDB(), iD)
}

// SkillUnlockActionExistsP checks if the SkillUnlockAction row exists. Panics on error.
func SkillUnlockActionExistsP(ctx context.Context, exec boil.ContextExecutor, iD string) bool {
	e, err := SkillUnlockActionExists(ctx, exec, iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SkillUnlockActionExistsGP checks if the SkillUnlockAction row exists. Panics on error.
func SkillUnlockActionExistsGP(ctx context.Context, iD string) bool {
	e, err := SkillUnlockActionExists(ctx, boil.GetContextDB(), iD)
	if err != nil {
		panic(boil.WrapErr(err))
	}

	return e
}

// SkillUnlockActionExists checks if the SkillUnlockAction row exists.
func SkillUnlockActionExists(ctx context.Context, exec boil.ContextExecutor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"skill_unlock_actions\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.IsDebug(ctx) {
		writer := boil.DebugWriterFrom(ctx)
		fmt.Fprintln(writer, sql)
		fmt.Fprintln(writer, iD)
	}
	row := exec.QueryRowContext(ctx, sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "entity: unable to check if skill_unlock_actions exists")
	}

	return exists, nil
}

// Exists checks if the SkillUnlockAction row exists.
func (o *SkillUnlockAction) Exists(ctx context.Context, exec boil.ContextExecutor) (bool, error) {
	return SkillUnlockActionExists(ctx, exec, o.ID)
}
